あなたは Alpha Factory (FX GA) の分析レビュアーです。Run 90 を C9 falsification-first で簡潔に独立分析してください。

# 使命/制約
live_criteria全達成個体を1つ。graduation=Stage C pass AND cross-pair pass。閾値緩和禁止、取引回数操作禁止(取引する戦略の進化は正当)。

# 8サイクル到達点
mission達成→cost-robust→warmstart→汎化定量化(R87 0/599)→選択圧bool/連続(R88/R89)→真因特定(EUR_JPY単一学習でanchor取引枯渇+低性能)→T117 multi-pair training(Stage A fitnessを複数ペアで集約)。

# Run 90 事実 (T117 multi-pair min集約 spike, pop48 gen20)
- multi_pair_training enabled(aggregate=min, anchors=USD_JPY, scope=stage_a)。wall-time13.5分。
- ★A/B/C 0/0/0(R89 single-pair: 2606/2142/892)。stage_a_pass全False。
- fitness_pen(min集約後) gen0 max-0.058→gen20 max-0.028、全gen Stage A閾値-0.0172未達。fitness_raw(観察sharpe)max0.0=集団がtargetでも改善せず。threshold-0.0172(R89同一)、anchor metric_unavailable=0(データ健全)。

# 診断仮説(反証可能性つき検討依頼)
min-collapse: fitness=min(target,anchor)。anchorは未成熟genomeで普遍的に低性能→minが常にanchorを拾う→target改善が報酬されず(min上がらない)集団がtargetでも改善せず崩壊。R89 single-pairはtarget直接報酬で進化(2606 passes)。mean集約=(target+anchor)/2 ならtarget改善がmean/2重みで報酬されtarget-good個体進化→A/B/C回復見込み(min-collapse回避)。

# 次サイクル候補
Critical=mean集約R91検証(T117実装済、CLIにaggregate渡す経路なし=--multi-pair-aggregate最小追加)。代替=weighted blend(target主+λanchor)、NSGA2多目的(既存nsga2_selection_enabledでtarget/anchor別目的Pareto、集約せず両立)。

# 求める出力(簡潔、日本語)
1. Facts要約(1-3行)
2. 解釈(反証可能性): (a)min-collapse診断は妥当か(min が target選抜圧を消す機序)、(b)mean集約がmin-collapseを回避しA/B/C回復させる見込みは妥当か/mean のリスク(anchor極低でmean<閾値で過剰淘汰、target高で救済されるか)、(c)mean vs weighted blend vs NSGA2多目的、低リスクでanchor汎化を促す最善は。NSGA2(集約せず両立)が本質的に優れるか mean先行が妥当か。
3. 次サイクル候補(Critical 1個/Warning 1-2個)。低リスク優先、閾値緩和・取引操作禁止、loop停止回避。
4. 全体判定 OK/CONCERN/CRITICAL_DRIFT
