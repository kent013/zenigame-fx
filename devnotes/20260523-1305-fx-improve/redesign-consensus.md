1. **revert 判断（trade_count_min / entry_count_min）**  
妥当です。提示データで「到達不能・逆効果」の根拠は十分です。  
`trade_count_min=50` への戻しは「取引削減禁止」違反ではありません。理由は「上限を削る」のではなく「構造的に品質最適な帯域（50-60）を再許容する」ためで、実際に `n=850` の厚い分布で品質優位が確認されています。

2. **`total_pnl_min 50000→70000` 引き上げ**  
妥当です。これは「意味ある引き上げ」です。  
`median=69970` 付近なので binding かつ達成可能で、`trade_count` のような構造ミスマッチより品質に直結します。  
このサイクルでは `dd` 厳格化は見送り推奨です。理由は変数を増やすと失敗要因の切り分けが難しくなるためです（まずは到達可能性回復を優先）。

3. **`sharpe_min=1.5` 維持**  
維持で良いです。hollow でも「引き上げ済み基準の後退回避」として妥当です。

4. **R95 の反証可能な成功基準**  
以下を全て満たせば達成、1つでも欠ければ未達で判定してください。  
1. 設定: `entry_count_min=50`, `trade_count_min=50`, `sharpe_min=1.5`, `total_pnl_min=70000`。  
2. 回復: `Stage C pass > 0`。  
3. 実用性: `mission_candidate_count >= 10`。  
4. 構造確認: `Stage B` で `trade_count>=100` 個体が再出現（`count >= 1`）。  
5. 再現性: 別 seed 1本でも `mission_candidate_count > 0` を再現。

5. **全体判定**  
**OK（条件付き）**。  
今回の再設計は合理的です。R95 は上記 5 条件で機械的に合否判定してください。