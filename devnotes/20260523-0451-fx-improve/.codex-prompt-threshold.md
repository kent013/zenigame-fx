あなたは Alpha Factory (FX GA) の戦略アドバイザーです。閾値引き上げ(North Star)の設計を確定してください。

# 経緯
cross-pair汎化7サイクル全0→戦略合議で(D)North Star回帰(閾値引き上げ)採用済。実装前post-hoc分析で閾値軸を再特定。

# 決定的データ (R89 Stage C 892個体, single-pair+warmstart)
- annualized sharpe: median 4.56 / max 5.60 → sharpe>=1.5は全892クリア(hollow、GA fitnessが既にsharpe最大化)。
- trade_count: min50/median51/p75 53/max97 → 96%がfloor50張り付き、>=100はゼロ。live_criteria@(sharpe>=1.5,trade>=100)=0個体。
- 高trade_count個体(trade=97)もannualized sharpe 3.42(>>1.5) → 高取引数と高sharpeは両立可能、ただし発見数少。
- 現best=trade51/sharpe5.60 = lucky-few-trade過学習の典型(AGENTS cycle23既知のfloor狙い撃ち)。

# config現状
- live_criteria: sharpe_min 1.0, trade_count_min 50, total_pnl_min 50000, max_drawdown_max 0.2, trade_count_max 5000。
- stage_gate.entry_count_min 50 (AGENTS L87: trade_count_min 50と整合化済、Stage A圧)。min_exposure_trade_count 1 (制約: 1<=min_exposure<trade_count_min)。trade_count_min_for_sharpe 30。

# refined施策案
trade_count_min 50→100引き上げ(真にbinding、lucky-few-trade排除、取引増=「取引削減禁止」と整合robustness)。entry_count_min 50→100整合引き上げ(GAを高取引数探索へ)。sharpe_min 1.0→1.5併せ(hollowだがNorth Star整合)。

# 求める出力(簡潔、日本語)
1. ★引き上げ幅: trade_count_min 50→100 が妥当か(現max97、binding過ぎず到達可能か。50→80など中間が安全か、100が適切か)。entry_count_min も同値100へ整合必須か。
2. GA探索成立性: entry_count_min=100でStage A圧をかければ高取引数(>=100)×高sharpe(>=1.5)戦略が進化する見込みか(現データ: trade97/sharpe3.42存在、100は僅か先)。それとも高取引数化でsharpe劣化しmission未達リスク高いか。
3. sharpe_min 1.0→1.5を併せるべきか、trade_count_minのみに集中すべきか(hollowなsharpe引き上げが何か害あるか)。
4. メタ過学習・禁止事項(取引削減禁止と整合か=取引増は正当、閾値緩和でないか=引き上げのみ)確認。default.yaml変更が多数test(calibrate*/config_schema_contract等)に与える影響への注意。
5. 反証可能な成功基準(R94で何を満たせば達成か)。

# 出力形式: 推奨1つに収束、判定OK/CONCERN/CRITICAL_DRIFT。
