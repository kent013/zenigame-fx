## Round 5 — Round 4 修正反映

Round 4 で Critical なし、 Warning 3 件 + Suggestion 1 件:

| Round 4 [Warning/Suggestion] | 修正対応 |
|---|---|
| [W1] Phase 1 / Phase 2 PR 境界混在 | 「コンポーネント変更」 セクションを Phase 1 (T059 PR、 EpochManager 単体テスト + .gitignore) と Phase 2 (24m 切替 + run_ga.py 組込 + audit_log) に明示分離、 「RunContext との接続」 セクション冒頭に「Phase 2 スコープ、 T059 PR では反映しない」 明記 |
| [W2] lock timeout 仕様未閉包 | `deadline = time.monotonic() + LOCK_TIMEOUT_SECONDS`、 `retry_interval_s = 0.1` で `BlockingIOError` 再試行ループ、 deadline 超で `EpochLockTimeout` raise + log |
| [W3] EpochDatasetMismatch ハンドリング漏れ | reserve 失敗 except 節に `EpochDatasetMismatch` / `EpochLockTimeout` / `DuplicateRunIdError` 追加 |
| [S] run_id 重複予約ガード | `if run_id in epoch_records: raise DuplicateRunIdError` 追加 |

改訂版概念設計: `devnotes/20260429-2113-todo-T059-epoch-window-manager/conceptual-design.md`

特に以下のセクション参照:
- 「コンポーネント変更」: Phase 1 / Phase 2 分離、 `run_ga.py` は Phase 2
- 「file lock 付き reservation」: deadline + retry_interval ループで timeout 仕様確定
- reserve_run_slot: 重複 run_id ガード追加、 6 種例外 (EpochCapExceeded / EpochStateIncompatible / EpochStateCorruptError / EpochDatasetMismatch / EpochLockTimeout / DuplicateRunIdError) を caller の except で全捕捉

## レビュー期待

これで Round 1-4 の Critical 9 件 + Warning すべて解消の最終確認。 APPROVED の場合は判定理由明記。 残課題があれば identify。
