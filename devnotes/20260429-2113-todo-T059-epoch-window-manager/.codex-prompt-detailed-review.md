あなたは経験豊富なクオンツ・システムアーキテクトです。 zenigame-fx Alpha Factory 改善の詳細設計をレビューしてください。

(C1-C9 自動挿入。 特に C1 Design-first / C2 parallel-path / C3 collider bias / C4 前提検証 / C6 fact-interpretation 分離 / C8 INCONCLUSIVE 第一級扱い)

【前提環境】
- Python 3.13 + numpy + pandas
- macOS / Linux 開発前提 (fcntl)
- 24GB メモリ、 6 ワーカー並列、 1 ワーカー 3GB

【レビュー観点】
1. コード正確性 (ロジック / エッジケース / off-by-one / NaN)
2. 既存コードとの整合性
3. パフォーマンス
4. テスト計画の網羅性
5. 副作用・後退リスク
6. 考慮漏れ
7. 実装モード妥当性
8. 波及変更網羅性 (AGENTS.md / skill / config / docs)
9. ルックアヘッド (本施策は infrastructure で N/A)
10. メモリ制約 (state file I/O 軽微、 N/A)
11. パフォーマンス (lock + atomic save、 軽量)
12. 前提検証 (C4): config 値 / code snippet が最新 main と一致
13. 並行計算経路 (C2): 「X が無い = バグ」 主張時 grep 痕跡
14. 相関 / 予測 claim collider bias (C3 / C7、 N/A)

【出力形式】
- 各施策ごとに APPROVE / REQUEST_CHANGES / INCONCLUSIVE
- [Critical] [Warning] [Suggestion]
- Critical/Warning に修正案
- 全体: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 日本語

---

## 詳細設計書

`devnotes/20260429-2113-todo-T059-epoch-window-manager/detailed-design.md` を Read で参照。

主要施策:
1. `src/alpha_factory/epoch_manager.py` 新規 (EpochWindow + EpochManager + 6 例外 + atomic reservation + state compat fingerprint + data horizon gate + reset)
2. `tests/alpha_factory/test_epoch_manager.py` 新規 (約 30 件の単体テスト)
3. `.gitignore` 追加

**Phase 1 (T059 PR) スコープ**: 上記 3 施策のみ。 `run_ga.py` 組込・`default.yaml` 24m 切替・`config.py` 拡張 は **Phase 2 (別 PR、 別 TODO)** で実施。 T059 PR 単独 merge で runtime に影響しないことを確認。

## 概念設計

`devnotes/20260429-2113-todo-T059-epoch-window-manager/conceptual-design.md` (Round 5 で APPROVED) も Read で参照。

特に:
- 「コンポーネント変更」: Phase 1 / Phase 2 分離
- 「state file format」: `slots_consumed_in_epoch` 単調増加 + `run_records` 入れ子辞書
- 「file lock 付き reservation」: deadline + retry_interval ループ
- 「24m memory gate」: T059 完了後の Phase 2 PR の前提

## 関連現行コード (重複確認用)

- `src/alpha_factory/calibrate_state.py:122` (history JSONL iteration pattern、 参考)
- `src/alpha_factory/fsp_updater.py:180` (atomic write pattern、 踏襲)
- `scripts/alpha_factory/calibrate_gate.py:28` (fcntl.flock 既存実用例)
- `src/alpha_factory/config.py:51` (DatasetConfig の現状 fields)
