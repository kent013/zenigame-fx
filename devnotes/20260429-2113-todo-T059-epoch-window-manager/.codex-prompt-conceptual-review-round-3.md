## Round 3 — Round 2 修正反映

Round 2 で 3 件 [Critical] (うち 1 件は追加 Critical の datetime 正規化) + 4 件 [Warning] + 3 件 [Suggestion] 反映:

| Round 2 [Critical/Warning/Suggestion] | 修正対応 |
|---|---|
| [C-extra] anchor_origin `Z` vs `+00:00` 偽不一致 | `_normalize_dt` / `_normalize_dt_to_iso` helper で datetime 正規化、 比較前に datetime 化 |
| [C4] 6m → 24m 移行時の ID 再利用汚染 | `reserve_run_slot(run_id, *, dataset_cfg)` で `cfg.dataset` が epoch window と一致しないなら `EpochDatasetMismatch` raise |
| [C5] mark_run_status epoch advance で消失 | `run_records: {epoch_index: {run_id: record}}` 入れ子辞書に、 旧 epoch running run を terminal まで保持 |
| [W2] safety margin 根拠未計測 | `MARGIN_DAYS=7` 定数化 + 毎回ログ出力で実測再校正 |
| [W3] fingerprint 項目不足 | `epoch_id_format_version` + `state_schema_version` 追加 |
| [W6] --reset-epoch-state 二重明示 + audit | `--reset-epoch-state --yes-reset-epoch-state --reason "..."` 3 つ必須 + `audit_log.jsonl` reset event 追記 |
| [W7] 24m memory gate DoD 拘束化 | T059 完了 → 24m 有効化 PR の前提条件として `max_workers=1/2` で RSS 実測 (3 GB/worker 超で別 TODO 必須) を文書化 |
| [W8a] schema_version bump policy | 互換破壊時は state_schema_version bump + fail-closed + reset 必須を明文化 |
| [S1] fcntl.flock 明示 | `fcntl.flock(LOCK_EX | LOCK_NB)` 直接使用、 抽象 wrapper 不要 |
| [S2] lock timeout 定数化 + ログ | `LOCK_TIMEOUT_SECONDS=10`、 acquire 開始/成功/失敗を log |
| [S3] epoch_index metadata 限定 | metadata 限定明記、 ID/判定キーに使わない |

改訂版概念設計: `devnotes/20260429-2113-todo-T059-epoch-window-manager/conceptual-design.md`

特に以下のセクション参照:
- 「state file format」: `run_records` 入れ子辞書、 fingerprint の `epoch_id_format_version` / `state_schema_version` 追加
- 「state compatibility fingerprint」: `_normalize_dt` で datetime 比較
- 「file lock 付き reservation」: `dataset_cfg` 引数追加 + `fcntl.flock` 明示
- 「state 破損時の reset 経路」: 3-flag 必須 + audit_log
- 「24m memory gate」: DoD 拘束として 24m 有効化 PR 前提

## レビュー期待

[Critical] [Warning] [Suggestion] で再評価。 APPROVED の場合は判定理由明記。 残課題があれば identify。
