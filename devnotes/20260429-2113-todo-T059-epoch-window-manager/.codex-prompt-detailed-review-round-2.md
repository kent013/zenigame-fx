## Round 2 — Round 1 修正反映

Round 1 で 2 件 [Critical] + 4 件 [Warning] + 2 件 [Suggestion] 反映:

| Round 1 [Critical/Warning/Suggestion] | 修正対応 |
|---|---|
| [C1] mark_run_status 全 epoch 走査と「異 epoch で同 run_id 許容」 矛盾 | **run_id を全 epoch で一意必須化**。 reserve_run_slot で全 epoch 走査の重複 check (`for past_epoch_key, past_records in state["run_records"].items(): if run_id in past_records: raise`)、 mark_run_status は run_id 単独で対象一意特定可能。 テスト名を `test_run_id_uniqueness_enforced_globally_across_epochs` に変更 |
| [C2] reset() 破損 state で自身が失敗 | reset 内で `EpochStateCorruptError` 捕捉、 `previous_state_snapshot = {"corrupt": True, "error": str(exc)}` 記録、 削除続行 |
| [W1] reset() lock 無し競合 | `reset` 全体を `with self._file_lock():` で保護 |
| [W2] _compute_fingerprint dataset_cfg 未使用 | 引数削除、 _init_state / _verify_compatibility で `dataset_cfg.instrument != self.instrument` guard を追加 |
| [W3] make_epoch_id 時刻付き window 衝突 | docstring に「window.start/end は 00:00 UTC 固定」 契約明記 |
| [W4] 破損 reset / 同 run_id 異 epoch テスト不足 | テスト追加 (`test_reset_recovers_even_when_state_json_is_corrupt`, `test_reserve_raises_duplicate_run_id_error_when_same_id_in_other_epoch`) |
| [W5] gitignore lock 名衝突 + 冗長 | 施策 3 を削除、 既存 `.cache/` 全体無視で対応 |
| [S1] make_epoch_id 時刻固定明記 | docstring に追記 |
| [S2] flaky test 制御 | `pytest-timeout` + `pytest.mark.flaky(reruns=2)` をテスト案に明記 |

改訂版詳細設計: `devnotes/20260429-2113-todo-T059-epoch-window-manager/detailed-design.md`

特に以下のセクション参照:
- 「Round 1 review 反映」 (新設): 9 修正 mapping
- 「施策 1 epoch_manager.py」: reserve_run_slot 全 epoch 一意 check、 reset 破損対応 + lock、 _compute_fingerprint 引数削除、 _init_state / _verify_compatibility instrument guard、 make_epoch_id 時刻固定 docstring
- 「施策 2 test_epoch_manager.py」: 追加 test 名 (`test_reset_recovers_even_when_state_json_is_corrupt`, `test_reserve_raises_duplicate_run_id_error_when_same_id_in_other_epoch`, `test_run_id_uniqueness_enforced_globally_across_epochs`, `test_reset_acquires_file_lock_to_prevent_concurrent_reservation`)
- 施策 3 削除 (Phase 1 スコープを 2 施策に縮小)

## レビュー観点

Round 1 の Critical 2 件 + Warning 4 件 + Suggestion 2 件すべて解消されているか、 新たな破壊点がないか。

CHANGES_REQUESTED の場合は具体的修正案、 APPROVED の場合は判定理由明記。
