あなたは経験豊富な Python コードレビュアーです。zenigame-fx T116(連続値選択圧+pass条件観測列)実装をレビュー。設計はdesign-review APPROVED(R2)。

# 施策(T115 bool→continuous拡張)
R88でT115 bool tie-break int(aggregate>0)が gen0飽和で上昇圧ゼロ(ii_lite_pass 0/619)。連続値化で勾配付与+pass3条件観測列で天井検証/proxy整合性監視。

# 実装(差分、5ステップ)
1. run_ga.py _selection_key: cp_pref=int(margin>threshold);(*score[:9],cp_pref,score[9]) → m=entry.cross_pair_margin; cp_val=float(m) if(m is not None and math.isfinite(m)) else -math.inf;(*score[:9],cp_val,score[9])。default(selection_pressure=False)はscore(10-tuple)そのまま=bit-exact。
2. run_ga.py _write_reports: selection_key_schema = 'v3_5_cross_pair_pressure_continuous' if _cp_sel_eff else 'v3_3'(v3_4破棄、R88 bool/R89 continuous識別)。
3. cross_pair.py CrossPairConfig.__post_init__: selection_pressure and selection_pressure_margin_threshold!=0.0 → ValueError(死に設定fail-closed)。
4. archive.py 4観測列(GENOMES_SCHEMA float64×3+int64×1 nullable + template None + cross-pair書込: skipped/None→全None、else cross_pair_mean_sharpe=_finite_or_none(metrics.get('mean_sharpe'))/min_sharpe/target_ratio(sharpe_target_cross_ratio)/pair_failure_count=sum(r.startswith('pair_failure:') for r in cp_result.reason_codes)). schema 66→70。観測専用(IndividualCacheEntry/selection_score非追加=selection非影響)。
5. tests: test_cross_pair_selection_pressure(bool→連続値順序: key[9]==0.9連続値/hi>mid>lo勾配/None・NaN→-inf最下位)+test_archive(66→70列、nullable4列追加)。

# 検証
136 affected tests pass(+full suite 3335 passed、唯一失敗test_v31は main由来既存)。ruff(私の変更clean、run_ga E501:197既存)/mypy(archive:379 float|None は persistence_score既存=main由来 +16行シフト、私の変更外)。default OFF run_ga_parallel pass=bit-exact。schema70列/fail-closed動作確認済。

# レビュー観点
1. ★default(selection_pressure=False)で _selection_key 10-tuple完全一致bit-exact維持か(連続値はTrue時のみ)。
2. 連続値 cp_val: float(m) if(not None and isfinite) else -inf、lex tuple idx9にfloat混在の比較安定性、-inf guard(None/NaN/inf)。bool→連続でgen0飽和を解消し勾配付与する設計意図と整合か。
3. 観測列4点セット(payload→archive、skipped/None一貫None)、pair_failure_count の reason_codes count正しさ、観測専用(selection非影響=IndividualCacheEntry/selection_score不変)。
4. schema bump v3_5_continuous の識別性(R88 v3_4 bool既存と区別)。
5. fail-closed(threshold!=0)の妥当性、margin_threshold引数が連続値経路で無視される一貫性。
6. 4段伝搬(cross_pair payload metrics→archive列)漏れ。_finite_or_none で object→float健全性。
7. メタ過学習ガード(連続値圧が aggregateに過適合しmean_sharpe乖離を観測列で監視可能か)。Reactive回避。
8. テスト網羅性。

# 出力
ファイルごと判定/[Critical][Warning][Suggestion](Critical/Warningに修正案)/全体判定/日本語。【収束】APPROVED または1反証可能仮説+1最小変更。

## 実装差分
```diff
diff --git a/scripts/alpha_factory/run_ga.py b/scripts/alpha_factory/run_ga.py
index a635db3..329a5df 100644
--- a/scripts/alpha_factory/run_ga.py
+++ b/scripts/alpha_factory/run_ga.py
@@ -281,12 +281,14 @@ def _selection_key(
     score = entry.selection_score
     if not selection_pressure:
         return score
-    # ON: fold_robust(index 8) と fitness_pen(index 9) の間に tie-break を挿入。
-    cp_pref = int(
-        entry.cross_pair_margin is not None
-        and entry.cross_pair_margin > margin_threshold
-    )
-    return (*score[:9], cp_pref, score[9])
+    # ON (T116 連続値化): fold_robust(index 8) と fitness_pen(index 9) の間に
+    # cross-pair margin の **連続値** を挿入 (T115 bool tie-break は gen0 飽和で
+    # 勾配ゼロだったため、連続値で pass 閾値方向の勾配を継続付与)。None/NaN/inf は
+    # -inf (最下位、cross-pair 評価なし個体は不利、pass 整合)。margin_threshold は
+    # 連続値経路では未使用 (CrossPairConfig.__post_init__ で !=0.0 を fail-closed)。
+    m = entry.cross_pair_margin
+    cp_val = float(m) if (m is not None and math.isfinite(m)) else -math.inf
+    return (*score[:9], cp_val, score[9])
 
 
 def _resolve_cross_pair_selection_pressure(cfg: Any) -> tuple[bool, str]:
@@ -1663,9 +1665,11 @@ def _write_reports(
             ),
         },
         "cross_pair_runtime_mode": cross_pair_mode,
-        # T115: pressure ON 時は selection_key が 11-tuple (v3_4)、OFF は 10-tuple (v3_3)。
+        # T116: pressure ON 時は selection_key が 11-tuple で cross-pair margin 連続値
+        # (v3_5_continuous)、OFF は 10-tuple (v3_3)。R88(bool,v3_4) と R89(continuous,
+        # v3_5) を A/B 監査で識別可能にする (Codex Round1 Critical)。
         "selection_key_schema": (
-            "v3_4_cross_pair_pressure" if _cp_sel_eff else "v3_3"
+            "v3_5_cross_pair_pressure_continuous" if _cp_sel_eff else "v3_3"
         ),
         "per_generation": sanitized_per_generation,
         "best": {
diff --git a/src/alpha_factory/archive.py b/src/alpha_factory/archive.py
index 88753b1..fc7987d 100644
--- a/src/alpha_factory/archive.py
+++ b/src/alpha_factory/archive.py
@@ -113,6 +113,13 @@ GENOMES_SCHEMA: pa.Schema = pa.schema(
         # = mean_sharpe - λ·std)。in-loop selection pressure の源。cross_pair 未実行
         # (enable=False / skipped) は None。
         pa.field("cross_pair_aggregate_fitness", pa.float64(), nullable=True),
+        # T116: cross-pair pass 3 条件の実値 + pair_failure 数 (観測専用、selection 非影響)。
+        # 天井検証 (max mean_sharpe が pass 0.15 近傍か) + proxy 整合性監視
+        # (aggregate climb 時 mean_sharpe も climb するか) + 偽陽性検知 (pair_failure)。
+        pa.field("cross_pair_mean_sharpe", pa.float64(), nullable=True),
+        pa.field("cross_pair_min_sharpe", pa.float64(), nullable=True),
+        pa.field("cross_pair_target_ratio", pa.float64(), nullable=True),
+        pa.field("cross_pair_pair_failure_count", pa.int64(), nullable=True),
         pa.field("graduated", pa.bool_(), nullable=False),
         # T-sharpe Phase 1A: trade-level Sharpe (v2) と calc version
         # T044: trade_sharpe_raw は **Stage A 値で固定** (selection 基準と
@@ -262,6 +269,10 @@ def _create_row_template() -> dict[str, Any]:
         "dsr": None,
         "ii_lite_pass": None,
         "cross_pair_aggregate_fitness": None,  # T115
+        "cross_pair_mean_sharpe": None,  # T116 観測
+        "cross_pair_min_sharpe": None,  # T116 観測
+        "cross_pair_target_ratio": None,  # T116 観測
+        "cross_pair_pair_failure_count": None,  # T116 観測
         "graduated": False,
         # T-sharpe Phase 1A
         "trade_sharpe_raw": None,
@@ -854,6 +865,10 @@ class GenomeArchive:
         if skipped or cp_result is None:
             row["ii_lite_pass"] = None
             row["cross_pair_aggregate_fitness"] = None  # T115
+            row["cross_pair_mean_sharpe"] = None  # T116
+            row["cross_pair_min_sharpe"] = None
+            row["cross_pair_target_ratio"] = None
+            row["cross_pair_pair_failure_count"] = None
         else:
             row["ii_lite_pass"] = bool(cp_result.passed)
             # T115: cross-pair 実測シグナル (in-loop selection pressure 源)。
@@ -861,6 +876,22 @@ class GenomeArchive:
             row["cross_pair_aggregate_fitness"] = _finite_or_none(
                 cp_result.metrics.get("aggregate_fitness")
             )
+            # T116: pass 3 条件の実値 (観測専用、selection 非影響)。
+            row["cross_pair_mean_sharpe"] = _finite_or_none(
+                cp_result.metrics.get("mean_sharpe")
+            )
+            row["cross_pair_min_sharpe"] = _finite_or_none(
+                cp_result.metrics.get("min_sharpe")
+            )
+            row["cross_pair_target_ratio"] = _finite_or_none(
+                cp_result.metrics.get("sharpe_target_cross_ratio")
+            )
+            # pair_failure 数: reason_codes の "pair_failure:" prefix を count
+            # (aggregate 上昇の偽陽性=pair_failure で集約値が壊れるケース検知)。
+            row["cross_pair_pair_failure_count"] = sum(
+                1 for r in cp_result.reason_codes
+                if str(r).startswith("pair_failure:")
+            )
         # T043: mission_score を payload から書き写す (stage_gate 側で計算済)。
         # base 評価が trade を出さず Sharpe=None だった場合は None になる。
         row["mission_score"] = _opt_float(payload, "mission_score")
diff --git a/src/alpha_factory/cross_pair.py b/src/alpha_factory/cross_pair.py
index f96beff..b1a8544 100644
--- a/src/alpha_factory/cross_pair.py
+++ b/src/alpha_factory/cross_pair.py
@@ -116,6 +116,15 @@ class CrossPairConfig:
             raise ValueError(
                 f"aggregator_lambda must be >= 0: {self.aggregator_lambda}"
             )
+        # T116: 連続値選択圧では margin_threshold は未使用。設定したのに効かない
+        # サイレント挙動を防ぐため fail-closed (Codex Round1 Warning1)。
+        if self.selection_pressure and self.selection_pressure_margin_threshold != 0.0:
+            raise ValueError(
+                "cross_pair.selection_pressure_margin_threshold must be 0.0 when "
+                "selection_pressure=True (T116 continuous selection uses the raw "
+                "cross_pair margin; threshold is no longer applied): "
+                f"got {self.selection_pressure_margin_threshold}"
+            )
         if not 0.0 <= self.sharpe_target_cross_ratio_min <= 1.0:
             raise ValueError(
                 "sharpe_target_cross_ratio_min must be in [0, 1]: "
```
