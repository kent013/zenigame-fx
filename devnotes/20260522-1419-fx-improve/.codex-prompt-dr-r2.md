Round1 [Critical]+[Warning]2件 を反映しました。

[Critical] selection_key_schema bump: 連続値版は v3_5_cross_pair_pressure_continuous を出力(_write_reports分岐: pressure effective&continuous→v3_5_continuous / OFF→v3_3)。bool経路破棄でv3_4は出さない。R88(bool,v3_4既存)/R89(continuous,v3_5)識別可能。
[Warning1] margin_threshold死に設定→fail-closed: CrossPairConfig.__post_init__ で selection_pressure=True かつ selection_pressure_margin_threshold!=0.0 を ValueError(連続値化でthreshold無効、0.0にせよ)。
[Warning2] 天井判定複数seed: R89(seed68)単独でなく seed69追加2runの max(mean_sharpe_cross) で天井確定後にmulti-pair。
[Suggestion] pair_failure監視列 cross_pair_pair_failure_count 追加(偽陽性検知、観測専用) + test更新(test_cross_pair_selection_pressure bool→continuous, test_archive schema列数)。

連続値指標は cross_pair_margin(aggregate_fitness)継続(stdペナルティ込みで pass3条件と方向整合、観測列でmean_sharpe整合性監視)。_selection_key: cp_val=float(m) if(m is not None and math.isfinite(m)) else -math.inf、return (*score[:9],cp_val,score[9])。default(selection_pressure=False)は10-tuple不変bit-exact。観測列(mean_sharpe/min_sharpe/target_ratio/pair_failure_count)はselection非影響。

この反映で Critical解消しAPPROVEDか確認。残あれば最小変更で。反証可能仮説(Codex提示): continuous化で aggregate上昇に mean_sharpe も同方向上昇(target_ratio劣化なし)を R89観測列で検証。
