【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。**ファイル読み込みは許可** (= T068 詳細設計 / T061-T067 main 実装の確認のため明示的に許可されている、 T064 で発生した Codex 誤解釈を防ぐ規範).

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】

### C1. Design-first 原則 (code grep より先に docs を読む)
コードの bug / 設計欠陥について claim を出す前に、以下を verify:
- docs/alpha_factory/ 配下の該当セクション（stage-gates.md / clause-architecture.md 等）を Read で確認
- devnotes/ 配下の関連 T-number（関数コメントや git log に出てくる T-xxx）を ls devnotes/ | grep T{xxx} で探して読む
- git log --all -S "識別子" で該当 code の変更履歴を辿る
- 該当機能と並行計算される可能性のある経路を grep で広く検索

### C2. 「X が無い = バグ」禁止ルール
関数 Y の signature / body に識別子 X が無いことを根拠に bug 判定してはならない。別経路での計算を grep で広く検索してから。

### C3. Collider bias 必須チェック（相関分析時）
2 つの変数 A, B の相関を計算して report する場合、Conditioning set と collider の有無を必ず明記。

### C4. 前提明示ルール
Claim / verdict を出す前に、prompt の先頭に「本分析の前提」を bullet でリスト化し、各前提が verified であることを明示。

### C5. 並列 sub-agent の framing 独立性チェック
独立検証のつもりで同じ framing の sub-agent を N 個回しても、それは 1 つの誤読の N 倍冗長実行。

### C6. Fact / Interpretation 分離ルール
「観察された事実 (Facts)」と「解釈・推論 (Interpretations)」を明確に区別する。1 文で mix しない。

### C7. Sample size ガード
n < 30 の相関 claim は明示的に因果解釈を避ける。n < 10 は相関 claim 禁止（observation のみ許可）。

### C8. INCONCLUSIVE を第一級 verdict に
INCONCLUSIVE は CONFIRMED / REJECTED と同格の選択肢。データ不足は正当な結論。

### C9. Falsification-first プロンプト
Round 1 は「この hypothesis の反証を探せ」という指示を先に書く。confirmation bias を強制しない。

---

# 役割

あなたは zenigame-fx Alpha Factory の **T068 PR 1 (= failure_handling.py 新規 + テスト)** の実装レビュアーです.

T068 PR 1 のスコープは以下の 2 ファイル新規作成のみ:

1. `src/alpha_factory/failure_handling.py` (新規) — graceful failure 伝搬 + safe-default fallback + error propagation + failure category enum (5 dataclass + 6 wrapper + 6 validator + 3 degraded builder + 3 集計関数 + internal helper)
2. `tests/alpha_factory/test_failure_handling.py` (新規) — 11 sub-suite × 112 件

詳細設計 (`devnotes/20260430-1430-todo-T068-failure-handling/detailed-design.md`、 約 1135 行) は全 Round APPROVED 済み. T061 / T062 / T064 main 実装は既に SSOT として確定済み.

# 重要な前提 (T067 で確立した規範)

詳細設計は `src/alpha_factory/ga/failure_handling.py` (= ga/ subdir) を指定するが、 既存 `src/alpha_factory/` は flat module 構成 (= ga/ subdir 不存在). ファイル配置規範 (= 既存 flat module との一貫性優先) に従い flat 配置 `src/alpha_factory/failure_handling.py` を採用 (T065 / T066 / T067 と同方針).

詳細設計の dataclass 定義 (例えば `InvariantFlags(is_feasible=False, ...)` の直接 boolean field、 `BCEvaluationResult` の `progress_pass` field、 `StageBResult(pooled_dd_per_fold_max=math.inf)` 等) は **T061-T064 main 実装と一部不整合**. 詳細設計 § 設計判断 で示された通り、 **main 実装を SSOT として** 詳細設計の擬似コードを修正適用する. 主な不整合と採用 SSOT:

- `InvariantFlags`: `is_feasible: bool` field ではなく `@property` (`session_close_drop_count: int`, `negative_equity_drop_open_count: int`, `infeasible_reason_codes: frozenset[InfeasibleReasonCode]` の 3 field 、 `is_feasible` は property)
- `CanonicalFiveResult`: 詳細設計の `sr_session_worst` ではなく `sr_session_worst_block_scale` / `sr_session_worst_annual_estimate` の 2 field、 `invariant_flags` ではなく `invariants` field、 加えて `per_bucket_sr` / `per_bucket_wr` / `low_sample_buckets` / `bucket_validator_version` field を持つ
- `MissionGapResult`: field 順序は `mission_inf_gap, constraint_violation, mission_margin, mission_signed_margin, per_metric_shortfall, is_feasible` (T062 main)
- `StageBResult`: `pooled_dd_per_fold_max: float | None` (None 許容、 詳細設計の `math.inf` ではない)、 `is_b_pass` field 必須
- `StageCLiteResult`: `cells_worst` / `sample_size_flag` / `n_pass_windows` field 必須、 `__post_init__` で n_pass_windows 値域 + len 整合検証
- `StageCResult`: `c_cf_result` 必須、 `stress_cf_result` / `per_pair_results` / `live_criteria_pass` / `stress_pass` / `cross_pair_pass` / `mission_fail_reason` / `shadow_robustness_score` field
- `BCEvaluationResult`: 詳細設計の `progress_pass` / `shadow_robustness_score` field は存在せず (top-level)、 `c_pass_depth: float` (Follow-up Phase 0 confirmed) / `b_result` / `c_lite_result` / `c_result` 経由で参照

# レビュー観点 (Falsification-first)

以下の hypothesis の **反証** を探してください. 各観点で問題がなければ "no issues found" と明記.

## H1: 詳細設計 SSOT 整合性 (main 実装 SSOT 優先)

- 詳細設計 § 施策 1 (行 43-965) のコード骨子と `src/alpha_factory/failure_handling.py` 実装が deviation していないか.
- 関数シグネチャ (概念設計 § 11.2) ・dataclass field・const 値 (`EXCEPTION_MESSAGE_MAX_LENGTH=500` / `DEGRADED_LOG_PF_CLIP_FLOOR=-2.0`) が完全一致するか.
- ただし、 詳細設計と T061-T064 main 実装の不一致 (上記「重要な前提」 参照) については main 経路を採用しているか.
- `StageType` Literal の 7 値 (canonical_five / mission_inf_gap / bc_eval / stage_a / stage_b / stage_c_lite / stage_c) と `FailureReason` Literal の 4 値 (exception_raised / contract_violation / non_finite_detected / state_inconsistency) が概念設計と一致するか.

## H2: 例外 catch 階層 (synthesis § 7.7)

- `except ValueError as exc:` (contract_violation) → `except Exception as exc:` (exception_raised) の 2 段 catch 構造が正しいか.
- BaseException 系 (SystemExit / KeyboardInterrupt / GeneratorExit / asyncio.CancelledError) が透過するか (Python 3.13 で `except Exception` が BaseException を捕捉しない仕様準拠).
- `_truncate_exception_message` が EXCEPTION_MESSAGE_MAX_LENGTH (500) 切詰めを Unicode safe (code point 単位) で実装しているか.

## H3: 4 段検査順序 (例外 → finite → state invariant)

- 各 wrapper (`evaluate_canonical_five_safe` / `evaluate_mission_inf_gap_safe` / `evaluate_bc_safe`) で「例外 catch (2 段) → finite check → state invariant check」 の 4 段構造が正しい順序か.
- 各段で failure 検出時に degraded result + FailureRecord + should_skip_downstream=True を返すか.
- success 時は (実 result, None, False) を返すか.

## H4: degraded result builder の T061-T064 main field 完全突合

- `build_degraded_canonical_five` が main `CanonicalFiveResult` の全 field (sr_session_worst_block_scale, sr_session_worst_annual_estimate, net_pnl_after_cost, max_dd, trade_count, session_block_win_rate_worst, per_bucket_sr, per_bucket_wr, low_sample_buckets, slack_*, gate_worst_gap, gate_pass, log_pf_clip, bucket_validator_version, invariants) を埋めているか.
- `build_degraded_canonical_five` で `invariants.is_feasible == False` を確実にする infeasible_reason_codes 設定 (`INPUT_EMPTY_TRADE_LIST` 等) が正しいか. 全 slack_*=-inf / log_pf_clip=DEGRADED_LOG_PF_CLIP_FLOOR (-2.0) / gate_worst_gap=+inf / gate_pass=False か.
- `build_degraded_mission_gap` が is_feasible=False / mission_inf_gap=+inf / constraint_violation=+inf / mission_signed_margin=-inf (= MISSION_SIGNED_MARGIN_INFEASIBLE_SENTINEL) / mission_margin=-inf / per_metric_shortfall=MappingProxyType({}) になっているか.
- `build_degraded_bc_result` が main `BCEvaluationResult` の全 field (individual_index, b_result, c_lite_result, c_result, mission_pass=FAIL, b_pooled_cf=None, pareto_axis_usable=False, c_pass_depth=0.0) を埋めているか.
- dummy sub-result builder (`_build_dummy_stage_b_result` / `_build_dummy_stage_c_lite_result` / `_build_dummy_stage_c_result`) が main 各 dataclass の `__post_init__` invariant (例: `StageCLiteResult.n_pass_windows <= len(per_window_results)`) を満たすか.

## H5: validate_finite_*  の field 完全列挙

- `validate_finite_canonical_five` が CanonicalFiveResult の全 float field を walk しているか (sr_session_worst_block_scale / sr_session_worst_annual_estimate / net_pnl_after_cost / max_dd / session_block_win_rate_worst / log_pf_clip / gate_worst_gap / 5 slack / per_bucket_sr 値 / per_bucket_wr 値). trade_count は int で除外.
- `validate_finite_mission_gap` が NaN のみ failure 化、 ±inf sentinel 許容 (詳細 D6).
- `validate_finite_bc_result` が top-level (c_pass_depth) + nested (b_pooled_cf / b_result.pooled_dd_per_fold_max / b_result.b_pooled_cf_result / b_result.per_fold_results 各 / c_lite_result.cells_worst / c_lite_result.per_window_results 各 / c_result.c_cf_result / c_result.stress_cf_result / c_result.per_pair_results 各 / c_result.shadow_robustness_score) を完全 walk しているか.

## H6: validate_state_invariant_* の § 8.4 truth table

- `validate_state_invariant_mission_gap` が is_feasible=True ⟺ (math.isfinite(mission_signed_margin) AND >= 0 AND mission_inf_gap == 0.0 AND constraint_violation == 0.0 AND mission_margin == 0.0) を厳密検査しているか.
- is_feasible=False で (mission_inf_gap == 0.0 AND constraint_violation == 0.0) は invariant 違反と判定するか.
- sentinel 符号整合 (mission_signed_margin: 大が良 → +inf 不可、 mission_inf_gap: 小が良 → -inf 不可、 constraint_violation: 小が良 → -inf 不可) を検査しているか.
- `validate_state_invariant_canonical_five` が is_feasible=True で 全 slack_* >= 0 AND finite を検査するか.
- `validate_state_invariant_bc_result` が pareto_axis_usable ⟺ (b_pooled_cf is not None) AND mission_pass=PASS ⟹ c_lite_result.progress_pass=PASS AND c_pass_depth ∈ [0.0, 1.75] を検査するか.

## H7: aggregate_failures + decide_run_abort + build_run_failure_summary

- aggregate_failures が stage filter (records から `r.stage == stage` のみ抽出) を行うか (Round 1 [W3]).
- failed_genome_ids が `tuple(sorted({...}))` で deterministic order か (詳細 Round 1 [C2]).
- n_failed_genomes が一意 genome 数 (record 数ではない、 Round 1 [C1]).
- `n_failed_genomes > eligible_count` で ValueError raise (defense in depth).
- `eligible_count < 0` で ValueError raise.
- `eligible_count=0` で all_failed=False / failure_rate=0.0.
- decide_run_abort が `summary.all_failed` のみ参照 (n_failed_genomes ベース判定).
- build_run_failure_summary が run_id 非空チェック + tuple 化 + any_stage_all_failed 集約.

## H8: 規範継承 (T058-T067 規範)

- 既存 caller signature 完全維持 (T068 は新規 module で touch なし).
- status field 方式 (FailureReason の Literal[…]).
- collider bias 独立性 (本 module は holiday_markets / dst_transition_markets / observability_flags 系に触れていない).
- SSOT 厳密性 (T061-T064 main dataclass 経路で構築).
- 半開区間 [start, end) — 該当箇所なし (本 module は state 検証のみ).
- ruff B905 `zip(strict=True)` — zip 利用なし.
- no-raise contract — wrapper / validator / degraded builder / aggregate は raise しない (例外: aggregate_failures の入口契約 ValueError + build_run_failure_summary の run_id 空チェック ValueError).
- MappingProxyType — `failures_by_reason: Mapping[str, int]` 型注釈 + 実体 MappingProxyType (詳細 Round 1 [C3]).
- NaN fail-fast — validate_finite_* で NaN 即検出.
- default-deny — degraded 個体は infeasible / mission_pass=FAIL.
- state を input dataclass に含めない — FailureRecord は per-individual / per-stage、 state は持たない.
- keyword 引数注入規範 — 全 wrapper で `*` 区切り keyword-only.
- frozen=True dataclass 統一.
- flat 配置 — `src/alpha_factory/failure_handling.py` (T065-T067 と同方針).
- monkeypatch 評価経路 test — evaluate_fn を caller DI で渡す API で対応 (mock 直接渡せる).

## H9: 5 段階 grep DoD

Phase 1 期待値 (= failure_handling.py が他 module から import されない、 0 件) を満たしているか:

```
段階 1: grep -rn --exclude-dir=tests -E "^from src\.alpha_factory\.failure_handling import|^import src\.alpha_factory\.failure_handling" src/alpha_factory scripts
段階 2: grep -rn -E "^import src\.alpha_factory\.failure_handling as|^from src\.alpha_factory\.failure_handling import .* as" src/alpha_factory tests scripts
段階 3: grep -rn -E "from \. import failure_handling|from \.failure_handling import" src/alpha_factory
段階 4: grep -rn -E "failure_handling|FailureRecord|FailureSummary|EvaluationOutcome|aggregate_failures|decide_run_abort" src/alpha_factory/__init__.py
段階 5: grep -rn -E "evaluate_canonical_five_safe|evaluate_mission_inf_gap_safe|evaluate_bc_safe|build_run_failure_summary|aggregate_failures|decide_run_abort" src/alpha_factory/stage_gate.py src/alpha_factory/archive.py scripts/alpha_factory/run_ga.py
```

期待: 全段階 0 件 (テスト以外).

## H10: テスト網羅性

- 詳細設計 § 施策 2 (11 sub-suite × 約 75 件) と test_failure_handling.py (11 sub-suite × 112 件) の sub-suite 完全カバー.
- PR DoD 必須 5 件 (sub-suite 2.1 の `*_catches_value_error_as_contract_violation` / `*_catches_general_exception_as_exception_raised` / `decide_run_abort_returns_true_when_all_failed_genomes_equals_eligible_count` / `aggregate_failures_uses_stage_local_eligible_count_as_denominator` / `validate_state_invariant_mission_gap_detects_is_feasible_inconsistency`) が等価 test 名で全部含まれている.
- BaseException 透過 test (KeyboardInterrupt / SystemExit / GeneratorExit) が wrapper 3 種で実装されているか (3 × 3 = 9 件).
- non_finite_detected / state_inconsistency / contract_violation / exception_raised の 4 reason coverage.
- frozen dataclass 検証 test (FailureRecord / FailureSummary / RunFailureSummary / EvaluationOutcome 4 件) が含まれているか.
- MappingProxyType test (failures_by_reason / per_metric_shortfall) が含まれているか.
- Unicode-safe truncation test (日本語 + emoji を含む長 string).

# 出力フォーマット

```
## 本分析の前提 (C4 規範)
- 前提 1 (verified)
- 前提 2 (verified)
...

## H1 — 詳細設計 SSOT 整合性
verdict: APPROVED / [Critical] / [Warning] / [Suggestion] / INCONCLUSIVE
fact: ...
interpretation: ...

## H2 — 例外 catch 階層
...

(以下 H10 まで)

## 総合 verdict
APPROVED / Round 続行
（APPROVED でない場合: 必須対応事項を [Critical] / [Warning] / [Suggestion] 別に列挙）
```

# 参照ファイル

- 実装: `src/alpha_factory/failure_handling.py`
- テスト: `tests/alpha_factory/test_failure_handling.py`
- 詳細設計: `devnotes/20260430-1430-todo-T068-failure-handling/detailed-design.md`
- 概念設計: `devnotes/20260430-1430-todo-T068-failure-handling/conceptual-design.md`
- T064 main: `src/alpha_factory/stage_bc_evaluator.py` (BCEvaluationResult / StageBResult / StageCLiteResult / StageCResult / StagePassStatus / SampleSizeFlag / MissionFailReason)
- T062 main: `src/alpha_factory/mission_inf_gap.py` (MissionGapResult / MISSION_SIGNED_MARGIN_INFEASIBLE_SENTINEL)
- T061 main: `src/alpha_factory/canonical_metrics.py` (CanonicalFiveResult / InvariantFlags / InfeasibleReasonCode / SessionBucket / LOG_PF_CLIP_RANGE)
- T067 main: `src/alpha_factory/loop_closure.py` (規範参考)

DoD: ruff clean / mypy clean / pytest tests/alpha_factory/test_failure_handling.py 112 passed / pytest tests/alpha_factory 1747 passed / pytest tests/scripts 189 passed (regression 0) / 5 段階 grep DoD 全 0 件.

レビューを開始してください.
