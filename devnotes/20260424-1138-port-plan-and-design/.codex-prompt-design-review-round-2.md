【思考原則】まず仮説。データに真摯。先人の知恵。機能の名前。値より構造。因果ループ切断禁止。

【使命】live_criteria 全達成 + ii-lite 通過の FX イントラデイ個体発見。

【絶対制約】イントラデイ / 両方向 / スワップ・スプレッド反映。

【禁止事項】1.期間延長 2.見栄え 3.GAハック 4.live_criteria緩和 5.複雑化 6.取引回数削減 7.オーバーナイト 8.値伝搬漏れ。

【ツール使用制限】テキスト分析のみ。

【C1-C9】Design-first / X無し=バグ禁止 / Collider / 前提明示 / framing / Fact-Interpretation / Sample size / INCONCLUSIVE / Falsification-first。

---

あなたは Round 1 で CHANGES_REQUESTED を出した詳細設計レビュアーです。Round 2 として再レビューします。

【Round 1 の指摘】
1. [Critical] obsolete コマンド呼び出し契約が実装と不一致（`--id --closed-at --run-id` は失敗）
   修正案: `obsolete "{todo_id}" --obsoleted-at "..." --reason "..."`
2. [Suggestion] 状態ファイル責務表に `deferred_analyses` 未記載なのに本文で追記している

【Round 2 の対応】
1. obsolete コマンドを修正案通りに置換: `obsolete "{todo_id}" --obsoleted-at "..." --reason "trigger stale in plan-and-design"`
2. 状態ファイル責務表に `deferred_analyses` 行を追加（standalone サイクル時 Phase B-1 完了時に append）

【判定依頼】
上記 2 点の対応で Round 1 の Critical / Suggestion が解消されたか判定してください。
他に新規 Critical / Warning があれば指摘してください。

【出力形式】
- 対応充足度評価
- 新規指摘あれば [Critical] [Warning] [Suggestion]
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE

【ループ収束ルール】APPROVED または「1 反証可能仮説 + 1 最小変更」に収束。

---

## 詳細設計（修正後）の該当箇所

### 修正 1: A-1 Conditional 自動昇格チェック内

```bash
# 陳腐化時
uv run python scripts/alpha_factory/todo_manager.py obsolete "{todo_id}" \
  --obsoleted-at "$(TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M')" \
  --reason "trigger stale in plan-and-design"
```

### 修正 2: 状態ファイル更新責務表（書き込み key 表）

| key | 書き込みタイミング | 値 |
|-----|------------------|-----|
| `skill` | Phase 開始時 | `"plan-and-design"` |
| `phase` | 各 Phase 開始時 | `"phase_A"` / `"phase_B"` / `"phase_C"` |
| `phase_detail` | 各 Step 開始時 | 現在の Step 名 |
| `tmp_dir` | Phase A 開始時 | 引数 `tmp_dir` |
| `cycle_focus` | Phase A 完了時 | `"ga_improvements"` / `"todos"` / `"mixed"` |
| `selected_todos` | Phase A 完了時 | 採用 TODO ID リスト |
| `skip_todos` | Phase A 完了時 | `[{id, reason}]` |
| `merge_candidates` | Phase A 完了時 | `[{ids, note}]` |
| `deferred_analyses` | Phase B-1 完了時（standalone サイクル時のみ） | `analysis-merged.md` パスを append |
| `improvement_plan` | Phase B-4 完了時 | `improvement-plan.md` のパス |
| `detailed_design` | Phase C-3 完了時 | `detailed-design.md` のパス |
| `last_updated` | 各書き込み時 | ISO8601 |

**読むのみで書き込まない** key（`zenigame-fx-improve-cycle` 管理）: `cycle_index` / `run_number` / `next_run_number` / `overrides` / `history` / `repeat_mode` / `observe_only` / `started_at`。

---

その他のセクション（Phase A/B/C プロンプト、合議ループ収束ルール、focus-theme fallback、未接続 hook、エラーハンドリング、使用例、テスト計画、リスク等）は Round 1 と同じ。Round 1 で 2,4,5,6,7,8,9,10,13 観点が「概ね整合」と判定されており、これらは未変更。

判定をお願いします。
