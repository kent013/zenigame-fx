# Cycle 22 詳細設計レビュー Round 3 (Round 2 残課題への対応)

## Round 2 残課題への対応

| # | Round 2 指摘 | 対応 | 詳細 |
|---|------|------|------|
| 1 | `median_oos_total_pnl >= 0` だけでは中央値で aggregate 赤字を許容 (11 小プラス + 9 大マイナス で median > 0 でも sum < 0) | **採用** | `sum_oos_total_pnl >= 0` を gate 条件 #3 として追加。collect / payload / archive / reason code / known_reason_codes / tests も同期 |
| 2 | finite guard を profit_safe_pfr 用 PnL 集計に隔離、legacy 経路の fold_sharpe / fold_reason は完全不変 | **採用** | `fold_total_pnl` 非有限のとき `oos_total_pnls.append(...)` をスキップするのみ。legacy 経路の `fold_sharpe` / `fold_reason` / `reason_counts` は触らない |

## 修正後コード (Round 3 確定)

### 1. fold loop (legacy 経路完全分離)

```python
import math
oos_total_pnls: list[float] = []  # profit_safe_pfr 専用、finite のみ

# 既存 fold_sharpe 代入直後 (legacy 経路の fold_sharpe / fold_reason / reason_counts は触らない):
fold_total_pnl_candidate: float | None = None
if bt.total_pnl is not None:
    candidate = float(bt.total_pnl)
    if math.isfinite(candidate):
        fold_total_pnl_candidate = candidate
if fold_total_pnl_candidate is not None:
    oos_total_pnls.append(fold_total_pnl_candidate)
# else: 非有限/None は oos_total_pnls に追加しないだけ。legacy 経路は完全不変
```

### 2. 集計 (median + sum)

```python
median_oos_total_pnl: float | None = None
sum_oos_total_pnl: float | None = None
if len(oos_total_pnls) >= 2:
    median_oos_total_pnl = float(_stats.median(oos_total_pnls))
    sum_oos_total_pnl = float(sum(oos_total_pnls))
elif len(oos_total_pnls) == 1:
    median_oos_total_pnl = float(oos_total_pnls[0])
    sum_oos_total_pnl = float(oos_total_pnls[0])
```

### 3. 判定分岐 (4 条件 AND)

```python
if stage_config.stage_b_gate_kind == "legacy":
    # 完全不変
    if median_oos < stage_config.stage_b_median_oos_sharpe_min:
        reasons.append("median_oos_sharpe<min")
    if positive_ratio < stage_config.stage_b_positive_fold_min:
        reasons.append("positive_fold_ratio<min")
elif stage_config.stage_b_gate_kind == "profit_safe_pfr":
    # 4 条件 AND:
    if positive_ratio_effective is None or positive_ratio_effective < stage_config.profit_safe_pfr_threshold:
        reasons.append("positive_fold_ratio_effective<min")
    if median_oos_total_pnl is None or median_oos_total_pnl < 0:
        reasons.append("median_oos_total_pnl<min")
    if sum_oos_total_pnl is None or sum_oos_total_pnl < 0:
        reasons.append("sum_oos_total_pnl<min")  # Round 3 追加
    if n_fold_effective < stage_config.profit_safe_pfr_min_n_fold:
        reasons.append("n_fold_effective_below_profit_safe_min")
else:
    raise ValueError(f"unknown stage_b_gate_kind: {stage_config.stage_b_gate_kind}")
```

### 4. payload 拡張 (sum_oos_total_pnl 追加)

```python
"payload": {
    # 既存 key 完全不変 ...
    "stage_b_gate_kind": stage_config.stage_b_gate_kind,
    "median_oos_total_pnl": median_oos_total_pnl,
    "sum_oos_total_pnl": sum_oos_total_pnl,  # Round 3 追加
    "oos_total_pnls": tuple(oos_total_pnls),  # effective fold only
    "profit_safe_pfr_threshold": stage_config.profit_safe_pfr_threshold,
    "profit_safe_pfr_min_n_fold": stage_config.profit_safe_pfr_min_n_fold,
}
```

### 5. archive 列 + tests/test_archive.py 更新 (Round 3 修正)

`collect_stage_b` で `entry["sum_oos_total_pnl"] = payload.get("sum_oos_total_pnl")` 追加。expected_columns に 3 列 (median / sum / kind) 追加。

### 6. generate_run_report.py known_reason_codes 4 件追加 (Round 3 修正)

```python
known_reason_codes |= {
    "positive_fold_ratio_effective<min",
    "median_oos_total_pnl<min",
    "sum_oos_total_pnl<min",  # Round 3 追加
    "n_fold_effective_below_profit_safe_min",
}
```

### 7. テスト追加 (Round 3 修正)

新規:
- `test_stage_b_profit_safe_pfr_sum_pnl_negative_fail`: median >= 0 でも sum < 0 で fail
- `test_stage_b_profit_safe_pfr_aggregate_robustness`: 11 fold +100 + 9 fold -200 で median=100 > 0、sum=-700 < 0 → fail (Round 2 想定シナリオ)
- `test_stage_b_legacy_fold_paths_unchanged_under_nonfinite_pnl`: legacy mode で `bt.total_pnl` が NaN/Inf でも fold_sharpe/fold_reason が変化しないことを確認 (隔離保証)

## 質問

**Q (Round 3)**: 上記対応で Round 2 の Warning 2 件が解消されたか? APPROVED で良いか?

特に:
1. `sum_oos_total_pnl >= 0` 追加で aggregate 赤字構造を防げるか
2. finite guard が legacy 経路の fold_sharpe/fold_reason を一切触らない設計になっているか

## 出力形式

- Round 2 指摘ごとの対応確認 (RESOLVED / PARTIAL / UNRESOLVED)
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 1 ラウンドで APPROVED 収束を強く目指す。

日本語で。
