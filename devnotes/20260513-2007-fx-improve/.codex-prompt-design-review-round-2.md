# Cycle 22 詳細設計レビュー Round 2 (Round 1 REQUEST_CHANGES への対応)

## 対応マトリクス

Round 1 で指摘された Critical 3 / Warning 3 / Suggestion 1 への対応:

| # | 指摘 | 重要度 | 対応 | 詳細 |
|---|------|--------|------|------|
| 1 | post-filter が archive 集計後だと stage_b_pass=False 反転後も stage_c_pass=True が残る不整合 | Critical | **採用 (廃止)** | `trade_sharpe_stage_b > 0` 条件を **削除**。`evaluate_stage_b` 内 3 条件 (pfr + median_oos_total_pnl + n_fold_effective) のみで判定。trade_sharpe 条件は次サイクル別 TODO へ |
| 2 | config 4 段伝搬不足 (_build_stage_gate / compute_base_config_hash) | Critical | **採用** | `_build_stage_gate` に新 field 3 個追加、`compute_base_config_hash` にも反映 |
| 3 | trade_sharpe_stage_b の None / 非有限 fail-closed | Critical | **間接対応 (#1 で消滅)** | post-filter 廃止により trade_sharpe_stage_b 条件自体を削除。non-finite guard は median_oos_total_pnl 側に適用 |
| 4 | median_oos_total_pnl NaN 防御 (math.isfinite) | Warning | **採用** | fold loop で `bt.total_pnl` を `math.isfinite` check、非有限は unavailable 扱い (= fold_sharpe=None と同じパスへ) |
| 5 | test_archive.py 列集合 assertion 更新 | Warning | **採用** | 新規列 `median_oos_total_pnl` / `stage_b_gate_kind` を expected_columns に追加 |
| 6 | generate_run_report.py known reason codes 追加 | Warning | **採用** | 3 個追加 (`positive_fold_ratio_effective<min` / `median_oos_total_pnl<min` / `n_fold_effective_below_profit_safe_min`) |
| 7 | StageGateConfig.__post_init__ 範囲検証 | Suggestion | **採用** | threshold finite 0..1、min_n_fold >= 1、stage_b_gate_kind 列挙チェック |

## 修正後の詳細設計 (差分要点)

### 1. evaluate_stage_b 判定 (trade_sharpe_stage_b 削除)

```python
if stage_config.stage_b_gate_kind == "legacy":
    # 完全不変
    if median_oos < stage_config.stage_b_median_oos_sharpe_min:
        reasons.append("median_oos_sharpe<min")
    if positive_ratio < stage_config.stage_b_positive_fold_min:
        reasons.append("positive_fold_ratio<min")
elif stage_config.stage_b_gate_kind == "profit_safe_pfr":
    # 3 条件 AND のみ:
    if positive_ratio_effective is None or positive_ratio_effective < stage_config.profit_safe_pfr_threshold:
        reasons.append("positive_fold_ratio_effective<min")
    if median_oos_total_pnl is None or median_oos_total_pnl < 0:
        reasons.append("median_oos_total_pnl<min")
    if n_fold_effective < stage_config.profit_safe_pfr_min_n_fold:
        reasons.append("n_fold_effective_below_profit_safe_min")
else:
    raise ValueError(f"unknown stage_b_gate_kind: {stage_config.stage_b_gate_kind}")
```

### 2. fold loop での finite guard (NaN 防御)

```python
import math
oos_total_pnls: list[float] = []  # effective fold のみ

# fold loop 内 fold_sharpe 代入後:
fold_total_pnl: float | None = None
if bt.total_pnl is not None:
    candidate = float(bt.total_pnl)
    if math.isfinite(candidate):
        fold_total_pnl = candidate

if fold_total_pnl is not None:
    oos_total_pnls.append(fold_total_pnl)
else:
    # 非有限 → fold 失敗扱い (legacy fold_sharpe=None と同じパス)
    if fold_reason is None:
        fold_reason = FoldUnavailableReason.OTHER
    fold_sharpe = None

# 集計時:
median_oos_total_pnl: float | None = None
if len(oos_total_pnls) >= 2:
    median_oos_total_pnl = float(_stats.median(oos_total_pnls))
elif len(oos_total_pnls) == 1:
    median_oos_total_pnl = float(oos_total_pnls[0])
```

effective fold 0 のときは `median_oos_total_pnl = None` で profit_safe_pfr mode は `median_oos_total_pnl<min` で fail-closed。

### 3. config 4 段伝搬

`src/alpha_factory/config.py:508` `_build_stage_gate`:

```python
kwargs["stage_b_gate_kind"] = str(raw.get("stage_b_gate_kind", "legacy"))
kwargs["profit_safe_pfr_threshold"] = float(raw.get("profit_safe_pfr_threshold", 0.4))
kwargs["profit_safe_pfr_min_n_fold"] = int(raw.get("profit_safe_pfr_min_n_fold", 20))
```

`src/alpha_factory/calibrate_state.py:56` `compute_base_config_hash` に新 field 3 個追加。

### 4. StageGateConfig.__post_init__ 範囲検証

```python
import math
if not math.isfinite(self.profit_safe_pfr_threshold):
    raise ValueError(f"profit_safe_pfr_threshold must be finite, got {self.profit_safe_pfr_threshold}")
if not (0.0 <= self.profit_safe_pfr_threshold <= 1.0):
    raise ValueError(f"profit_safe_pfr_threshold must be in [0, 1], got {self.profit_safe_pfr_threshold}")
if self.profit_safe_pfr_min_n_fold < 1:
    raise ValueError(f"profit_safe_pfr_min_n_fold must be >= 1, got {self.profit_safe_pfr_min_n_fold}")
if self.stage_b_gate_kind not in ("legacy", "profit_safe_pfr"):
    raise ValueError(f"stage_b_gate_kind must be 'legacy' or 'profit_safe_pfr', got {self.stage_b_gate_kind!r}")
```

### 5. archive 列追加 + tests/test_archive.py 更新

```python
# src/alpha_factory/archive.py collect_stage_b 内:
entry["median_oos_total_pnl"] = payload.get("median_oos_total_pnl")
entry["stage_b_gate_kind"] = payload.get("stage_b_gate_kind", "legacy")

# tests/alpha_factory/test_archive.py:183 付近 expected_columns:
{"...", "median_oos_total_pnl", "stage_b_gate_kind"}
```

GENOME_ENTRY_SCHEMA_VERSION = 2 据え置き (optional/nullable column 追加で互換)。

### 6. generate_run_report.py known reason codes 追加

```python
# scripts/alpha_factory/generate_run_report.py:534 付近:
known_reason_codes = {
    # 既存全 codes ...
    "positive_fold_ratio_effective<min",
    "median_oos_total_pnl<min",
    "n_fold_effective_below_profit_safe_min",
}
```

### 7. テスト計画 (~15 件)

- 既存 stage_gate テスト ~10 件
- 追加 Round 2 対応 テスト:
  - `test_stage_b_profit_safe_pfr_nonfinite_fold_total_pnl`: NaN/Inf で fold unavailable
  - `test_stage_gate_config_post_init_validation`: 範囲外で ValueError
  - `test_build_stage_gate_reads_new_fields`: yaml の 3 field を読む
  - `test_compute_base_config_hash_new_fields`: 新 field 変更で hash が変わる
  - `test_generate_run_report_new_reason_codes`: 新 reason code が known_reason_codes に含まれる

---

## 質問

**Q (Round 2)**: 上記対応で Round 1 の Critical 3 + Warning 3 + Suggestion 1 が全て解消されたか? 全体判定 APPROVED で良いか?

特に以下を確認:
1. trade_sharpe_stage_b 条件削除で本施策の有効性は維持されるか (median_oos_total_pnl >= 0 だけで赤字許容構造を解消できるか)
2. finite guard で fold_total_pnl 非有限を unavailable 扱いにすることで legacy 経路に影響しないか
3. config 4 段伝搬 + compute_base_config_hash の対応で漏れがないか

---

## 出力形式

- Round 1 指摘ごとの対応確認 (RESOLVED / PARTIAL / UNRESOLVED)
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 残課題があれば最小変更で明示
- 1 ラウンドで APPROVED 収束を強く目指す。

日本語で。
