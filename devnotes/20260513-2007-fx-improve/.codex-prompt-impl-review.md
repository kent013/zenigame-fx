# T099 cycle 22 実装レビュー依頼 (Round 1)

## 役割

あなたは経験豊富な Python コードレビュアーです。zenigame-fx Alpha Factory の T099 cycle 22 (profit_safe_pfr opt-in) 実装をレビューしてください。

zenigame-fx-codex-review の使命・禁止事項・C1-C9 を継承。FX 固有制約 (イントラデイ / ロング・ショート / スワップ・スプレッド純利益反映)。

【レビュー観点】
1. 設計との一致性 (devnotes/20260513-2007-fx-improve/detailed-design.md APPROVED Round 3)
2. 正確性 (ロジック / エッジケース / NaN)
3. パフォーマンス
4. 一貫性 (命名、pattern)
5. テスト網羅性
6. ruff / mypy 準拠
7. 禁止事項違反なし

【転記漏れ・伝搬漏れチェック】
- config → GaConfig → genome.meta → consumer の 4 段伝搬
- GENOMES_SCHEMA → _create_row_template → collect_stage_* → flush 出力
- 新規 column の 4 点セット (schema / template / collect / test)

【ループ収束ルール】1 ラウンドで APPROVED 収束を強く目指す。Critical なしならば即 APPROVED。

【出力形式】
- ファイルごとに判定 (APPROVE / REQUEST_CHANGES / INCONCLUSIVE)
- [Critical] / [Warning] / [Suggestion] と修正案
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 日本語

---

## 詳細設計書 (APPROVED Round 3)

`devnotes/20260513-2007-fx-improve/detailed-design.md` の確定設計。要点:

### C1: Stage B gate `profit_safe_pfr` opt-in mode

- 新規 StageGateConfig field 3 個 (default 値で完全互換):
  - `stage_b_gate_kind: Literal["legacy", "profit_safe_pfr"] = "legacy"`
  - `profit_safe_pfr_threshold: float = 0.4`
  - `profit_safe_pfr_min_n_fold: int = 20`
- `evaluate_stage_b` 改修:
  - fold loop で profit_safe_pfr 用 `oos_total_pnls: list[float]` を集計 (finite な fold-level total_pnl のみ、legacy 経路の fold_sharpe / fold_reason / reason_counts は完全不変)
  - 集計: `median_oos_total_pnl` / `sum_oos_total_pnl`
  - 判定分岐: legacy 完全不変 vs profit_safe_pfr (4 条件 AND + Round 3 oos_total_pnl_unavailable fail-closed)
  - payload に新規 6 key 追加 (両 mode で記録、観測列)
- archive 列 3 個追加 (`median_oos_total_pnl` / `sum_oos_total_pnl` / `stage_b_gate_kind`)、GENOME_ENTRY_SCHEMA_VERSION = 2 据え置き
- config 4 段伝搬: `_build_stage_gate` 3 field + `compute_base_config_hash` 3 field
- StageGateConfig.__post_init__ 範囲検証
- CLI `--stage-b-gate-kind {legacy, profit_safe_pfr}`
- generate_run_report.py known_reason_codes 5 個追加 (`positive_fold_ratio_effective<min` / `median_oos_total_pnl<min` / `sum_oos_total_pnl<min` / `n_fold_effective_below_profit_safe_min` / `oos_total_pnl_unavailable`)

---

## 実装差分

実装 diff は `devnotes/20260513-2007-fx-improve/.codex-impl-diff.txt` (573 行)。

主な変更ファイル (合計 8 files, +406/-5 行):
| ファイル | 変更行 | 概要 |
|---------|--------|------|
| `src/alpha_factory/stage_gate.py` | +94 | StageGateConfig 3 field + __post_init__ 検証 + evaluate_stage_b fold loop / 判定分岐 / payload |
| `src/alpha_factory/config.py` | +8 | `_build_stage_gate` に 3 field 追加 |
| `src/alpha_factory/calibrate_state.py` | +5 | `compute_base_config_hash` に 3 field 追加 |
| `src/alpha_factory/archive.py` | +26 | schema 3 列 + _create_row_template 3 行 + collect_stage_b 抽出 |
| `scripts/alpha_factory/run_ga.py` | +33 | CLI `--stage-b-gate-kind` 追加 + main override + log |
| `scripts/alpha_factory/generate_run_report.py` | +6 | known_reason_codes 5 個追加 |
| `tests/alpha_factory/test_archive.py` | +8 | expected_columns 3 列追加 |
| `tests/alpha_factory/test_stage_gate.py` | +231 | T099 用 5 クラス 16 テスト追加 |

完全 diff は `.codex-impl-diff.txt` を参照。

---

## テスト結果

- **T099 新規 16 件: 全 pass**
  - TestT099ProfitSafePfrConfig: 6 件 (defaults / 範囲検証)
  - TestT099BuildStageGateReadsNewFields: 2 件 (yaml loader)
  - TestT099ComputeBaseConfigHashNewFields: 3 件 (gate_kind / threshold / min_n_fold 切替で hash 変化)
  - TestT099EvaluateStageBPayloadKeys: 2 件 (payload key 確認)
  - TestT099ProfitSafePfrReasonCodes: 3 件 (legacy 完全不変 + profit_safe_pfr fail + 隔離保証)
- **既存 test_archive (112) + test_stage_gate (83) 全 pass** (default legacy で完全互換)
- **全体: 3109 passed, 2 skipped, 7 deselected, 1 xfailed**
- **既存 failure 15 件は T095 案件 (T092 fold guard fixture)、本変更とは無関係**:
  - `tests/scripts/test_run_ga_parallel.py` 13 件、`tests/scripts/test_alpha_factory_run_ga.py` 2 件、`tests/alpha_factory/test_t058_integration.py` 1 件 (= 計 15 件) 全て同じ `stage_b_fold_guard.evaluating: compute_max_folds=4 < wf_min_safe_folds=5` で fail。main HEAD でも同じ failure。

## ruff / mypy

- **ruff**: T099 関連ファイルで clean。`scripts/alpha_factory/run_ga.py:192` の E501 は既存 issue (本 T099 範囲外)。
- **mypy**: `src/alpha_factory/archive.py:335` の `float | None` narrowing 警告は **既存問題 (TODO T096)**、本 T099 変更とは無関係。T099 関連変更は mypy clean。

---

## 質問

### Q1. ファイル別 APPROVE/REQUEST_CHANGES 判定

各ファイルの変更について Critical / Warning がないか確認。特に:
- `stage_gate.py` の fold loop での finite guard が legacy 経路に副作用を与えていないか (fold_sharpe / fold_reason / reason_counts に触れていない)
- `evaluate_stage_b` 判定分岐の `else` 節 (`raise ValueError`) が __post_init__ で阻止される前提で OK か
- `archive.py` の collect_stage_b で `payload.get` の型 check (`isinstance(gate_kind, str)`) は妥当か
- `run_ga.py` の override 処理が dataclass.replace で frozen 維持されているか

### Q2. テスト網羅性

新規 16 件テストで以下を担保:
- StageGateConfig 新 field defaults / 範囲検証 ✅
- yaml loader 経由 4 段伝搬 ✅
- compute_base_config_hash cross-run scope key 反映 ✅
- payload key 5 個追加 (両 mode) ✅
- legacy 経路完全不変 (隔離保証) ✅
- profit_safe_pfr mode fail-closed ✅

考慮漏れがあれば指摘ください。

### Q3. 設計と実装の一致性

詳細設計 (Round 3 APPROVED) で挙げた条件:
- pfr_effective >= 0.4 ∧ median_oos_total_pnl >= 0 ∧ sum_oos_total_pnl >= 0 ∧ n_fold_effective >= 20
- + Round 3 受入条件 `oos_total_pnl_unavailable` (= len(oos_total_pnls) < n_fold_effective で fail-closed)
- finite guard を profit_safe_pfr 用 PnL 集計に隔離 (legacy 経路完全不変)

これらが正確に実装されているか確認してください。

### Q4. 30 RUN 連続ループ実行中の影響

cycle 22 で T099 を実装、cycle 23-51 (29 cycle) で seed sweep する予定。default `legacy` で完全互換のため、cycle 23 以降の seed sweep には影響しない設計。これは妥当か?

---

## 出力形式

- 各ファイルの判定 (APPROVE / REQUEST_CHANGES / INCONCLUSIVE)
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 1 ラウンド APPROVED 目標

日本語で。
