【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】
（C1-C9 適用）

---

## システム指示

あなたは経験豊富な Python コードレビュアーです。zenigame-fx Alpha Factory の改善実装をレビューしてください。

【レビュー観点】
1. 設計との一致性（detailed-design.md と diff の対応）
2. 正確性（ロジック / エッジケース / NaN / float 演算順序の等価性）
3. パフォーマンス（cache hit/miss コスト、メモリ）
4. 一貫性（命名規約、import 順、docstring）
5. テスト網羅性（cache invariance / LRU / id reuse guard / lockstep 等）
6. ruff / mypy 準拠（既に PASS 確認済み、観点としてのみ）
7. 禁止事項違反なし
8. selection invariance リスク（pair_specific.py のオリジナル実装は中間変数を作らず `(float(b.bid.open) + float(b.ask.open)) * 0.5` を直接書いていたが、新統合実装は中間変数 `bo, ao` を経由する。IEEE 754 float 加算は associativity を持つため、同 expression の中間変数化は bit-identical。この equivalence 検証も観点に含める）

【重点チェック】
- import 境界 (P9): `_bars_cache.py` が `numpy / collections / src.domain.price` のみ依存し、他 primitive module を import していないか
- LRU lockstep invariant: `_CACHE` と `_REFS` の key 集合が常に一致するか
- id 再利用ガード: `_REFS[key] is bars` の identity 検査が functional に正しいか
- alias rename: 各 primitive module で `_bars_to_mid_ohlc` local alias が維持され、内部呼び出し全てが書き換え不要であること

【出力形式】
- ファイルごとに判定
- [Critical] [Warning] [Suggestion]
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 日本語

---

## 詳細設計書

(以下は devnotes/20260425-0351-bars-mid-ohlc-cache/detailed-design.md の主要部抜粋)

### 施策一覧
| # | 施策名 | 変更ファイル |
|---|--------|------------|
| 1 | `_bars_cache.py` 新規 (id+len LRU cache) | `src/alpha_factory/primitives/_bars_cache.py` |
| 2 | 3 モジュールの `_bars_to_mid_ohlc` 重複削除 | `directional_generic.py / modulator_generic.py / pair_specific.py` |
| 3 | cache invariance テスト | `tests/alpha_factory/primitives/test_bars_cache.py` |

### 制約
- Selection invariance: 返却配列は既存 3 モジュールと bit-identical
- import 境界 P9: `_bars_cache.py` から他 primitive module を import 禁止
- thread safety: backtest path は thread-parallel 不可（GA 並列は process-level のみ）
- bars 非再代入契約: 同一 list 内の要素置換 (len 不変) は検知不可（運用契約）

### 期待効果（hypothesis）
- `_bars_to_mid_ohlc` 合計 tottime 60-85% 削減
- profile 全体 4-7% 短縮

---

## 実装差分

```diff
diff --git a/src/alpha_factory/primitives/_bars_cache.py b/src/alpha_factory/primitives/_bars_cache.py
new file mode 100644
+"""bars → mid OHLC 変換の shared cache (Cycle 3 / T030).
... (file content - see below)
"""
+from __future__ import annotations
+from collections import OrderedDict
+from collections.abc import Sequence
+import numpy as np
+from src.domain.price import PriceBar
+
+__all__ = ["MAX_ENTRIES", "bars_to_mid_ohlc", "clear_cache"]
+MAX_ENTRIES: int = 8
+
+_CACHE: OrderedDict[tuple[int, int], tuple[np.ndarray, np.ndarray, np.ndarray, np.ndarray]] = OrderedDict()
+_REFS: OrderedDict[tuple[int, int], Sequence[PriceBar]] = OrderedDict()
+
+def _compute(bars: Sequence[PriceBar]) -> tuple[np.ndarray, np.ndarray, np.ndarray, np.ndarray]:
+    length = len(bars)
+    o = np.empty(length, dtype=np.float64)
+    h = np.empty(length, dtype=np.float64)
+    low = np.empty(length, dtype=np.float64)
+    c = np.empty(length, dtype=np.float64)
+    for i, b in enumerate(bars):
+        bo = float(b.bid.open); bh = float(b.bid.high); bl = float(b.bid.low); bc = float(b.bid.close)
+        ao = float(b.ask.open); ah = float(b.ask.high); al = float(b.ask.low); ac = float(b.ask.close)
+        o[i] = (bo + ao) * 0.5
+        h[i] = (bh + ah) * 0.5
+        low[i] = (bl + al) * 0.5
+        c[i] = (bc + ac) * 0.5
+    return o, h, low, c
+
+def bars_to_mid_ohlc(bars):
+    key = (id(bars), len(bars))
+    cached = _CACHE.get(key)
+    if cached is not None and _REFS.get(key) is bars:
+        _CACHE.move_to_end(key)
+        _REFS.move_to_end(key)
+        return cached
+    result = _compute(bars)
+    _CACHE[key] = result
+    _REFS[key] = bars
+    while len(_CACHE) > MAX_ENTRIES:
+        evicted_key, _ = _CACHE.popitem(last=False)
+        _REFS.pop(evicted_key, None)
+    return result
+
+def clear_cache() -> None:
+    _CACHE.clear()
+    _REFS.clear()

diff --git a/src/alpha_factory/primitives/directional_generic.py
@@
+from src.alpha_factory.primitives._bars_cache import (
+    bars_to_mid_ohlc as _bars_to_mid_ohlc,
+)
@@
-def _bars_to_mid_ohlc(bars) -> tuple[...]:  # (削除)
-    ...

diff --git a/src/alpha_factory/primitives/modulator_generic.py
@@
+from src.alpha_factory.primitives._bars_cache import (
+    bars_to_mid_ohlc as _bars_to_mid_ohlc,
+)
@@
-def _bars_to_mid_ohlc(bars) -> tuple[...]:  # (削除、複製理由コメントも削除)
-    ...

diff --git a/src/alpha_factory/primitives/pair_specific.py
@@
+from src.alpha_factory.primitives._bars_cache import (
+    bars_to_mid_ohlc as _bars_to_mid_ohlc,
+)
@@
-def _bars_to_mid_ohlc(bars: Sequence[PriceBar]) -> tuple[...]:  # (削除)
-    # 元実装は中間変数を経由せず `(float(b.bid.open) + float(b.ask.open)) * 0.5` を直接記述。
-    # 統合実装 _compute は中間変数 (bo, ao) を経由するが式・順序は同じ → IEEE 754 で bit-identical
-    ...

# tests/alpha_factory/primitives/test_bars_cache.py 新規 (9 test):
# - test_cache_hit_returns_identical_arrays
# - test_cache_miss_on_different_bars_object
# - test_cache_miss_on_length_change
# - test_cache_correctness_vs_direct_compute
# - test_lru_eviction_at_max_entries
# - test_lru_hit_updates_order
# - test_cache_and_refs_lockstep
# - test_identity_guard_rejects_id_reuse
# - test_clear_cache_empties_both_dicts
```

完全な diff は以下の path にあります（必要なら Read ツールで参照可能）:
`/Users/ishitoya/repository/zenigame-fx/devnotes/20260425-0413-todo-T030/impl-diff.txt`

実ファイル:
- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T030/src/alpha_factory/primitives/_bars_cache.py`
- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T030/src/alpha_factory/primitives/directional_generic.py`
- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T030/src/alpha_factory/primitives/modulator_generic.py`
- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T030/src/alpha_factory/primitives/pair_specific.py`
- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T030/tests/alpha_factory/primitives/test_bars_cache.py`

詳細設計書:
- `/Users/ishitoya/repository/zenigame-fx/devnotes/20260425-0351-bars-mid-ohlc-cache/detailed-design.md`
- `/Users/ishitoya/repository/zenigame-fx/devnotes/20260425-0351-bars-mid-ohlc-cache/conceptual-design.md`

---

## テスト結果

```
$ uv run pytest tests/alpha_factory/primitives/test_bars_cache.py -x
9 passed in 0.39s

$ uv run pytest tests/alpha_factory/primitives/ -x
262 passed in 3.31s

$ uv run pytest tests/broker/ tests/backtest/ tests/alpha_factory/ tests/dsl/ -x
744 passed, 1 skipped in 9.18s

$ uv run pytest tests/ -x
929 passed, 1 skipped, 3 warnings in 12.59s

$ uv run ruff check src/ tests/
All checks passed!

$ uv run mypy src/
Success: no issues found in 90 source files
```

---

レビューを実施し、APPROVED / CHANGES_REQUESTED / INCONCLUSIVE のいずれかで判定してください。
