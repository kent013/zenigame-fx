## Round 4: Round 3 Critical への対応確認

Round 3 で 1 Critical + 2 Warning を受けました。修正しました。最終確認をお願いします。

### 対応マップ

| Round 3 指摘 | 対応 |
|---|---|
| [Critical] `id(bar)` が Python の object id 再利用で偽 hit を起こし得る | cache key を **object reference そのもの** に変更。`self._cached_bar: PriceBar | None` を保持し、`bar is self._cached_bar` で判定。strong reference を保持するため cache valid な間は object が生存し、id 再利用問題は根絶。新規 test `test_snapshot_object_reference_identity` を追加（同一 bar_time + 同一内容だが別 object で cache miss を確認） |
| [Warning] P9 の説明が「object identity = 内容 identity」では不十分 | P9 を「id 再利用問題なし (strong reference 保持)」を明記するよう書き換え |
| [Warning] invalidate 網羅性の最終確認（cash/positions 書き込み経路の全列挙）が不足 | 実装開始チェックリストに「**invalidate 網羅性チェック**」セクションを追加。`self._cash` 変更経路 (deposit / apply_bar_holding_cost / _close_one) と `self._positions` 変更経路 (_open_position / _close_one) の全 grep + 各末尾に `self._invalidate()` 配置確認を必須化 |

### 差分（前提 P9）

**Before:**
```
| P9 | cache key が snapshot 依存変数を十分に識別する（bar 同一時刻・異価格で stale を返さない） | **Verified** | Round 2 Critical 対応: cache key を `id(bar)` に変更し object identity で判定。PriceBar は `@dataclass(frozen=True)` なので同一 object は必ず同一内容 |
```

**After:**
```
| P9 | cache key が snapshot 依存変数を十分に識別する（bar 同一時刻・異価格で stale を返さない、id 再利用で偽 hit を起こさない） | **Verified** | Round 2 Critical + Round 3 Critical 対応: cache key は `id(bar)` ではなく **object reference を直接保持** (`self._cached_bar: PriceBar | None`) し、`bar is self._cached_bar` で判定。これにより (a) 同一時刻・異価格 (異なる object) は miss、(b) `id()` 再利用問題は起きない（strong reference を保持しているため cache valid な間は object が生存） |
```

### 差分（実装の骨子）

**Before:**
```
- `self._cached_bar_id: int | None = None`  # `id(bar)` を保持
- `_snapshot_at(bar)` の最初で `cache_valid and id(bar) == cached_bar_id` なら cache hit
```

**After:**
```
- `self._cached_bar: PriceBar | None = None`  # **object reference を保持**（Round 3 Critical 対応: `id()` 再利用回避）
- `_snapshot_at(bar)` の最初で `cache_valid and bar is self._cached_bar` なら cache hit
```

### 差分（cache key 設計）

```
**cache key 設計 (Round 2 Critical + Round 3 Critical 対応)**:
- `bar.bar_time` でも `id(bar)` でもなく、**object reference そのもの** を保持（`self._cached_bar: PriceBar | None`）
- 判定は `bar is self._cached_bar`（Python identity 比較）
- **Round 3 Critical 対応**: `id(bar)` は object 解放後に再利用されうる。object reference を保持すれば cache valid な間は strong reference が object を生存させるため、id 再利用問題は根絶
- **Round 2 Critical 対応**: 同一 `bar_time` でも異なる object は miss（object identity = 内容 identity は PriceBar frozen であることから保証）
- engine loop は同一 iteration 内で 3 経路すべて同一 reference を渡すため（P10）、cache hit は確実に発生する
- 次 iteration で異なる bar object が渡された瞬間に `is` comparison が False になり cache miss
```

### 差分（実装開始チェックリストに invalidate 網羅性追加）

```
#### invalidate 網羅性チェック（Round 3 Warning 対応）

MockBroker 内で `self._cash` / `self._positions` を変更する全経路を列挙し、**各経路の末尾に `self._invalidate()` が配置される** ことを verify:

- [ ] **`self._cash` 変更経路**: `grep -n "self\._cash" src/broker/mock.py` で全代入・加減算を列挙。想定: `__init__`（初期化, invalidate 不要）/ `deposit`（+=） / `apply_bar_holding_cost`（-=） / `_close_one`（+= raw_pnl）
- [ ] **`self._positions` 変更経路**: `grep -n "self\._positions\[" src/broker/mock.py` で代入・`pop`・dict 操作を列挙。想定: `_open_position`（追加）/ `_close_one`（pop で削除）/ `_close_all_internal`（_close_one ループ経由、個別 invalidate は `_close_one` が担う）
- [ ] 各経路の末尾に `self._invalidate()` が配置されていることを詳細設計のコードで確認
- [ ] 該当経路外での cash / positions 書き込みが無いことを `grep` で 2 重チェック（tests/ は除外）
```

### 差分（新規 test）

```
- **`test_snapshot_object_reference_identity` (Round 3 Critical)**: 同一 bar_time + 同一内容だが異なる object の PriceBar を渡して、cache miss が発生することを `is` 比較で確認（`id()` 再利用を模擬した test）
```

---

## Round 4 レビュー依頼

Round 1-3 の全 Critical / Warning をすべて対応したつもりです。判定をお願いします:

- **APPROVED**: Phase 2（詳細設計）へ進む
- **CHANGES_REQUESTED**: 残存 Critical / Warning を明示
- **INCONCLUSIVE**: 判断不能な前提不足があれば明示

特に以下を確認:
1. object reference cache で id 再利用問題が真に解消されているか
2. invalidate 網羅性チェックリストが十分か（cash / positions 以外の隠れた mutation が無いか）
3. 既存の conceptual design で他に未解決の Critical が無いか
