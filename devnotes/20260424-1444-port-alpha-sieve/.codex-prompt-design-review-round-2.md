## Round 2: 修正対応

### Round 1 NEEDS_REVISION への対応マトリクス

| Round 1 指摘 | 対応内容 |
|--------------|----------|
| `_compute_dsr_safe` と `statistics.compute_dsr` のシグネチャ確定 | ✅ §2.8 全面書き換え。`statistics.deflated_sharpe_ratio` の実シグネチャを引用し、`n_trials>=2 / mean_sr_trials / std_sr_trials` の trial pool 統計量が **Sieve 単独では構成不能** と判定。**Phase 2 結論: `oos_dsr` 常に None、レポート `--` 固定**。Phase 4 で「Stage C 通過個体プール全体の SR 分布」を入力に正式接続。`_compute_dsr_safe(*, sharpe) -> None` の placeholder に簡略化。`SieveEvalResult.oos_dsr` の型は `float \| None` 据え置き。 |
| `_build_oos_backtest_config` の SSOT 化 + 全フィールド網羅 | ✅ §2.9 全面書き換え。**観察事実**: `run_ga.py` summary.json L617-621 は `initial_cash/leverage/units` の 3 フィールドしか出力せず、`max_spread_bps/holding_cost_per_day_bps/session_close_utc_hours` を summary 単独で再現不可と判定。→ **YAML config を SSOT として再ロード** (`load_config(config_path)` → `BacktestSectionConfig`) し、summary.json は **整合性チェック** (mismatch なら `logger.warning`) のみ。CLI に `--config config/alpha_factory/default.yaml` 引数追加。フィールド対応表で網羅性を明示。 |
| テストカバレッジ追加 (a) `_evaluate_one` 通過パス / (b) DSR fail-soft / (c) `_build_oos_backtest_config` 引き継ぎ | ✅ §3.2 にケース 14-18 追加: `test_evaluate_one_success_with_real_backtest` / `test_evaluate_one_returns_no_data_for_empty_bars` / `test_compute_dsr_safe_returns_none_in_phase2` / `test_build_oos_backtest_config_uses_yaml_ssot` / `test_build_oos_backtest_config_warns_on_mismatch`。総 18 ケース。 |

### 推奨事項への対応

- `src/alpha_factory/bars_loader.py` 早期抽出 → 本 TODO スコープ外（標準的 Phase 4 リファクタリング、別 TODO で計画）。Phase 2 では Codex Round 1 評価通り「一時措置として許容」する。

### 質問

これら修正で Round 1 の 2 修正要求と 6 観点の懸念が解消されたか確認願います。
**APPROVED / NEEDS_REVISION / REJECTED** で判定してください。

修正後の `detailed-design.md` 全文（特に §2.8 / §2.9 / §3.2）を Read で確認可能です。

修正後の主要差分要約:
- §2.8: DSR は Phase 2 で恒常 None に確定（trial pool 構成不能性を根拠とする）
- §2.9: `BacktestSectionConfig` 経由で YAML SSOT、summary は整合性チェックのみ
- §3.2: テスト 14-18 追加でカバレッジ穴埋め
