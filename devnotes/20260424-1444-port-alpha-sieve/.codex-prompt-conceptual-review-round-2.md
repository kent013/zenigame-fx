## Round 2: 修正対応

### Round 1 NEEDS_REVISION への対応マトリクス

| Round 1 指摘 | 対応 |
|--------------|------|
| `sieve_start = holdout_end + 1d` → `+5d` 短期エンバーゴ | ✅ `sieve_embargo_days=5` を §2.2 に追加。`sieve_start = holdout_end + sieve_embargo_days`。 |
| 単一 90d → 複数窓（45d×2 等）オプション仕様明記 | ✅ §2.2 末尾に「Phase 4 拡張予定: 複数非連続窓 (例: 45d × 2 with gap) で CSCV (Bailey et al. 2014) ベースの過学習確率推定へ接続」を明記。 |
| `trade_count > 10` → `>= 30` または相対基準 | ✅ `trade_count_min=30 (>=)` に引き上げ (§2.3)。相対基準 `max(30, ceil(0.5*stage_c_trade_count))` は Phase 4 calibrate-gate で実測ベースに。 |
| 取引発生日数下限 (例: 非ゼロ取引日 >=15) 追加 | △ Phase 4 で実装検討と明記。Phase 2 では `trade_count >= 30` で一次対応とする（個体単位 trade list から日数集計するロジックは将来追加）。 |
| DSR をレポート併記 | ✅ §2.3 / §2.5 / concept stub にレポート併記を明記、ゲート判定には Phase 2 では使わない方針。 |
| レポート先頭に `status: ok/no_candidates/no_data/error` | ✅ §2.5 に `status` ヘッダ追加。仕様も列挙。 |
| `no_data` 発生時に不足シンボル/期間を列挙 | ✅ §2.5 `no_data 詳細` セクションを追加。 |
| 「本設計は CSCV 簡易版」と明記 | ✅ §2.2 末尾、§4 反証検討、§concept stub の対応箇所に明記。 |
| `criteria_snapshot` / `cost_model` をヘッダ固定出力 | ✅ §2.5 にヘッダ追加。`cost_model` は run summary.backtest_config 引き継ぎ要約。 |

### 仮説 (revised)

- **H1**: Stage C 通過個体を holdout 後 5 日エンバーゴ + 90 日 OOS で再評価し、`sharpe > 0.5 AND trade_count >= 30 AND total_pnl > 0` を要求すれば、「holdout 期間に偶然合致しただけの個体」を 1/2 ~ 1/3 に絞り込める。
- **H2**: 5 日エンバーゴ + 90 日非対称により、Stage C との境界依存を緩和。レジーム持続による真の独立性は Phase 4 複数窓で対応。
- **H3**: `trade_count_min=30` で小標本 Sharpe バイアスを一次抑制。DSR はレポート併記、Phase 4 で hard gate 化候補。

### 修正後の主要差分

#### §2.2 OOS 期間定義
```
| `sieve_embargo_days` | 5（Stage C と Sieve の境界依存緩和、López de Prado 2018 Ch.7 の embargo 思想） |
| `sieve_start` | `holdout_end + sieve_embargo_days` (UTC, 0:00) |
| `sieve_end` | `sieve_start + sieve_window_days` (UTC, 0:00) |
```
**Phase 4 拡張予定**: 複数非連続窓（例: `45d × 2 with gap`）に拡張し、CSCV (Bailey et al. 2014) ベースの過学習確率推定へ接続。本 Phase 2 実装は CSCV の簡易版として **単一追加 OOS 窓** を先行導入する位置付け。

#### §2.3 通過基準
```python
PASS_CRITERIA = {
    "sharpe_min": 0.5,           # sharpe > 0.5  (strict greater-than)
    "trade_count_min": 30,       # trade_count >= 30
    "total_pnl_min": 0.0,        # total_pnl > 0  (strict greater-than)
}
```

#### §2.5 出力仕様（ヘッダ追加）
- `status: ok | no_candidates | no_data | error`
- `criteria_snapshot` セクション
- `cost_model` セクション（run summary.backtest_config 引き継ぎ要約）
- `no_data 詳細` セクション
- 通過個体一覧に `DSR (info)` カラム

### 質問

これら修正で Round 1 で指摘いただいた 5 観点 (OOS 期間 / 通過基準 / defensive / 学術引用 / スコープ境界) が解消されたかご確認ください。
**APPROVED / NEEDS_REVISION / REJECTED** のいずれかで判定してください。

修正後の `conceptual-design.md` 全文と `docs/alpha_factory/concepts/alpha-sieve.md` 全文を Read で確認可能です（ファイルパスは前ラウンドと同じ）。

**注意点**:
- 「取引発生日数の下限 >=15 の追加」は Phase 2 では trade list ベースで集計する追加実装が要るため、Phase 4 calibrate-gate と一緒に対応する方針です。Phase 2 で必須と判断される場合は再修正します。
