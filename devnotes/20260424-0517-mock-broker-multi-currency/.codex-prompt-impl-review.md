【思考原則】仮説先行、データに真摯、機能名回帰、因果ループ追跡。
【使命】zenigame-fx Alpha Factory の live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと。
【絶対制約】イントラデイ前提、ロング・ショート両方向、スワップ・スプレッド fitness 反映。
【禁止事項】評価期間延長、数値操作、GA ハック、閾値緩和、過剰複雑化、取引回数削減、オーバーナイト、値伝搬漏れ。
【ツール使用制限】コマンド・書き込み禁止、分析に集中。Read のみ可。
【レビュー重点】転記漏れ: 1) パラメータ 4 段接続、2) archive 4 点セット、3) 関数パラメータ伝搬、4) ログ出力。
【監査必須】C1-C9: design-first / 「X 無い=バグ」禁止 / collider bias / 前提明示 / framing 独立性 / fact-interpretation 分離 / sample size / INCONCLUSIVE 第一級 / falsification-first。

---

## 役割

あなたは zenigame-fx の実装レビュアー。T019 の実装 (worktree: `worktrees/todo-T019`) に対して APPROVED / NEEDS_REVISION を返してほしい。

## 背景

- TODO: T019 MockBroker 非 JPY-quote 通貨ペア対応
- 概念設計 (APPROVED): `devnotes/20260424-0517-mock-broker-multi-currency/conceptual-design.md`
- 詳細設計 (APPROVED): `devnotes/20260424-0517-mock-broker-multi-currency/detailed-design.md`
- 背景: T016 cross-pair shadow で、ANCHOR_PAIRS 全 6 target に非 JPY-quote anchor が含まれ、MockBroker の `home=JPY` 固定制約で構造的 pair_failure が発生していた。

## 実装変更サマリ

| ファイル | 変更 |
|---|---|
| `src/broker/mock.py` | `InstrumentMeta` に `pip_size`/`display_precision` default 付き追加 + `__post_init__` 検証 + `default_pip_size_for_quote` / `default_display_precision_for_quote` 静的ヘルパ。`MockBroker.__init__` を keyword-only + `home_currency: str \| None = None` (None → meta.quote_currency 自動採用) + `NotImplementedError` のメッセージ更新 |
| `src/broker/margin.py` | docstring + エラーメッセージを `fx_rate_provider` (Phase 4) に統一 |
| `tests/_helpers.py` | `eur_jpy_meta` / `eur_usd_meta` / `usd_cad_meta` 追加、`usd_jpy_meta` に pip_size/precision 明示 |
| `tests/alpha_factory/test_cross_pair.py` | `_dummy_meta` / `_make_stage_c_inputs` に JPY pip_size/precision 明示追加 |
| `tests/alpha_factory/test_swim_lane.py` | `_stub_meta` に JPY pip_size/precision 明示追加 |
| `scripts/` (7 ファイル) | `InstrumentMeta(...)` に `pip_size=InstrumentMeta.default_pip_size_for_quote(pair.quote_currency)` と `display_precision=...` 追加 |
| `tests/broker/test_mock_multi_currency.py` | 新規 17 テスト (EUR_USD/USD_CAD P&L sanity、home_currency 明示/mismatch、pip validation、scale 不変性 S2/S3、cross-pair shadow no pair_failure) |
| `docs/alpha_factory/cross-pair.md` | Phase 2 構造的制約節を「解消した範囲 / 解消されない範囲 (Phase 4)」に分けて更新 |
| `docs/alpha_factory/terminology.md` | pip_size / display_precision / Per-pair Home Mode 用語追加 |

## テスト結果

- `uv run pytest tests/` → 815 passed, 1 skipped (798 baseline + 17 新規)
- `uv run pytest tests/broker/test_mock_multi_currency.py` → 17 passed
- `uv run mypy src/broker/ tests/broker/` → 6 files checked, 0 errors
- `uv run ruff check src/broker/ tests/broker/ tests/_helpers.py` → All checks passed!

## レビュー観点

以下を verify し、APPROVED か NEEDS_REVISION か判定してほしい:

1. **実装の正確性** — `src/broker/mock.py::MockBroker.__init__` の per-pair home 解決ロジックが設計どおりか。`NotImplementedError` が "Phase 4" / "fx_rate_provider" を含むか。
2. **scale 不変性検証の反証可能性** — `tests/broker/test_mock_multi_currency.py::test_scale_invariance_sharpe_s2` / `test_scale_invariance_equity_returns_s3` が `initial_cash × units` 同時 10 倍スケールで Sharpe / equity returns の不変性を **反証可能な形で** 検証しているか、margin_call 非発生の assert も入っているか。
3. **値の転記漏れ** — `InstrumentMeta(...)` 全 13 箇所 (test 4 + scripts 7 + _helpers 1 + paper_trade 1) で pip_size / display_precision が **quote currency に合った値** で伝搬しているか。JPY-quote test fixture で USD default (0.0001) が沈黙して乗るリスクが解消されているか。
4. **Phase 4 拡張点の退行防止** — `fx_rate_provider` 命名が mock.py / margin.py / conceptual-design / terminology.md で統一されているか。
5. **cross-pair 本体 (`src/alpha_factory/cross_pair.py`) への影響** — 現状コード変更なし、`_run_pair_sharpe` が MockBroker を引数無指定で生成する経路が T019 後に意味を持ち始めたことを理解しているか。
6. **docs の整合性** — cross-pair.md / terminology.md の更新が「T019 で解消した範囲の限定表現」を正しく反映しているか。

参照してよいファイル (Read のみ):
- worktrees/todo-T019/src/broker/mock.py
- worktrees/todo-T019/src/broker/margin.py
- worktrees/todo-T019/tests/broker/test_mock_multi_currency.py
- worktrees/todo-T019/tests/_helpers.py
- worktrees/todo-T019/tests/alpha_factory/test_cross_pair.py
- worktrees/todo-T019/tests/alpha_factory/test_swim_lane.py
- worktrees/todo-T019/scripts/ 配下の 7 ファイル
- worktrees/todo-T019/docs/alpha_factory/cross-pair.md
- worktrees/todo-T019/docs/alpha_factory/terminology.md
- worktrees/todo-T019/devnotes/20260424-0517-mock-broker-multi-currency/*

出力フォーマット:

```
## Verdict
APPROVED | NEEDS_REVISION

## 主要論点
- ...

## 必須修正（NEEDS_REVISION 時のみ）
- ...

## 推奨修正（任意）
- ...

## 根拠
- ...
```
