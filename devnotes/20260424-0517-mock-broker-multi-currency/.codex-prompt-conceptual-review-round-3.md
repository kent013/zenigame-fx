Round 2 必須修正への対応を以下に示す。APPROVED 判定を求める。

## 対応マトリクス

| 必須修正 | 対応 |
|---|---|
| 1. scale sanity test を `initial_cash=C` と `initial_cash=k*C` の Sharpe 不変性 `abs(sharpe_C - sharpe_kC) < ε` assert に変更 | §9 受け入れ基準 2 に追記（k=10、margin 非拘束シナリオ、ε=1e-6）|
| 2. `units=U` / `units=k*U` の Sharpe 不変性も同様に assert | §9 受け入れ基準 2 に追記 |

## 推奨修正の対応

| 推奨 | 対応 |
|---|---|
| Phase 4 契約の 1 行明記 (`home != quote` かつ `fx_rate_provider is None` で fail-fast) | §8 末尾に「Phase 4 契約予約 (fail-fast 規約)」として明示。本 TODO のエラーメッセージも "Phase 4 requires fx_rate_provider" に更新する |

## 改訂後の §8 末尾・§9.2 抜粋

### §8 末尾（追加）

> Phase 4 契約予約（fail-fast 規約）: `home_currency != meta.quote_currency` かつ `fx_rate_provider is None` の場合、broker コンストラクタは fail-fast で `NotImplementedError` を維持する（本 TODO のエラーメッセージは "Phase 4 requires fx_rate_provider" に更新）。

### §9.2 受け入れ基準（追加）

> - Scale 不変性直接検証 (initial_cash): 同 pair・同価格系列で `initial_cash=C` と `k*C` の Sharpe 差 < 1e-6 (margin 非拘束条件)
> - Scale 不変性直接検証 (units): 同 pair・同 initial_cash で `units=U` と `k*U` の Sharpe 差 < 1e-6

---

この対応で APPROVED 可能か、判定してほしい。
