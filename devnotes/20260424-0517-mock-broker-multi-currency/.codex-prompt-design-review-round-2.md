Round 1 必須修正への対応を以下に示す。APPROVED 判定を求める。

## 対応マトリクス

| 必須修正 | 対応 |
|---|---|
| 1. scale 不変性検証を trade-based Sharpe または `initial_cash × units` 同時スケール方式に修正 | §6.2 を全面改訂、(S2) `initial_cash × units` 同時 k 倍スケール → Sharpe 不変 + (S3) equity returns 要素一致 の 2 アプローチを採用。テストケース 8-11 を「sharpe / equity returns × 2 pair」に再構成。margin_call 非発生 assert も追加 |
| 2. Phase 4 予約名を `fx_rate_provider` に統一 | §5 で margin.py の `fx_converter` → `fx_rate_provider` に修正、命名統一を明記。conceptual-design §8 および本詳細設計 §2 InstrumentMeta docstring も全て `fx_rate_provider` に統一済 |
| 3. 呼び出し元監査を `MockBroker(` だけでなく `InstrumentMeta(` / `home_currency=` / broker ファクトリ経由に拡張 | §3.1 で `MockBroker(` の全 20 箇所を表形式で enumerate。§3.2 で `InstrumentMeta(` の全 13 箇所を enumerate し、JPY test-only と scripts/ factory 経路に分類。`home_currency=` grep は T019 前でヒットなし（§7）|
| 4. JPY ペアの `InstrumentMeta` 直接生成箇所で pip_size=0.01 / display_precision=3 明示（`_dummy_meta` 含む） | §3.2 + §9 実装順序 step 5-6 で `_dummy_meta` / `_stub_meta` / `_make_stage_c_inputs` の更新を明示 |
| 5. `docs/alpha_factory/cross-pair.md` の「T019 で解消」を限定表現に | §8 「T019 で解消した範囲（限定）」と「T019 で解消されない範囲（Phase 4）」に分けて明記 |

## 推奨修正の対応

| 推奨 | 対応 |
|---|---|
| `MockBroker.__init__` を keyword-only 化 | §3 で `*,` を追加して `home_currency` を keyword-only に。既存 20 箇所の呼び出しは全て引数無指定なので破壊的変更なし（§3.1 検証済）|
| `NotImplementedError` 文言検証を 1 test に集約 | §6.1 テストケース 6 `test_mismatched_home_quote_raises_not_implemented` に集約 |

## 追加の補強

- `InstrumentMeta.default_pip_size_for_quote(quote_currency)` / `default_display_precision_for_quote(quote_currency)` 静的ヘルパを追加（§2）。これにより scripts/ 7 箇所の factory pattern が `InstrumentMeta.default_pip_size_for_quote(pair.quote_currency)` で自動派生でき、JPY / USD-quote で正しい値を取得する。
- §9 実装順序に scripts/ 7 箇所の書き換えを step 7 として追加。

---

改訂後の詳細設計は `devnotes/20260424-0517-mock-broker-multi-currency/detailed-design.md` を参照。
この対応で APPROVED 可能か判定してほしい。
