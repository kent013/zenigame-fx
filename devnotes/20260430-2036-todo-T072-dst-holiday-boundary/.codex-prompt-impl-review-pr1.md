【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】

### C1. Design-first 原則 (code grep より先に docs を読む)
コードの bug / 設計欠陥について claim を出す前に、以下を verify:
- docs/alpha_factory/ 配下の該当セクション（stage-gates.md / clause-architecture.md 等）を Read で確認
- devnotes/ 配下の関連 T-number（関数コメントや git log に出てくる T-xxx）を ls devnotes/ | grep T{xxx} で探して読む
- git log --all -S "識別子" で該当 code の変更履歴を辿る
- 該当機能と並行計算される可能性のある経路を grep で広く検索

### C2. 「X が無い = バグ」禁止ルール
関数 Y の signature / body に識別子 X が無いことを根拠に bug 判定してはならない。別経路での計算を grep で広く検索してから。

### C3. Collider bias 必須チェック（相関分析時）
2 つの変数 A, B の相関を計算して report する場合、Conditioning set と collider の有無を必ず明記。

### C4. 前提明示ルール
Claim / verdict を出す前に、prompt の先頭に「本分析の前提」を bullet でリスト化し、各前提が verified であることを明示。

### C5. 並列 sub-agent の framing 独立性チェック
独立検証のつもりで同じ framing の sub-agent を N 個回しても、それは 1 つの誤読の N 倍冗長実行。

### C6. Fact / Interpretation 分離ルール
「観察された事実 (Facts)」と「解釈・推論 (Interpretations)」を明確に区別する。1 文で mix しない。

### C7. Sample size ガード
n < 30 の相関 claim は明示的に因果解釈を避ける。n < 10 は相関 claim 禁止（observation のみ許可）。

### C8. INCONCLUSIVE を第一級 verdict に
INCONCLUSIVE は CONFIRMED / REJECTED と同格の選択肢。データ不足は正当な結論。

### C9. Falsification-first プロンプト
Round 1 は「この hypothesis の反証を探せ」という指示を先に書く。confirmation bias を強制しない。

---

# T072 PR 1 実装レビュー (impl-review)

## 役割

あなたは zenigame-fx Alpha Factory cascade port v2 Phase 2 配線実装の **TODO 15 番目 (= T072 PR 1)** の実装レビュアー。

T072 は **DST/holiday session boundary contract** = broker 配信 schedule (BrokerTradingSchedule) と市場 holiday 観測 (MarketHolidayCalendar) を完全分離し、 DST table + date_overrides + 半開区間 [start, end) で SessionBlock を駆動する layer の新設。 Phase 2 Phase1 単体実装、 caller 配線は Phase 2 申し送り。

設計 SSOT は `devnotes/20260430-2036-todo-T072-dst-holiday-boundary/detailed-design.md` 全 1620 行。 概念設計は Round 1-5 / 詳細設計は Round D1-D2 全 Codex APPROVED 済 (= 設計が SSOT)。

ファイル読み込みは許可。

## レビュー対象 (T072 PR 1 のスコープ)

### 新規ファイル

1. `src/backtest/calendar.py` — 詳細設計 § 3 / § 4 の 18 シンボル実装 (= dataclass 5 個 + pure function 7 個 + YAML loader 2 個 + 定数 5 個 + Literal type 2 個)
2. `tests/backtest/test_calendar.py` — F1-F53 (= 86 unit test)
3. `config/calendars/broker_trading_schedule.yaml` — DST table 13 region 完全列挙 (2022-2027) + broker_full_close_holidays 11 件 + date_overrides 8 件
4. `config/calendars/tokyo_market_holidays.yaml` — 国民の祝日 + TSE close 110 件 (2022-2027)
5. `config/calendars/london_market_holidays.yaml` — UK bank holidays 51 件 (2022-2027)
6. `config/calendars/ny_market_holidays.yaml` — NYSE / Federal Reserve holidays 60 件 (2022-2027)

### 既存ファイル変更

7. `src/backtest/session_block.py` — SessionBlock に **3 field 追加** (open_minutes / granularity_seconds / observability_flags) + **1 メソッド追加** (to_record) + expected_bar_count / schedule_status を property 化 + aggregate_session_blocks signature 拡張 (mode 必須化、 broker_schedule / calendars / granularity_seconds optional) + aggregate_session_blocks_production wrapper 新設
8. `src/backtest/engine.py` — `aggregate_session_blocks(...)` 呼出に `mode="test"` 追加 (= Phase 1 backward-compat、 Phase 2 で wrapper 化予定)
9. `tests/backtest/test_session_block.py` — 既存 7 caller 全部に `mode="test"` 追加 (= Phase 1 backward-compat)
10. `docs/alpha_factory/stage-gates.md` — T072 セクション追記 (+91 行)

### スコープ外 (touch しない)

- T070 BLOCK_BUCKET_RANGES_UTC への DST 例外連携 caller (Phase 2 別 PR、 T070 follow-up)
- T061 / T064 / T066 / T071 の SessionBlock T072 field 消費 (Phase 2)
- run_ga.py / backtest_runner で broker_schedule / calendars を load + BacktestResult 経由配線 (Phase 2)
- run report / archive で to_record(include_derived=True) export (Phase 2)
- OANDA Developer Portal 公式 spec 確認後の broker_full_close_holidays / date_overrides 確定 (Phase 2 production gate)

## 実装内容サマリ (詳細設計 § 1-9 と 1:1 対応)

### 1. データモデル (詳細設計 § 3)

**ScheduleStatus**: `Literal["regular", "closed_full", "closed_partial"]`
**MarketCode**: `Literal["tokyo", "london", "ny"]`
**M1_PLUS_GRANULARITIES**: `frozenset({60, 120, 240, 300, 600, 900, 1800, 3600, 7200, 14400})` = 10 種類、 全 28800 を割り切る
**HOLIDAY_CALENDAR_VERSION** / **BROKER_SCHEDULE_VERSION** = "1.0.0"
**BUCKET_FULL_MINUTES** = 480 / **BUCKET_FULL_MINUTES_X60** = 28800
**RECORD_SCHEMA_VERSION** = "1.0.0"
**SESSION_BLOCK_STORAGE_FIELDS** = 12 path / **SESSION_BLOCK_DERIVED_FIELD_PATHS** = 4 path

**BrokerSeasonalCloseSpec** (frozen dataclass):
- `name / region_start / region_end / close_hour_utc / reopen_hour_utc`
- __post_init__: 0..23 hour、 region_start <= region_end

**BrokerSchedulingProvenance** (frozen dataclass):
- `source / verified_at: date / confidence: Literal["high","medium","low"] / notes`

**BrokerTradingSchedule** (frozen dataclass):
- `dst_aware_close_table: tuple[BrokerSeasonalCloseSpec, ...]`
- `broker_full_close_holidays: frozenset[date]`
- `date_overrides: Mapping[date, tuple[int, int]]`
- `period_start / period_end / provenance / version`
- __post_init__ I-1..I-7: version / period / dst_table 連続 cover (pairwise) / full_close 範囲 / overrides 範囲 / overrides ∩ full_close = ∅
- `open_window_for_utc_date(d)` SSOT 優先順位 1.date_overrides → 2.broker_full_close → 3.Sat → 4.Sun → 5.Fri → 6.Mon-Thu

**MarketHolidayCalendar** (frozen dataclass):
- `market: MarketCode / period_start / period_end / holidays: frozenset[date] / schema_version`
- __post_init__: market 妥当性 / schema_version / period / holidays ⊂ period
- `contains(d)` raw lookup

**ObservabilityFlags** (frozen dataclass):
- `dst_transition_markets: frozenset[MarketCode]` (⊂ {london, ny})
- `holiday_markets: frozenset[MarketCode]` (⊂ {tokyo, london, ny})
- `all_g3_market_holiday` property = (len == 3)

**SessionBlock** (T070 改造):
- T072 追加 3 field: `open_minutes=480 / granularity_seconds=60 / observability_flags=default_factory`
- __post_init__ で T072 invariant: granularity in M1_PLUS_GRANULARITIES / BUCKET_FULL_MINUTES_X60 % granularity == 0 / 0..480
- I8 tolerance: open block で bar_count > expected + max(5, ceil(expected*0.01)) → `session_block_bar_count_exceeds_tolerance` warning
- closed_full block (open=0) で bar_count > 0 → `closed_full_unexpected_bars` warning (別系列)
- property: `expected_bar_count` / `schedule_status` / `is_partial_bar_block`
- `to_record(include_derived=False)` で 12 path STORAGE / `include_derived=True` で +4 path DERIVED + `record_schema_version="1.0.0"`

### 2. アルゴリズム (詳細設計 § 4)

- **is_dst_transition(market, d)**: zoneinfo (IANA tz) 経由で d 00:00 と d+1 00:00 の utcoffset 比較。 Tokyo は常に False。
- **is_market_holiday(market, d, calendar)**: calendar.market 一致確認 + raw lookup
- **compute_observability_flags(d, calendars)**: dst_markets (london/ny) + holiday_markets (tokyo/london/ny) を frozenset 化
- **compute_bucket_open_minutes(d, bucket, schedule)**: bucket UTC ∩ broker open window の overlap (半開区間)
- **compute_expected_bar_count(open_minutes, granularity_seconds)**: floor (= H4 で 120 min → 0)
- **validate_calendar_coverage(calendars, schedule, dataset_span)**: 両端 inclusive 検証 (Round D1 [C6])
- **load_market_holiday_calendar(market, yaml_path)** / **load_broker_trading_schedule(yaml_path)**: `_DuplicateKeyRejectLoader` (= duplicate key + merge key `<<` reject) 経由で SafeLoader 拡張

### 3. aggregate_session_blocks 改造 (詳細設計 § 4.9)

- mode は **必須引数** (Round D1 [C4])
- mode="production" で broker_schedule + calendars 両方必須 (= 片方 None で ValueError)
- mode="test" で 4 通り全許容 (default 値で T070 互換)
- broker_schedule None → open_minutes=480 default、 calendars None → flags=default
- production caller は `aggregate_session_blocks_production(...)` wrapper 経由 (Round D2 [C3] [S3])

### 4. YAML schema (詳細設計 § 6)

- `broker_trading_schedule.yaml`: 2022-2027 の NY DST season 13 region 連続 cover (= disjoint、 隙間なし)
  - 春切替日が DST region の region_start (Round 3 [C4])
  - close_hour_utc=22 (standard) / 21 (DST)、 reopen_hour_utc 同
  - broker_full_close_holidays = 11 件 (= クリスマス / 新年)
  - date_overrides = 8 件 (= クリスマス前日早閉まり / 米独立記念日早閉まり)
  - provenance.confidence = "medium" (OANDA spec 要再確認)
- `tokyo_market_holidays.yaml` / `london_market_holidays.yaml` / `ny_market_holidays.yaml`: 2022-2027 の各 market 公式 holiday を列挙、 confidence="medium"

### 5. テスト (詳細設計 § 5、 86 test 全 pass)

- **F1-F5 pure function** (5 test)
- **F8-F14 BrokerTradingSchedule invariant** (12 test、 property-based 1 本含む)
- **F15-F22 open_window_for_utc_date** (8 test)
- **F23-F28 MarketHolidayCalendar** (6 test)
- **F29-F33 ObservabilityFlags** (5 test)
- **F34-F37 compute_bucket_open_minutes / compute_expected_bar_count** (13 test、 半開区間境界 + H4 floor 含む)
- **F38-F44 SessionBlock backward-compat + tolerance warning** (12 test、 to_record schema 整合 / closed_full warning 別系列含む)
- **F45-F49 aggregate_session_blocks 改造 + production wrapper** (12 test、 grep audit 含む)
- **F50-F53 validate_calendar_coverage** (6 test、 inclusive boundary 含む)
- **5.2 YAML 検証** (2 test、 実 YAML load + coverage)

## DoD 確認

- `uv run pytest tests/backtest/test_calendar.py` — **86 passed**
- `uv run pytest tests/backtest/test_session_block.py` — **31 passed**
- `uv run pytest tests/` — **2566 passed, 1 skipped, 1 xfailed (regression 0)**
- `uv run ruff check src/backtest/calendar.py src/backtest/session_block.py tests/backtest/test_calendar.py tests/backtest/test_session_block.py` — **clean**
- `uv run mypy src/` — **clean (117 source files)**
- 既存 ruff errors 8 件 (`tests/scripts/test_batch_metrics.py:240` 等) は T072 PR 1 スコープ外 (T058 PR 3 でも未触)

## レビュー観点

以下の観点で本 T072 PR 1 の実装を critical review してください。 APPROVED か REQUEST_CHANGES の verdict と、 各観点の Findings を記載してください。

### 観点 1: 詳細設計 SSOT との一致

- `src/backtest/calendar.py` の 18 シンボル / class / 関数 が § 3 / § 4 と一致しているか
- BrokerTradingSchedule の __post_init__ I-1..I-7 順序が § 3.4 (Round D1 [W1] 反映) と一致しているか
- open_window_for_utc_date の SSOT 優先順位 (1. date_overrides → 2. full_close → 3. Sat → 4. Sun → 5. Fri → 6. Mon-Thu) が § 3.4 と一致しているか
- _DuplicateKeyRejectLoader が duplicate key + merge key (`<<`) の双方を reject しているか (Round D1 [C2] / Round D2 [C2] [S2])
- aggregate_session_blocks の mode が **必須引数** で、 production mode の reject ロジックが正しいか (Round D1 [C4])
- aggregate_session_blocks_production wrapper が mode="production" 固定で broker_schedule / calendars が型上必須か (Round D2 [C3])
- to_record の出力 schema が SESSION_BLOCK_STORAGE_FIELDS / SESSION_BLOCK_DERIVED_FIELD_PATHS の path 形式と一致しているか (Round D1 [C5] / Round D2 [C1])
- 半開区間 [start, end) 規約が docstring + 実装で統一されているか (Round D1 [C3])
- validate_calendar_coverage の dataset_span が両端 inclusive で docstring 明記されているか (Round D1 [C6])

### 観点 2: collider bias 規範の遵守 (詳細設計 § 1.4 / § 9.6)

- compute_bucket_open_minutes が broker_schedule **のみ** で決定し、 market holiday に touch しないか (= holiday を expected_bar_count に混ぜないという SSOT)
- ObservabilityFlags.holiday_markets が観測情報のみで、 schedule_status / open_minutes に影響しないか
- docstring + コメントで「holiday_markets 単独で session_pass_pattern / SR 計算分母を drop / filter してはならない」 が明記されているか
- docs/alpha_factory/stage-gates.md の T072 セクションに collider bias 規範が記述されているか

### 観点 3: __post_init__ 検証順序依存 (Round D1 [W1])

- I-1 (version) → I-2 (period) → I-3/I-4 (dst_table 連続 cover) → I-5 (full_close 範囲) → I-6 (overrides 範囲) → I-7 (overrides ∩ full_close = ∅) の順序が遵守されているか
- I-7 が I-5 / I-6 後で実行され、 妥当な input でのみ重複検出が走るか
- pairwise 使用で I-4 が連続 region の隣接性を確認しているか

### 観点 4: 半開区間 [start, end) 規約の整合性 (Round D1 [C3])

- open_window_for_utc_date 戻り値が半開区間 (= 0 <= start <= end <= 1440)
- compute_bucket_open_minutes の overlap 計算が `max(0, min(end_a, end_b) - max(start_a, start_b))` で半開区間 SSOT を踏襲しているか
- 境界値 (= start_a == end_b) で overlap = 0 (空区間) を返すことがテスト F37_boundary_overlap で検証されているか
- 一方で date 単位の period / dataset_span は両端 inclusive (詳細設計 § 4.6 で異なる粒度の規約区別)

### 観点 5: production wrapper の構造的強制 (Round D2 [C3] [S3])

- aggregate_session_blocks_production が mode="production" を内部固定し、 caller が誤って mode="test" を渡せない構造か
- Phase 1 では engine.py が `aggregate_session_blocks(..., mode="test")` を直接呼ぶが、 Phase 2 で wrapper に切替予定であることがコメントで明示されているか
- F49_production_callers_no_test_mode test が **mode="production" の直接呼出を禁止** (= wrapper 経由のみ許可) するロジックで実装されているか
- session_block.py 内の wrapper 自体は mode="production" を直接呼ぶが、 grep test で除外されているか

### 観点 6: YAML schema lint 整合性 (Round D1 / Round D2)

- _DuplicateKeyRejectLoader が yaml.constructor.ConstructorError を raise するか (= 静かに上書きしない)
- merge key `<<` reject により duplicate 迂回が構造的に不可能か (F14_yaml_merge_key_reject / F14_yaml_duplicate_after_merge_attempt_rejected で検証)
- broker_trading_schedule.yaml の dst_aware_close_table が 13 region で 2022-01-01..2027-12-31 を連続 cover しているか
- DST 切替日 (NY 春 = 3月第2日曜 / NY 秋 = 11月第1日曜) が IANA tz transition と一致しているか (例: 2024-03-10 / 2024-11-03 / 2025-03-09 / 2025-11-02 等)
- broker_full_close_holidays / date_overrides が period 内 (= 2022-2027) であり、 重複していないか

### 観点 7: backward-compat (詳細設計 § 8)

- T070 既存 fixture (= test_session_block.py) で SessionBlock(default) 構築時に T072 field の default 値が等価かつ hash 一致 (F40_eq_hash_compat)
- 既存 caller (= engine.py:run_backtest) が `mode="test"` 追加で動作継続 (= Phase 1 互換)
- aggregate_session_blocks signature 変更で test_session_block.py の 7 既存 caller が `mode="test"` 追加で全 pass
- to_record(include_derived=False) と asdict が schema 整合 (= 既存 caller の予期しない field 変更がないか)

### 観点 8: tolerance warning 別系列 (Round 5 [W4] / Round D1 [W3])

- open block tolerance 違反: `session_block_bar_count_exceeds_tolerance` で logger.warning
- closed_full block 異常 bar: `closed_full_unexpected_bars` で別系列 logger.warning (= open block warning は出ない)
- module-level logger 取得 (= `logger = logging.getLogger(__name__)`)、 __post_init__ 内で getLogger しない (Round D1 [W3])
- F44_tolerance_warning_open / F44_closed_full_warning_separate_log が両 warning を分離検証している

### 観点 9: テスト網羅性 (詳細設計 § 5)

- F1-F53 (= test matrix) 全 test_id が実装され pytest 関数名と 1:1 対応 (枝番は `Fxxx_<behavior>` 形式)
- BrokerTradingSchedule の property-based test (Round D1 [S4]) が実装されているか (hypothesis 不在のため manual randomized test で代替)
- 実 YAML (config/calendars/*.yaml) を load + validate_calendar_coverage で coverage 確認するテストがあるか
- production caller wrapper grep test (F49_production_callers_no_test_mode) が repo 内の `mode="production"` 直接呼出を 0 件に固定しているか

### 観点 10: T058-T071 規範の継承

- flat 配置 + main 実装 SSOT + 「ファイル読み込みは許可」 + worktree commit に Codex review files + collider bias 独立性 が整合しているか
- T070 BLOCK_BUCKET_RANGES_UTC SSOT が unchanged (= 改変なし、 Phase 2 の T070 follow-up で別 PR)
- T071 SessionEntropyMetric / cpps_archive (T066) / stage_bc_evaluator (T064) への申し送り (= collider bias 規範) が明文化されているか

## verdict 形式

最後に、 以下の形式で verdict を返してください:

```
## verdict
APPROVED | REQUEST_CHANGES

## 主要 Findings (重要度順)

### [Critical]
N. ...

### [Warning]
N. ...

### [Suggestion]
N. ...
```

REQUEST_CHANGES の場合は、 修正対象の行番号と修正後コード snippet を必ず含めてください。
APPROVED の場合は、 軽微な改善提案 (Suggestion) は含めて構いません。

主要レビュー対象ファイルへのアクセス:
- `src/backtest/calendar.py` (新規 ~720 行)
- `src/backtest/session_block.py` (T070 既存 + T072 追加 ~510 行)
- `tests/backtest/test_calendar.py` (新規 ~1400 行)
- `tests/backtest/test_session_block.py` (T070 既存に mode="test" 追加)
- `src/backtest/engine.py` (mode="test" 追加 1 行)
- `config/calendars/*.yaml` (新規 4 ファイル)
- `docs/alpha_factory/stage-gates.md` (T072 セクション末尾追記)
- `devnotes/20260430-2036-todo-T072-dst-holiday-boundary/detailed-design.md` (= 設計 SSOT)
