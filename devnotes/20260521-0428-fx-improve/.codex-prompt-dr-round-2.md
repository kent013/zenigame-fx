Round 1 の [Critical] と [Suggestion] を反映しました。対応マトリクス:

| # | 指摘 | 重要度 | 対応 |
|---|------|--------|------|
| 1 | MTM/margin parity: _exit_price 一括 stress 化で Decimal だけ MTM に stress が乗り kernel と parity 崩壊 | Critical | 採用。契約を「stress は realized fill (entry/exit 約定) のみ」に固定。MTM/margin は両経路とも生価格。MockBroker は _exit_price を一括変更せず、realized exit 呼出点(_close_position)でのみ補正。_unrealized_pnl(MTM) は生価格のまま不変。kernel も realized fill(L185/194/203/206)のみ補正、MTM(L166-170/231-234)と margin(L243-247)は生価格不変 |
| 2 | 丸め規約: 負数 floor 除算で long/short 非対称 | Suggestion | 採用。adj は非負 spread_diff=max(0,ask-bid) から `((m_num-m_den)*spread_diff)//(2*m_den)` で計算、符号は side で最後に適用。負数除算なし |
| 3 | m>1 kernel-vs-decimal parity test | 必須 | 採用。テスト計画に (a) m=1.5 で kernel/Decimal の trade列・exit_reason・realized PnL 一致、(b) margin 境界で exit_reason 一致(MTM生価格検証)、(c) m=1.0 で両経路ビット同一 を追加 |

整数比 (m_num,m_den) 表現で m=1.0→(1,1)→(m_num-m_den)=0→adj=0→ビット同一。Decimal→整数比変換は既存 max_spread num/den 変換(engine.py:365-370)と同型。

この修正で全体 APPROVED に到達するか確認してください。残 Critical/Warning があれば最小変更で提示してください。
