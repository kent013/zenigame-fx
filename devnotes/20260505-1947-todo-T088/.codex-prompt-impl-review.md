【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。

【絶対制約】
- イントラデイ前提
- ロング・ショート両方向許容
- スワップ・スプレッドを fitness に反映

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する
2. 見た目の数値をよくしようとする改善
3. GA をハックしてステージを進めようとする
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する
6. 取引回数を削減して見かけの成績を上げようとする
7. オーバーナイト保有前提の設計
8. ゲノム archive スキーマ変更時の値伝搬漏れ

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【監査・レビュー時の必須チェック — False-positive 再発防止】
C1-C9 (省略、本プロンプトは zenigame-fx-codex-review SKILL.md の C1-C9 を全て継承)

---

# 役割・タスク

あなたは経験豊富な Python コードレビュアーです。zenigame-fx Alpha Factory の改善実装をレビューしてください。

## レビュー対象

TODO T088: `_indicators.py` Numba JIT 化

3 関数 (`rolling_max` / `rolling_min` / `_wilder_smooth`) を Numba JIT 化して profile-optimize cycle 1/3 のボトルネック削減を狙う。 数値出力は現行と numerically identical (NaN 同位置 + 有限値 bit-identical)。

## レビュー観点

1. **設計との一致性**: 詳細設計書 (Round 2 APPROVED) との実装一致
2. **正確性**: NaN 契約 / circular buffer head/tail/size 管理 / `_wilder_smooth` recurrence の seed = NaN early return
3. **パフォーマンス**: `@numba.njit(cache=True, fastmath=False)` 適用、 内側ループ内 NumPy 関数呼び出しなし
4. **一貫性**: 命名規約 (`_rolling_max_jit` / `_rolling_min_jit` / `_wilder_smooth_recurrence_jit`)
5. **テスト網羅性**: 各施策に parity test 1 本 + adx 経由 parity 1 本、 case 網羅 (通常 / n=1 / 重複値連続 / 先頭NaN / 途中NaN / 全NaN / length<n)
6. **ruff / mypy 準拠**: 変更ファイルで `uv run ruff check` / `uv run mypy` 通過済
7. **禁止事項違反なし**: 数値出力不変 (見た目数値改善ではない)

## 転記漏れ・伝搬漏れ重点チェック

- 内部実装変更なので config / GaConfig / genome.meta / archive 4 段伝搬は該当なし
- `import numba` 追加 + `from collections import deque` 削除のみ
- `_wilder_smooth` の seed 計算式 (`np.nanmean` if NaN含む else `mean`) を Python 側で完全に同じ演算順序で維持しているか

## 出力形式

- ファイルごとに判定
- [Critical] [Warning] [Suggestion]
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 日本語

---

## 詳細設計書

(devnotes/20260505-1857-indicators_numba_jit/detailed-design.md Round 2 APPROVED 抜粋)

### 施策 1: `rolling_max` Numba JIT 化

- ファイル: `src/alpha_factory/primitives/_indicators.py:116`
- 現行 deque を fixed-size int64 circular buffer (head/tail/size) に置換
- `@numba.njit(cache=True, fastmath=False)`
- comparator `<=` で右から drop (現行と完全一致)

### 施策 2: `rolling_min` Numba JIT 化

- 施策 1 と完全対称、 comparator `>=` のみ違い

### 施策 3: `_wilder_smooth` Numba JIT 化 (Round 1 Critical 反映)

- recurrence loop のみ Numba JIT 化、 seed は **Python 側で `np.nanmean/mean` を維持**して exact parity 保証
- `_wilder_smooth_recurrence_jit(values, out, n, seed)` で in-place 書き込み
- seed = NaN なら out 全 NaN で early return (現行 `np.nanmean` 全 NaN warning + NaN 返す semantics と整合)
- 途中 NaN は前値維持 (現行契約)

### 新規テスト

1. `TestRollingMaxNumbaParity` — 通常 / n=1 / 重複値連続 / length<n
2. `TestRollingMinNumbaParity` — 同上
3. `TestWilderSmoothNumbaParity` — 全 finite / 先頭NaN / 途中NaN / 全 NaN / length<n
4. `TestAdxViaWilderSmoothNumbaParity` — adx() 全体経路 (atr/+DI/-DI/dx/adx) で _wilder_smooth oracle 版と完全一致

---

## 実装差分

```diff
diff --git a/src/alpha_factory/primitives/_indicators.py b/src/alpha_factory/primitives/_indicators.py
@@ -19,7 +19,7 @@
 from __future__ import annotations

-from collections import deque
+import numba

 import numpy as np

@@ -116,21 +116,40 @@
 def rolling_max(values: np.ndarray, n: int) -> np.ndarray:
-    """Monotonic deque による O(N) rolling max。warmup (i<n-1) は NaN。"""
+    """Monotonic deque による O(N) rolling max。warmup (i<n-1) は NaN。
+
+    Numba JIT 化 (T088 cycle 1 profile-optimize): collections.deque を fixed-size
+    int64 circular buffer (head/tail インデックス) に置換。 現行 comparator
+    (`<=` で右から drop) をそのまま再現、 数値出力は現行と numerically identical。
+    """
     if n <= 0:
         raise ValueError(f"n must be >= 1, got {n}")
     values = np.asarray(values, dtype=np.float64)
+    return _rolling_max_jit(values, n)
+
+
+@numba.njit(cache=True, fastmath=False)
+def _rolling_max_jit(values: np.ndarray, n: int) -> np.ndarray:
+    length = values.shape[0]
+    out = np.full(length, np.nan, dtype=np.float64)
+    # circular buffer: 最大 n 個保持
+    dq = np.empty(n, dtype=np.int64)
+    head = 0
+    tail = 0
+    size = 0
+    for i in range(length):
+        # window 外を front から drop
+        while size > 0 and dq[head] <= i - n:
+            head = (head + 1) % n
+            size -= 1
+        # 小さい値を back から drop
+        while size > 0:
+            back_idx = (tail - 1 + n) % n
+            if values[dq[back_idx]] <= values[i]:
+                tail = back_idx
+                size -= 1
+            else:
+                break
+        dq[tail] = i
+        tail = (tail + 1) % n
+        size += 1
+        if i >= n - 1:
+            out[i] = values[dq[head]]
+    return out

 def rolling_min(values: np.ndarray, n: int) -> np.ndarray:
-    """Monotonic deque による O(N) rolling min。warmup (i<n-1) は NaN。"""
+    """ ... (rolling_max と完全対称、 comparator `>=` のみ違い) """
     ...
+
+@numba.njit(cache=True, fastmath=False)
+def _rolling_min_jit(values, n):
+    # rolling_max と同じ構造、 comparator `>=`
+    ...

 def _wilder_smooth(values: np.ndarray, n: int) -> np.ndarray:
-    """Wilder smoothing: seed = mean(values[:n]), α = 1/n. ..."""
+    """Wilder smoothing: seed = mean(values[:n]), α = 1/n. ...
+
+    T088 cycle 1 profile-optimize: recurrence loop のみ Numba JIT 化、 seed は
+    現行 np.nanmean/mean を維持して exact parity を保証。
+    """
     ...
     length = len(values)
     out = np.full(length, np.nan, dtype=np.float64)
     if length < n:
         return out
-    seed = float(np.nanmean(values[:n])) if np.any(np.isnan(values[:n])) else float(values[:n].mean())
+    seed = (
+        float(np.nanmean(values[:n]))
+        if np.any(np.isnan(values[:n]))
+        else float(values[:n].mean())
+    )
+    if not np.isfinite(seed):
+        return out
     out[n - 1] = seed
-    prev = seed
-    for i in range(n, length):
-        v = values[i]
-        if np.isnan(v):
-            out[i] = prev
-            continue
-        cur = (prev * (n - 1) + v) / n
-        out[i] = cur
-        prev = cur
+    _wilder_smooth_recurrence_jit(values, out, n, seed)
     return out
+
+@numba.njit(cache=True, fastmath=False)
+def _wilder_smooth_recurrence_jit(values, out, n, seed):
+    """In-place: out[n:length] を recurrence で埋める。"""
+    length = values.shape[0]
+    prev = seed
+    n_minus_1 = float(n - 1)
+    n_float = float(n)
+    for i in range(n, length):
+        v = values[i]
+        if v == v:  # not NaN
+            cur = (prev * n_minus_1 + v) / n_float
+            out[i] = cur
+            prev = cur
+        else:
+            out[i] = prev
```

(実装差分の完全版は `devnotes/20260505-1947-todo-T088/diff.txt` 参照)

---

## テスト結果サマリー

```
$ uv run pytest tests/alpha_factory/primitives/
============== 308 passed, 2 warnings in 8.40s ==============

$ uv run pytest tests/alpha_factory/primitives/test_indicators.py
============== 48 passed, 2 warnings in 0.29s ==============

$ uv run ruff check src/alpha_factory/primitives/_indicators.py tests/alpha_factory/primitives/test_indicators.py
All checks passed!

$ uv run mypy src/alpha_factory/primitives/_indicators.py
Success: no issues found in 1 source file
```

新規 parity test 14 件 + 既存 indicator test 全 PASS。 NaN 契約 (warmup / 途中 NaN 前値維持 / seed=NaN early return) 全 case 検証済。 ADX 経由 parity (atr / +DI / -DI / dx / adx の 5 値) も `_wilder_smooth` oracle と bit-identical。

注: `tests/alpha_factory/test_t058_integration.py::test_end_to_end_writes_v2_summary_json` および `tests/scripts/test_run_ga_parallel.py` 13 件は **main HEAD で pre-existing failure** (T087 Stage B fold guard 起因)、 T088 と無関係。

---

## レビュー依頼事項

1. detailed-design.md (Round 2 APPROVED) の実装が完全に反映されているか
2. circular buffer head/tail/size 管理に off-by-one / wrap-around バグがないか (n=1 edge case 含む)
3. `_wilder_smooth` seed 計算が現行と完全に同じ演算順序で計算されているか (`np.any(np.isnan(values[:n]))` 分岐含む)
4. `_wilder_smooth_recurrence_jit` の `v == v` で NaN 判定 + 前値維持が現行 `np.isnan(v)` と等価か
5. parity test の case 網羅 (n=1 / 重複値連続 / 先頭NaN / 途中NaN / 全NaN / length<n) が detailed-design の要求と整合か
6. その他 [Critical] / [Warning] / [Suggestion]

全体判定 (APPROVED / CHANGES_REQUESTED / INCONCLUSIVE) を出力してください。
