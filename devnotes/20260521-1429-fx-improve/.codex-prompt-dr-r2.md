Round1 の [Critical]2件 + [Warning]2件 を反映し具体化しました。

[Critical1] 本番parallel経路への配線: run_ga.py:2004 の LaneEvalContext.cp_inputs に CrossPairLaneInputs を配線。enable時のみ非None。parallel_eval.py:344 が cp_inputs not None で cross-pair実行。GraduationLane.pair_bars も逐次経路用に供給。
[Critical2] 固定2anchor: --cross-pair-anchors 廃止し cross_pair.ANCHOR_PAIRS[target]自動採用(EUR_JPY→EUR_USD+USD_JPY)。config cross_pair.enable:bool=False(default OFF)、CLI --cross-pair-enable。ANCHOR_PAIRSに無いtargetはskip+warning。
[Warning1] cross_pair_runtime_mode = "enabled" if cp_inputs is not None else "skipped_single_instrument"。
[Warning2] anchorは holdout-only loader (_load_holdout_only: _stream_bars を holdout区間のみ)。Stage A/Bロードしない。

具体実装(run_ga.py:2004付近):
cp_inputs=None; if cfg.cross_pair.enable: anchors=ANCHOR_PAIRS.get(tgt); if anchors: pbm={tgt:tuple(bundle.bars_holdout)}+各anchorのholdout(_load_holdout_only); mm={...}; cp_inputs=CrossPairLaneInputs(target_pair=tgt,pair_bars_map=pbm,meta_map=mm); GraduationLaneにも供給。 lane_ctx=LaneEvalContext(...,cp_inputs=cp_inputs). cross_pair_mode=cp_inputs基準。
CrossPairLaneInputs(parallel_eval.py:82): target_pair/pair_bars_map:Mapping[str,tuple[PriceBar,...]]/meta_map、frozen+pickle対応(worker broadcast安全)。ANCHOR_PAIRS全6target対応確認済。
anchor bars ロード失敗は fail-closed(RuntimeError、cross-pair検証が目的のため部分実行無意味)。
default enable=False で cp_inputs=None=現状skipped完全不変。shadow維持(graduationはcross_pair.passedで自然機能、Stage C passed非介入はRound1 Suggestionで確認済)。

メモリ: target+2anchor=3ペア×holdout60d M1(~60k bars)×worker broadcast≈数百MB(24GB OK)。

この改訂で Critical 解消し APPROVED か確認してください。残あれば最小変更で。R87: --cross-pair-enable + warmstart0.1 motif=R85 seed68 で cross_pair_runtime_mode=enabled・ii_lite_pass bool化・graduation確認。
