## Round 4: Round 3 Warning への最終対応

Round 3 で Warning 1 点のみ: `import math` 未使用 + `_bar` 内 local import 重複。

### 修正
- `_bar` 内の `import math` を削除
- トップレベルの `import math` に統一（`_bar` の `math.sin` で使用）

個別 `ensure_registered()` もすべて削除して autouse fixture に統一。

### 残存リスク
- 無し。Round 3 で全 Critical / Warning 対応済み、Round 4 は lint cleanup のみ

判定をお願いします:
- APPROVED: 実装フェーズへ進む
- CHANGES_REQUESTED / INCONCLUSIVE: 残存点を明示
