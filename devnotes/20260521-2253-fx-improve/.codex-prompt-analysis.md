あなたは Alpha Factory (FX イントラデイ GA) の分析レビュアーです。Run 87 を C9 falsification-first で簡潔に独立分析してください。

# 使命/制約
live_criteria 全達成個体を1つ見つける。graduation = Stage C pass AND cross-pair(ii-lite) pass。達成後は閾値引き上げ(緩和禁止)。イントラデイ/ロングショート/コスト控除後純益。

# 5サイクルの到達点
mission を 達成可能(R83)/cost-robust(R85/T110)/再現可能(R86/T101 warmstart) にした。cycle5でT114 cross-pair enableを実装。

# Run 87 事実 (T114 cross-pair enable + warmstart0.1 + seed68)
- cross_pair_runtime_mode=enabled (anchors EUR_USD+USD_JPY、n_pairs=3)。ii_lite_pass None→bool化成功。
- Stage A/B/C=2495/1970/599。★Stage C 599個体すべて ii_lite_pass=False、cross-pair汎化0/599、graduation=0。
- mission_signed_margin_c_shadow 中央値-1.367(min-15.6,max0.919,正は4/599のみ)、b_shadow中央値-3.147。
- → EUR_JPY mission個体は100% in-sample過学習、margin深い負(限界的失敗でなく全く通らない)。

# 根本原因仮説
GA fitness_pen = EUR_JPY in-sample sharpe − α·size。cross-pair は最終Stage Cでのみ評価され、GA選択/fitnessに汎化シグナルが入らない → GAは汎化方向に探索圧ゼロ → 純in-sample winnerに収束。

# 候補 (cycle6)
(a)multi-pair training(fitnessを複数ペアで評価、本質的だが大規模・cross-pair eval×3で~7h/runの更なる増大)、(b)cross-pair in-loop selection pressure(GA選択キー/fitnessにcross-pair反映、コスト対策=間引き/top-Nのみeval/近似proxy必須)、(c)overfit penalty(安価な汎化proxy例:sub-period成績分散をfitnessに、cross-pair evalなし)。

# 求める出力 (簡潔、日本語)
1. Facts要約(1-3行)
2. 解釈(各仮説に反証可能性): (a)0/599が「深い過学習」で根本原因がGAの汎化選択圧ゼロという診断の妥当性、(b)cross-pair eval高コスト下で実行可能な汎化施策はどれか、(c)安価な汎化proxy(cross-pair evalなしで in-sample過学習を抑制)は成立するか
3. 次サイクル候補(Critical 1個 / Warning 1-2個)。低リスク最優先(段階導入/default不変)、メタ過学習ガード(Reactive Parametric禁止)、閾値緩和禁止、loop停止回避。
4. 全体判定 OK/CONCERN/CRITICAL_DRIFT
