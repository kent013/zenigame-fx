あなたは経験豊富なクオンツ・システムアーキテクトです。zenigame-fx cycle6 の cross-pair in-loop selection pressure 詳細設計をレビューしてください。

# 背景/真因
mission(EUR_JPY live_criteria)は達成可能+cost-robust+再現可能だが R87(T114 cross-pair enable)で汎化を定量化: Stage C 599個体すべて ii_lite_pass=False、cross-pair汎化0/599、margin median-1.37(深い過学習)。根本原因: GA fitness_pen=EUR_JPY in-sample sharpe のみ、cross-pairは最終評価のみ→GA選択に汎化シグナルゼロ→純in-sample winner収束(Codex CRITICAL_DRIFT)。

# 設計の核(新規eval不要)
R87でcross-pairは cross_pair.enable=True時 evaluate_genome内でStage B pass個体全件で既計算済(margin=archive row の mission_signed_margin_c_shadow/_b_shadow)。GenomeEntry(IndividualCacheEntry)に未伝搬。→既計算marginを row→IndividualCacheEntry→selection_score へ伝搬するだけ。コストR87同等。

# 変更
1. CrossPairConfig: selection_pressure:bool=False(default OFF=selection_score不変bit-exact、enable=True前提) + selection_pressure_margin_threshold:float=0.0。_build_cross_pair読込、__post_init__でpressure&not enableはwarning(fail-soft)。
2. CLI --cross-pair-selection-pressure + _args_to_overrides。
3. IndividualCacheEntry に cross_pair_margin:float|None=None追加(selection_score propertyは純粋保持、config非依存)。
4. cache構築(run_ga.py:1206): cross_pair_margin=_coerce_optional_float(row.get("mission_signed_margin_c_shadow") or row.get("mission_signed_margin_b_shadow"))。4段伝搬(row→entry→_selection_key)。
5. _selection_key(255): selection_pressure/margin_threshold引数追加。OFF(default)=現行10-tuple そのまま(bit-exact)。ON=fold_robust(9)とfitness_pen(10)の間に int(cross_pair_margin is not None and >threshold) 挿入→11-tuple(fold_robustより下位/fitness_penより上位=弱いtie-break)。呼出4箇所(_select_best/_breed_next_gen 2箇所/1236)にthread。

# 現行selection_score (run_ga.py:207、10-tuple lex)
(feasible, -violation, stage_b_pass_and_feasible, stage_b_pass, stage_c_feasible, C_pass, B_pass, A_pass, fold_robust, fitness_pen)。末尾fitness_pen。

# 現行 cache構築 (run_ga.py:1206)
IndividualCacheEntry(generation=,fitness_pen=fp,stage_a_pass=,stage_b_pass=,stage_c_pass=,feasible=,violation_magnitude=,stage_c_feasible=,fold_robust=,trade_count_full_dataset=,pareto_*=) を row(archive dict)から構築。

# R88検証
ON+enable+warmstart+seed68/69 A/B(vs R87 OFF baseline)、ii_lite_pass率/mission_signed_margin_c_shadow改善・汎化個体>0(改善なければロールバック)。

# レビュー観点
1. ★default(selection_pressure=False)で selection_score 10-tuple完全不変・rng/population bit-exactか(_selection_keyがOFF時に新要素挿入しない設計)。
2. 4段伝搬(row→IndividualCacheEntry→_selection_key)の漏れ、margin列名(mission_signed_margin_c_shadow/_b_shadow)とfallbackの妥当性、符号(大が良い=正が汎化寄り)。
3. tie-break位置(fold_robustとfitness_penの間に int(margin>threshold))が汎化圧として妥当か/弱すぎ/強すぎ。bool化(margin>0)でなく連続margin直接の方が良いか。
4. _selection_key thread漏れ(呼出4箇所)。selection_score property純粋保持の是非。
5. Reactive Parametric回避(cross_pair marginはStructuralシグナルか)。メタ過学習。
6. selection_pressure&not enable時のfail-soft(warning、tie-break要素0で無圧)。
7. コスト(新規evalなし=R87同等)、メモリ。
8. テスト網羅(bit-exact/伝搬/順序)。

# 出力
施策判定 APPROVE/REQUEST_CHANGES/INCONCLUSIVE、[Critical][Warning][Suggestion](Critical/Warningに修正案)、全体判定、日本語。【収束】APPROVED または1反証可能仮説+1最小変更。

## 詳細設計書
# 詳細設計: Run 88 施策 (cycle 6: cross-pair in-loop selection pressure)

## 使命・制約
mission 個体を多ペア汎化させる。GA 選択に cross-pair 汎化シグナルを弱く注入し、in-sample 過学習(0/599 汎化)から脱却。default OFF で挙動完全不変(selection_score bit-exact)。閾値緩和なし(加点のみ)。メタ過学習ガード: cross_pair_margin は Structural シグナル(Reactive Parametric でない)。

## 真因 (cycle 6 確定)
GA fitness_pen = EUR_JPY in-sample sharpe のみ。cross-pair は最終評価でのみ計算され GA 選択に汎化シグナルが入らない → 0/599 汎化(margin median -1.37)。

## ★ 設計の核 (新規 eval 不要)
R87 で cross-pair は `cross_pair.enable=True` 時に evaluate_genome 内で Stage B pass 個体全件で既に計算済 (margin は archive row の `mission_signed_margin_c_shadow` / `_b_shadow`)。GenomeEntry(IndividualCacheEntry) には未伝搬。
→ **既計算 margin を row→IndividualCacheEntry→selection_score へ伝搬するだけ**。新規 eval なし、コスト R87 同等。top-N 疎注入は将来最適化(本サイクル範囲外)。

### target_metric / failure_mode / causal_path / falsification / success_criterion
- target_metric: ii_lite_pass 率 / mission_signed_margin_c_shadow の baseline(R87)比改善、汎化個体(ii_lite_pass=True)>0。
- failure_mode: 0/599 汎化 = GA 汎化探索圧ゼロ。
- causal_path: cross-pair margin を selection_score に弱く反映 → 汎化寄りの個体が選択で残り繁殖 → 世代を経て margin 分布が正方向シフト・ii_lite_pass=True 出現。
- falsification: R88(ON)で ii_lite_pass 率/margin が R87 比改善せず or 汎化個体=0 → 施策無効、ロールバック。
- success_criterion: R88 で ii_lite_pass=True>=1 or mission_signed_margin_c_shadow median が有意改善 (e.g. -1.37 → -1.0 以上)。

## 変更箇所

### 1. config (`src/alpha_factory/cross_pair.py` CrossPairConfig)
- `selection_pressure: bool = False` (default OFF=selection_score 不変 bit-exact)。`cross_pair.enable=True` が前提 (margin 計算に必要)。
- `selection_pressure_margin_threshold: float = 0.0` (tie-break の margin 閾値、Principled: 正 margin=cross-pair 寄与ありを優遇)。
- `_build_cross_pair` (config.py:607) に `_strict_bool(raw.get("selection_pressure"), False)` + threshold 読込。
- `__post_init__`: selection_pressure=True かつ enable=False なら warning (margin 計算されないため無効) — fail-soft (raise でなく warn、default OFF 不変)。

### 2. CLI (`run_ga.py`)
- `--cross-pair-selection-pressure` (store_const True default None) + `_args_to_overrides` の cross_pair section に `selection_pressure`。

### 3. IndividualCacheEntry (`run_ga.py:~160`)
- `cross_pair_margin: float | None = None` 追加 (pareto_* と同様の optional)。
- selection_score は **純粋保持** (config 非依存)。反映は _selection_key で行う (下記)。

### 4. cache 構築 (`run_ga.py:1206` IndividualCacheEntry(...))
- `cross_pair_margin=_coerce_optional_float(row.get("mission_signed_margin_c_shadow"))` を追加。
  - c_shadow が None の Stage B 個体は b_shadow fallback: `row.get("mission_signed_margin_c_shadow") or row.get("mission_signed_margin_b_shadow")` (Codex で fallback 要否確認)。
- ★ 4 段伝搬 (Codex impl-review 重点): row(eval payload→archive 列) → IndividualCacheEntry → _selection_key。row に margin 列があることを確認済 (R87 archive に mission_signed_margin_c_shadow 存在)。

### 5. _selection_key (`run_ga.py:255`) — selection_pressure 反映
現状: `_selection_key(entry, fallback)` が entry.selection_score (10-tuple) or _legacy を返す。
変更: `_selection_key(entry, fallback, selection_pressure: bool = False, margin_threshold: float = 0.0)` に拡張。
- selection_pressure=False (default): **現状の 10-tuple をそのまま返す (bit-exact)**。
- selection_pressure=True: 10-tuple の fold_robust(9要素目) と fitness_pen(10要素目) の間に `int(entry.cross_pair_margin is not None and entry.cross_pair_margin > margin_threshold)` を挿入 → 11-tuple。fold_robust より下位・fitness_pen より上位 = 「同 fold_robust なら cross-pair 寄与ある個体を fitness_pen より優先」= 弱い tie-break 圧。
- 呼出側 (_select_best 1234 / _breed_next_gen 960/989 / 1236) に selection_pressure/threshold を thread (cfg.cross_pair から)。

### default bit-exact 保証 (最重要)
- selection_pressure=False で _selection_key は現行 10-tuple をそのまま返す (新要素挿入なし)。cross_pair_margin フィールド追加は selection_score property に影響しない (property は margin を見ない)。∴ rng/selection/population 完全不変。既存テスト全 pass が条件。

### テスト計画
- [ ] CrossPairConfig.selection_pressure default False / _build_cross_pair 読込 / threshold default 0.0。
- [ ] IndividualCacheEntry.cross_pair_margin default None。
- [ ] ★ bit-exact: selection_pressure=False で _selection_key が現行 10-tuple と完全一致 (cross_pair_margin 設定有無に依らず)。既存 run_ga_parallel pass。
- [ ] selection_pressure=True で 11-tuple、margin>threshold 個体が同 fold_robust の低 margin 個体より上位 (順序テスト)。
- [ ] cache 構築で row.mission_signed_margin_c_shadow → cross_pair_margin populate (fallback 含む)。
- [ ] selection_pressure=True & enable=False で warning (margin None → tie-break 要素 0 で実質無圧、壊れない)。

### リスク
- 中。default OFF で bit-exact。リスクは _selection_key の thread 漏れ (呼出箇所 4 つ全てに渡す) / margin の符号意味取り違え (大が良い=正が汎化寄り)。コストは R87 同等 (新規 eval なし)。
- 選択圧が弱すぎて効かない可能性 → R88 falsification で検証 (改善なければ tie-break 位置を上げる or 将来 fitness ブレンド)。

## Run 88 実行パラメータ
| パラメータ | 値 |
|-----------|-----|
| instrument | EUR_JPY |
| --cross-pair-enable | 有効 (margin 計算に必要) |
| --cross-pair-selection-pressure | 有効 |
| warmstart-ratio | 0.1 (motif=R86、mission 母集団確保) |
| seed | 68 と 69 (A/B、vs R87 baseline) |
| pop/gen | 96/60 |

> R88(ON) で ii_lite_pass 率 / mission_signed_margin_c_shadow が R87(同 seed, pressure OFF) 比改善するか、汎化個体>0 が出るかを A/B 検証。改善なければロールバック。

## Codex consensus + design-review: 未実施 (次ステップ)
重点: 4 段伝搬漏れ / default bit-exact / _selection_key thread / margin 符号と fallback / tie-break 位置 (fold_robust と fitness_pen の間) の妥当性 / 選択圧の強さ / Reactive Parametric 回避 (margin は Structural)。
