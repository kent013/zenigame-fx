【思考原則 / 使命 / 制約 / 禁止事項 / ツール使用制限】
zenigame-fx-codex-review 継承 (前 prompt と同じ)。 ループ収束ルール: 1 round で APPROVED または「1 仮説 + 1 最小変更」。

---

# レビュー対象

cycle 5 詳細設計 (C5-1: 再現性 check)。 run 引数のみ変更で実装変更なし。

# 変更内容

- run 引数: `--seed 42` → `--seed 43`
- config / コード / docs / テスト 変更なし
- 波及変更なし

# 期待効果

- Run 57 (seed=42) と Run 58 (seed=43) で fp 分布 / Best 個体 / unique fp ratio / Stage 通過数の差分観測
- elite collapse 悪化 (Run 57 で 10%) が seed 起因か構造起因か切り分け
- 新 baseline (cycle 4 C1 後) の variance プロファイル取得 → cycle 6 で DSR 配線時のテスト設計を補強

# レビュー観点

1. **メタ過学習ガード**: Principled Parametric として正当か (seed 変更は探索独立性確保)
2. **禁止事項抵触**: 1 (期間延長) / 2 (見栄え) / 4 (緩和) / 6 (取引数削減) / 7 (オーバーナイト) いずれにも該当しないか
3. **時間効率**: 1 cycle 内で完結するか
4. **実装リスク**: なし (run 引数のみ) を確認

# 質問

## Q1. 全体判定: APPROVED / REQUEST_CHANGES / INCONCLUSIVE
1 round で完結を期待。 [Critical] / [Warning] / [Suggestion] があれば指摘。

## Q2. R1-R3 (Run 58 結果に基づく後続 cycle の調査テーマ) で見落としている論点はあるか？
- R1: Run 58 fp が大幅悪化 → 「Run 57 は単発 lucky draw」
- R2: Run 58 完走時間が Run 57 から乖離
- R3: Run 58 が Run 57 と同一結果 → seed が効いていない

# 出力形式

```markdown
## Q1 判定: APPROVED / REQUEST_CHANGES / INCONCLUSIVE
（[Critical] / [Warning] / [Suggestion] が該当時のみ）

## Q2 R1-R3 補足

## 全体判定
APPROVED / REQUEST_CHANGES / INCONCLUSIVE
```
