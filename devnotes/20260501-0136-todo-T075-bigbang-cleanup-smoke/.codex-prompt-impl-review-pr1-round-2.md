# Round 2: Round 1 指摘の対応マトリクス + 修正内容

Round 1 verdict は **NEEDS_REVISION**、 1 Critical / 2 Warning / 1 Suggestion を全反映しました。

## 対応マトリクス

| Round 1 ID | 指摘内容 | 対応 |
|---|---|---|
| C-01 | `PerRunSmokeDoDResult.overall_evidence_class` の整合 invariant 未実装 (= items.status 集約と overall_evidence_class の一致を検証していない) | smoke.py に `_aggregate_evidence_class` helper を追加 (= EvidenceClass 4 値 max 集約、 順序: hard_fail > warning > inconclusive > ok)、 `PerRunSmokeDoDResult.__post_init__` で `items.status` から再集約して `overall_evidence_class` 不一致時 ValueError raise. test_smoke.py に `test_per_run_dod_result_overall_aggregation_invariant` (F23d) 追加で hard_fail 隠蔽 / warning 隠蔽の不正構築 + 正しい構築の 3 ケース検証. |
| W-01 | F35/F36 と実テストのトレーサビリティ差分 (= scripts/** / config/** の実ファイル走査テスト不在) | test_smoke.py に `test_smoke_module_no_runtime_import_from_scripts` (F35) + `test_smoke_module_no_config_yaml_keys` (F36) を追加. scripts/**/*.py を AST 解析で `src.alpha_factory.smoke` import 経路 0 件確認 + config/**/*.yaml を yaml.safe_load + 再帰 dot-notation で `smoke` / `bigbang_cleanup` / `release_action_recommendation` key 0 件確認. parser_failure_mode='fail_closed' (= parse 失敗を test FAIL に倒す) も実装. |
| W-02 | conformance fixture の 5 metric 厳密検証が弱い (= 5 metric 必須完全一致アサーション不足) | `test_classifier_supported_metric_names_returns_frozenset` (F12) に expected 5 metric (= ab_divergence / epoch_consistency / warmstart_shortfall / bypass_ratio / session_entropy) との完全一致アサーション追加. |
| S-01 | `decide_release_action` の fail-closed 防御 (= severity が "hard_fail"/"warning" 以外で "no_blocker_observed" に流れる) を明示化 | smoke.py の `decide_release_action` 先頭に severity 範囲外 (= "inconclusive" 等の外部不整合入力) → `hold_for_review` + `requires_manual_review=True` の fail-closed 分岐追加. test_smoke.py に `test_decide_unexpected_severity_fail_closed` (F30c) 追加で severity="inconclusive" → hold_for_review 検証. |

## 結果

- pytest tests/alpha_factory/test_smoke.py: **75 passed** (Round 1 71 → +4 新規 test)
- pytest tests/alpha_factory/ (regression): **2050 passed, 1 xfailed** (Round 1 から +4、 0 regression)
- ruff check src/alpha_factory/smoke.py tests/alpha_factory/test_smoke.py: All checks passed
- mypy src/alpha_factory/smoke.py tests/alpha_factory/test_smoke.py: Success, no issues

## 主な変更箇所 (再レビュー)

1. `src/alpha_factory/smoke.py`:
   - `_aggregate_evidence_class(statuses)` helper 追加 (詳細設計 § 3.1 _EVIDENCE_CLASS_ORDER 厳格適用)
   - `PerRunSmokeDoDResult.__post_init__` で items.status 集約 invariant 強制 (C-01)
   - `decide_release_action` 先頭 fail-closed 分岐 (S-01)

2. `tests/alpha_factory/test_smoke.py`:
   - F12 conformance: 5 metric 完全一致アサーション (W-02)
   - F23d: PerRun 集約 invariant test (C-01)
   - F30c: decide fail-closed test (S-01)
   - F35: scripts/**/*.py AST 走査 (W-01)
   - F36: config/**/*.yaml dot-notation 走査 (W-01)
   - `_aggregate_test_statuses` helper 追加 + `_make_per_run_result` の `overall` を None 許容 (= 自動算出)

## 追加レビュー観点

特にこの Round 1 指摘の修正で **新たに導入された** invariant / fail-closed 経路に対して以下を確認してください:

1. `_aggregate_evidence_class` の order が詳細設計 § 3.1 _EVIDENCE_CLASS_ORDER と一致しているか
2. `PerRunSmokeDoDResult.__post_init__` の invariant が caller (= 概念 § 4 / 詳細 § 4.1) の集約規範を破らないか
3. `decide_release_action` の fail-closed 分岐が **classify_smoke_outcome の通常経路** (= severity ∈ {hard_fail, warning, ok}) を壊していないか
4. F35 / F36 が scripts/** / config/** の実ファイル走査で fail_closed 動作を正しく担保しているか (= parse 失敗時 test FAIL)

その他、 残存の Critical / Warning / Suggestion / 改善提案がないか再確認してください。

ファイル読み込みは許可されています。 リポジトリ root: `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T075-pr1/`
