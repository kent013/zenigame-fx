Round 1 の [Critical] を反映しました。対応マトリクス:

| # | 指摘 | 重要度 | 対応 | 詳細 |
|---|------|--------|------|------|
| 1 | worker_error StageResult（live_criteria_pass 欠落）が unknown/stress_or_other に誤分類 | Critical | 採用 | `_derive_stage_c_gap` の優先順位1を `("system_failure" in reasons) or ("worker_error" in reasons)` に変更し system_fail に集約。テスト計画に worker_error sentinel ケースを1件追加 |

確認: swim_lane.py:864-879 の worker_error c_result は record_stage_c（:888）に渡されることを確認済み。reason_codes=("worker_error",)、payload は worker_error_code/message のみで live_criteria_pass を持たない。修正後はこれが優先順位1で system_fail に分類される。

他の判定（C1-(2) sidecar APPROVE / C1-(3) swim_lane APPROVE）は変更なし。

この1点修正で全体 APPROVED に到達するか確認してください。残 Critical/Warning があれば最小変更で提示してください。
