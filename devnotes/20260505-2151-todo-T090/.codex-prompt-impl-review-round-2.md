## Round 1 指摘への対応マトリクス

| 指摘 | 対応 | 場所 |
|---|---|---|
| [Suggestion] docstring の「現行と完全に一致する」→ 数値結果のスコープ限定 (cast 順序変更による例外発生タイミングまで「完全同一」と読めない表現に) | **対応済**: docstring を「**数値結果** (有限値の bit-identical / quiet NaN の同位置発生) に関して現行と一致する。例外発生タイミングは cast 順序変更により異なり得るため、例外境界での完全同一性は保証しない (signal 値計算用 mid OHLC として影響なし)。」に修正 | `src/alpha_factory/primitives/_bars_cache.py` `_compute` docstring |
| [Warning] `test_compute_microbenchmark_non_regression` が hard assert で通常実行され、設計意図 (観測) と運用 (gate) が不整合。CI ノイズで false positive リスク | **対応済**: `@pytest.mark.skipif(not os.environ.get("ZENIGAME_FX_RUN_BENCH"), reason=...)` で **default skip** に変更。 `ZENIGAME_FX_RUN_BENCH=1` 設定時のみ観測実行可能 | `tests/alpha_factory/primitives/test_bars_cache.py` `test_compute_microbenchmark_non_regression` |

## 修正後 diff

```diff
diff --git a/src/alpha_factory/primitives/_bars_cache.py b/src/alpha_factory/primitives/_bars_cache.py
@@ -62,6 +62,18 @@ def _compute(
     """mid OHLC (bid/ask 平均) を float64 配列で返す。cache 無しの pure compute。

     元の 3 モジュールに複製されていた実装を統合した canonical 実装。
+
+    profile-optimize cycle 3/3 (T090): bid/ask 属性アクセスを 1 bar あたり
+    1 回ずつ local 変数化 (b.bid → bid、b.ask → ask)、 中間変数
+    (bo, bh, bl, bc, ao, ah, al, ac) を削除して直接 mid 計算に集約。
+    LOAD_ATTR 削減のみが目的、 加算・乗算の演算順序および
+    `float(...)` cast 回数 (8 回/bar) は完全同一。
+    cast 順序は ``bo,bh,bl,bc,ao,ah,al,ac`` 一括先行 → mid 計算 から
+    OHLC 単位で交互 (``open bid+ask → high bid+ask → ...``) へ変更されるが、
+    各 mid 計算式 ``(float(bid.X) + float(ask.X)) * 0.5`` は **数値結果**
+    (有限値の bit-identical / quiet NaN の同位置発生) に関して現行と一致する。
+    例外発生タイミングは cast 順序変更により異なり得るため、 例外境界での
+    完全同一性は保証しない (signal 値計算用 mid OHLC として影響なし)。
     """

diff --git a/tests/alpha_factory/primitives/test_bars_cache.py b/tests/alpha_factory/primitives/test_bars_cache.py
+import os
...
+@pytest.mark.skipif(
+    not os.environ.get("ZENIGAME_FX_RUN_BENCH"),
+    reason=(
+        "microbenchmark は観測目的 (Codex Round 1 推奨)。 共有 CI 環境では "
+        "ジッタによる false positive を避けるため default skip。 ローカル観測は "
+        "`ZENIGAME_FX_RUN_BENCH=1` 設定で実行可能。"
+    ),
+)
+def test_compute_microbenchmark_non_regression() -> None:
+    """T090: 軽量版 `_compute` が現行 oracle に対して退行していないことを観測。
+
+    Codex Round 1 推奨 + Round 2 反映: 必須 gate ではなく観測 (default skip)。
+    `ZENIGAME_FX_RUN_BENCH=1` 設定時のみ実行され、 軽量版が oracle の
+    median × 1.10 倍を超えないことを assert。 timing は `perf_counter_ns`
+    で 25 回 median (warmup 3 回除外) + 順序効果緩和のため interleave。
+    ...
+    """
```

## 再テスト結果

```
$ uv run pytest tests/alpha_factory/primitives/test_bars_cache.py -v
tests/alpha_factory/primitives/test_bars_cache.py::test_cache_hit_returns_identical_arrays PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_cache_miss_on_different_bars_object PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_cache_miss_on_length_change PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_cache_correctness_vs_direct_compute PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_lru_eviction_at_max_entries PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_lru_hit_updates_order PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_cache_and_refs_lockstep PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_identity_guard_rejects_id_reuse PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_clear_cache_empties_both_dicts PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_compute_local_var_refactor_numerically_identical PASSED
tests/alpha_factory/primitives/test_bars_cache.py::test_compute_microbenchmark_non_regression SKIPPED
======================== 10 passed, 1 skipped in 0.37s =========================

$ ZENIGAME_FX_RUN_BENCH=1 uv run pytest .../test_compute_microbenchmark_non_regression -v
tests/.../test_compute_microbenchmark_non_regression PASSED
============================== 1 passed in 0.25s ===============================

$ uv run ruff check src/alpha_factory/primitives/_bars_cache.py tests/alpha_factory/primitives/test_bars_cache.py
All checks passed!

$ uv run mypy src/alpha_factory/primitives/_bars_cache.py
Success: no issues found in 1 source file
```

## 確認依頼

Round 1 で指摘いただいた 2 点 ([Warning] microbenchmark hard assert / [Suggestion] docstring scope) は両方対応済みです。 これで全体判定 APPROVED に変更できますか?
追加で気になる点があれば CHANGES_REQUESTED として具体的に教えてください。
