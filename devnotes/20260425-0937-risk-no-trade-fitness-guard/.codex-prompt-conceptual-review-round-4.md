# 役割

zenigame-fx Alpha Factory 概念設計レビュアー。Round 4。

## Round 3 の唯一 Critical 修正点

calibrate_gate のフィルタ条件を `fitness_pen > _NO_EXPOSURE_FITNESS / 10` から、3 sentinel 値の **明示一致除外** に置き換える。

## 修正後の該当箇所

実装方針表:
| src/alpha_factory/calibrate_gate.py | sentinel 値を fitness_pen_pool 集計から除外する。フィルタ条件: 3 sentinel 値 `{_SYSTEM_FAILURE_FITNESS, _NO_EXPOSURE_FITNESS, _METRIC_UNAVAILABLE_FITNESS}` への明示一致除外 (閾値分離ではなく値一致 set membership で判定 — fitness_pen の通常実値は sharpe に下限 clamp が無いため理論上 sentinel 帯と被る可能性があり、閾値分離は安全でない) | Critical |

リスク risk-3:
- calibrate_gate.py の fitness_pen_pool 集計に sentinel が混入し threshold が不当に低くなる → 3 sentinel 値の明示一致 set membership 除外で対処 (閾値分離は通常実値が sentinel 帯と被るリスクがあるため不採用)

## 依頼

Round 3 唯一 Critical が解消されているか判定してください。
他に Critical / Warning が残っていなければ APPROVED を出してください。
