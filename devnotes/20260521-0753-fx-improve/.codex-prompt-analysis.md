あなたは Alpha Factory (FX イントラデイ GA) の分析レビュアーです。Run 85 を C9 falsification-first で簡潔に独立分析してください。

# 使命/制約
live_criteria 全達成個体を1つ見つける。達成後は閾値引き上げ(緩和禁止)。イントラデイ/ロングショート両方向/スワップ・スプレッド控除後純益。

# Run 85 事実 (P2=Stage C stress cost-robustness 化 適用後の検証 run、seed=68)
- P2 検証成功: stress_pnl_degradation 全436個体で非ゼロ化 (min3475/med11454/max22235、R83は全件0=toothless)。Stage C stress が実際に PnL を悪化させるようになった。
- Stage A/B/C = 717/436/43 (R83 seed=68 と完全同一、GA は seed 固定で deterministic)。best g51_i71 live all_pass=True (sharpe4.24/pnl65210/dd1.49%/tc51)、真の spread×1.5 cost stress(~11k悪化)下でも Stage C 通過 = cost-robust 実証。
- Stage C 通過数が不変な理由: stress gate 閾値 (spread_stress_min_total_pnl=0/min_sharpe=0) が緩く43個体は~11k cost を吸収。
- ★ seed variance: R82(s67)=A2687/B941/C0, R83(s68)=A717/B436/C43, R84(s69)=A391/B62/C0, R85(s68)=43。seed間でStage C=0/43/0、Stage B 15倍変動。R83=R85完全一致でdeterministic per-seed だが seed-locked。R85のStage C個体初出現はgen43(遅い)。
- graduation=0 (cross-pair ii-lite shadow-only、ii_lite_pass全件None)。
- 前ループ知見: profit_safe_pfr seed variance極端は既知、DSR proxy全pass=0。

# 候補アプローチ
(a) warmstart(T101登録済): 既知mission個体g51_i71を初期集団注入→seed非依存に保持。低リスク(初期集団注入のみ評価不変)。
(b) multi-seed評価: 複数seedでGA→winner intersect。コストN倍。
(c) population安定化/diversity: GA dynamics大改変、リスク高。
(d) cross-pair ii-lite gate昇格(P3): graduation配線、別軸。

# 求める出力 (簡潔、日本語)
1. Facts要約(1-3行)
2. 解釈(各仮説に反証可能性): (a)mission達成は seed=68 deterministic だが seed非依存に再現しない件の評価、(b)seed variance が真のrobustness gap である件、(c)warmstart が再現性確保に有効か/「答えの注入」で過学習リスクはないか
3. 次サイクル候補(Critical 1個=seed variance対処の具体方針 / Warning 1-2個)。低リスク最優先(loop停止回避)、閾値緩和禁止。
4. 全体判定 OK/CONCERN/CRITICAL_DRIFT
