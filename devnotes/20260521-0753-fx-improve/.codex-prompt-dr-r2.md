Round 1 の指摘を反映しました。

[Critical] mutate signature 修正: gen0 注入は `cand = mutate(src_g, rng)` (name 引数なし、operators.py:507 準拠) の後、必ず `replace(cand, name=f"g0_ws{i}", units=cfg.backtest.units)` で改名 (genome_from_dict が元 name 復元するため、g0_ws* で g0_i* と衝突回避)。provenance に g0_ws* も登録。
[Warning] invariance テスト追加: warmstart_ratio=0.0 で load_warmstart_motifs/mutate が呼ばれないこと(spy) + 同seed gen0 genome列(g0_i*)が baseline 完全一致を固定テスト化。
[Suggestion] i==0 は非mutateアンカー (motifs[0]=mission_score降順最良 を name改名のみで厳密保持)、i>=1 は mutate 派生。load_warmstart_motifs は mission_score 降順 sort。成功基準に「再現性検証であり汎化検証でない (R85→R86 同一dataset)」と明記。

default warmstart_ratio=0.0 → n_ws=0 → ws_genomes=[] → population は現行 random_genome のみ・name=g0_i*・rng消費順 完全同一 (絶対条件、テストで保証)。

この修正で Critical 解消し APPROVED に到達するか確認してください。残課題あれば最小変更で。
