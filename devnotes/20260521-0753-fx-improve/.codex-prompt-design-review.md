あなたは経験豊富なクオンツ・システムアーキテクトです。zenigame-fx の T101 warmstart 実装の詳細設計をレビューしてください。

# 目的
mission 個体 (live_criteria 全達成) を seed 非依存に得る再現性確保。GA は seed-locked (seed68→Stage C43, seed67/69→0)。warmstart は既知 mission/Stage-C 個体を初期集団に注入する足場で、評価/閾値/selection/GA dynamics は不変、default OFF で挙動完全不変。

# 既存インフラ整合 (確定済)
loop_closure.py/cpps_archive.py の warmstart(T066/T067) は Phase2統合(T102,未配線・大規模)の一部で実GA loop未配線。CPPS配線=T102は範囲外(cycle1 Codex REJECT済)。→ T101 simple initialize_population 注入を採用(独立・低リスク)。

# 設計要点
1. 新規 warmstart.py: load_warmstart_motifs(archive)→ stage_c_pass&total_pnl>=20000 の genome_json を Genome 復元 (上限64, 空でfail-soft[])。
2. GAConfig: warmstart_ratio(default 0.0=不変)/warmstart_motif_archive、__post_init__検証、yaml loader追加。
3. run_ga.py gen0 初期集団(現状: random_genome のみ population_size 個): warmstart_ratio>0 かつ archive指定時のみ n_ws=int(pop*ratio) 個を motif の mutate で生成、残りrandom。default 0.0→n_ws=0→population は現行と完全同一(同random_genome列・同rng消費順)。
4. mutate は既存breeding mutation流用。CLI --warmstart-ratio/--warmstart-motif-archive。
5. R86: seed=67と69(mission=0だったseed)でwarmstart_ratio=0.1, motif=R85 archive 適用、live all_pass>=1 が受入基準。

# 現行コード (run_ga.py gen0 初期集団)
```python
for gen in range(cfg.ga.generations + 1):
    if gen == 0:
        population = [random_genome(rng, name=f"g0_i{i}", units=..., max_clause=..., max_depth=..., registry=rg_registry) for i in range(cfg.ga.population_size)]
        provenance = {g.name: (None, None) for g in population}
    else:
        population, provenance = _breed_next_gen(prev_population, cache, cfg.ga, rg_registry, rng, gen)
```

# レビュー観点 (重点)
1. ★ default warmstart_ratio=0.0 での挙動完全不変: n_ws=0 で warmstart 経路に入らず rng 消費順・population が現行とビット同一であることの設計的保証 (最重要)。
2. mission個体注入が「答えの注入で過学習」でなく「再現性確保の足場」である正当性 (評価/閾値不変、注入個体も同じ live_criteria で評価される)。
3. 注入個体が selection で即淘汰されないか (g51_i71 は fitness_pen 0.045 と低め — fitness による淘汰リスク。受入基準R86で検証する設計で妥当か)。
4. motif の Genome 復元 (genome_json→Genome) の健全性、mutate 流用の整合 (注入個体の name/provenance)。
5. archive 自己参照 (R85 archive を R86 の warmstart source に) の妥当性、過学習/データリーク懸念 (同一 dataset/holdout を使う点)。
6. config 4段伝搬 (yaml→GAConfig→cfg.ga→run_ga)、CLI passthrough の波及 (AGENTS.md/SKILL.md)。
7. テスト網羅性。

# 出力
施策判定 APPROVE/REQUEST_CHANGES/INCONCLUSIVE、[Critical][Warning][Suggestion](Critical/Warningに修正案)、全体判定、日本語。
【ループ収束】本ラウンドで APPROVED または1反証可能仮説+1最小変更に絞る。

## 詳細設計書
# 詳細設計: Run 86 施策 (T101 warmstart adaptation, cycle 4)

## 使命・制約
live_criteria 全達成個体を seed 非依存に得る (再現性)。warmstart は初期集団への既知良個体注入 = 探索の足場で、評価関数・閾値・selection・GA dynamics は不変。default OFF で挙動完全不変。

## 既存インフラ整合 (調査結果)
- `loop_closure.py` / `cpps_archive.py` の warmstart (T066/T067) は **Phase 2 統合 (T102, 未配線・大規模) の一部**。実 GA run loop (run_ga.py:2077 は plain random_genome) には未配線。CPPS 配線 = T102 で cycle 1 Codex REJECT 済 (大規模・loop停止リスク)。
- → **T101 の simple initialize_population 注入を採用** (CPPS 機構と独立、低リスク)。既存 CPPS warmstart は触らない。

## 施策 T101: warmstart pool injection (default OFF)

### target_metric / failure_mode / causal_path / falsification / success_criterion
- target_metric: mission 個体の seed 非依存再現 (seed 67/68/69 各 run で live all_pass>=1)。
- failure_mode: mission 達成が seed-locked (seed68→43, seed67/69→0)。GA 探索軌道が seed 依存で profitable region 到達が不安定。
- causal_path: 既知 mission/Stage-C 個体を初期集団に注入 → gen 0 から評価・選択対象 → seed に依らず profitable region の足場が存在 → mission 個体が保持/再発見される。
- falsification: warmstart 適用後も seed 67/69 で live all_pass=0 のまま (注入個体が選択で淘汰される or 評価不能)。
- success_criterion: R86 (seed=67, 69) で各 live all_pass>=1。

### 変更箇所
1. **`src/alpha_factory/warmstart.py` (新規)**:
   - `load_warmstart_motifs(archive_path: Path) -> list[Genome]`: archive Parquet から `stage_c_pass==True AND total_pnl>=20000` の行の `genome_json` を抽出し Genome に復元。同 cluster (primitive set Jaccard>=0.7) merge は任意 (まず全件 list でも可、Codex Warning の多様性は mutation で担保)。空/不在時は `[]` 返し fail-soft。
   - 上限 (例 max_motifs=64) で過大注入を防ぐ。
2. **`src/alpha_factory/config.py` GAConfig (131)**:
   - `warmstart_ratio: float = 0.0` (default 0.0 = 完全不変), `warmstart_motif_archive: str | None = None`。
   - `__post_init__` で `0.0 <= warmstart_ratio <= 1.0` 検証。
   - yaml loader (config.py:515 付近) に `warmstart_ratio` / `warmstart_motif_archive` 読込追加。
3. **`scripts/alpha_factory/run_ga.py` gen 0 初期集団 (2077)**:
   ```python
   if gen == 0:
       n_ws = int(cfg.ga.population_size * cfg.ga.warmstart_ratio)
       ws_genomes = []
       if n_ws > 0 and cfg.ga.warmstart_motif_archive:
           motifs = load_warmstart_motifs(Path(cfg.ga.warmstart_motif_archive))
           for i in range(n_ws):
               if not motifs: break
               seed_g = motifs[rng.randrange(len(motifs))]
               ws_genomes.append(mutate(seed_g, name=f"g0_ws{i}", mutation_rate=cfg.ga.mutation_rate, ...))
       n_rand = cfg.ga.population_size - len(ws_genomes)
       population = ws_genomes + [random_genome(rng, name=f"g0_i{i}", ...) for i in range(n_rand)]
   ```
   - **default warmstart_ratio=0.0 → n_ws=0 → population は現行と完全同一 (random_genome のみ、rng 消費順も不変)**。これが数値同一性の絶対条件。
   - mutate は既存の breeding mutation を流用 (src/ga の mutation 関数)。注入個体も provenance に登録。
4. **CLI** (run_ga.py argparse): `--warmstart-ratio` / `--warmstart-motif-archive` 追加 + cfg.ga へ反映。
5. **波及**: AGENTS.md / SKILL.md に新 CLI オプション記載 (run-alpha-factory が passthrough)。

### default 不変の保証 (最重要)
- warmstart_ratio=0.0 (default) で n_ws=0 → ws_genomes 空 → population = 現行と同じ random_genome 列 (同 rng・同順)。既存全テスト pass が条件。
- rng 消費: warmstart 経路は `n_ws>0` のときのみ rng を消費。n_ws=0 で rng 状態は現行と完全一致。

### テスト計画
- [ ] load_warmstart_motifs unit: R85 archive (.cache/alpha_factory/runs/genomes_run_20260520_202321.parquet) から motif 抽出、stage_c_pass&pnl>=20000 のみ、空 archive で []。
- [ ] GAConfig warmstart_ratio 検証 (範囲外 raise)、default 0.0。
- [ ] initialize: warmstart_ratio=0.0 で population が baseline と完全一致 (同 seed で genome 列同一)。
- [ ] warmstart_ratio=0.1 で初期集団に warmstart 由来個体 (g0_ws*) が pop*0.1 個含まれる。
- [ ] 既存 run_ga / config テスト全 pass (default 不変)。

### リスク
- 低-中。default OFF で挙動不変。リスクは rng 消費順の混入 (n_ws=0 で経路に入らないことをテストで保証)。mutate 関数の流用ミス。
- 注入個体が即淘汰される可能性は受入基準 (R86) で検証 (falsification)。

## Run 86 実行パラメータ (warmstart 検証)
| パラメータ | 値 |
|-----------|-----|
| instrument | EUR_JPY |
| population-size | 96 |
| generations | 60 |
| mutation-rate | 0.5 |
| max-workers | 2 |
| stage-b-gate-kind | profit_safe_pfr |
| **warmstart-ratio** | **0.1** |
| **warmstart-motif-archive** | **.cache/alpha_factory/runs/genomes_run_20260520_202321.parquet** (R85) |
| seed | **67 と 69 を各 run** (R84/R82 で mission=0 だった seed で再現性検証) |

> R86 は seed=67, seed=69 の 2 run。warmstart で各 run live all_pass>=1 なら受入基準達成 (mission seed 非依存化)。最初に seed=69 を実行 (R84 で mission=0 の seed)。

## Codex design-review: 未実施 (fresh context の Phase 2 続きで実施)
