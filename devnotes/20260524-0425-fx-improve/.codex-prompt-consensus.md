あなたは zenigame-fx Alpha Factory の GA 改善を支援する Codex です。今回は「閾値引き上げ幅の確定」を合議します。

# 背景
North Star = live_criteria 全達成個体を 1 つ見つけ、達成後は閾値を**引き上げる**（緩和禁止）。

cycle14 で **North Star 閾値引き上げが独立 2 seed で再現確認**された:
- 閾値: sharpe_min=1.5, total_pnl_min=70000, max_drawdown_max=0.2, trade_count 50-5000。
- R95 (seed A): StageC pass 378、全て新基準達成。pnl max 95470。
- R96 (seed B): StageC pass 209、全て新基準達成。pnl max **80520**。best ann_sharpe5.359/pnl80520/dd1.79%/trade51。
- R95/R96 は gen0 個体重複 1/191 で独立 RNG（別 seed）であることを実証済み。warmstart/config/pop96/gen60 は同一。
- Codex 5 条件: (1)config正 (2)StageC>0 (3)mission_candidate≥10 (4)Stage B trade≥100再出現 (5)別seed再現。→ 4/5 達成（条件4=Stage B trade≥100 のみ未達=補助指標、mission必須でない）。

# 今回の論点: total_pnl_min を 70000 から幾つに引き上げるか
両 seed の StageC 個体（他基準 sharpe1.5/dd20/trade50-5000 は全 pass 済）に対する total_pnl_min 別 pass 数:

| total_pnl_min | R95 pass | R96 pass |
|---------------|----------|----------|
| 70000 (現) | 378 | 209 |
| 72000 | 175 | 14 |
| 74000 | 57 | 12 |
| 75000 | 52 | 8 |
| 76000 | 45 | 5 |
| 78000 | 30 | 1 |
| 80000 | 18 | 1 |
| 82000 | 12 | **0** |
| 90000 | 2 | **0** |

# 私（Claude）の判断
- 引き上げ幅は**弱い方の seed (R96, max80520) で律速**される。82k 以上は R96=0 → 条件5（別seedでも>0）を破壊。
- 両 seed で実用水準（条件3 の mission_candidate≥10）を保つ上限は **74000**（R95=57, R96=12）。75k は R96=8 で条件3 を割る。
- より保守的にするなら 72000（R96=14、第3seed への頑健性マージン）。
- 当初の素案「90k」は R96 で再現崩壊するため棄却。

# 質問（high reasoning で判定してください）
1. total_pnl_min の引き上げ先として **72000 / 74000 のどちらが妥当か**（または別値）。弱seed律速・条件3（≥10）維持・第3seed頑健性のトレードオフで判断してください。
2. この引き上げは禁止事項（閾値緩和・取引数操作・期間延長・overnight保有・default挙動変更・メタ過学習）に抵触しないか確認してください。
3. 反証可能な R97 成功基準を 5 条件で提示してください（cycle13 の様式を踏襲）。

回答は簡潔に。最後に「全体判定: OK / 条件付きOK / NG」で締めてください。
