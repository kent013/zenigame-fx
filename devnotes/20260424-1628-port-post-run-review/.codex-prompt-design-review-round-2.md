Round 1 の Critical 2 件 + Warning 5 件 + Suggestion 1 件に対応しました。対応マトリクス:

| # | Round 1 指摘 | 対応 |
|---|---|---|
| C1 (flock 不在) | macOS で flock が無い | 詳細設計の Phase 3 を `/usr/bin/lockf -k -s -t 60 ...lock sh -c '...'` 形式に書き換え。概念設計 §2.6 も同様に lockf へ変更 |
| C2 (improve-cycle 実起動手順) | コメント書き換えのみで実起動手順なし | 施策 2 に「Post-Run Review BG 起動」節を新規追加。Step 1 marker 検査 / Step 2 5 Agent 起動 (micro-stagger) / Step 3 marker 書き込み / Step 4 fire-and-forget 続行 / 失敗時を明文化 |
| W (RUN_ID 未定義) | 早期スキップで RUN_ID 未固定 | Phase 0 を新規追加し `THEME=$1 / RUN_ID=$2 / TMP_DIR=$3` を冒頭で固定。SUMMARY_FILE 変数も統一 |
| W (summary 30 文字 fail-fast) | todo-add は警告止まり | Phase 3 内に `[ ${#SUMMARY} -gt 30 ]` で fail-fast (skip して次案) を追加 |
| W (シェルクォート脆弱) | TITLE/SUMMARY の `'` で壊れる | Phase 3 を環境変数経由 (`PRR_TITLE` 等) で `lockf` 内 `sh -c` に渡す形に書き換え |
| W (concept 両 hook 表記矛盾) | concept stub が両 hook 起動と読める | concepts/post-run-review.md を「launch owner = improve-cycle Phase 1 末尾のみ、analyze-run は手動起動のみ」に統一 |
| W (max_parallel 未定義) | 5 Agent x Phase 4 GA 重複時の運用ガード | リスク節に `max_parallel_review_agents = 5` (固定) と「実測ベースの cap は別 TODO、現時点 C8 INCONCLUSIVE」を明記 |
| S (レビュー観点 #2 旧 prefix) | `[review:THEME]` 残存 | `[r:{code}]` に修正済 |

主要差分は施策 1 Phase 0 / Phase 3 と施策 2 「Post-Run Review BG 起動」新節です。再判定をお願いします。

特に確認してほしい点:

1. `lockf -k -s -t 60` のオプション選定 (-k で lock file 保持 / -s で silent / -t 60 で 60 秒 timeout) は妥当か？ macOS lockf に固有の落とし穴はないか？
2. 環境変数経由 (`PRR_TITLE` 等) で `sh -c` に値を渡す方式は、ユーザー入力に `'` `"` `$` `\` 等が含まれた場合も安全か？
3. improve-cycle Step 2 の「sleep 1 を間に挟む micro-stagger」を Agent ツールの `run_in_background: true` 連続起動と組み合わせた場合、stagger が効くか (`run_in_background` は同期返却なので連続起動の間隔を自分で制御可能か)？
4. summary 30 文字 fail-fast で skip した案は、Phase 4 申し送りに残すべきか、それとも完全 reject するべきか？
