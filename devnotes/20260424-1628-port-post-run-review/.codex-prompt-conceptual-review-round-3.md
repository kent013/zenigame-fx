Round 2 の Critical 指摘 (race の確率低減 → 排他保証への引き上げ) と list 出力フォーマットの不整合 (jq → awk) に対応しました。Round 2 で残った Suggestion / Warning にも対応しています。

## 対応マトリクス (Round 3)

| # | Round 2 指摘 | 対応 |
|---|---|---|
| C2 (再) | micro-stagger は確率低減のみで排他保証にならない、`add` は ID 重複 error を返さない | §2.6 を **`flock /tmp/zenigame-fx-todo-add.lock` で next-id + add 全体を排他クリティカルセクション化**する設計に変更。todo_manager.py 改修なしで OS 標準 flock を skill 側で直接ラップ |
| C1 (補修) | `list | jq .open[]` は現行 list 出力 (Markdown) と不整合 | §2.6 末尾に **awk を使った Markdown 行 grep の実装方針**を追加。`list --json` は別 TODO に分離 |
| W (analyze-run 単独運用) | analyze-run スタンドアロン時にレビュー hook が走らない件の運用 fallback | §2.8 に「runbook に手動起動コマンドを明記」を追加 |
| S (risk-management 副分類) | risk-management → stage-gate マッピングは max_pos/time_stop を取りこぼしやすい | (見送り) summary prefix は 30 文字制約のため二重 tag は厳しい。本 TODO ではマッピング表のみで運用、過剰タグ化は様子見後の改善 TODO で検討 |

## 主要差分 (修正後 §2.6 抜粋)

```
TODO 追加の race 吸収 (todo_manager.py 改修なし、`flock` で排他保証):

1. post-run-review skill 内 Phase 3: 各 /zenigame-fx-todo-add 呼び出しは flock /tmp/zenigame-fx-todo-add.lock -c '...' でラップ
2. todo-add skill (Step 3-4) を本 TODO で flock 化: next_id + add を flock で 1 命令 (システム全体で 1 ロック)
3. launcher micro-stagger: 1 秒間隔起動 (lock wait 上振れ抑制、必須ではない)
4. 次 cycle Phase 1-2 重複検出: 残った場合は obsolete で吸収

(将来的な todo_manager.py add-auto 統合は別 TODO)

has-open-theme 等価判定の実装方針 (現行 list 出力に対応):

todo_manager.py list は Markdown 行を stdout に出すだけ (JSON ではない)。Phase 1-0 の同 review-theme Open 検出は awk で:

  uv run python scripts/alpha_factory/todo_manager.py list \
    | awk '/^## Open/{open=1;next} /^##/{open=0} open && /\[review:THEME\]/{print; found=1} END{exit !found}'
```

## 確認事項

1. `flock /tmp/zenigame-fx-todo-add.lock` での next-id + add 排他クリティカルセクション化は、現行 `todo_manager.py` 非改修・skill md only の制約下で **排他保証**として妥当か？
2. `awk` による Markdown 行 grep (`/^## Open/{open=1;next} /^##/{open=0}`) は現行 list 出力 (`## Open (N) ...`) に対して堅牢か？ (テーブルヘッダ行 / 空行 / 別 section 開始の検出)
3. risk-management → stage-gate マッピングの「副分類タグ見送り」判断は妥当か？ (30 文字 summary 制約と運用負荷を勘案)

これら 3 点が APPROVED 相当と判断できれば、本 Round 3 で概念設計レビューを完了したい。
