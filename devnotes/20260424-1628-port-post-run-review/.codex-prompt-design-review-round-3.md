Round 2 の Warning + Suggestion に対応しました:

| # | Round 2 指摘 | 対応 |
|---|---|---|
| W (--tmp_dir 引数解析) | `TMP_DIR=$3` だと `$3 = '--tmp_dir'` になる | Phase 0 を while/case で `--tmp_dir <path>` と `--tmp_dir=<path>` 両対応に書き換え |
| S (flock 残存) | 詳細設計内 2 箇所に flock 表記残存 | L161 と L533 (レビュー観点 #1) を lockf に統一 |
| 補足 (Codex 回答 #2) | `|` / 改行で TODO.md テーブル破損 | Phase 4 に「title/summary に `|` / 改行が混入する場合のサニタイズ規約」を追加 |
| 補足 (Codex 回答 #4) | summary_too_long 案は申し送りに残すべき | Phase 4 に「reason: summary_too_long を付けて申し送り」を明記 + 推奨フォーマット追加 |

主要差分:

1. Phase 0 の Bash 引数解析を `while [ $# -gt 0 ]; do case "$1" in --tmp_dir) ...; --tmp_dir=*) ...; *) 位置引数 ...; esac done` 形式に書き換え
2. flock → lockf 統一 (残存 2 箇所)
3. Phase 4 にサニタイズ + summary_too_long 申し送り規約追加

これで施策 1 を APPROVE できるか再判定をお願いします。残るのは Suggestion レベルなら APPROVE で締めて構いません (本 TODO は md only port、834 baseline 不変)。
