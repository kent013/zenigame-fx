# Round 2: 仮説 H grep 反証 + 真の root cause 特定

## Codex 仮説 H grep + Read で反証

stage_gate.py:1043, 1057-1060 確認:
```python
bt = compute_metrics(
    res.trades, res.equity_curve,
    trade_count_min_for_sharpe=fold_min_trade_count,  # = 10
)
fold_sharpe = float(bt.trade_sharpe_raw) if bt.trade_sharpe_raw is not None else None
if fold_sharpe is None:
    fold_reason = _classify_fold_unavailable(
        trade_count=bt.trade_count, trade_count_min=fold_min_trade_count,
    )
```

= `fold_trade_count_min=10` は **per-fold で正しく適用されている**:
- compute_metrics で `trade_count_min_for_sharpe=10` を渡す
- trade_count<10 の fold は trade_sharpe_raw=None → fold_sharpe=None → unavailable 扱い

g8_i33 / g12_i17 の `stage_b_unavailable_reason_counts: {"trade_count_below_min": 0, ...}` = **全 9 fold で trade_count>=10 成立** (= Stage B 18 ヶ月期間で 90+ trade)。

= **仮説 H 棄却**。 archive 列 trade_count=16/17 は **Stage C 評価で再計算された値** (= 60 日 holdout 期間)。

## 真の root cause 候補

### Hypothesis I: Stage B (18 ヶ月) と Stage C (60 日 holdout) の期間スケーリング不整合

- Stage B: 18 ヶ月期間 = 約 540 日 (実 dataset で 120 営業日 stage_b_window)、 9 fold 成立 = 90+ trade
- Stage C: 60 日 holdout = 16-17 trade (= 1 trade/3-4 日)
- = **長期窓では十分 trade、 短期 holdout では極小**
- = 「実 trade 頻度は時期に偏在」 (= cycle 2 で発見した time-concentrated 取引仮説と同じ)

### Hypothesis J: Stage C の live_criteria 整合性不足

g8_i33 の archive で stage_c_pass=False、 trade_sharpe_stage_c=NaN
- Stage C 評価で何が起きているか不明
- live_criteria.trade_count_min=50 < Stage C trade_count=16/17 で reject 想定

## 質問

### Q1. 仮説 I (期間スケーリング不整合) の反証可能性
- (a) Stage B 内 fold ごとの trade_count を archive に記録する diagnostic で、 fold 内 trade 数の時期分布を観察
- (b) もし fold ごとに trade_count が大きく変動 (= time-concentrated 確証)、 仮説 I 支持
- (c) もし全 fold で trade_count が均一 (10-20 程度)、 単純に Stage C 期間が短すぎる構造

### Q2. cycle 21 の最小変更 (= 1 つに絞る)
- (案 X) Stage B 内 fold trade_count distribution を archive に追加 (= scheme 拡張)
- (案 Y) Stage C 期間設定の SSOT 監査 (= live_criteria.trade_count_min=50 と holdout_days=60 の整合性)
- (案 Z) 期間スケーリングを構造的に整合させる (= Stage B/C で同じ trade 密度を要求、 設計改革大規模)

### Q3. Stage C 詳細確認 (= synthesis § 4.3 / stage_gate.py の evaluate_stage_c を Read で確認推奨)
g8_i33 / g12_i17 が stage_c_pass=False の reason は何か。 trade_count<50 で fail なら live_criteria 整合性回復のための Stage B 接続強化が次の打ち手。

### Q4. 全体結論 + cycle 21 完了後の cycle 22 予告
