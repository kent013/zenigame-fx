**判定**: NEEDS_REVISION

**コメント**:
- 単一 instrument で `GraduationLane.pair_bars={X: bars_18m}` だけ入れる案は、現実装の cross-pair 契約と整合していません。[`LaneManager.graduation_criteria`](\/Users\/ishitoya\/repository\/zenigame-fx\/src\/alpha_factory\/swim_lane.py#L345) は `cross_pair_result is None` を不通過にし、[`evaluate_cross_pair`](\/Users\/ishitoya\/repository\/zenigame-fx\/src\/alpha_factory\/cross_pair.py#L206) は target+anchor 2 本を要求するので、自分自身だけでは `skipped=True` になり graduation が永久に 0 件になります。修正指示: Phase 2 単一 instrument mode では「target+anchor の 3 ペア bars を読む」か、「cross-pair/graduation は monitor disabled」と明示して summary でも区別してください。
- `effective_fitness = fitness_pen + 1000/10000/100000` をそのまま GA の `best_fitness` として外に出すのは危険です。[`analyze_run.py`](\/Users\/ishitoya\/repository\/zenigame-fx\/scripts\/alpha_factory\/analyze_run.py#L118) と [`generate_run_report.py`](\/Users\/ishitoya\/repository\/zenigame-fx\/scripts\/alpha_factory\/generate_run_report.py#L63) は `best.fitness` / `history.best_fitness` を `ga_config.fitness_metric` の値として解釈しています。修正指示: 選択用は `(stage_c_pass, stage_b_pass, stage_a_pass, fitness_pen)` の tuple 比較にし、外部出力の `best.fitness` と `history.best_fitness` は従来通り Stage A `fitness_pen` か明示された metric を維持し、必要なら `selection_score` を別フィールド追加にしてください。
- `dataset.start/end` を「Stage B 期間」に意味変更する案は後方互換ではありません。現行 config コメントは単純なバックテスト期間ですし、[`default.yaml`](\/Users\/ishitoya\/repository\/zenigame-fx\/config\/alpha_factory\/default.yaml#L8) とレポート系は `dataset.start/end/bars` をそのまま表示前提です。修正指示: `dataset.start/end` は従来意味のまま残し、追加で `stage_windows.stage_b_start/end` や `holdout_start/end` を導入してください。
- `holdout` が取れないときに Stage B 末尾を流用する fallback は、Stage C の意味を壊します。[`evaluate_stage_c`](\/Users\/ishitoya\/repository\/zenigame-fx\/src\/alpha_factory\/stage_gate.py#L536) は live criteria の最終ゲートなので、ここで in-sample を混ぜると leakage です。修正指示: 本番コードは fail-fast にし、slice fallback は test fixture 専用オプションに限定してください。
- `src/alpha_factory/config.py` の新設自体は賛成ですが、`AlphaFactoryConfig.live_criteria` を `stage_gate.live_criteria` と別保持する設計は drift を生みます。[`StageGateConfig`](\/Users\/ishitoya\/repository\/zenigame-fx\/src\/alpha_factory\/stage_gate.py#L61) が既に live criteria を内包しています。修正指示: loader で top-level YAML `live_criteria` を `StageGateConfig` に注入し、必要なら `AlphaFactoryConfig.live_criteria` は alias property にしてください。`StageGateConfig` / `CrossPairConfig` の再エクスポート自体は問題ありません。
- run_ga が population を持ち、LaneManager が評価を持つ責務分離は概ね妥当ですが、系譜情報が抜けます。[`GenomeArchive.collect_stage_a`](\/Users\/ishitoya\/repository\/zenigame-fx\/src\/alpha_factory\/archive.py#L299) は `parent_a/parent_b` を受け取れる一方、[`LaneManager._run_tier1_generation`](\/Users\/ishitoya\/repository\/zenigame-fx\/src\/alpha_factory\/swim_lane.py#L451) はそれを渡していません。修正指示: lane の population を `Genome` 裸ではなく provenance 付きエントリにするか、`run_generation` に parent map を渡してください。
- 出力契約の「top-level キー維持」主張とサンプル JSON が一致していません。現行 `summary.json` は `dataset.instrument/start/end/bars` を持ちますが、設計サンプルは `instrument` を top-level に出し `dataset.bars` を削っています。[現行 run_ga](\/Users\/ishitoya\/repository\/zenigame-fx\/scripts\/alpha_factory\/run_ga.py#L245) と [`generate_run_report.py`](\/Users\/ishitoya\/repository\/zenigame-fx\/scripts\/alpha_factory\/generate_run_report.py#L38) を見る限り、この変更はそのまま壊れます。修正指示: 既存キーは完全維持し、新情報は `dataset.bars_stage_a/bars_stage_b/bars_holdout`、`per_generation`、`archive_parquet` のように追加だけにしてください。