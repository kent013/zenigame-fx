## Round 1 → Round 2 改訂内容を反映済 — 再レビュー依頼

Round 1 で指摘された [Critical] 2 件 + [Warning] 4 件 + [Suggestion] 3 件すべてに対応しました。 同じフォーマット (`[VERDICT] / [Critical] / [Warning] / [Suggestion]`) で再評価をお願いします。

### Round 1 → Round 2 改訂内容

| Round 1 指摘 | 改訂対応 |
|---|---|
| [C1] state 二重化 (StageAGenerationInput.state vs evaluate_generation 引数) | **`StageAGenerationInput.state` を削除**、 `evaluate_generation(state, inputs, ...)` 引数のみで統一 (single source of truth) |
| [C2] 乖離更新が C7/C8 抵触 (低標本 corr で機械的更新) | `update_divergence_state(prev, corr, *, corr_sample_size)` 追加。 `n<10`: ValueError raise、 `10<=n<30`: INCONCLUSIVE (state 不変、 last_corr のみ更新)、 `n>=30`: 通常更新。 `CORR_SAMPLE_SIZE_INCONCLUSIVE_MIN=10` / `CORR_SAMPLE_SIZE_RELIABLE_MIN=30` const |
| [W1] bucket_validator が evaluate_fn 未配線 | `evaluate_fn(trades, bars, thresholds, bd_universe, *, bucket_validator=None)` に signature 統一、 evaluate_generation で keyword arg で渡す経路明記 |
| [W2] Helper 入力ガード不足 | `compute_q_force_with_divergence`: base_q_force ∈ [0.15, 0.40] / steps >= 0 範囲 check。 `select_top_q_force_indices`: q_force ∈ [0.15, 0.40] check |
| [W3] 同点 tie-break 未仕様 (sort 入力順依存) | `select_top_q_force_indices` の sort key を `(-score, index)` に確定 (deterministic tie-break by index ascending) |
| [W4] 「学術引用」 ラベル不一致 | 節名を「根拠 / 先行実装」 に変更、 内部 synthesis + 既存実装参照主体を明記 |
| [S1] evaluate_generation 契約簡素化 | [C1] 統合済 |
| [S2] テスト追加 (state 二重指定 reject / tie-break 決定性 / corr n 不足 state 据え置き) | テスト計画に追加: `test_stage_a_generation_input_does_not_have_state_field`、 `test_select_top_q_force_uses_deterministic_tie_break_by_index_ascending`、 `test_update_divergence_state_inconclusive_sample_size_n_10_to_29_keeps_steps_unchanged` |
| [S3] T063 側に default-deny 不変条件テスト | `test_stage_a_result_does_not_have_a_fail_indices_field` 追加 |

---

### Round 2 詳細設計 — 主要改訂セクション抜粋

#### state 単一情報源 (Round 1 [C1])

```python
@dataclass(frozen=True)
class StageAGenerationInput:
    """state は本 dataclass に持たない、 evaluate_generation 引数で統一."""
    generation: int
    individuals: tuple[StageAIndividualInput, ...]
    feasible_ratio_ema: float
    # state は廃止 (single source of truth)


def evaluate_generation(state: StageAControllerState, inputs, ...) -> tuple[StageAResult, StageAControllerState]:
    """state は引数のみ、 inputs.state は存在しない."""
```

#### sample-size guard (Round 1 [C2])

```python
CORR_SAMPLE_SIZE_INCONCLUSIVE_MIN: Final[int] = 10  # n < 10: ValueError
CORR_SAMPLE_SIZE_RELIABLE_MIN: Final[int] = 30      # n < 30: INCONCLUSIVE

def update_divergence_state(prev, corr, *, corr_sample_size):
    if corr_sample_size < 10:
        raise StageAInputError("C7 sample size guard violation")
    if corr_sample_size < 30:
        # INCONCLUSIVE: state.divergence_offset_steps 不変、 last_corr のみ更新
        return StageAControllerState(
            divergence_offset_steps=prev.divergence_offset_steps,
            last_a_b_correlation=corr,
        )
    # 通常更新 (n >= 30)
    ...
```

#### bucket_validator 配線 (Round 1 [W1])

```python
def evaluate_generation(state, inputs, *, evaluate_fn, live_criteria, bucket_validator=None):
    for ind in inputs.individuals:
        cf_result = evaluate_fn(
            ind.trades, ind.bars, thresholds, ind.business_day_universe,
            bucket_validator=bucket_validator,  # keyword arg で配線
        )
```

#### deterministic tie-break (Round 1 [W3])

```python
def select_top_q_force_indices(scores, q_force):
    # sort key = (-score, index)、 同点時 index 昇順
    sorted_scores = sorted(scores, key=lambda x: (-x[1], x[0]))
    ...
```

#### 入力ガード強化 (Round 1 [W2])

```python
def compute_q_force_with_divergence(base_q_force, steps):
    if not (Q_FORCE_BASE_MIN <= base_q_force <= Q_FORCE_DIVERGENCE_MAX):
        raise StageAInputError(...)
    if steps < 0 or steps > DIVERGENCE_OFFSET_STEPS_MAX:
        raise StageAInputError(...)


def select_top_q_force_indices(scores, q_force):
    if not (Q_FORCE_BASE_MIN <= q_force <= Q_FORCE_DIVERGENCE_MAX):
        raise StageAInputError(...)
    ...
```

---

レビュー観点 (Round 1 と同じ): 数式 § 5.1 / § 8.7 厳密準拠 / state 単一情報源 / sample-size guard / bucket_validator 配線 / 入力ガード / tie-break / Phase 分離 / C2 grep / その他盲点。

`[VERDICT] / [Critical] / [Warning] / [Suggestion]` で返してください。
