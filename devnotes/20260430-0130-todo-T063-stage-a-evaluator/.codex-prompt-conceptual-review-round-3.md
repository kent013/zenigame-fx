## Round 2 → Round 3 改訂内容を反映済 — 再レビュー依頼

Round 2 で指摘された [Critical] 1 件 + [Warning] 3 件 + [Suggestion] 3 件すべてに対応しました。 同じフォーマット (`[VERDICT] / [Critical] / [Warning] / [Suggestion]`) で再評価をお願いします。 残修正が無ければ APPROVED で結構です。

### Round 2 → Round 3 改訂内容

| Round 2 指摘 | 改訂対応 |
|---|---|
| [C] Decision 3 の `target_n` が空集合ケースと矛盾 (`n_hard_pass=0` でも `target_n=1`) | `target_n = 0 if n_hard_pass == 0 else max(1, int(n_hard_pass * q_force))` に修正、 空集合時 `threshold_score=None` と整合 |
| [W1] `divergence_offset_steps` 上限 13 がマジックナンバー | `DIVERGENCE_OFFSET_STEPS_MAX = math.ceil((Q_FORCE_DIVERGENCE_MAX - Q_FORCE_BASE_MIN) / Q_FORCE_DIVERGENCE_STEP)` で定数から導出、 q_force_with_divergence の clamp で実効上限 0.40 を保護 |
| [W2] Decision 2 (net_pnl_min 比例縮小) の synthesis § 5.4 対応未明示 | 採用根拠に「synthesis § 5.4 で Stage C が `12w + spread stress + cross-pair shadow validation` で mission_pass を確定 → mission stringency は Stage C で確保」 を 1 文追加 |
| [W3] default-deny を receiver 契約まで落とす | T065 PR DoD に検証観点追加: `test_a_fail_individual_excluded_from_nsga_selection`、 `test_a_fail_individual_excluded_from_archive_admission` |
| [S1] index 安定性明記 | `evaluate_generation` 入力契約に「inputs.individuals[i].index == i 前提、 並列評価時は caller (T065) の責務」 を追加 |
| [S2] derive_stage_a_thresholds 前提ガード | `baseline_dataset_days > 0`、 `window_days > 0`、 `window_days <= baseline`、 `trade_count_max >= trade_count_min`、 必須 keys 存在 を ValueError raise で実装 |
| [S3] (Round 2 で「Round 3 で APPROVED 進行可能」 と評価) | Critical 解消で APPROVED 期待 |

---

### Round 3 概念設計 — 主要改訂セクション抜粋

#### Decision 3 修正

```python
if n_hard_pass == 0:
    target_n = 0  # 空集合 (threshold_score = None)
else:
    target_n = max(1, int(n_hard_pass * q_force))  # zenigame 同等
```

#### 定数導出

```python
DIVERGENCE_OFFSET_STEPS_MAX = math.ceil(
    (Q_FORCE_DIVERGENCE_MAX - Q_FORCE_BASE_MIN) / Q_FORCE_DIVERGENCE_STEP
)  # = ceil((0.40 - 0.15) / 0.02) = 13
# 上限ガード: q_force_with_divergence の clamp により実効上限は 0.40 で保護
```

#### Decision 2 根拠強化

「synthesis § 5.4 で Stage C が `12w + spread stress + cross-pair shadow validation` で `mission_pass` を確定 → mission stringency は Stage C で確保」 を 1 文明記。

#### default-deny 検証観点

T065 PR DoD に追加:
- `test_a_fail_individual_excluded_from_nsga_selection`
- `test_a_fail_individual_excluded_from_archive_admission`

#### Index 安定性

```python
def evaluate_generation(...):
    """前提: inputs.individuals[i].index == i (caller が deterministic に index 付与).
    a_pass_indices 内の int は inputs.individuals の同 index 個体に対応.
    並列評価時 (T070) は parallel-safe index 維持を caller (T065) の責務とする."""
```

#### derive_stage_a_thresholds 前提ガード

```python
def derive_stage_a_thresholds(live_criteria, window_days=56, baseline_dataset_days=730):
    """前提ガード:
    - baseline_dataset_days > 0
    - window_days > 0
    - window_days <= baseline_dataset_days
    - live_criteria.trade_count_max >= live_criteria.trade_count_min
    - live_criteria の必須 keys 存在
    違反時 ValueError raise (caller=T065 の orchestration bug indicator)
    """
```

---

レビュー観点 (Round 1, 2 と同じ): 数式 § 5.1 / § 8.7 厳密準拠 / Decision 1-4 / state immutability / index 安定性 / 前提ガード / Phase 分離 / C2 grep / 学術引用 / その他盲点。

`[VERDICT] / [Critical] / [Warning] / [Suggestion]` で返してください。
