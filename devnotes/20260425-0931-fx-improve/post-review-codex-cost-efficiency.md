前提（Fact）
- Run 9 では `trade_count>0` 個体が存在する一方で、全個体 `total_pnl=0.0`。
- これは「戦略性能」以前に、コストを含むPnL集計経路の一貫性欠落を示す強いシグナル。
- 既存Open TODOは無し、直近Closed TODOは主にskill移植・キャッシュ系で、下記提案と実質重複なし。

以下、**cost-efficiency（コスト現実性）**に効く構造改善を優先度順で提案します。

| 順位 | 提案 | 優先度 | テーマ固有の効果評価 | 実装難易度 | 実装規模 | trade_count影響予測 |
|---|---|---|---|---|---|---|
| 1 | **PnL単一台帳化（Fill起点のイベントソーシング）+ 不変条件監査** | **Critical** | `trade_count>0` かつ `total_pnl=0` を構造的に検出・遮断。`gross_pnl / spread / slippage / swap / fee` を同一経路で積み上げ、どこでゼロ化されたか追跡可能。live/backtest乖離分析の土台になる。 | 高 | 大 | 不変 |
| 2 | **スプレッド・サーフェス導入（通貨ペア×時間帯×イベント状態）** | **Critical** | 固定/単純spreadを排除し、ロンドン・NY重複時間、指標発表時、ロールオーバー前後の拡大を再現。過小コスト見積もりを恒常的に削減。 | 中 | 中 | 不変 |
| 3 | **約定モデルの2層化（Slippage + Market Impact）** | **High** | 「価格到達=全量約定」をやめ、ボラ/流動性 proxy に応じた不利約定・部分約定を反映。バックテスト上の過大エッジを抑制し、実運用との執行品質差を縮小。 | 高 | 大 | 不変 |
| 4 | **セッションクローズ実行ポリシーの明示化（NYクローズ/週末ギャップ対応）** | **High** | `session_close` を「フィルタ」ではなく「執行ルール」として扱い、強制クローズ時の実効spread拡大・週末持越し禁止/許容をモデル化。イントラデイ前提との整合が取れる。 | 中 | 中 | 不変 |
| 5 | **Swap/Overnightコストの日次アクルーアル化（3倍日・通貨ペア別）** | **Medium** | イントラデイクローズ中心でも、例外的持越し時の損益歪みを除去。`time_stop_min` が短くても、運用実態に合わせた資金コスト反映が可能。 | 中 | 短 | 不変 |
| 6 | **Live-Backtestパリティ検証ハーネス（同一注文を両系で再生）** | **Medium** | 同一注文列を backtest execution adapter と live shadow adapter で比較し、乖離要因を `spread/slippage/latency/reject/partial fill` に自動分類。改善優先度をデータで固定化。 | 中 | 中 | 増（副次） |

補足（Interpretation）
- 最優先は「1」。ここが未整備だと、2〜6を入れても `total_pnl` 経路の欠損で効果測定自体が壊れます。
- 2〜6はすべて**取引回数削減を主効果にしない**設計で、`live_criteria.trade_count_min` との整合を維持します。