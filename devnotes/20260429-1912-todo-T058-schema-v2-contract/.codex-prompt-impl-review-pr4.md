【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】

### C1. Design-first 原則 (code grep より先に docs を読む)
コードの bug / 設計欠陥について claim を出す前に、以下を verify:
- docs/alpha_factory/ 配下の該当セクション（stage-gates.md / clause-architecture.md 等）を Read で確認
- devnotes/ 配下の関連 T-number（関数コメントや git log に出てくる T-xxx）を ls devnotes/ | grep T{xxx} で探して読む
- git log --all -S "識別子" で該当 code の変更履歴を辿る
- 該当機能と並行計算される可能性のある経路を grep で広く検索

### C2. 「X が無い = バグ」禁止ルール
関数 Y の signature / body に識別子 X が無いことを根拠に bug 判定してはならない。別経路での計算を grep で広く検索してから。

### C3. Collider bias 必須チェック（相関分析時）
2 つの変数 A, B の相関を計算して report する場合、Conditioning set と collider の有無を必ず明記。

### C4. 前提明示ルール
Claim / verdict を出す前に、prompt の先頭に「本分析の前提」を bullet でリスト化し、各前提が verified であることを明示。

### C5. 並列 sub-agent の framing 独立性チェック
独立検証のつもりで同じ framing の sub-agent を N 個回しても、それは 1 つの誤読の N 倍冗長実行。

### C6. Fact / Interpretation 分離ルール
「観察された事実 (Facts)」と「解釈・推論 (Interpretations)」を明確に区別する。1 文で mix しない。

### C7. Sample size ガード
n < 30 の相関 claim は明示的に因果解釈を避ける。n < 10 は相関 claim 禁止（observation のみ許可）。

### C8. INCONCLUSIVE を第一級 verdict に
INCONCLUSIVE は CONFIRMED / REJECTED と同格の選択肢。データ不足は正当な結論。

### C9. Falsification-first プロンプト
Round 1 は「この hypothesis の反証を探せ」という指示を先に書く。confirmation bias を強制しない。

---

# T058 PR 4 実装レビュー (impl-review)

## 役割

あなたは zenigame-fx Alpha Factory cascade port v2 Phase 2 配線実装の **PR 4** (= 施策 7 + 施策 8 = `src/alpha_factory/diagnostics_sidecar.py` 拡張 + `src/alpha_factory/fsp_updater.py` で v2 propagate) の実装レビュアー。

T058 は 12 施策で構成される schema v2 contract の big-bang baseline。
- PR 1 (基盤層 = schema_contract / RunContext / SchemaContractConfig、 commit `a4bf1bf`)
- PR 2 (archive.py v2 schema、 commit `f810c24`)
- PR 3 (calibrate_gate_history v2 + calibrate_state、 commit `2a75de8`)
は main に merge 済。

PR 4 は post-RUN diagnostics + FSP updater 経路に v2 propagate を実装する。 既存 caller 完全維持で optional kwargs として追加する設計。

設計 SSOT は `devnotes/20260429-1912-todo-T058-schema-v2-contract/detailed-design.md` の以下範囲:
- 施策 7 (diagnostics_sidecar): 行 1132-1228
- 施策 8 (fsp_updater): 行 1232-1313

T058 全 6 round Codex APPROVED 済 (= 詳細設計が SSOT)。

## Round 1 falsification 指示

Round 1 は「この PR 4 実装が設計 SSOT (詳細設計 § 施策 7 / § 施策 8) と既存 caller 互換性のいずれかを破っている」 hypothesis を反証する観点で進めること。 confirmation bias を排除するため、 まず仕様逸脱・互換破壊・伝搬漏れ・転記漏れを洗い出してから verdict を決める。

## レビュー対象 (PR 4 のスコープ)

### 変更ファイル

1. `src/alpha_factory/diagnostics_sidecar.py` — STAGE_A_PROVENANCE_SCHEMA に v2 必須 2 field 追加 (diagnostics_schema_version / dataset_epoch_id 先頭挿入) + `build_sidecar_table` / `write_stage_a_provenance` に optional `run_context` / `mode` kwargs 追加 (default LOG_ONLY、 epoch_legacy fallback)
2. `src/alpha_factory/fsp_updater.py` — `_detect_archive_schema_version(table)` helper 新規 + `_read_archive_with_fsp_compat` に `mode` kwarg 追加 (v1 archive で warning or raise) + `_atomic_write_parquet` に `mode` kwarg 追加 (v2 fields missing で warning + fallback or raise) + `run_fsp_updater` に `schema_mode` kwarg 追加して内部 caller 5 箇所 (read 1 + atomic_write 4) に伝搬
3. `tests/alpha_factory/test_diagnostics_sidecar.py` — schema field count を 10 → 12 に更新 + T058 v2 検証 test 9 件追加 (schema 先頭順序契約 / run_context 補完 / epoch_legacy fallback / mutate 防止 / LOG_ONLY 互換 / FAIL_CLOSED 補完で raise しない / validator 単体検証)
4. `tests/alpha_factory/test_fsp_updater.py` — T058 test 13 件追加 (helper 直接検証 3 件 + read mode kwarg 検証 4 件 + atomic_write 補完 / raise / mutate 防止 4 件 + run_fsp_updater 互換 / FAIL_CLOSED 伝搬 2 件)

### スコープ外 (touch しない、 PR 5+ で実装)

- `scripts/alpha_factory/run_ga.py` の RunContext 注入 (= PR 5 範囲、 詳細設計 行 1316-1402 の施策 9)
- `scripts/alpha_factory/run_alpha_sieve.py` (= PR 5 範囲)
- T058 PR 1 で作成済みの schema_contract.py (assert_diagnostics_v2 / DIAGNOSTICS_CONTRACT_V2 / DIAGNOSTICS_SCHEMA_VERSION は既に存在、 PR 4 では追加なし)
- T058 PR 2 / PR 3 で更新済みのファイル
- Tier 2 軽量ガード (= PR 6 範囲)

## 実装内容サマリ

### 施策 7 (diagnostics_sidecar.py)

**STAGE_A_PROVENANCE_SCHEMA**:
- 先頭 2 field に v2 必須を追加 (詳細設計 行 1147-1164):
  - `diagnostics_schema_version: int32` (nullable=False)
  - `dataset_epoch_id: string` (nullable=False)
- 既存 10 field (lane_id / generation / individual_name / metric_stage / trade_count / total_pnl_stage_a / sharpe_stage_a / stage_a_pass / stage_b_pass / stage_c_pass) は順序維持。

**`build_sidecar_table(rows, *, run_context=None, mode=LOG_ONLY)`**:
- 既存 signature `(rows: Sequence[dict])` を完全維持し、 optional kwargs を追加。
- 各 row を shallow copy して mutate を回避 (caller の dict は不変)。
- `dataset_epoch_id` が空/欠落なら fallback 補完: `run_context.dataset_epoch_id` または `"epoch_legacy"`。
- `diagnostics_schema_version` が無ければ `DIAGNOSTICS_SCHEMA_VERSION` (= 2) で setdefault。
- `assert_diagnostics_v2(row, mode=mode)` で passive validation (LOG_ONLY: warning / FAIL_CLOSED: raise)。

**`write_stage_a_provenance(collector, output_path, *, run_context=None, mode=LOG_ONLY)`**:
- 既存 signature 完全維持 + optional kwargs 追加。
- `build_sidecar_table` に kwargs を伝搬。
- 既存 fail-open は維持 (try/except の Exception で warning + None 返却)。 `SchemaContractError` (FAIL_CLOSED 由来) も同じ except でキャッチされて None 返却となる点は コメントで明示 (行 156-159)。

### 施策 8 (fsp_updater.py)

**`_detect_archive_schema_version(table) -> int | None`** 新規 helper:
- `genome_entry_schema_version` 列が存在しない → `None` (v1 archive)。
- 列が存在する場合、 値の最大値 (mixed-version 観測時の保守的見積もり) を返す。
- 空 table または全値 None の場合: 空 table は GENOME_ENTRY_SCHEMA_VERSION (= 2) を返し、 全値 None は v1 とみなして None。

**`_read_archive_with_fsp_compat(archive_path, *, mode=LOG_ONLY) -> pd.DataFrame`**:
- 既存返却契約 (df 単独) 完全維持。
- v1 archive (= `_detect_archive_schema_version is None`) で:
  - LOG_ONLY: `fsp_updater.v1_archive_detected` warning ログ + df 返却 (旧挙動互換)
  - FAIL_CLOSED: `SchemaVersionError` raise
- 既存の FSP 列 None 補完経路は維持。

**`_atomic_write_parquet(df, target_path, schema, *, mode=LOG_ONLY) -> None`**:
- 既存 signature `(df, target_path, schema)` 完全維持 + `mode` kwarg 追加。
- v2 必須 field (`genome_entry_schema_version` / `dataset_epoch_id`) 欠落で:
  - LOG_ONLY: `fsp_updater.write.v2_fields_missing` warning + df を shallow copy して fallback 補完 (`epoch_legacy` / `GENOME_ENTRY_SCHEMA_VERSION`) → 既存 atomic write 経路を継続。 caller の DataFrame は破壊的変更しない。
  - FAIL_CLOSED: `SchemaContractError` raise (= 書込せず)。

**`run_fsp_updater(..., *, schema_mode=LOG_ONLY)`**:
- 既存 positional 引数群を完全維持 + `schema_mode` kwarg を追加。
- 内部 caller 5 箇所 (`_read_archive_with_fsp_compat` 1 + `_atomic_write_parquet` 4) に `mode=schema_mode` を伝搬。
- 旧 caller (kwarg 省略) は LOG_ONLY default で従来挙動維持。
- ローカル変数 `mode` がドメイン固有の `fsp_runtime_mode` 文字列として既存実装で使われていたため、 衝突回避のため `runtime_mode` にリネームした (3 箇所、 振る舞い変更なし)。

### caller 側 (= 内部 caller のみ更新、 外部 entry point は PR 5 で更新)

- `scripts/alpha_factory/run_ga.py` 経路は PR 5 範囲 (= 詳細設計 行 1316-1402 の施策 9) なので、 PR 4 では touch せず。
- 詳細設計 行 1303 の通り、 現行 `run_ga.py` には FSP updater の直接呼出は存在しない (post-RUN scripts/alpha_factory/run_fsp_updater.py 経由のみ)。
- `assert_diagnostics_v2` / `DIAGNOSTICS_SCHEMA_VERSION` / `DIAGNOSTICS_CONTRACT_V2` は PR 1 で schema_contract.py に既に追加済 (PR 4 では新規追加なし、 import のみ)。

## DoD 確認

- `uv run pytest tests/alpha_factory/test_diagnostics_sidecar.py tests/alpha_factory/test_fsp_updater.py tests/alpha_factory/test_schema_contract.py -x` — **99 passed**
- `uv run pytest tests/alpha_factory/ -x` — **966 passed (regression 0)**
- `uv run pytest tests/scripts/ -x` — **173 passed (regression 0)**
- `uv run ruff check src/alpha_factory/diagnostics_sidecar.py src/alpha_factory/fsp_updater.py src/alpha_factory/schema_contract.py tests/alpha_factory/test_diagnostics_sidecar.py tests/alpha_factory/test_fsp_updater.py tests/alpha_factory/test_schema_contract.py` — **All checks passed!**
- `uv run mypy src/` — **Success: no issues found in 102 source files**
- 既存 ruff errors (`tests/alpha_factory/primitives/test_indicators_causality.py:136`, `tests/scripts/test_batch_metrics.py:211-240` の 8 件) は PR 4 スコープ外 (PR 1-3 でも未触、 main と diff なし)

## レビュー観点

以下の観点で本 PR 4 の実装を critical review してください。 APPROVED か REQUEST_CHANGES の verdict と、 各観点の Findings を記載してください。

### 観点 1: 詳細設計 SSOT との一致 (施策 7 / 施策 8)
- STAGE_A_PROVENANCE_SCHEMA の v2 field 追加位置 / 順序 / nullable=False が設計 行 1149-1151 と一致しているか
- `build_sidecar_table` の signature 変更が設計 行 1167-1182 と一致しているか (rows 受取 / optional kwargs / epoch_legacy fallback / setdefault / assert_diagnostics_v2)
- `write_stage_a_provenance` の signature / 返却型 `Path | None` / fail-open が設計 行 1185-1203 と一致しているか
- `_detect_archive_schema_version` の None / max 判定が設計 行 1245-1250 と一致しているか
- `_read_archive_with_fsp_compat` の df 単独返却維持 + mode 連動が設計 行 1253-1266 と一致しているか
- `_atomic_write_parquet` の v2 fields 欠落時 fallback 補完が設計 行 1269-1294 と一致しているか
- 内部 caller 伝搬 (詳細設計 行 1300 の 5 箇所、 行 412/443/465/497 + read 1) が漏れなく更新されているか

### 観点 2: Backward compat (既存 caller 完全互換)
- `build_sidecar_table(rows)` の positional 呼出が既存 (kwargs 省略) で動作するか
- `write_stage_a_provenance(collector, output_path)` の positional 呼出が既存で動作するか (試験 `test_writes_parquet_with_collector_records` / `test_empty_collector_returns_none_no_write` が PASS)
- `_read_archive_with_fsp_compat(path)` の positional 呼出が既存で動作するか
- `_atomic_write_parquet(df, target_path, schema)` の positional 呼出が既存で動作するか (試験 `TestAtomicWriteParquet` が PASS)
- `run_fsp_updater(...)` の旧 signature (schema_mode 省略) で従来挙動 (LOG_ONLY) が維持されているか

### 観点 3: 4 段伝搬契約 (転記漏れチェック)
PR 4 で touch する伝搬経路:
- `run_fsp_updater(schema_mode=...)` → `_read_archive_with_fsp_compat(mode=...)` → `_detect_archive_schema_version` 判定 → warning or raise
- `run_fsp_updater(schema_mode=...)` → `_atomic_write_parquet(mode=...)` × 4 callsite → v2 fields 欠落で warning+fallback or raise
- `write_stage_a_provenance(run_context=..., mode=...)` → `build_sidecar_table(run_context=..., mode=...)` → `assert_diagnostics_v2` lint
これらの kwarg が grep 上で全 5 箇所 (FSP) + 1 箇所 (sidecar) に行き渡っているか

### 観点 4: passive validation の正しさ
- `assert_diagnostics_v2` を schema_contract.py から import して使っているか
- `build_sidecar_table` の補完経路で必須 field が常に埋まる invariant が保証されているか (= LOG_ONLY / FAIL_CLOSED いずれの mode でも builder 経由なら raise しない)
- `_atomic_write_parquet` の `_FSP_V2_REQUIRED_COLUMNS` が schema_contract の GENOME_ENTRY_CONTRACT_V2 と整合しているか (= `genome_entry_schema_version`, `dataset_epoch_id` の 2 field)
- `_read_archive_with_fsp_compat` の v1 検出が `genome_entry_schema_version` 列の有無のみで判定し、 列値の int 型違反等は別経路 (`_detect_archive_schema_version` の valid_versions フィルタ) で扱う責務分離が明確か

### 観点 5: T067 移行への影響範囲明確化
- 設計の T067 切替 (FAIL_CLOSED 切替) で本 PR 4 の touch range は default を LOG_ONLY → FAIL_CLOSED に変えるだけ (もしくは config から渡す既存経路で挙動切替) で完了することが明示されているか
- v1 archive skip と v2 必須 field 補完経路は T067 で fail-closed 化される旨が docstring/コメントから読み取れるか

### 観点 6: テスト網羅性
詳細設計 行 1220-1225 (diagnostics) と 行 1306-1311 (fsp_updater) の test 計画:

diagnostics_sidecar:
- `test_stage_a_provenance_schema_includes_v2_required_fields`
- `test_build_sidecar_table_injects_dataset_epoch_id_from_run_context`
- `test_write_stage_a_provenance_includes_v2_metadata`
- `test_diagnostics_lint_log_only_warns_on_missing_field`
- `test_diagnostics_lint_fail_closed_raises_on_missing_field`

fsp_updater:
- `test_detect_archive_schema_version_returns_none_for_v1_table`
- `test_detect_archive_schema_version_returns_2_for_v2_table`
- `test_read_archive_log_only_warns_on_v1`
- `test_read_archive_fail_closed_raises_on_v1`
- `test_atomic_write_log_only_warns_and_fills_defaults_when_v2_fields_missing`
- `test_atomic_write_fail_closed_raises_when_v2_fields_missing`

これらが全て実装されているか、 名前と挙動が一致しているか確認してください。

### 観点 7: T058 cascade port v2 Phase 2 配線における整合性
- PR 1 の schema_contract.py の API (assert_diagnostics_v2, DIAGNOSTICS_SCHEMA_VERSION, GENOME_ENTRY_SCHEMA_VERSION, SchemaEnforcementMode, SchemaContractError, SchemaVersionError) を正しく import / 利用しているか
- PR 2 の archive.py の `epoch_legacy` stub と PR 4 の diagnostics fallback / FSP fallback の stub 値が一致しているか (= 同じ epoch で archive と diagnostics と FSP が同期する保証)
- PR 5+ (run_ga full propagate / sieve / Tier 2) で同じ pattern を踏襲できる contract を提供できているか

### 観点 8: collider bias 独立性 / SSOT 厳密性 (T064 follow-up 規範)
- `_detect_archive_schema_version` の helper 化が「FSP 読込後の判定用」 (設計 行 1246) として SSOT 化されているか (= caller が version を欲しい場合は helper を呼ぶ規範になっているか)
- LOG_ONLY mode の warning ログが `assert_diagnostics_v2` 経由で集約され、 FSP updater 側でも `fsp_updater.v1_archive_detected` / `fsp_updater.write.v2_fields_missing` のように構造化キーで分離されているか (= 観測可能性 + collider 分離)

## verdict 形式

最後に、 以下の形式で verdict を返してください:

```
## verdict
APPROVED | REQUEST_CHANGES

## 主要 Findings (重要度順)

### [Critical]
N. ...

### [Warning]
N. ...

### [Suggestion]
N. ...
```

REQUEST_CHANGES の場合は、 修正対象の行番号と修正後コード snippet を必ず含めてください。
APPROVED の場合は、 軽微な改善提案 (Suggestion) は含めて構いません。
