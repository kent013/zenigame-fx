# Round 2: Round 1 [REQUEST_CHANGES] への対応

## Round 1 blocker の対応マトリクス

### 1. E3 FAIL — T067 移行 3 点セットを 3 文書で統一

**修正内容**:

`docs/alpha_factory/stage-gates.md` 行 295-303 と
`.claude/skills/zenigame-fx-calibrate-gate/SKILL.md` 行 116-125 の T067 完全置換予定
段落を統一フォーマット (= 番号付き 3 点セット) に書き換えた。 同 3 点セットを
`devnotes/.../dod-verification-pr7.md` の表中 #19 + #20 + #21 経由で参照する形で
統一性を担保 (DoD report 自体は project workflow のため箇条書き形式は維持)。

統一フォーマット:
```
T067 (将来) で T058 transitional 設計から定常設計に完全置換予定。 移行 3 点セット:

1. `schema_contract.enforcement_mode` を `log_only` → `fail_closed` に切替え
2. cross-run scope key から `dataset_span` を **完全撤廃**
3. cross-run scope key を `dataset_epoch_id` **単独**に化 (T059 deterministic
   epoch_id 生成と組合せ)
```

参照:
- `docs/alpha_factory/stage-gates.md` (修正後の T067 セクション 行 295-303)
- `.claude/skills/zenigame-fx-calibrate-gate/SKILL.md` (修正後の T067 セクション
  行 116-125)

### 2. B1/B2 FAIL — DoD report の表現を検証実態に合わせる

**#1 「全 12 施策 main マージ済」表現** を **PARTIAL** に修正:
```
| 1 | ... | PARTIAL (本 PR 7 で完了予定) — PR 1-6 (= 施策 1-11) は main 着地済、
PR 7 (= 施策 12 統合テスト + DoD 検証 + docs 更新) は本 commit 内容として worktree
に存在し、 main 着地は本 PR merge 後に確定する。 PR 7 commit 後、 main merge 確認
をもって本項目を完全 PASS と扱う |
```

**#17 「warning なし」主張** を実 test assertion に裏付け:

`tests/alpha_factory/test_t058_integration.py::test_end_to_end_writes_v2_archive_with_all_required_fields`
を修正し、 ``capture_logs()`` で archive flush 中に下記 3 イベントが **発火 0 件** で
あることを直接 assert に加えた:

- `schema_contract.passive_validation_failed`
- `schema_contract.invalid_epoch_id`
- `archive.flush.schema_lint_summary`

DoD report #17 の文言も「PASS — `capture_logs()` で ... の発火が **0 件** であることを
直接 assert (lint warning なしで通過)」に書き換え。

DoD report 結論は `20 PASS + 1 PARTIAL` (#1 = main merge 確定待ち) に更新。

---

## NIT への対応 (任意、 主要な必要分のみ)

### A2 NIT — calibrate も RunContext 連結 cross-cutting test に含めるべき?

**判断**: 現行の `test_run_context_is_propagated_to_archive_and_diagnostics_components`
は archive + diagnostics_sidecar の 2 component を共通 RunContext で end-to-end 検証
している。 calibrate-gate は RunContext を直接 attribute 注入する形ではなく
`base_config_hash` / `dataset_epoch_id` を引数で受け取る (= `calibrate_state.py:177`
で scope key 組立) ため、 共通 RunContext を共有する形の test にはなじまない (= 別
TODO の calibrate-gate CLI smoke が SSOT)。 PR 7 スコープ内では追加対応せず、 既存
`tests/alpha_factory/test_calibrate_state.py` の `dataset_epoch_id` scope key 確認で
代替する。

### B3 NIT — T067 文言 3 点統一 → 上記 1. で対応済

### E1 NIT — docs の `calibrate_history_schema_version` と実装 `schema_version` 判定
不一致

**判断**: 詳細設計 §1027-1080 の SSOT に従えば、 `read_history` は `from_dict_or_none`
経由で **両 field** (`calibrate_history_schema_version` + 既存 `schema_version`) を
扱う。 `dataset_span` 撤廃のタイミング (T067) で `schema_version` の役割は
`calibrate_history_schema_version` に統合される計画。 docs/SKILL.md の文言は v2 必須
field の主軸として `calibrate_history_schema_version` を据える形で更新済 (T058
v2 schema 移行ノート subsection)。 既存 `schema_version` が引き続き metadata として
維持される旨は SKILL.md 行 105 「T054 既存 metadata: `schema_version`, ...」で
記述済。 PR 7 段階で追加修正不要と判断。

### F1 NIT — untracked artifact (`reports/...`, `.codex-*`)

**判断**: `reports/calibrate-gate/history.jsonl` と
`reports/run-reports/run-1/diagnostics/` は test_calibrate_gate_cli 等の既存テストの
test isolation 不備で生成された stray artifact (PR 7 の test ではなく既存テストの
副産物、 同様の現象は main の HEAD 直後にも再現する)。 PR 7 では touch 範囲を増や
さない方針なので、 これらは commit に含めず .gitignore 候補として別 TODO 扱いと
する。 commit 時には `git add` を明示的に PR 7 範囲ファイル (= `tests/alpha_factory/
test_t058_integration.py` 新規 + `devnotes/.../dod-verification-pr7.md` 新規 +
`devnotes/.../impl-review-pr7-round-{N}.md` + `devnotes/.../.codex-{prompt,session}-
impl-review-pr7*` + `docs/alpha_factory/stage-gates.md` 修正 + `.claude/skills/
zenigame-fx-calibrate-gate/SKILL.md` 修正) に限定する。

---

## 再検証してほしい観点

1. T067 移行 3 点セットが `docs/alpha_factory/stage-gates.md` と
   `.claude/skills/zenigame-fx-calibrate-gate/SKILL.md` で**同一フォーマット (番号付き
   3 点)** で統一されているか
2. `tests/alpha_factory/test_t058_integration.py` の更新後 (warning-no-emit assert) が、
   詳細設計 行 1532 「v2 contract lint が log warning なしで通過」を実 assertion で
   裏付けるのに十分か (= LOG_ONLY mode で必須 4 field が揃った状態の archive flush で
   lint warning が一切出ないことを直接機械検証)
3. DoD report #1 の **PARTIAL** 表記が C8 INCONCLUSIVE 原則 (= main merge 確定前は
   完全 PASS とせず PARTIAL で留保) に整合しているか
4. NIT 群 (A2/B3/E1/F1) の判断が C2 「X が無い = バグ」禁止ルール / C5 並列独立性 /
   T064 follow-up SSOT 厳密性に整合しているか

これらを Round 1 と同じ verdict 形式 (`[APPROVED]` / `[APPROVED with NITs]` /
`[REQUEST_CHANGES]`) で返答せよ。

修正ファイル:
- `docs/alpha_factory/stage-gates.md` (行 295-303)
- `.claude/skills/zenigame-fx-calibrate-gate/SKILL.md` (行 116-125)
- `tests/alpha_factory/test_t058_integration.py` (`test_end_to_end_writes_v2_archive_with_all_required_fields`)
- `devnotes/20260429-1912-todo-T058-schema-v2-contract/dod-verification-pr7.md`
  (#1 PARTIAL + #17 文言更新 + 結論)
