【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】

### C1. Design-first 原則 (code grep より先に docs を読む)
コードの bug / 設計欠陥について claim を出す前に、 docs/alpha_factory/ / devnotes/ 配下の該当セクション、関連 T-number、 git log を verify。

### C2. 「X が無い = バグ」禁止ルール
関数 Y の signature / body に識別子 X が無いことを根拠に bug 判定してはならない。別経路での計算を grep で広く検索してから。

### C3. Collider bias 必須チェック / C4. 前提明示 / C5. 並列 sub-agent / C6. Fact 分離 / C7. Sample size / C8. INCONCLUSIVE / C9. Falsification-first

---

# T058 PR 5 実装レビュー (impl-review)

## 役割

あなたは zenigame-fx Alpha Factory cascade port v2 Phase 2 配線実装の **PR 5** (= 施策 9 + 施策 10 + GenomeArchive.load 拡張 = `scripts/alpha_factory/run_ga.py` で RunContext 生成 + 全 artifact propagate + `scripts/alpha_factory/run_alpha_sieve.py` loader mode 連動 + `src/alpha_factory/archive.py` の `GenomeArchive.load` signature 拡張) の実装レビュアー。

T058 は 12 施策で構成される schema v2 contract の big-bang baseline。
- PR 1 (基盤層 = schema_contract / RunContext / SchemaContractConfig、 commit `a4bf1bf`)
- PR 2 (archive.py v2 schema、 commit `f810c24`)
- PR 3 (calibrate_gate_history v2 + calibrate_state、 commit `2a75de8`)
- PR 4 (diagnostics_sidecar / fsp_updater v2 propagate、 commit `a9733f3`)
は main に merge 済。

PR 5 は GA 起動経路の主要 caller (`run_ga.py`) に RunContext を注入し、 全 artifact (archive / calibrate-gate / diagnostics_sidecar / summary.json / history.json / best_genome.json / population.jsonl / run cache JSON) に dataset_epoch_id を propagate する。 また `run_alpha_sieve.py` を `GenomeArchive.load(mode=, return_schema_version=True)` の tuple 受取に切替え、 loader 側で v1 archive を mode に従って handle する経路を確立する。 `GenomeArchive.load` 自体に mode + return_schema_version kwargs を追加する基盤拡張も含む。

設計 SSOT は `devnotes/20260429-1912-todo-T058-schema-v2-contract/detailed-design.md` の以下範囲:
- 施策 9 (run_ga.py + RunContext + 全 artifact propagate): 行 1316-1404
- 施策 10 (run_alpha_sieve.py loader mode 連動): 行 1408-1439

T058 全 6 round Codex APPROVED 済 (= 詳細設計が SSOT)。

## Round 1 falsification 指示

Round 1 は「この PR 5 実装が設計 SSOT (詳細設計 § 施策 9 / § 施策 10) と既存 caller 互換性のいずれかを破っている」 hypothesis を反証する観点で進めること。 confirmation bias を排除するため、 まず仕様逸脱・互換破壊・伝搬漏れ・転記漏れを洗い出してから verdict を決める。

## レビュー対象 (PR 5 のスコープ)

### 変更ファイル

1. `src/alpha_factory/run_context.py` — `generate_epoch_id_stub(dataset_cfg) -> str` を新規追加 (T058 stub、 T059 で deterministic 生成へ置換)。 `__all__` に "generate_epoch_id_stub" を追加。 既存 `RunContext` dataclass は変更なし。

2. `src/alpha_factory/archive.py` — `GenomeArchive.load` signature 拡張:
   - 旧: `load(parquet_path: Path) -> pa.Table`
   - 新: `load(parquet_path, *, mode=LOG_ONLY, return_schema_version=False) -> pa.Table | tuple[pa.Table, int | None]`
   - 既存 caller (1-arg) は `return_schema_version=False` default で旧挙動 (Table 単独返却) 維持
   - 新 caller (run_alpha_sieve) は `return_schema_version=True` で `(Table, int | None)` tuple 受取
   - `_detect_archive_schema_version(table) -> int | None` static helper を新規追加 (`fsp_updater._detect_archive_schema_version` と同型 SSOT)
   - v1 archive 検出時: LOG_ONLY で warning + sv=None 返却、 FAIL_CLOSED で `SchemaVersionError` raise
   - 新規 import: `SchemaVersionError`

3. `scripts/alpha_factory/run_ga.py`:
   - 新規 import: `RunContext`, `generate_epoch_id_stub`, `CASCADE_CONTRACT_VERSION`, `assert_run_report_v2`
   - `_resolve_stage_a_threshold` の signature に `dataset_epoch_id: str` kwarg 追加 (PR 3 で literal "epoch_legacy" 直書きしていた箇所を caller 経由に置換、 詳細設計 行 1078-1085)
   - `main()` 起動初期で `dataset_epoch_id = generate_epoch_id_stub(cfg.dataset)` を計算 → threshold 解決に渡す
   - run_id / run_number 確定後に `RunContext` インスタンスを生成 (詳細設計 行 1334-1341)
   - `GenomeArchive(run_id, run_number, run_context=run_context, enforcement_mode=cfg.schema_contract.to_mode())` で注入 (詳細設計 行 1344-1349)
   - `write_stage_a_provenance` 呼出に `run_context=run_context, mode=cfg.schema_contract.to_mode()` を追加 (PR 4 で optional 化された kwargs)
   - `_write_reports` の signature に `run_context: RunContext` を必須 kwarg として追加し、 内部で `dataset_epoch_id = run_context.dataset_epoch_id` SSOT を確立
   - `summary` dict に `cascade_contract_version: CASCADE_CONTRACT_VERSION` (int=2) と `dataset_epoch_id: dataset_epoch_id` を追加 (詳細設計 行 1354-1362)。 `schema_version: "1.1"` (string) は **既存維持** (詳細設計 行 1404、 test 互換性)
   - summary.json write 直前で `assert_run_report_v2(summary, mode=cfg.schema_contract.to_mode())` を呼出
   - history.json の各 entry (per-generation snapshot) に `"dataset_epoch_id": dataset_epoch_id` を追加 (詳細設計 行 1366-1370)
   - best_genome.json は `genome_to_dict(best_genome)` の出力に `"dataset_epoch_id": dataset_epoch_id` を merge (詳細設計 行 1372-1373)
   - population.jsonl の各 line dict に `"dataset_epoch_id": dataset_epoch_id` を追加 (詳細設計 行 1375-1377)
   - run cache JSON (`.cache/alpha_factory/runs/{run_id}.json`) に `"dataset_epoch_id": run_context.dataset_epoch_id` を追加 (詳細設計 行 1379-1381)
   - `ga.run.start` log に `dataset_epoch_id` を追加 (観測可能性)

4. `scripts/alpha_factory/run_alpha_sieve.py`:
   - 新規 import: `SchemaEnforcementMode`
   - `_load_stage_c_passers(parquet_path, *, mode=LOG_ONLY)` に `mode` kwarg 追加 (詳細設計 行 1421)
   - `GenomeArchive.load(parquet_path, mode=mode, return_schema_version=True)` で tuple 受取 (詳細設計 行 1422-1426)
   - tuple であることを assert で確定
   - `schema_version is None` (= v1 archive、 LOG_ONLY 経路) で `alpha_sieve.v1_archive_skipped` warning + 空 list 返却 (詳細設計 行 1427-1432、 sieve 結果から除外)
   - FAIL_CLOSED 経路は `GenomeArchive.load` 内で raise されるためここに到達しない
   - `main()` で `cfg_for_mode = load_config(args.config, overrides={})` から mode を取得して `_load_stage_c_passers(archive_path, mode=sieve_mode)` に渡す (詳細設計 行 1421)

5. `tests/alpha_factory/test_archive.py` — T058 PR 5 test 5 件追加:
   - `test_load_backward_compat_returns_table_only_when_kwargs_omitted` (旧 caller 1-arg で Table 単独返却維持)
   - `test_load_with_return_schema_version_returns_tuple_for_v2_archive` (v2 archive で (Table, 2))
   - `test_load_log_only_warns_and_returns_none_sv_for_v1_archive` (v1 archive で warning + sv=None)
   - `test_load_fail_closed_raises_on_v1_archive` (FAIL_CLOSED で SchemaVersionError raise)
   - `test_load_empty_v2_archive_returns_v2_schema_version` (空 v2 archive は raise しない、 fsp_updater helper と同型判定)

6. `tests/scripts/test_run_ga_parallel.py` — T058 PR 5 test 7 件追加:
   - `test_summary_json_includes_cascade_contract_version_and_dataset_epoch_id`
   - `test_summary_json_schema_version_remains_string_one_one` (= "1.1" string 維持)
   - `test_cascade_contract_version_is_int_in_summary_json` (型固定、 bool 排除)
   - `test_history_json_each_entry_includes_dataset_epoch_id`
   - `test_best_genome_json_includes_dataset_epoch_id`
   - `test_population_jsonl_each_line_includes_dataset_epoch_id`
   - `test_run_cache_json_includes_dataset_epoch_id`

7. `tests/scripts/test_run_alpha_sieve.py` — T058 PR 5 test 3 件追加:
   - `test_run_alpha_sieve_log_only_skips_v1_archive_with_warning`
   - `test_run_alpha_sieve_fail_closed_raises_on_v1_archive`
   - `test_run_alpha_sieve_v2_archive_processed_normally`

8. `tests/alpha_factory/test_calibrate_state.py` — `_resolve_stage_a_threshold` の signature 変更に追従して既存 3 test の呼出に `dataset_epoch_id="epoch_legacy"` kwarg を追加 (regression 維持、 PR 5 で signature 変更したため修正必須)

### スコープ外 (touch しない、 PR 6+ で実装)

- Tier 2 軽量ガード (= PR 6 範囲、 詳細設計 行 1443-1469)
- 統合テスト (= PR 7 範囲、 詳細設計 行 1474-1483)
- PR 1-4 で main に merge 済の他経路 (run_context.py の RunContext dataclass, schema_contract.py の RUN_REPORT_CONTRACT_V2 / assert_run_report_v2 / CASCADE_CONTRACT_VERSION は PR 1 で既に追加済、 PR 5 では import のみ)
- PR 1-4 で touch した archive.py の v2 schema / flush 経路 (PR 5 では `load` のみ拡張)

## DoD 確認

- `uv run pytest tests/alpha_factory/test_archive.py -x` — **73 passed**
- `uv run pytest tests/scripts/test_run_ga_parallel.py -x` — **11 passed** (T058 PR 5 test 7 + 既存 4)
- `uv run pytest tests/scripts/test_run_alpha_sieve.py -x` — **22 passed** (T058 PR 5 test 3 + 既存 19)
- `uv run pytest tests/alpha_factory/ tests/scripts/ -x` — **1154 passed (regression 0)**
- `uv run pytest tests/` — **1546 passed, 1 skipped (regression 0)**
- `uv run ruff check src/alpha_factory/run_context.py src/alpha_factory/archive.py src/alpha_factory/schema_contract.py scripts/alpha_factory/run_ga.py scripts/alpha_factory/run_alpha_sieve.py tests/alpha_factory/test_archive.py tests/scripts/test_run_ga_parallel.py tests/scripts/test_run_alpha_sieve.py tests/alpha_factory/test_calibrate_state.py` — **All checks passed!**
- `uv run mypy src/` — **Success: no issues found in 102 source files**
- 既存 ruff errors (`scripts/alpha_factory/{analyze_run,compare_batch_runs,todo_manager}.py` 等の 16 件) は PR 5 スコープ外 (PR 1-4 でも未触、 main と diff なし)
- `scripts/alpha_factory/run_ga.py:1363` の mypy エラー (`os.cpu_count() if os.cpu_count() else False` の比較) は pre-existing (T052 commit deb6b208 由来、 main にも存在、 PR 5 で導入していない)

## レビュー観点

以下の観点で本 PR 5 の実装を critical review してください。 APPROVED か REQUEST_CHANGES の verdict と、 各観点の Findings を記載してください。

### 観点 1: 詳細設計 SSOT との一致 (施策 9 / 施策 10)
- `RunContext` 生成位置 (run_id / run_number 確定後、 archive 生成前) が設計 行 1334-1341 と一致しているか
- `GenomeArchive(run_id, run_number, run_context=, enforcement_mode=)` の注入が設計 行 1344-1349 と一致しているか
- `summary.json` の field 追加が設計 行 1354-1362 と一致しているか:
  - `cascade_contract_version: CASCADE_CONTRACT_VERSION` (int=2)
  - `dataset_epoch_id: dataset_epoch_id` (string)
  - `schema_version: "1.1"` (string、 既存維持) **必須**
- `history.json` の各 entry に `dataset_epoch_id` 付与が設計 行 1366-1370 と一致しているか
- `best_genome.json` の merge が設計 行 1372-1373 と一致しているか
- `population.jsonl` の各 line dict が設計 行 1375-1377 と一致しているか
- `run cache JSON` が設計 行 1379-1381 と一致しているか
- `assert_run_report_v2(summary, mode=...)` 呼出位置 (summary.json write 直前) が設計 行 1362 と一致しているか
- `_load_stage_c_passers` の `GenomeArchive.load(..., return_schema_version=True)` 経路が設計 行 1417-1432 と一致しているか

### 観点 2: Backward compat (既存 caller 完全互換)
- `GenomeArchive.load(path)` の 1-arg 呼出が既存挙動 (Table 単独返却) を維持しているか (test_archive.py の 5 箇所、 test_alpha_factory_run_ga.py 等の既存 caller)
- `_load_stage_c_passers(parquet_path)` の kwarg 省略呼出が LOG_ONLY default で動作するか (test_run_alpha_sieve.py の既存 test 全 PASS)
- `summary.json` の `schema_version` が **string "1.1"** のまま (= int でも `"2.0"` でもない) であることが test で固定されているか
- `_resolve_stage_a_threshold` の signature 変更 (kwarg `dataset_epoch_id` 追加) で既存 test (test_calibrate_state.py 3 件) が新 signature に追従しているか

### 観点 3: 4 段伝搬契約 (転記漏れチェック)
PR 5 で touch する伝搬経路:
- `cfg.dataset` → `generate_epoch_id_stub(cfg.dataset)` → `dataset_epoch_id` 変数 → `RunContext.dataset_epoch_id` → archive flush / sidecar write / summary / history / best_genome / population / run cache JSON
- `RunContext.dataset_epoch_id` SSOT が `_write_reports` 内で `dataset_epoch_id = run_context.dataset_epoch_id` として 1 度だけ取り出され、 全 artifact 書込で同じ変数を使い回しているか (= 同一 RUN 内で異なる epoch_id が漏れる可能性が無いか)
- `cfg.schema_contract.to_mode()` が archive / sidecar / summary lint / sieve loader の 4 callsite に正しく伝搬しているか

### 観点 4: passive validation の正しさ
- `assert_run_report_v2` を schema_contract.py から import して使っているか (PR 1 で追加済 API)
- `cascade_contract_version` は **int** で、 `schema_version` は **string** で型分離されているか (= Codex Round 5 [Critical] 4 「string か int か」議論の結論を踏襲)
- `assert_run_report_v2` の type 検査経路 (cascade_contract_version が int でなければ warning or raise) が設計 行 234-247 と整合しているか

### 観点 5: GenomeArchive.load 拡張の SSOT 性
- `_detect_archive_schema_version` static method が `fsp_updater._detect_archive_schema_version` と同型ロジック (列なし → None / 空 table → 2 / 全 None → None / 値の最大値) であるか (詳細設計 行 1245-1250 / 1422 SSOT)
- 旧 caller の 1-arg 呼出で `_detect_archive_schema_version` が呼ばれず schema_version 検査が skip される (= 旧挙動完全維持) ことが意図的に保証されているか
- 新 caller (`return_schema_version=True`) で v1 archive 検出時、 LOG_ONLY なら warning + (Table, None) 返却、 FAIL_CLOSED なら SchemaVersionError raise であることが test で固定されているか

### 観点 6: T067 移行への影響範囲明確化
- 設計の T067 切替 (FAIL_CLOSED 切替) で本 PR 5 の touch range は default を LOG_ONLY → FAIL_CLOSED に変えるだけ (もしくは config.schema_contract.enforcement_mode から渡す既存経路で挙動切替) で完了することが docstring/コメントから読み取れるか
- T059 (Epoch Manager) で `generate_epoch_id_stub` が deterministic 関数に置換される際、 引数 (DatasetConfig) signature 互換性が維持されるよう docstring 化されているか

### 観点 7: テスト網羅性
詳細設計 行 1395-1401 (run_ga) と 行 1436-1439 (run_alpha_sieve) の test 計画:

run_ga (test_run_ga_parallel.py):
- `test_summary_json_includes_cascade_contract_version_and_dataset_epoch_id`
- `test_summary_json_schema_version_remains_string_one_one`
- `test_history_json_includes_dataset_epoch_id` (= test_history_json_each_entry_includes_dataset_epoch_id 名で実装)
- `test_best_genome_json_includes_dataset_epoch_id`
- `test_run_cache_json_includes_dataset_epoch_id`
- `test_cascade_contract_version_is_int_in_summary_json`

run_alpha_sieve (test_run_alpha_sieve.py):
- `test_run_alpha_sieve_log_only_skips_v1_archive_with_warning`
- `test_run_alpha_sieve_fail_closed_raises_on_v1_archive`
- `test_run_alpha_sieve_v2_archive_processed_normally`

archive (test_archive.py、 GenomeArchive.load 拡張):
- backward compat (1-arg) / v2 archive tuple / v1 archive LOG_ONLY warning / FAIL_CLOSED raise / 空 v2 archive で raise しない

これらが全て実装されているか、 名前と挙動が一致しているか確認してください。

### 観点 8: T058 cascade port v2 Phase 2 配線における整合性
- PR 1-4 で main merge 済の API (RunContext / SchemaEnforcementMode / SchemaContractError / SchemaVersionError / CASCADE_CONTRACT_VERSION / assert_run_report_v2 / GENOME_ENTRY_SCHEMA_VERSION) を正しく import / 利用しているか
- `epoch_legacy` stub 値が PR 1-4 (archive template / calibrate / diagnostics / fsp) と PR 5 (run_ga RunContext / summary / history / best_genome / population / run cache / sieve) で一致しているか (= 同じ epoch で全経路が同期する保証)
- PR 6+ (Tier 2 / 統合テスト) で同じ pattern を踏襲できる contract を提供できているか (RunContext SSOT / dataset_epoch_id 単一変数経由 / mode kwarg)

### 観点 9: collider bias 独立性 / SSOT 厳密性 (T064 follow-up 規範)
- `RunContext.dataset_epoch_id` が 1 Run 内で **唯一の真値** として確立されているか (= run_ga 内で literal "epoch_legacy" を直書きしている場所が残っていないか)
- 観測可能性: `ga.run.start` log + summary.json + run cache JSON で同じ dataset_epoch_id が表示され、 cross-validation 可能か
- LOG_ONLY mode の warning ログが構造化キー (`alpha_sieve.v1_archive_skipped`, `archive.load.v1_archive_detected`, `schema_contract.passive_validation_failed`) で分離されているか

## verdict 形式

最後に、 以下の形式で verdict を返してください:

```
## verdict
APPROVED | REQUEST_CHANGES

## 主要 Findings (重要度順)

### [Critical]
N. ...

### [Warning]
N. ...

### [Suggestion]
N. ...
```

REQUEST_CHANGES の場合は、 修正対象の行番号と修正後コード snippet を必ず含めてください。
APPROVED の場合は、 軽微な改善提案 (Suggestion) は含めて構いません。
