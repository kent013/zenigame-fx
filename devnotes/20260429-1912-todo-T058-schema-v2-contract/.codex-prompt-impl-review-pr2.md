【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】
以下の discipline を全レビュー・監査で遵守すること。

### C1. Design-first 原則 (code grep より先に docs を読む)
コードの bug / 設計欠陥について claim を出す前に、以下を verify:
- docs/alpha_factory/ 配下の該当セクション（stage-gates.md / clause-architecture.md 等）を Read で確認
- devnotes/ 配下の関連 T-number（関数コメントや git log に出てくる T-xxx）を ls devnotes/ | grep T{xxx} で探して読む
- git log --all -S "識別子" で該当 code の変更履歴を辿る
- 該当機能と並行計算される可能性のある経路を grep で広く検索

### C2. 「X が無い = バグ」禁止ルール
関数 Y の signature / body に識別子 X が無いことを根拠に bug 判定してはならない。別経路での計算を grep で広く検索してから。

### C3. Collider bias 必須チェック（相関分析時）
2 つの変数 A, B の相関を計算して report する場合、Conditioning set と collider の有無を必ず明記。

### C4. 前提明示ルール
Claim / verdict を出す前に、prompt の先頭に「本分析の前提」を bullet でリスト化し、各前提が verified であることを明示。

### C5. 並列 sub-agent の framing 独立性チェック
独立検証のつもりで同じ framing の sub-agent を N 個回しても、それは 1 つの誤読の N 倍冗長実行。

### C6. Fact / Interpretation 分離ルール
「観察された事実 (Facts)」と「解釈・推論 (Interpretations)」を明確に区別する。1 文で mix しない。

### C7. Sample size ガード
n < 30 の相関 claim は明示的に因果解釈を避ける。n < 10 は相関 claim 禁止（observation のみ許可）。

### C8. INCONCLUSIVE を第一級 verdict に
INCONCLUSIVE は CONFIRMED / REJECTED と同格の選択肢。データ不足は正当な結論。

### C9. Falsification-first プロンプト
Round 1 は「この hypothesis の反証を探せ」という指示を先に書く。confirmation bias を強制しない。

---

# T058 PR 2 実装レビュー (impl-review)

## 役割

あなたは zenigame-fx Alpha Factory cascade port v2 Phase 2 配線実装の **PR 2** (= 施策 4 = `archive.py` の GENOMES_SCHEMA に v2 4 field 追加 + `flush()` で passive validation 連動) の実装レビュアー。

T058 は 12 施策で構成される schema v2 contract の big-bang baseline で、 PR 1 (基盤層 = schema_contract / RunContext / SchemaContractConfig) は main に merge 済 (commit `a4bf1bf`)。 PR 2 では archive.py の schema を v1 → v2 に拡張し、 flush 時に PR 1 の `assert_genome_entry_v2` validator を passive lint として呼ぶ。

設計 SSOT は `devnotes/20260429-1912-todo-T058-schema-v2-contract/detailed-design.md` (= 施策 4 = 行 651-889 付近)、 概念設計は `conceptual-design.md`。 全 6 round Codex APPROVED 済。

## レビュー対象 (PR 2 のスコープ)

### 変更ファイル

1. `src/alpha_factory/archive.py` — schema v2 + lint 連動
2. `tests/alpha_factory/test_archive.py` — v2 schema test 追加 + 既存 test 更新
3. `tests/scripts/test_run_alpha_sieve.py` — `_make_archive_row` fixture に v2 4 field 追加 (新 schema の non-null 制約への適合のみ、 PR 5 で本書込)

### スコープ外 (touch しない、 PR 3-7 で実装)

- `src/alpha_factory/calibrate_state.py` / `calibrate_gate_history.py`
- `src/alpha_factory/diagnostics_sidecar.py`
- `src/alpha_factory/fsp_updater.py`
- `scripts/alpha_factory/run_ga.py` / `run_alpha_sieve.py`
- T058 PR 1 で作成済みの schema_contract.py / run_context.py / config.py (= 既に main にあるので import するだけ、 再実装しない)

## 実装内容サマリ

### 施策 4 (archive.py)

**GENOMES_SCHEMA** (43 → 47 col):
- `genome_entry_schema_version: int32, non-null`
- `dataset_epoch_id: string, non-null`
- `archive_role: string, nullable` (T066 で書込)
- `source_stage: string, nullable` (T063-T064 で書込)

**_create_row_template** に default 追加:
- `genome_entry_schema_version = GENOME_ENTRY_SCHEMA_VERSION` (= 2)
- `dataset_epoch_id = "epoch_legacy"` (T059 で deterministic 値に置換予定の stub、 grammar `[a-z0-9_]+` 適合)
- `archive_role = None`
- `source_stage = None`

**GenomeArchive dataclass** に kw_only field 追加:
- `run_context: RunContext | None = None`
- `enforcement_mode: SchemaEnforcementMode = LOG_ONLY`
- `_lint_warning_count: int` (per-run 集計、 init=False)

既存呼出 `GenomeArchive(run_id=..., run_number=...)` は backward compat (kw_only=True で signature 変更なし)。

**flush()** に lint 連動:
1. clean_rows ループで row.dataset_epoch_id が empty/None なら run_context.dataset_epoch_id で補完
2. row.setdefault genome_entry_schema_version = 2 (defensive)
3. assert_genome_entry_v2(row, mode=enforcement_mode) を呼び ValidationResult を集計
4. LOG_ONLY mode で result.ok=False なら _lint_warning_count++ (FAIL_CLOSED は validator 内で raise)
5. flush 末尾に schema_lint_summary の per-run 集約 log (warning_count / mode / run_id)

### tests/alpha_factory/test_archive.py

既存 `test_schema_has_43_columns` → `test_schema_has_47_columns` に rename + expected set に v2 4 field 追加。
`test_template_default_values` / `test_schema_nullable_attributes` に T058 field 追記。

新 test (T058 ブロック末尾):
- `test_t058_genomes_schema_includes_v2_required_fields` — 4 field 存在 + 型 + nullability
- `test_t058_create_row_template_initializes_v2_fields_with_defaults` — default 値 (epoch_legacy 含む)
- `test_t058_genome_archive_init_with_legacy_signature_works_without_run_context` — backward compat
- `test_t058_genome_archive_init_accepts_run_context_kwarg` — 新 kwarg 経由
- `test_t058_flush_injects_dataset_epoch_id_from_run_context` — empty 補完
- `test_t058_flush_keeps_epoch_legacy_when_run_context_none` — 注入なしで stub のまま
- `test_t058_flush_log_only_does_not_raise_for_grammar_violation` — schema_lint_summary log + invalid_epoch_id event
- `test_t058_flush_log_only_summary_aggregates_multiple_warnings` — per-run 集約
- `test_t058_flush_log_only_no_summary_when_no_warnings` — 0 件で log 抑制
- `test_t058_flush_fail_closed_raises_on_grammar_violation` — SchemaContractError raise
- `test_t058_flush_fail_closed_passes_when_all_v2_fields_valid` — happy path
- `test_t058_flush_persists_v2_field_values_through_parquet_roundtrip` — Parquet 書き戻し

### tests/scripts/test_run_alpha_sieve.py

`_make_archive_row` fixture に v2 4 field 追加 (schema が non-null になったため fixture が失敗するのを修正)。

## DoD 確認

- `uv run pytest tests/alpha_factory/test_archive.py -x` — **66 passed** (既存 + 新規)
- `uv run pytest tests/alpha_factory/ -x` — **922 passed** (regression 0)
- `uv run pytest tests/scripts/ -x` — **173 passed** (regression 0)
- `uv run pytest tests/` — **1487 passed, 1 skipped** (全 regression 0)
- `uv run ruff check src/alpha_factory/archive.py tests/alpha_factory/test_archive.py tests/scripts/test_run_alpha_sieve.py` — **clean**
- `uv run mypy src/` — **clean (102 source files)**
- 既存 ruff errors 8 件は `tests/scripts/test_batch_metrics.py` 等 PR2 スコープ外で git stash 検証済 (PR 2 では触らない)

## archive_version (schema_version) 扱いの判断

詳細設計 行 1524 の「`tests/scripts/test_run_ga_parallel.py` (cascade_contract_version + dataset_epoch_id 含む、 既存 schema_version: '1.1' 維持確認)」 は **summary.json (run_report) 側** の `schema_version: "1.1"` (string) のことで、 archive Parquet の schema 内バージョン (`genome_entry_schema_version: int32 = 2`) とは別物。 本 PR 2 では archive.py のみ touch するため、 summary.json の `schema_version: "1.1"` は touch せず、 archive 内では `genome_entry_schema_version` (新規追加 int32 col、 値=2) を用いる方針 (= 詳細設計 SSOT 通り)。 これは PR 2 内で混同しないようにする論点。

## レビュー観点

以下の観点で本 PR 2 の実装を critical review してください。 APPROVED か REQUEST_CHANGES の verdict と、 各観点の Findings を記載してください。

### 観点 1: 詳細設計 SSOT との一致
- 施策 4 の GENOMES_SCHEMA 定義 (47 col)、 template default、 flush 処理が詳細設計 行 651-756 の指示と一致しているか
- 4 field の型 / nullability / 順序が設計通りか (詳細設計 行 672-688)
- "epoch_legacy" stub が grammar [a-z0-9_]+ に適合しているか (Round 3 [Warning] 3)

### 観点 2: Backward compat (Codex Round 4 [Critical] 3)
- 既存 GenomeArchive 呼出 (`run_ga.py` / `cross_pair.py` / 既存 test) が kwarg 省略で動くか
- flush(output_dir=...) の既存契約が維持されているか (init で out_dir を受け取らない)
- 既存 test の regression が 0 件か

### 観点 3: passive validation 連動の正しさ
- LOG_ONLY mode で raise しないこと
- FAIL_CLOSED mode で grammar 違反時に SchemaContractError raise
- per-run summary log が集約されている (warning ごとに 1 回ではなく flush 末尾 1 回)
- _lint_warning_count が flush 開始時に reset されている (= 同一 archive で 2 回 flush しても累積しない)
- run_context が None なら dataset_epoch_id 補完しない (= template の "epoch_legacy" stub のまま)

### 観点 4: 4 段伝搬契約 (転記漏れチェック)
GENOMES_SCHEMA → _create_row_template → collect_stage_* → flush の 4 段階で
- archive_role / source_stage は collect_stage_* で書き込まない設計 (T066 / T063-T064 で値書込) → template の None のまま flush で出る、 これが本 PR 2 の意図通りか
- import-time guard `_TEMPLATE_KEYS == _SCHEMA_NAMES` の assert が新 4 field を全て含んでいるか
- last-mile guard (flush の row keys mismatch check) で新 4 field が漏れなく検証されるか

### 観点 5: テスト網羅性
- 詳細設計 行 871-883 のテスト計画 (12 件) のうち、 PR 2 スコープに該当するもの (archive 関連) は実装されているか
- `test_load_*` 系 (詳細設計 行 877-879) は load() メソッドの拡張なので、 PR 2 スコープ外? それとも touch すべき?
  - 設計 行 829-855 の load() 拡張 (return_schema_version、 mode 引数) は本 PR で実装していない (= scope 外、 PR 5 = run_alpha_sieve.py 側で touch する想定で、 archive.load は既存 signature 維持)
  - 本 PR 2 では archive.load() は touch せず、 PR 5 (run_alpha_sieve) に委ねる方針で OK か?

### 観点 6: T058 cascade port v2 Phase 2 配線における整合性
- PR 1 の schema_contract.py の API (assert_genome_entry_v2, ValidationResult, SchemaEnforcementMode) を正しく import / 利用しているか
- PR 1 の RunContext の使い方 (dataset_epoch_id 属性アクセス) が正しいか
- PR 3-7 (calibrate / diagnostics / fsp / run_ga / run_alpha_sieve) で同じパターンを踏襲しやすい contract を提供できているか

## verdict 形式

最後に、 以下の形式で verdict を返してください:

```
## verdict
APPROVED | REQUEST_CHANGES

## 主要 Findings (重要度順)

### [Critical]
N. ...

### [Warning]
N. ...

### [Suggestion]
N. ...
```

REQUEST_CHANGES の場合は、 修正対象の行番号と修正後コード snippet を必ず含めてください。
APPROVED の場合は、 軽微な改善提案 (Suggestion) は含めて構いません。
