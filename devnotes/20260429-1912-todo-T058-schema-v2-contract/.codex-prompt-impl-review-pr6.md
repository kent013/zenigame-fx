【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】

### C1. Design-first 原則 (code grep より先に docs を読む)
コードの bug / 設計欠陥について claim を出す前に、 docs/alpha_factory/ / devnotes/ 配下の該当セクション、関連 T-number、 git log を verify。

### C2. 「X が無い = バグ」禁止ルール
関数 Y の signature / body に識別子 X が無いことを根拠に bug 判定してはならない。別経路での計算を grep で広く検索してから。

### C3. Collider bias 必須チェック / C4. 前提明示 / C5. 並列 sub-agent / C6. Fact 分離 / C7. Sample size / C8. INCONCLUSIVE / C9. Falsification-first

---

# T058 PR 6 実装レビュー (impl-review)

## 役割

あなたは zenigame-fx Alpha Factory cascade port v2 Phase 2 配線実装の **PR 6** (= 施策 11 = Tier 2 軽量ガード = `scripts/alpha_factory/{extract_batch_metrics,generate_run_report,compare_batch_runs,analyze_run}.py` 4 scripts に `assert_epoch_id_present_for_display` の呼出を追加 + `src/alpha_factory/schema_contract.py` の関数を fail-open 拡張) の実装レビュアー。

T058 は 12 施策で構成される schema v2 contract の big-bang baseline。
- PR 1 (基盤層 = schema_contract / RunContext / SchemaContractConfig、 commit `a4bf1bf`)
- PR 2 (archive.py v2 schema、 commit `f810c24`)
- PR 3 (calibrate_gate_history v2 + calibrate_state、 commit `2a75de8`)
- PR 4 (diagnostics_sidecar / fsp_updater v2 propagate、 commit `a9733f3`)
- PR 5 (run_ga.py + run_alpha_sieve.py + GenomeArchive.load 拡張、 commit `ba11aaa`)
は main に merge 済。

PR 6 は post-RUN の display 系 4 scripts に Tier 2 軽量ガード (warning ベースの `assert_epoch_id_present_for_display` 呼出) を追加し、 PR 1-5 で integrated された upstream propagation が display layer まで到達することを保証する。 fail-open ガードのため、 dataset_epoch_id 欠落でも処理は失敗させない (= 詳細設計 行 1469-1470 「副作用なし、 warning のみ」規範厳守)。

設計 SSOT は `devnotes/20260429-1912-todo-T058-schema-v2-contract/detailed-design.md` の以下範囲:
- 施策 11 (Tier 2 軽量ガード): 行 1443-1470

T058 全 6 round Codex APPROVED 済 (= 詳細設計が SSOT)。

## Round 1 falsification 指示

Round 1 は「この PR 6 実装が設計 SSOT (詳細設計 § 施策 11) を破っている」 hypothesis を反証する観点で進めること。 confirmation bias を排除するため、 まず仕様逸脱・互換破壊・伝搬漏れ・転記漏れを洗い出してから verdict を決める。 特に以下を重点的にチェック:

1. 4 scripts への呼出追加で fail-open 規範が守られているか (= raise していないか)
2. `dataset_epoch_id` の display 出力 (run-{N}.md / analysis-claude.md / batch_summary.json / comparison_report.md / metrics report.md) が PR 1-5 の upstream propagation と整合しているか
3. 既存テスト (test_batch_metrics.py 9 件、 test_generate_run_report*.py 等) が regression していないか
4. PR 6 スコープ外 (PR 1-5 で main merge 済の他経路) を touch していないか

## レビュー対象 (PR 6 のスコープ)

### 変更ファイル

1. `src/alpha_factory/schema_contract.py` — `assert_epoch_id_present_for_display` を fail-open 拡張:
   - 旧: `obj is None or "dataset_epoch_id" not in obj` で warning
   - 新: 上記に加えて `obj["dataset_epoch_id"]` が `None` または空文字でも warning
   - log event 名は `schema_contract.tier2_epoch_id_missing` (詳細設計 行 413 SSOT 維持)
   - kwargs `artifact=...`, `keys=sorted(...)` は維持
   - SchemaContractError は引き続き raise しない (詳細設計 行 1469-1470)

2. `scripts/alpha_factory/extract_batch_metrics.py`:
   - 新規 import: `from src.alpha_factory.schema_contract import assert_epoch_id_present_for_display`
   - `extract()` 関数内、 summary.json load 直後に `assert_epoch_id_present_for_display(summary, artifact="extract_batch_metrics.summary")` を呼出
   - `metrics` dict に `"dataset_epoch_id": summary.get("dataset_epoch_id")` を追加 (per-run propagation 用)
   - `render_markdown` で `**dataset_epoch_id**: \`{m.get('dataset_epoch_id') or '—'}\`` を header に追加
   - `main()` の `if args.report is not None:` 直後に `assert_epoch_id_present_for_display(metrics, artifact="extract_batch_metrics.report.md")` を呼出

3. `scripts/alpha_factory/generate_run_report.py`:
   - 新規 import: `from src.alpha_factory.schema_contract import assert_epoch_id_present_for_display`
   - `main()` のレポート header 構築部 (line 334-336 付近) に `f"**dataset_epoch_id**: \`{summary.get('dataset_epoch_id') or '—'}\`"` を append
   - `main()` の最後、 `out_path.write_text` 直前に `assert_epoch_id_present_for_display(summary, artifact="run_report.md")` を呼出

4. `scripts/alpha_factory/compare_batch_runs.py`:
   - 新規 import: `from src.alpha_factory.schema_contract import assert_epoch_id_present_for_display`
   - `_aggregate()` で `summary["dataset_epoch_ids"] = [m.get("dataset_epoch_id") for m in metrics_list]` を追加 (per-run propagation 保存)
   - `_run_single()` で per-run metrics + batch_summary.json + comparison_report.md の 3 callsite に Tier 2 ガード呼出
   - `_run_compare()` で baseline / treatment 両方の per-run metrics に Tier 2 ガード呼出
   - `_render_single` の Per-run 一覧テーブルに `dataset_epoch_id` カラムを追加

5. `scripts/alpha_factory/analyze_run.py`:
   - 新規 import: `from src.alpha_factory.schema_contract import assert_epoch_id_present_for_display`
   - `main()` のレポート header に `f"**dataset_epoch_id**: \`{summary.get('dataset_epoch_id') or '—'}\`"` を追加
   - `main()` の最後、 `args.tmp_dir.mkdir(parents=True, exist_ok=True)` 直前に `assert_epoch_id_present_for_display(summary, artifact="analyze_run.analysis-claude.md")` を呼出

6. `tests/alpha_factory/test_schema_contract.py` — Tier 2 拡張 test 3 件追加:
   - `test_assert_epoch_id_present_for_display_warns_on_empty_string`
   - `test_assert_epoch_id_present_for_display_warns_on_none_value`
   - `test_assert_epoch_id_present_for_display_includes_artifact_name_in_log`

7. `tests/scripts/test_tier2_epoch_id_propagation.py` (新規) — 6 test:
   - `test_run_report_md_includes_dataset_epoch_id_in_header` (詳細設計 行 1463)
   - `test_batch_summary_json_preserves_per_run_dataset_epoch_id` (行 1464、 = extract_batch_metrics の JSON output)
   - `test_comparison_report_md_references_dataset_epoch_id` (行 1465)
   - `test_analyze_run_md_references_dataset_epoch_id` (行 1466)
   - `test_extract_batch_metrics_report_md_includes_dataset_epoch_id` (行 1467)
   - `test_tier2_guard_does_not_fail_when_dataset_epoch_id_missing` (fail-open 規範検証、 詳細設計 行 1469-1470 SSOT)

### スコープ外 (touch しない)

- 統合テスト (= PR 7 範囲、 詳細設計 行 1474-1483)
- 既に main にある PR 1-5 の実装ファイル (run_context.py / config.py / archive.py / calibrate_gate_history.py / calibrate_state.py / diagnostics_sidecar.py / fsp_updater.py / run_ga.py / run_alpha_sieve.py — schema_contract.py の `assert_epoch_id_present_for_display` 拡張のみ OK、 他の API は触らない)
- docs / SKILL.md (= PR 7 範囲)

## DoD 確認

- `uv run pytest tests/alpha_factory/test_schema_contract.py -x` — **26 passed** (T058 PR 6 拡張 test 3 + 既存 23)
- `uv run pytest tests/scripts/test_tier2_epoch_id_propagation.py -x` — **6 passed**
- `uv run pytest tests/scripts/test_batch_metrics.py -x` — **9 passed (regression 0)**
- `uv run pytest tests/alpha_factory/ tests/scripts/ -x` — **1164 passed (PR 5 1155 + PR 6 9 = regression 0)**
- `uv run ruff check {touched files}` — PR 6 で touch した範囲は clean (pre-existing 15 errors は touch 範囲外、 baseline と diff なし)
- `uv run mypy src/` — **Success: no issues found in 102 source files**
- 既存 ruff errors (`scripts/alpha_factory/{analyze_run,compare_batch_runs,todo_manager}.py` の `f"# Batch before/after comparison"` F541 等、 `tests/scripts/test_batch_metrics.py` の E501 等、 計 15 件) は PR 6 スコープ外 (PR 1-5 でも未触、 main と diff なし)

## レビュー観点

以下の観点で本 PR 6 の実装を critical review してください。 APPROVED か REQUEST_CHANGES の verdict と、 各観点の Findings を記載してください。

### 観点 1: 詳細設計 SSOT との一致 (施策 11)
- `assert_epoch_id_present_for_display` の log event 名 (`schema_contract.tier2_epoch_id_missing`) と kwargs (`artifact`, `keys`) が詳細設計 行 401-416 と一致しているか
- 4 scripts の Tier 2 ガード呼出箇所が詳細設計 行 1446-1449 (extract:134/280, generate:800, compare:252/256/262, analyze:168) の意図と整合しているか (現実装の正確な行番号は変動するが、 「summary load 直後 / report write 直前」の意図が守られているか)
- fail-open 規範 (詳細設計 行 1469-1470) が守られているか — Tier 2 ガードは raise せず warning のみ
- テスト 5 ケース (詳細設計 行 1462-1467) が網羅されているか

### 観点 2: fail-open 性 (Tier 2 軽量ガード規範厳守)
- `assert_epoch_id_present_for_display` は SchemaContractError を引き続き raise しないこと
- 4 scripts は dataset_epoch_id 欠落時も exit 0 を返すこと (= 軽量ガードで運用事故化させない、 詳細設計 行 1469-1470)
- empty string / None / key 欠落の 3 種類 全て warning trigger の対象になっているか

### 観点 3: Backward compat (既存 caller 完全互換)
- `assert_epoch_id_present_for_display` の signature (obj, *, artifact) 変更なし
- 4 scripts の CLI signature (argparse 引数) 変更なし
- `metrics` dict / `summary` dict / `batch_summary.json` の既存キーが維持されているか (新規キーのみ追加)
- 既存 9 件 test_batch_metrics.py が regression 0 で通っているか

### 観点 4: dataset_epoch_id の display propagation
- run-{N}.md (generate_run_report.py) / analysis-claude.md (analyze_run.py) / metrics report.md (extract_batch_metrics.py) / comparison_report.md (compare_batch_runs.py) の **全 4 出力**で dataset_epoch_id が表示されること
- batch_summary.json (compare_batch_runs.py) で per-run dataset_epoch_id が保持されること
- per-run metrics JSON (extract_batch_metrics.py) で dataset_epoch_id が保持されること

### 観点 5: 4 段伝搬契約 (転記漏れチェック)
PR 6 で touch する伝搬経路:
- summary.dataset_epoch_id (PR 5 で書込) → metrics["dataset_epoch_id"] (PR 6 で抽出) → batch_summary.json["dataset_epoch_ids"] (PR 6 で aggregate) → comparison_report.md の per-run table (PR 6 で render)
- summary.dataset_epoch_id → run-{N}.md header (generate_run_report)
- summary.dataset_epoch_id → analysis-claude.md header (analyze_run)
- summary.dataset_epoch_id → metrics report.md header (extract_batch_metrics)

これら 4 経路が all 接続されているか、 転記漏れがないか確認してください。

### 観点 6: 観測可能性
- LOG_ONLY mode で 4 scripts それぞれの artifact name (`run_report.md` / `analyze_run.analysis-claude.md` / `extract_batch_metrics.{summary,report.md}` / `compare_batch_runs.*`) が log の `artifact` kwarg で区別できるか
- `keys=sorted(obj.keys())` で warning 発火時に obj の構造が観測できるか
- 構造化キー (`schema_contract.tier2_epoch_id_missing`) が PR 1-5 の他 warning イベント (`alpha_sieve.v1_archive_skipped` / `archive.load.v1_archive_detected` / `schema_contract.passive_validation_failed`) と分離されているか

### 観点 7: テスト網羅性
詳細設計 行 1462-1467 の test 計画:
- `test_run_report_md_includes_dataset_epoch_id_in_header`
- `test_batch_summary_json_preserves_per_run_dataset_epoch_id`
- `test_comparison_report_md_references_dataset_epoch_id`
- `test_analyze_run_md_references_dataset_epoch_id`
- `test_extract_batch_metrics_report_md_includes_dataset_epoch_id`

これらが全て実装されているか、 名前と挙動が一致しているか確認してください。 加えて fail-open regression test (`test_tier2_guard_does_not_fail_when_dataset_epoch_id_missing`) が追加されていることが望ましい (詳細設計 行 1469-1470 SSOT を test で固定)。

### 観点 8: T058 cascade port v2 Phase 2 配線における整合性
- PR 1-5 で main merge 済の API (assert_epoch_id_present_for_display は PR 1 で追加済) を正しく import / 利用しているか
- `epoch_legacy` stub 値が PR 1-5 (run_ga / archive / calibrate / diagnostics / fsp / sieve) と PR 6 (display 4 scripts) で一致しているか (= 同じ epoch で全経路が同期する保証)
- PR 7 (統合テスト) で同じ pattern を踏襲できる contract を提供できているか

### 観点 9: collider bias 独立性 / SSOT 厳密性 (T064 follow-up 規範)
- summary.dataset_epoch_id が **唯一の真値** として 4 scripts で参照されているか (= literal "epoch_legacy" を直書きしている場所が無いか)
- Tier 1 (assert_run_report_v2, assert_genome_entry_v2, ...) と Tier 2 (assert_epoch_id_present_for_display) の責務分離が守られているか — Tier 2 は表示層のみ、 store/lint には触らない
- LOG_ONLY mode で warning が大量発生する可能性に対して artifact name で集約観測できるか

## verdict 形式

最後に、 以下の形式で verdict を返してください:

```
## verdict
APPROVED | REQUEST_CHANGES

## 主要 Findings (重要度順)

### [Critical]
N. ...

### [Warning]
N. ...

### [Suggestion]
N. ...
```

REQUEST_CHANGES の場合は、 修正対象の行番号と修正後コード snippet を必ず含めてください。
APPROVED の場合は、 軽微な改善提案 (Suggestion) は含めて構いません。
