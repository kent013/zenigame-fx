## Round 4 — Round 3 修正反映

Round 3 [Critical] 1 件 + [Warning] 3 件 + [Suggestion] 2 件を反映:

1. [Critical] LOG_ONLY 統一と「read 時 v1 fail」 文言の二重化 → 「T058 で実施するもの」 から `read 時 v1 fail` 削除、 「制約・前提」 から「v1 は読まない」 を「v1 検出時 warning + skip、 fail は T067」 に修正。 v1 fail 表記は T067 セクションのみに残す
2. [Warning] Tier 2 coverage (sieve-R{N}.md, extract_batch_metrics --report) → Tier 2 inventory に D7/D8 追加
3. [Warning] enforcement_mode の値検証 / fallback / unknown key → `SchemaContractConfig.__post_init__` で許容値検証、 fallback default = `log_only`、 unknown key 拒否
4. [Warning] RunContext T058 一括実施は破壊半径過大 → 段階導入: T058 で構造定義 + 主要 3 component (archive / calibrate / diagnostics)、 全 component 必須化は T059/T063 合流
5. [Suggestion] Tier 2 軽量ガード → `assert_epoch_id_present_for_display(report_obj)` 共通適用 (warning + Counter)
6. [Suggestion] cascade_contract_version 型固定テスト → JSON schema or pytest property test で型固定

改訂版概念設計: `devnotes/20260429-1912-todo-T058-schema-v2-contract/conceptual-design.md`

特に以下のセクション参照:
- 「Artifact inventory (2 層構造)」: Tier 2 D7/D8 追加 + Tier 2 軽量ガード明記
- 「スコープ縮小ポリシー」: T058 LOG_ONLY 統一 / RunContext 段階導入明記
- 「制約・前提」: SchemaContractConfig 値検証 / fallback / unknown key 規約

これで T058 の概念設計は contract 基盤として完成した認識。 APPROVED 判定可能か、 残課題あれば identify。

## レビュー期待

[Critical] / [Warning] / [Suggestion] で再評価。 全 Critical / Warning が解消されていれば APPROVED 判定。 これ以上 round を重ねる必要があれば再修正、 APPROVED なら詳細設計 (Phase 2) に進む。
