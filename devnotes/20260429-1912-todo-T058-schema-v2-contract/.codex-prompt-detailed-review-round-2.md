## Round 2 — Round 1 修正反映

Round 1 で 6 件 [Critical] + 4 件 [Warning] + 2 件 [Suggestion] が出ました。 反映:

| Round 1 [Critical/Warning] | 修正対応 |
|---|---|
| [Critical 1] RunContext.schema_contract_mode 不在 | enforcement_mode は AlphaFactoryConfig.schema_contract → GenomeArchive `enforcement_mode` kwarg で直接 inject、 RunContext には load しない |
| [Critical 2] GenomeArchive.__init__ 必須化破壊 | 既存シグネチャ `(run_id, run_number)` 維持、 `flush(output_dir=...)` 契約維持、 `run_context` / `enforcement_mode` を optional kwargs として追加のみ |
| [Critical 3] HistoryRecord 後方互換 | `read_history` で `(TypeError, ValueError)` catch、 `HistoryRecord.from_dict_or_none` classmethod 追加で v1 record を None 返却 |
| [Critical 4] append_record path keyword-only 衝突 | `append_record(record, path=DEFAULT_HISTORY_PATH, *, mode=...)` で positional 互換維持 |
| [Critical 5] LOG_ONLY 矛盾 | archive.load() / fsp `_read_archive_with_fsp_compat` は **(table, schema_version: int|None)** flag tuple を返却、 LOG_ONLY 時 caller が None で skip 判定。 `_atomic_write_parquet` は LOG_ONLY なら warning + 最善努力 (default 列補完) で write、 FAIL_CLOSED で raise |
| [Critical 6] dataset_epoch_id stub で dataset_span 完全置換は contamination 再導入 | T058 では **dataset_span を残し、 dataset_epoch_id は AND 結合の追加条件**、 完全置換は T067 |
| [Warning 7] history.json list 形式非互換 | history.json は list 形式維持、 各 entry に dataset_epoch_id 追加 |
| [Warning 8] STAGE_A_PROVENANCE_SCHEMA 列追加不明示 | schema 定義に `diagnostics_schema_version` / `dataset_epoch_id` 列を明示追加 |
| [Warning 9] C4 列数 (33 → 43 col) | 43 col 前提に再同期、 +4 col → 47 col |
| [Warning 10] 波及変更 (docs/skill) 不足 | DoD に `docs/alpha_factory/stage-gates.md` § T054 + `.claude/skills/zenigame-fx-calibrate-gate/SKILL.md:99` 追加 |
| [Suggestion 11] LOG_ONLY warning 洪水 | per-run counter + 末尾 summary log で集約 |
| [Suggestion 12] C3/C7 N/A 明記 | 詳細設計に N/A 明記 |

改訂版詳細設計: `devnotes/20260429-1912-todo-T058-schema-v2-contract/detailed-design.md`

特に以下のセクション参照:
- 「Round 1 review 反映」: 12 修正 mapping
- 「施策 4 archive.py」: GenomeArchive 既存シグネチャ維持 + load tuple 返却
- 「施策 5 calibrate_gate_history」: append_record positional 互換 + read_history v1 skip
- 「施策 6 calibrate_state」: dataset_span 残置 + dataset_epoch_id 追加条件 (AND)
- 「施策 7 diagnostics_sidecar」: STAGE_A_PROVENANCE_SCHEMA 列明示追加
- 「施策 8 fsp_updater」: read/write 両方 mode 連動 (LOG_ONLY 最善努力 fallback)
- 「施策 9 run_ga.py」: history.json list 形式維持
- 「DoD」: docs/skill 反映 + 波及テスト

## レビュー観点 (継続)

各施策ごとに APPROVE / REQUEST_CHANGES / INCONCLUSIVE 再判定。 Round 1 の Critical 6 件 + Warning 4 件すべて解消されているか、 新たな破壊点がないか。 特に:

- GenomeArchive の既存 test 互換 (`tests/alpha_factory/test_archive.py:161`)
- append_record の既存 positional 呼出 (`scripts/alpha_factory/calibrate_gate.py:452`, `tests/alpha_factory/test_calibrate_gate_history.py:59`)
- analyze_run.py の history.json list 前提 (現行 `analyze_run.py:40`)
- calibrate_state の dataset_span 残置で T067 切替時の cleanup tier 確認
- archive.load tuple 返却の caller 全箇所への影響

CHANGES_REQUESTED の場合は具体的修正案、 APPROVED の場合は判定理由明記。
