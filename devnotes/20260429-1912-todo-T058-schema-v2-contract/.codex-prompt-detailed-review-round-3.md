## Round 3 — Round 2 修正反映

Round 2 で 5 件 [Critical] + 3 件 [Warning] + 1 件 [Suggestion] が出ました。 反映:

| Round 2 [Critical/Warning] | 修正対応 |
|---|---|
| [C1] archive.load 戻り値契約不整合 | `load(path, *, mode, return_schema_version=False)` 後方互換、 既存 caller table 維持、 新 caller tuple |
| [C2] GenomeArchive 既存シグネチャ違反 | `out_dir` を __init__ から完全削除、 `flush(output_dir=...)` 既存契約のみ |
| [C3] diagnostics_sidecar 関数契約変更 | 既存 signature `write_stage_a_provenance(collector, ...)` 維持、 optional kwargs 追加のみ |
| [C4] LOG_ONLY 警告集約 非機能 | validator を `ValidationResult` 返却に変更、 result.ok で集計 |
| [C5] fsp_updater API 変更範囲未閉包 | `_read_archive_with_fsp_compat` は df 単独返却維持、 schema_version は `_detect_archive_schema_version` helper で別経路、 caller 全更新リスト明記 |
| [W6] C4 列数 33 col 残存 | 完全削除、 47 col 統一 |
| [W7] calibrate_state rename 波及 | 既存関数名 `load_calibrated_threshold` 維持、 `dataset_epoch_id` 引数追加のみ |
| [W8] DoD test path 誤参照 | `tests/scripts/test_calibrate_gate_drift.py` に修正 |
| [S9] 契約変更一覧 + caller 全件 | API 契約変更マトリクス (12 関数 × signature) を末尾に追加 |

改訂版詳細設計: `devnotes/20260429-1912-todo-T058-schema-v2-contract/detailed-design.md`

特に以下のセクション参照:
- 「Round 2 review 反映」 (新設): 9 修正 mapping
- 「API 契約変更マトリクス」 (新設): 12 関数の旧/新 signature + 後方互換 flag + 影響 caller
- 「施策 4 archive.py」: GenomeArchive __init__ から out_dir 削除、 load() return_schema_version flag、 ValidationResult 返却
- 「施策 5 calibrate_gate_history」: read_history で (TypeError, ValueError) catch
- 「施策 6 calibrate_state」: load_calibrated_threshold 関数名維持
- 「施策 7 diagnostics_sidecar」: 既存 signature 維持 + optional kwargs
- 「施策 8 fsp_updater」: 互換レイヤ + caller 全更新リスト
- DoD: test path 修正

## レビュー観点 (継続)

各施策を APPROVE / REQUEST_CHANGES / INCONCLUSIVE 再判定。 Round 1+2 の Critical 11 件すべて解消されているか、 新たな破壊点 / 矛盾がないか。 特に:

- API 契約変更マトリクスで挙げた 12 関数の signature が他施策の擬似コードと整合しているか
- `ValidationResult` 返却型が各 validator + caller (archive.flush 等) で一貫
- archive.load の `return_schema_version` flag が新 caller (run_alpha_sieve / fsp_updater) で正しく使われているか
- calibrate_state の dataset_span 残置が T067 cleanup tier として明確に申し送られているか
- DoD の docs/skill 反映 (stage-gates.md § T054 + zenigame-fx-calibrate-gate/SKILL.md:99) が Phase 3 の TODO 登録時に正しく拾われるか

CHANGES_REQUESTED の場合は具体的修正案、 APPROVED の場合は判定理由明記。
