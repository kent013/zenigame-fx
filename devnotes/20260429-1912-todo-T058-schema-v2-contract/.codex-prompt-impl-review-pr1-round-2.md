# T058 PR 1 impl-review Round 2 — Round 1 [Warning] F-001 対応マトリクス

## Round 1 verdict: APPROVED_WITH_CONCERNS
## Round 1 [Warning] F-001 対応

| Finding | 修正内容 |
|---|---|
| F-001: `assert_genome_entry_v2` の invalid epoch ログが SSOT (詳細設計 行 335 `schema_contract.invalid_epoch_id`) と一致しない | `assert_genome_entry_v2` を SSOT どおり修正: grammar 違反検出時は `validate_epoch_id` から raise された SchemaContractError を FAIL_CLOSED で再 raise (caller-friendly な "violates grammar" message を保持)、 LOG_ONLY mode では `schema_contract.invalid_epoch_id` 専用 event を warning 発火、 加えて missing/invalid 集約 event `schema_contract.passive_validation_failed` を従来通り発火する 2 段ログ構成へ。 |

### 具体的変更
**`src/alpha_factory/schema_contract.py` `assert_genome_entry_v2`**:
- `validate_epoch_id` で raise された `SchemaContractError` を FAIL_CLOSED mode では即時 re-raise (元 message 「dataset_epoch_id violates grammar [a-z0-9_]+: 'Epoch-2026'」 を保持)
- LOG_ONLY mode では `schema_contract.invalid_epoch_id` event (artifact / value 添付) を warning 発火し、 invalid 集合に追加
- 末尾 `schema_contract.passive_validation_failed` 集約 event は missing / invalid いずれかがあれば従来通り発火

### テスト追加 / 修正
- `tests/alpha_factory/test_schema_contract.py`:
  - `test_assert_genome_entry_v2_fail_closed_raises_on_invalid_epoch_id`: error message regex を `genome_entry` から SSOT message `violates grammar` に修正
  - `test_assert_genome_entry_v2_log_only_invalid_epoch_id_fires_dedicated_log_event` 新規追加: mock logger で `schema_contract.invalid_epoch_id` と `schema_contract.passive_validation_failed` の両方が発火することを assert

### Round 1 [Suggestion] S-001 (専用 test ファイル分離) 対応
本 PR では実装対応なし、 詳細設計の test 計画は `tests/alpha_factory/test_config.py` に追記指示だが PR 1 では SSOT 追跡性向上のため `test_config_schema_contract.py` 専用ファイルへ分離した。 設計文書側への追記は別 PR で対応 (PR 1 範囲外、 SSOT 追跡 layer のみ)。

## DoD 再 verify
- `uv run pytest tests/alpha_factory/test_schema_contract.py tests/alpha_factory/test_run_context.py tests/alpha_factory/test_config_schema_contract.py` → **40 passed** (Round 1 39 + Round 2 専用 event 検証 1)
- `uv run pytest tests/alpha_factory/` → 全 pass (規模感確認: 910 件、 Round 1 909 + 1 件追加)
- `uv run mypy src/` → clean
- 5 段階 grep DoD 不変

## 依頼
F-001 への対応が SSOT 整合・テストカバレッジ・後方互換すべてを満たしているか確認し、 verdict を返してください。

## 出力形式
```
## Verdict
APPROVED | REJECTED | APPROVED_WITH_CONCERNS

## Findings
### [Critical/Warning/Suggestion]
- ...

## (APPROVED_WITH_CONCERNS の場合) 残対応マトリクス
| Finding | 修正内容 |
|---|---|
```
