【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】

### C1. Design-first 原則 (code grep より先に docs を読む)
コードの bug / 設計欠陥について claim を出す前に、以下を verify:
- docs/alpha_factory/ 配下の該当セクション（stage-gates.md / clause-architecture.md 等）を Read で確認
- devnotes/ 配下の関連 T-number（関数コメントや git log に出てくる T-xxx）を ls devnotes/ | grep T{xxx} で探して読む
- git log --all -S "識別子" で該当 code の変更履歴を辿る
- 該当機能と並行計算される可能性のある経路を grep で広く検索

### C2. 「X が無い = バグ」禁止ルール
関数 Y の signature / body に識別子 X が無いことを根拠に bug 判定してはならない。別経路での計算を grep で広く検索してから。

### C3. Collider bias 必須チェック（相関分析時）
2 つの変数 A, B の相関を計算して report する場合、Conditioning set と collider の有無を必ず明記。

### C4. 前提明示ルール
Claim / verdict を出す前に、prompt の先頭に「本分析の前提」を bullet でリスト化し、各前提が verified であることを明示。

### C5. 並列 sub-agent の framing 独立性チェック
独立検証のつもりで同じ framing の sub-agent を N 個回しても、それは 1 つの誤読の N 倍冗長実行。

### C6. Fact / Interpretation 分離ルール
「観察された事実 (Facts)」と「解釈・推論 (Interpretations)」を明確に区別する。1 文で mix しない。

### C7. Sample size ガード
n < 30 の相関 claim は明示的に因果解釈を避ける。n < 10 は相関 claim 禁止（observation のみ許可）。

### C8. INCONCLUSIVE を第一級 verdict に
INCONCLUSIVE は CONFIRMED / REJECTED と同格の選択肢。データ不足は正当な結論。

### C9. Falsification-first プロンプト
Round 1 は「この hypothesis の反証を探せ」という指示を先に書く。confirmation bias を強制しない。

---

# T058 PR 3 実装レビュー (impl-review)

## 役割

あなたは zenigame-fx Alpha Factory cascade port v2 Phase 2 配線実装の **PR 3** (= 施策 5 + 施策 6 = `src/alpha_factory/calibrate_gate_history.py` の HistoryRecord v2 化 + `src/alpha_factory/calibrate_state.py` の scope key 拡張 + 既存 caller 呼出更新) の実装レビュアー。

T058 は 12 施策で構成される schema v2 contract の big-bang baseline。 PR 1 (基盤層 = schema_contract / RunContext / SchemaContractConfig、 commit `a4bf1bf`) + PR 2 (archive.py v2 schema、 commit `f810c24`) は main に merge 済。 PR 3 では既存 calibrate-gate state file 経路 (T054) を v2 schema に拡張し、 dataset_epoch_id を **追加条件 (AND 結合)** として導入する (synthesis § 12.1: T067 までは dataset_span ガードを残す)。

設計 SSOT は `devnotes/20260429-1912-todo-T058-schema-v2-contract/detailed-design.md` の以下範囲:
- 施策 5 (calibrate_gate_history): 行 891-1037
- 施策 6 (calibrate_state): 行 1040-1129

T058 全 6 round Codex APPROVED 済 (= 詳細設計が SSOT)。

## レビュー対象 (PR 3 のスコープ)

### 変更ファイル

1. `src/alpha_factory/calibrate_gate_history.py` — HistoryRecord に v2 必須 field 追加 + `__post_init__` validate + `from_dict_or_none` classmethod + `append_record` lint 連動 + `read_history` v1 skip
2. `src/alpha_factory/calibrate_state.py` — `SCHEMA_VERSION` 1→2 + `load_calibrated_threshold` に `dataset_epoch_id` 引数追加 + `_record_matches` に AND 条件追加
3. `scripts/alpha_factory/calibrate_gate.py` — `HistoryRecord` 構築時に `dataset_epoch_id="epoch_legacy"` 渡し
4. `scripts/alpha_factory/run_ga.py` — `load_calibrated_threshold` 呼出に `dataset_epoch_id="epoch_legacy"` 追加 (RunContext 全 propagate は PR 5 で実装、 ここは最小限)
5. `tests/alpha_factory/test_calibrate_gate_history.py` — v2 fixture + 新 T058 test class 追加
6. `tests/alpha_factory/test_calibrate_state.py` — v2 fixture + dataset_epoch_id AND 条件 test 追加
7. `tests/scripts/test_calibrate_gate_drift.py` — `_record_dict` fixture に v2 必須 field 追加 (CLI subprocess 経由 read_history が v2 lint で skip しないように)

### スコープ外 (touch しない、 PR 4-7 で実装)

- `src/alpha_factory/diagnostics_sidecar.py`
- `src/alpha_factory/fsp_updater.py`
- `scripts/alpha_factory/run_alpha_sieve.py`
- `scripts/alpha_factory/run_ga.py` の load_calibrated_threshold 呼出**以外**の RunContext 配線 (= PR 5 範囲、 詳細設計 行 1316-1402 の施策 9)
- T058 PR 1 で作成済みの schema_contract.py / run_context.py / config.py への大規模追加
- T058 PR 2 で更新済みの archive.py

## 実装内容サマリ

### 施策 5 (calibrate_gate_history.py)

**HistoryRecord** に v2 必須 field 追加 (default 値で keyword 互換):
- `calibrate_history_schema_version: int = 2` (= `CALIBRATE_HISTORY_SCHEMA_VERSION`)
- `dataset_epoch_id: str = ""` (空文字は `__post_init__` の `validate_epoch_id` で reject)

**`__post_init__`** で v2 必須検証:
- `calibrate_history_schema_version != CALIBRATE_HISTORY_SCHEMA_VERSION` → ValueError
- `validate_epoch_id(dataset_epoch_id)` で grammar `^[a-z0-9_]+$` 検証 (空文字も reject)

**`from_dict_or_none(obj)` classmethod**:
- `dataset_epoch_id` 不在 / 空 → None
- `calibrate_history_schema_version != 2` → None
- それ以外で `cls(**obj)` を試み、 TypeError/ValueError → None

**`append_record(record, path=DEFAULT_HISTORY_PATH, *, mode=LOG_ONLY)`**:
- `assert_calibrate_history_v2(record_dict, mode=mode)` で passive lint
- LOG_ONLY default で warning + Counter のみ

**`read_history(path, last_n=None)`**:
- `from_dict_or_none` 経由で v1/invalid record 自動 skip + warning log (`calibrate_history.v1_or_invalid_record_skipped`)

### 施策 6 (calibrate_state.py)

- `SCHEMA_VERSION` を 1 → 2 に更新
- `load_calibrated_threshold` シグネチャを **all kw-only** に変更し、 `dataset_epoch_id: str` を追加 (詳細設計 行 1063-1074 通り)
- `_record_matches` に `if rec_epoch_id != dataset_epoch_id: return False` を `dataset_span` チェック直後に追加 (T067 で dataset_span 廃止予定の moveable guard、 設計 行 1086-1090)

### caller 修正

- `scripts/alpha_factory/calibrate_gate.py:422` の HistoryRecord 構築時に `dataset_epoch_id="epoch_legacy"` 追加
- `scripts/alpha_factory/run_ga.py:1078` の `load_calibrated_threshold` 呼出に `dataset_epoch_id="epoch_legacy"` 追加

`epoch_legacy` は archive.py / PR 2 で template default として既に使われている stub 値で、 grammar `[a-z0-9_]+` 適合。 T059 で deterministic な epoch-rolling 識別子に置換予定。 PR 5 で RunContext 経由の full propagate に移行 (= 詳細設計 § 9 / 行 1316-1402)。

## DoD 確認

- `uv run pytest tests/alpha_factory/test_calibrate_gate_history.py -x` — **27 passed**
- `uv run pytest tests/alpha_factory/test_calibrate_state.py -x` — **26 passed**
- `uv run pytest tests/alpha_factory/ -x` — **938 passed (regression 0)**
- `uv run pytest tests/scripts/ -x` — **173 passed (regression 0)**
- `uv run pytest tests/` — **1503 passed, 1 skipped, 4 warnings (全 regression 0)**
- `uv run ruff check src/alpha_factory/calibrate_gate_history.py src/alpha_factory/calibrate_state.py scripts/alpha_factory/calibrate_gate.py scripts/alpha_factory/run_ga.py tests/alpha_factory/test_calibrate_gate_history.py tests/alpha_factory/test_calibrate_state.py tests/scripts/test_calibrate_gate_drift.py` — **clean**
- `uv run mypy src/` — **clean (102 source files)**
- 既存 ruff errors 8 件 (`tests/scripts/test_batch_metrics.py:240` 等) は PR 3 スコープ外 (PR 2 でも未触)

## RunContext 生成最小化の判断 (= PR 5 への持ち越し)

詳細設計 行 1108-1118 の caller pattern は `run_context.dataset_epoch_id` を経由する想定だが、 PR 3 タスク指示 (「run_ga.py 側 RunContext 生成は最小限 (= 詳細設計 行 1316-1402 の施策 9 = PR 5 範囲なので、 PR 3 では呼出箇所のみ修正、 RunContext 全 artifact propagate は PR 5 で実装)」) に従い、 `run_ga.py:1078` では literal stub `"epoch_legacy"` を直接渡す方針を採用 (= archive.py の template default と完全一致、 grammar 適合)。 RunContext 自体の生成 + GenomeArchive / DiagnosticsCollector / FSP への注入は PR 5 で行う。

## レビュー観点

以下の観点で本 PR 3 の実装を critical review してください。 APPROVED か REQUEST_CHANGES の verdict と、 各観点の Findings を記載してください。

### 観点 1: 詳細設計 SSOT との一致 (施策 5 / 施策 6)
- HistoryRecord field 順序 / default 値 / `__post_init__` 内容が設計 行 914-944 と一致しているか
- `from_dict_or_none` の判定ロジックが設計 行 1003-1014 と一致しているか
- `read_history` の skip + warning log 経路が設計 行 1019-1024 と一致しているか
- `_record_matches` の `dataset_span` ガード残置 + `dataset_epoch_id` AND 結合が設計 行 1086-1090 と一致しているか
- `load_calibrated_threshold` シグネチャが設計 行 1063-1074 (all kw-only) と一致しているか

### 観点 2: Backward compat
- 既存 HistoryRecord 呼出 (`calibrate_gate.py:422` の keyword arg 形式) が壊れていないか — `calibrate_history_schema_version` / `dataset_epoch_id` に default 値があるため keyword 互換
- `append_record(record, path)` の positional 呼出 (詳細設計 行 948 の指摘) が依然動くか
- `load_calibrated_threshold` を all kw-only に変更したことで、 既存 caller (`run_ga.py:1078` + tests) が keyword 経由で呼んでいるため互換維持
- 既存 v1 history.jsonl record は read_history で skip + warning (= big-bang 切替前の互換期間運用、 設計 行 988-991)

### 観点 3: 4 段伝搬契約 (転記漏れチェック)
HistoryRecord field 追加 → calibrate_gate.py 書込 → state file load の 3 段で:
- HistoryRecord に `dataset_epoch_id` field 追加 → __post_init__ で validate
- calibrate_gate.py で `dataset_epoch_id="epoch_legacy"` 書込
- calibrate_state.py の `_record_matches` で `rec.get("dataset_epoch_id")` 読み出し + match 判定
- run_ga.py の `load_calibrated_threshold` 呼出で `dataset_epoch_id="epoch_legacy"` を渡し
- これらの値が全て一致 (`epoch_legacy`) で初期通過することを保証している

### 観点 4: passive validation の正しさ
- `append_record` の LOG_ONLY mode で必須 field 欠落時に raise しないこと (Counter のみ)
- `assert_calibrate_history_v2` を schema_contract.py から正しく import しているか
- `__post_init__` の `validate_epoch_id` (= SchemaContractError raise) と `assert_calibrate_history_v2` (= LOG_ONLY warning) の責務が分離されているか
- `read_history` の skip log が v1 record (dataset_epoch_id 不在) と invalid record の両方を捕捉しているか

### 観点 5: T067 移行への影響範囲明確化
- 設計の T067 切替 (dataset_span 廃止 + dataset_epoch_id 単独 scope) で本 PR 3 の `_record_matches` から dataset_span チェック行のみ削除すれば移行完了することが、 コメントで明示されているか (設計 行 1100-1102)
- `_record_matches` の `if rec_epoch_id != dataset_epoch_id` ガードが `dataset_span` チェック直後に置かれており、 T067 で dataset_span 廃止しても残存することが明確か

### 観点 6: テスト網羅性
- 詳細設計 行 1027-1032 のテスト計画 4 件が `test_calibrate_gate_history.py` に実装されているか:
  - `test_history_record_v2_constructs_with_dataset_epoch_id`
  - `test_history_record_rejects_invalid_epoch_id_grammar`
  - `test_append_record_writes_v2_fields_to_jsonl`
  - `test_append_record_log_only_warns_on_missing_via_raw_dict_path`
- 詳細設計 行 1121-1125 のテスト計画 3 件が `test_calibrate_state.py` に実装されているか:
  - `test_load_calibrated_threshold_filters_by_dataset_epoch_id`
  - `test_load_calibrated_threshold_returns_none_for_v1_records`
  - `test_load_calibrated_threshold_skips_record_with_different_epoch_id`
- `test_calibrate_gate_drift.py` の `_record_dict` fixture が v2 必須 field を含み、 CLI subprocess 経由 (read_history) で skip されないことを保証しているか

### 観点 7: T058 cascade port v2 Phase 2 配線における整合性
- PR 1 の schema_contract.py の API (assert_calibrate_history_v2, validate_epoch_id, CALIBRATE_HISTORY_SCHEMA_VERSION, SchemaEnforcementMode) を正しく import / 利用しているか
- PR 2 の archive.py の `epoch_legacy` stub と calibrate_gate.py / run_ga.py の stub 値が一致しているか (= 同じ epoch で archive と calibrate-gate state が同期する保証)
- PR 4-7 (diagnostics / fsp / run_alpha_sieve / run_ga full propagate) で同じ pattern を踏襲できる contract を提供できているか

## verdict 形式

最後に、 以下の形式で verdict を返してください:

```
## verdict
APPROVED | REQUEST_CHANGES

## 主要 Findings (重要度順)

### [Critical]
N. ...

### [Warning]
N. ...

### [Suggestion]
N. ...
```

REQUEST_CHANGES の場合は、 修正対象の行番号と修正後コード snippet を必ず含めてください。
APPROVED の場合は、 軽微な改善提案 (Suggestion) は含めて構いません。
