## Round 6 — Round 5 修正反映

Round 5 で 1 件 [Critical] + 3 件 [Warning] + 1 件 [Suggestion] が出ました。 反映:

| Round 5 [Critical/Warning/Suggestion] | 修正対応 |
|---|---|
| [C1] `load_calibrated_threshold` 既存シグネチャ違反 | T058 では `dataset_epoch_id` のみ追加、 既存引数 (`history_path` 明示、 `dataset_span: tuple[str, str]`、 `threshold_floor`、 `threshold_ceiling`) 完全維持。 caller (`run_ga.py:1078`) の current pattern も変更なし |
| [W1] `archive.load` 表記不一致 | マトリクスを `GenomeArchive.load` (staticmethod) に統一 |
| [W2] tuple 受取 caller 一本化 | 「tuple 受取は run_alpha_sieve のみ」 明記、 fsp_updater は df 単独返却 + helper 経路 |
| [W3] FSP caller 全更新リスト | run_ga.py の FSP 呼出は現行不存在のため削除、 fsp_updater 内部の同 module caller のみ列挙 |
| [S1] flush 行明記 | マトリクス flush 行を `flush(output_dir: Path | None = None) -> Path` に詳細化 |

改訂版詳細設計: `devnotes/20260429-1912-todo-T058-schema-v2-contract/detailed-design.md`

## レビュー観点

Round 1-5 の Critical 計 21 件 + Warning 残のすべてが解消されているか、 新たな矛盾がないか最終確認。 特に:
- `load_calibrated_threshold` が既存 caller (`run_ga.py:1078`) を完全維持しつつ `dataset_epoch_id` だけ追加できているか
- `GenomeArchive.load` 表記の文書全体一貫性
- tuple 受取は run_alpha_sieve 単独
- FSP caller list が現行存在経路のみ

CHANGES_REQUESTED の場合は具体的修正案、 APPROVED の場合は判定理由明記。
