あなたは zenigame-fx Alpha Factory GA 改善を支援する Codex です。high reasoning で次の一手を再合議します。
前回あなたは (D) anti-overfit 選択圧を推奨し、実装・実測しました。結果を踏まえ再判断してください。

# 確定 frontier (再掲)
sharpe1.5/pnl70k/dd2%/trade50/pfr0.4。R101 control で 723 個体 (holdout ann med5.582/pnl med82k/
dd全<2%/pnl max92360/p90 91020)。これが現状の達成上限。

# これまで実証的に**否定**されたレバー (全て full run で in-loop 検証済)
1. 閾値引き上げ: dd20%→2% のみ品質改善。pnl70k→74k は in-loop 崩壊 (stage_c=0)。
   fold-consistency 0.55 非相関。nsga2 selection は median 退行 (tail のみ)。
2. (B) 新 primitive F15 MTFTrendPullback (上位足×下位足): registry バグ修正後に正しく実評価 →
   Stage A 76/Stage B 41 通過も **Stage C(holdout) 0** = MTF edge は holdout に非汎化 (overfit)。
   stage_c 全体も 534<723 に低下 (F15 探索が予算浪費)。REJECTED。
3. (D) anti-overfit 選択圧 --robust-selection (fold 安定性 = pfre/fold_sign_ratio/OOS PnL IQR を
   連続値 selection key に挿入, holdout 不使用): R107a (pnl70k, seed70, robust ON) で
   **Gate1 大幅退行**: stage_c_pass 247 (<650)、median ann 4.87 (<5.4)。
   robust breeding 圧が fold 安定だが高性能でない個体に population を誘導し、**達成 frontier を崩壊**。
   → REJECTED。
4. cross-pair 汎化: cycle12 まで 7 連敗 + 全 run ii_lite=0。

# 観察された一貫パターン
表現力拡張 (F15) も anti-overfit 圧 (robust) も pnl 閾値引き上げ (74k) も、いずれも「in-sample/fold で
得たものが holdout に残らない / frontier を犠牲にする」という同一の壁に当たる。723 個体 @ ann5.58/pnl82k
は EUR_JPY M1 intraday + この期間における **頑健な達成上限**である可能性が極めて高い。
データ: 単一ペア EUR_JPY M1 OHLC、出力[-1,+1] bounded、recurrence(過去のみ)、max_clause=2、
intraday(overnight 禁止)。期間延長・取引数操作・holdout 利用・overnight 保有は全て禁止。

# 質問 (high reasoning, 簡潔に)
1. (B)(D) 両構造方向が実証否定された今、**正の期待値が残る具体的レバーは存在するか**。
   存在するなら 1 つ特定し、なぜ holdout 汎化の壁を越えうるか反証可能な形で述べよ。
   (候補例: robust 重みの大幅縮小+selection key 最下位挿入の弱圧版 / 別 primitive family b 時間帯
   or c vol regime / fitness 関数への安定性項の弱い組込み / それ以外)。
2. それとも、確定 frontier を「頑健な達成上限」と結論し **総括 (D')** すべきか。
   総括する場合、AGENTS.md に残すべき最重要教訓 3 点と、北極星 (閾値引き上げ) を諦めない場合に
   将来再挑戦する価値がある唯一の方向を 1 つ挙げよ。
3. EV・実装コスト・過去の負の結果の重みで、次の一手を 1 つに収束。

最後に「全体判定: ...」で次の一手 (具体レバー 1 つ or 総括 D') を明示。
