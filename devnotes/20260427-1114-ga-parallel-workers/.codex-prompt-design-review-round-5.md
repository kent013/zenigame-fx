# Round 5: Round 4 Critical 反映後の最終確認 (5 ラウンド目 / 上限到達)

## 対応マトリクス (Round 4 → v5)

| Round 4 指摘 | 分類 | v5 対応 |
|---|---|---|
| §1 Critical: MappingProxyType pickle 非互換 | Critical | `__getstate__` で `mappingproxy → dict` 変換 → pickle 可能に。`__setstate__` で `dict → MappingProxyType` 再 wrap (深い不変性復元)。`pair_bars_map` value も `__setstate__` 内で tuple 化を再適用 |
| §2 Warning: pool_pids private API 依存 | Warning | `try-except AttributeError` で握り潰し空集合返却 + warning ログ。失敗時は `all_children_*` 指標経路にフォールバック |
| §2 Warning: `_build_preflight_b_result` 到達不能経路の意図確認テスト | Warning | `test_build_preflight_b_result_raises_when_payload_missing` 追加 |
| §3 必須: pickle round-trip テスト | Required | `test_lane_eval_context_pickle_round_trip_preserves_immutability` + `test_cross_pair_lane_inputs_picklable_directly` 追加 |
| §4: schema_version bump 明記 | Suggestion | 旧値・新値・追加 field を表で明記、実装時 grep 確認手順 |

## 修正版 v5 抜粋

### CrossPairLaneInputs (pickle 対応)

```python
@dataclass(frozen=True)
class CrossPairLaneInputs:
    target_pair: str
    pair_bars_map: Mapping[str, tuple[PriceBar, ...]]
    meta_map: Mapping[str, InstrumentMeta]

    def __post_init__(self) -> None:
        frozen_pair_bars = MappingProxyType({
            k: tuple(v) for k, v in self.pair_bars_map.items()
        })
        frozen_meta = MappingProxyType(dict(self.meta_map))
        object.__setattr__(self, "pair_bars_map", frozen_pair_bars)
        object.__setattr__(self, "meta_map", frozen_meta)

    def __getstate__(self) -> dict[str, Any]:
        return {
            "target_pair": self.target_pair,
            "pair_bars_map": dict(self.pair_bars_map),
            "meta_map": dict(self.meta_map),
        }

    def __setstate__(self, state: Mapping[str, Any]) -> None:
        object.__setattr__(self, "target_pair", state["target_pair"])
        object.__setattr__(
            self, "pair_bars_map",
            MappingProxyType({k: tuple(v) for k, v in state["pair_bars_map"].items()}),
        )
        object.__setattr__(
            self, "meta_map",
            MappingProxyType(dict(state["meta_map"])),
        )
```

### GenomeEvaluator.pool_pids (fallback)

```python
@property
def pool_pids(self) -> set[int]:
    if self._pool is None:
        return set()
    try:
        return {p.pid for p in self._pool._pool if p.pid is not None}
    except AttributeError:
        logger.warning(
            "parallel_eval.pool_pids_unavailable: "
            "multiprocessing.pool.Pool._pool not accessible, "
            "falling back to all_children_* metrics"
        )
        return set()
```

### pickle round-trip テスト

```python
def test_lane_eval_context_pickle_round_trip_preserves_immutability():
    cp = CrossPairLaneInputs(
        target_pair="EUR_JPY",
        pair_bars_map={"USD_JPY": (fixture_bars(1)[0],)},
        meta_map={"USD_JPY": fixture_meta()},
    )
    ctx = LaneEvalContext(
        lane_id="tier1_EUR_JPY",
        bars_a=tuple(fixture_bars(10)),
        bars_b=tuple(fixture_bars(10)),
        bars_holdout=tuple(fixture_bars(10)),
        meta=fixture_meta(),
        bt_cfg=fixture_bt_cfg(),
        cp_inputs=cp,
    )
    blob = pickle.dumps(ctx)
    restored = pickle.loads(blob)
    assert restored == ctx
    from types import MappingProxyType
    assert isinstance(restored.cp_inputs.pair_bars_map, MappingProxyType)
    with pytest.raises(TypeError):
        restored.cp_inputs.pair_bars_map["X"] = ()


def test_cross_pair_lane_inputs_picklable_directly():
    cp = CrossPairLaneInputs(
        target_pair="EUR_JPY",
        pair_bars_map={"USD_JPY": (fixture_bars(1)[0],)},
        meta_map={"USD_JPY": fixture_meta()},
    )
    restored = pickle.loads(pickle.dumps(cp))
    assert restored == cp
```

### invariant violation / fallback テスト

```python
def test_build_preflight_b_result_raises_when_payload_missing():
    lane_manager = LaneManager(...)
    ctx_without_payload = LaneEvalContext(..., preflight_underfilled=False, preflight_payload=None)
    with pytest.raises(RuntimeError, match="invariant violation"):
        lane_manager._build_preflight_b_result(fixture_genome(), ctx_without_payload)


def test_pool_pids_returns_empty_set_when_pool_internal_attr_missing(monkeypatch, caplog):
    evaluator = GenomeEvaluator(max_workers=2, ...)
    monkeypatch.delattr(evaluator._pool, "_pool", raising=False)
    pids = evaluator.pool_pids
    assert pids == set()
    assert "pool_pids_unavailable" in caplog.text
    evaluator.close()
```

### schema_version bump 明記

| 現行値 (実装時 grep 確認) | 新値 | 追加 field |
|---|---|---|
| `"X.Y"` | `"X.(Y+1)"` | `per_generation[*].stage_*_seconds_total/_max`, `per_generation[*].peak_rss_mb_per_worker`, top-level `max_rss_mb_per_worker` |

## 再レビュー依頼

1. **`__getstate__/__setstate__` で MappingProxyType pickle 非互換を解消**: 構造的不変性 (mappingproxy + tuple 化) を保ったまま pickle round-trip が成立するか?
2. **pool_pids private API fallback**: `try-except AttributeError + 空集合 + warning` で「将来 CPython 内部変更時の graceful degradation」が成立するか?
3. **テスト計画 5h/5i/5j 追加で必要十分か?** 残るカバレッジ穴はあるか?
4. **総合判定**: 全施策 (1-7) で APPROVED に到達したか?
5. これは 5 ラウンド目 (max_rounds=5) の最終回。CHANGES_REQUESTED が残る場合は「次フェーズで対応すべき非ブロッカー残課題」として明記してください。

判定 (APPROVED / CHANGES_REQUESTED / INCONCLUSIVE) を出力してください。
