# Round 3: Round 2 detailed-review 反映後の最終確認

## 対応マトリクス (Round 2 → v3)

| Round 2 指摘 | 分類 | v3 対応 |
|---|---|---|
| §1 Critical: LaneEvalContext 深い不変性不足 (assert は -O で無効、cp_inputs/preflight_payload は dict のまま) | Critical | (1) `__post_init__` の `assert` を **`raise TypeError`** に置換 / (2) `cp_inputs` を専用 frozen dc `CrossPairLaneInputs` に格上げ (`pair_bars_map` の value も tuple 化) / (3) `preflight_payload` を専用 frozen dc `PreflightPayload` に格上げ / (4) フィールド型を全て frozen dc / int / tuple に絞る |
| §2 Warning: `_build_preflight_b_result` の preflight_payload エイリアシング | Warning | StageResult.metrics dict を **呼び出し毎に新規作成**、frozen dc から値を **コピー** で取り出す。具体実装を pseudocode に追記 |
| §3 Warning: cp_inputs pickle サイズ計測 | Warning | `test_lane_eval_context_pickle_size_within_budget` 追加 (200MB 上限 assert) |
| §3 Warning: children(recursive=True) 指標の混同 | Warning | `measure_peak_rss_mb(pool_pids: set[int] \| None)` で worker pid を明示指定。`ga_worker_*` / `all_children_*` 指標を分離。`GenomeEvaluator.pool_pids` プロパティを公開 |
| §4 Warning: max_workers 警告のみで OOM リスク | Warning | `--strict-memory-guard` CLI flag 追加 (opt-in fail-fast)。autopilot で推奨利用 |
| §3 Warning: summary schema 後方互換 | Warning | `schema_version` を bump し `additionalProperties: true` 方針を docs に明記 |
| §4 Warning: 決定論テスト Stage B/C / reason_codes / archive 未カバー | Warning | `test_parallel_matches_sequential_full_stage_results` 追加: 全段 StageResult を `_normalize_stage_result` (timing 系除外) でハッシュ比較 |
| §4 Suggestion: preflight Stage B mock 呼ばれない明示テスト | Suggestion | `test_evaluate_genome_does_not_call_stage_b_when_preflight_underfilled` 追加 |
| §5 Suggestion: 施策2完了時点の直列回帰チェックポイント | Suggestion | 実装順序にチェックポイント明記 |

## 修正版 v3 抜粋

### 専用 frozen dataclass 階層

```python
@dataclass(frozen=True)
class PreflightPayload:
    n_unique_dates: int
    wf_min_unique_dates: int
    max_folds: int
    wf_min_folds_required: int
    n_bars: int


@dataclass(frozen=True)
class CrossPairLaneInputs:
    target_pair: str
    pair_bars_map: Mapping[str, tuple[PriceBar, ...]]  # value も tuple
    meta_map: Mapping[str, InstrumentMeta]


@dataclass(frozen=True)
class LaneEvalContext:
    lane_id: str
    bars_a: tuple[PriceBar, ...]
    bars_b: tuple[PriceBar, ...]
    bars_holdout: tuple[PriceBar, ...]
    meta: InstrumentMeta
    bt_cfg: BacktestConfig
    cp_inputs: CrossPairLaneInputs | None = None
    preflight_underfilled: bool = False
    preflight_payload: PreflightPayload | None = None

    def __post_init__(self) -> None:
        # assert ではなく raise (-O で無効化されない)
        if not isinstance(self.bars_a, tuple):
            raise TypeError(f"bars_a must be tuple: got {type(self.bars_a).__name__}")
        if not isinstance(self.bars_b, tuple):
            raise TypeError(f"bars_b must be tuple: got {type(self.bars_b).__name__}")
        if not isinstance(self.bars_holdout, tuple):
            raise TypeError(f"bars_holdout must be tuple: got {type(self.bars_holdout).__name__}")
        if self.cp_inputs is not None and not isinstance(self.cp_inputs, CrossPairLaneInputs):
            raise TypeError(f"cp_inputs must be CrossPairLaneInputs or None")
        if self.preflight_underfilled and self.preflight_payload is None:
            raise ValueError("preflight_payload must be set when preflight_underfilled=True")
        if self.preflight_payload is not None and not isinstance(self.preflight_payload, PreflightPayload):
            raise TypeError(f"preflight_payload must be PreflightPayload")
```

### _build_preflight_b_result (エイリアシング回避)

```python
def _build_preflight_b_result(self, genome, ctx):
    p = ctx.preflight_payload  # frozen dc → 値はそのまま安全
    return StageResult(
        stage="B", passed=False,
        metrics={  # ← 毎回新規 dict
            "stage": "B", "genome_name": genome.name, "n_bars": p.n_bars,
            "wall_time_seconds": 0.0,
            "payload": {  # ← payload も新規 dict
                "n_unique_dates": p.n_unique_dates,
                "wf_min_unique_dates": p.wf_min_unique_dates,
                "max_folds": p.max_folds,
                "wf_min_folds_required": p.wf_min_folds_required,
                # ... 既存と同じ偽結果 fields ...
            },
        },
        reason_codes=("stage_b_pre_flight_underfilled",),
    )
```

### measure_peak_rss_mb (worker pid 限定)

```python
def measure_peak_rss_mb(pool_pids: set[int] | None = None) -> dict[str, float]:
    """指標を `ga_worker_*` (pool_pids 指定時) と `all_children_*` (未指定時) で分離."""
    process = psutil.Process()
    main_rss_mb = process.memory_info().rss / 1024 / 1024
    ga_worker_rss, all_child_rss = [], []
    for child in process.children(recursive=True):
        try:
            rss_mb = child.memory_info().rss / 1024 / 1024
        except psutil.NoSuchProcess:
            continue
        if pool_pids is not None and child.pid in pool_pids:
            ga_worker_rss.append(rss_mb)
        elif pool_pids is None:
            all_child_rss.append(rss_mb)
    return {
        "main_rss_mb": main_rss_mb,
        "ga_worker_max_rss_mb": max(ga_worker_rss, default=0.0),
        "ga_worker_total_rss_mb": sum(ga_worker_rss),
        "n_ga_workers": len(ga_worker_rss),
        "all_children_max_rss_mb": max(all_child_rss, default=0.0),
        "all_children_total_rss_mb": sum(all_child_rss),
        "n_all_children": len(all_child_rss),
    }
```

### --strict-memory-guard CLI flag

```python
p.add_argument(
    "--strict-memory-guard",
    action="store_true",
    help="max_workers が memory budget を超えたら起動時 fail-fast (autopilot 推奨)",
)
```

### 全段カバレッジテスト

```python
@pytest.mark.parametrize("max_workers", [1, 2])
def test_parallel_matches_sequential_full_stage_results(max_workers, fixture_population):
    """Stage A/B/C 全段の StageResult と reason_codes を正規化ハッシュ比較."""
    # ... eval_seq vs eval_par、_normalize_stage_result で timing 系除外 ...

def _normalize_stage_result(r):
    if r is None: return None
    payload = dict(r.metrics.get("payload", {}))
    return (r.stage, r.passed, tuple(r.reason_codes), tuple(sorted(
        (k, v) for k, v in payload.items() if k not in ("wall_time_seconds",)
    )))
```

## 再レビュー依頼

1. **Round 2 Critical (深い不変性) は完全解消されたか?**
   - `assert` → `raise TypeError`、`cp_inputs` / `preflight_payload` の専用 frozen dc 化、`pair_bars_map` value も tuple 化、で「構造的不変性」を主張できるレベルに到達したか
2. **新たな Critical / Warning が無いか?** 特に:
   - `CrossPairLaneInputs.pair_bars_map` の `Mapping[str, tuple]` 型は immutable と言えるか? `MappingProxyType` で wrap すべきか?
   - `_build_preflight_b_result` の `payload` 内ネスト dict が他で参照共有されないか
   - `measure_peak_rss_mb` の `pool_pids` 取得タイミング (pool 起動直後 vs 各世代後で pid が変わる可能性)
3. **`schema_version` bump** (例: "2.0" → "2.1") 提案は妥当か。それとも `additionalProperties: true` 方針だけで十分か
4. **`--strict-memory-guard`** flag の対象範囲 (起動時のみ vs 世代毎ピーク監視まで) は本 TODO のスコープとして適切か
5. **テスト計画**: 5b/5c/5d/5e の追加で必要十分か

判定 (APPROVED / CHANGES_REQUESTED / INCONCLUSIVE) と残る指摘を出力してください。
