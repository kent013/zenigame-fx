# Round 4: Round 3 Critical 反映後の最終確認

## 対応マトリクス (Round 3 → v4)

| Round 3 指摘 | 分類 | v4 対応 |
|---|---|---|
| §1 Critical: Mapping 実体の可変エイリアス経路 | Critical | `CrossPairLaneInputs.__post_init__` で **defensive copy** + **value tuple 化** + **MappingProxyType wrap**。`object.__setattr__` で frozen dc に再代入。MappingProxyType は Python 3.10+ で picklable (Verified) |
| §2 Warning: `_build_preflight_b_result` fail-fast | Warning | 関数先頭で `if ctx.preflight_payload is None: raise RuntimeError(...)` |
| §2 Warning: pool_pids stale | Warning | `GenomeEvaluator.pool_pids` プロパティを **getter 呼び出し時点で動的再取得** する契約を §設計に明文化。maxtasksperchild=None 前提だが将来 stale 化に備える |
| §2 Warning: pickle size 200MB 固定閾値 | Warning | 絶対閾値 200MB AND 相対閾値 (baseline 1.5 倍以内) 併用 |
| §3: schema_version bump | Suggestion | summary.json schema_version を bump (例 "2.0" → "2.1")、`additionalProperties: true` 方針も併記 |
| §4: --strict-memory-guard 範囲 | OK | 起動時 fail-fast のみ (世代中ピーク監視は次フェーズ) — そのまま採用 |
| §5 Suggestion: 外部 dict 変更で LaneEvalContext 不変 | Suggestion | `test_cross_pair_lane_inputs_immutable_against_external_dict_mutation` 追加 |
| §5 Suggestion: worker PID 更新後 RSS 欠損なし | Suggestion | `test_measure_peak_rss_uses_current_pool_pids_not_stale` 追加 |

## 修正版 v4 抜粋

### CrossPairLaneInputs (深い不変性確保)

```python
from types import MappingProxyType

@dataclass(frozen=True)
class CrossPairLaneInputs:
    target_pair: str
    pair_bars_map: Mapping[str, tuple[PriceBar, ...]]
    meta_map: Mapping[str, InstrumentMeta]

    def __post_init__(self) -> None:
        # defensive copy + value tuple 化 + MappingProxyType wrap
        # (Round 3 §1 Critical 反映: 外部 dict 変更で context 内部が変化しないことを構造的に保証)
        frozen_pair_bars = MappingProxyType({
            k: tuple(v) for k, v in self.pair_bars_map.items()
        })
        frozen_meta = MappingProxyType(dict(self.meta_map))
        object.__setattr__(self, "pair_bars_map", frozen_pair_bars)
        object.__setattr__(self, "meta_map", frozen_meta)
```

### _build_preflight_b_result (fail-fast)

```python
def _build_preflight_b_result(self, genome, ctx):
    if ctx.preflight_payload is None:
        raise RuntimeError(
            "_build_preflight_b_result called without preflight_payload "
            "(invariant violation)"
        )
    p = ctx.preflight_payload
    return StageResult(stage="B", passed=False, metrics={...新規 dict...},
                       reason_codes=("stage_b_pre_flight_underfilled",))
```

### GenomeEvaluator.pool_pids (動的取得)

```python
@property
def pool_pids(self) -> set[int]:
    """worker process pid を **呼び出し時点で動的取得** する。

    maxtasksperchild=None (Phase 2 default) では起動時 pid と一致するが、
    将来有限化した場合に stale pid を返さないことを契約として保証。
    """
    if self._pool is None:
        return set()
    # multiprocessing.pool.Pool 内部 _pool 属性 (CPython 実装依存だが安定)
    return {p.pid for p in self._pool._pool if p.pid is not None}
```

### Mapping 実体凍結テスト (§5f)

```python
def test_cross_pair_lane_inputs_immutable_against_external_dict_mutation():
    src_pair_bars = {"USD_JPY": list(fixture_bars(100))}
    src_meta = {"USD_JPY": fixture_meta()}
    cp_inputs = CrossPairLaneInputs(
        target_pair="EUR_JPY",
        pair_bars_map=src_pair_bars, meta_map=src_meta,
    )
    src_pair_bars["EUR_USD"] = []
    src_pair_bars["USD_JPY"].append(fixture_bars(1)[0])
    src_meta["NEW"] = fixture_meta()
    assert "EUR_USD" not in cp_inputs.pair_bars_map
    assert len(cp_inputs.pair_bars_map["USD_JPY"]) == 100
    assert "NEW" not in cp_inputs.meta_map


def test_cross_pair_lane_inputs_pair_bars_map_is_mappingproxy():
    cp_inputs = CrossPairLaneInputs(target_pair="EUR_JPY",
                                     pair_bars_map={"USD_JPY": (fixture_bars(1)[0],)},
                                     meta_map={"USD_JPY": fixture_meta()})
    with pytest.raises(TypeError):
        cp_inputs.pair_bars_map["XXX"] = ()
    with pytest.raises(TypeError):
        cp_inputs.meta_map["XXX"] = fixture_meta()
```

### pickle size 相対閾値テスト (§5e v4)

```python
def test_lane_eval_context_pickle_size_within_budget():
    baseline_ctx = LaneEvalContext(...cp_inputs なし...)
    full_ctx = replace(baseline_ctx, cp_inputs=fixture_cross_pair_lane_inputs())
    baseline_mb = len(pickle.dumps(baseline_ctx)) / 1024 / 1024
    full_mb = len(pickle.dumps(full_ctx)) / 1024 / 1024
    assert full_mb < 200, f"absolute budget exceeded: {full_mb:.1f}MB"
    assert full_mb < baseline_mb * 1.5, f"cp_inputs overhead too large"
```

## 再レビュー依頼

1. **Round 3 Critical (Mapping 実体凍結) は完全解消されたか?**
   - `defensive copy` (`dict(...)`) + `value tuple 化` + `MappingProxyType wrap` の組合せで、外部 dict 変更が context に伝播しないことを構造的に排除できているか
   - `MappingProxyType` の Python 3.10+ picklable 仕様前提は問題ないか

2. **新たな Critical / Warning が無いか?**
   - `multiprocessing.pool.Pool._pool` は CPython 実装依存だが Python 3.13 で stable と判断して良いか? もし不安なら `multiprocessing.pool.Pool._processes` の代替経路は？
   - `_build_preflight_b_result` の RuntimeError raise 経路が「決して到達しない」場合のテスト戦略は？

3. **テスト計画**: 5f / 5g 追加で必要十分か?

4. **schema_version bump**: 詳細設計内に schema_version の現行値と新値を明記すべきか?

5. **総合判定**: 全施策 (1-7) で APPROVED に到達したか?

判定 (APPROVED / CHANGES_REQUESTED / INCONCLUSIVE) と残る指摘を出力してください。
