# cycle 3 詳細設計 Round 2 (Round 1 指摘への対応)

## Round 1 全指摘への対応マトリクス

| # | 指摘 | 重要度 | 対応 | 詳細 |
|---|------|--------|------|------|
| 1 | run_ga.py に pyarrow.parquet 未 import + 外側 Exception 過広 | **Critical** | **採用** | `import pyarrow.parquet as pq` を run_ga.py:46 周辺に追加、 hook 外側の `try/except` を撤去し関数内 fail-open のみに依存 |
| 2 | `n_used` の意味曖昧 (仕様 "NaN 除外後" vs 実装 `len(top)`) | Warning | **採用** | `n_used` を `n_selected` (= top_n、 NaN 除外前) にリネーム、 NaN 除外後の件数は metric ごとに別列 (`fold_sign_n_valid`, `pfre_n_valid`) で持つ |
| 3 | `fold_sign_positive_ratio` 定義が Stage B (`> 0`) とズレ | Warning | **採用** | `fold_sign_strict_positive_ratio` (= `> 0`) にリネーム、 Stage B 契約と一致 |
| 4 | summary.json に `diagnostics_stage_a_top_fold` 未配線 | Warning | **採用** | summary.json に sidecar 相対 path を `diagnostics_stage_a_top_fold` field で書く (T033 と同パターン) |
| 5 | テスト 7 件は不足 | Warning | **採用** | 統合テスト 2 件 (run_ga 経由で sidecar 生成 / `--no-report` で skip) + archive 列欠落 graceful 1 件、 計 10 件に拡張 |
| 6 | `int(...)` 切り捨てより `ceil` が「上位 20%」の意味に忠実 | Suggestion | **採用** | `math.ceil(pop_n * 0.20)`、 ただし `pop_n=0` の場合は呼び出し側で skip |
| 7 | 5760 vs 5856 行 (population × generations+1) | Suggestion | **採用** | リスク表のコメント数値訂正のみ、 実装影響なし |

## 修正後 sidecar スキーマ (20 列に拡張)

| # | column | dtype | nullable | 説明 |
|---|--------|-------|----------|------|
| 1 | diagnostics_schema_version | int32 | False | =1 |
| 2 | dataset_epoch_id | string | False | 同定 |
| 3 | run_id | string | False | 同定 |
| 4 | generation | int32 | False | 集計対象世代 |
| 5 | top_selector | string | False | 固定 "fitness_pen" |
| 6 | top_pct | float64 | False | 0.20 |
| 7 | population_n | int32 | False | その世代の Stage A pass 数 |
| 8 | top_n | int32 | False | math.ceil(population_n * 0.20)、 最低 1 |
| 9 | n_selected | int32 | False | 実際 selection された件数 (= top_n、 名称明確化) |
| 10 | fold_sign_n_valid | int32 | False | fold_sign_ratio が NaN でない件数 |
| 11 | pfre_n_valid | int32 | False | positive_fold_ratio_effective が NaN でない件数 |
| 12 | fold_sign_mean | float64 | True | NaN 除外 mean |
| 13 | fold_sign_median | float64 | True | NaN 除外 median |
| 14 | fold_sign_strict_positive_ratio | float64 | True | fold_sign_ratio > 0.0 比率 (Stage B 契約と一致) |
| 15 | positive_fold_ratio_effective_mean | float64 | True | NaN 除外 mean |
| 16 | positive_fold_ratio_effective_median | float64 | True | NaN 除外 median |
| 17 | positive_fold_ratio_effective_nan_ratio | float64 | True | NaN 比率 (= 1 - pfre_n_valid / n_selected) |
| 18 | n_fold_effective_mean | float64 | True | NaN 除外 mean |
| 19 | fitness_pen_mean | float64 | True | NaN 除外 mean |
| 20 | fitness_pen_median | float64 | True | NaN 除外 median |

## 修正後 build_top_fold_table (要点)

```python
import math

for gen, grp in sa_only.groupby("generation", sort=True):
    pop_n = len(grp)
    if pop_n == 0:
        continue  # Stage A pass 0 の世代は row 出さず
    top_n = max(1, math.ceil(pop_n * TOP_PCT_DEFAULT))
    top = grp.nlargest(top_n, "fitness_pen")
    n_selected = len(top)

    fsr = top["fold_sign_ratio"].dropna() if "fold_sign_ratio" in top.columns else pd.Series(dtype=float)
    fsr_n_valid = len(fsr)
    fsr_mean = float(fsr.mean()) if fsr_n_valid > 0 else None
    fsr_median = float(fsr.median()) if fsr_n_valid > 0 else None
    fsr_strict_pos_ratio = float((fsr > 0.0).sum() / fsr_n_valid) if fsr_n_valid > 0 else None

    pfre = top["positive_fold_ratio_effective"] if "positive_fold_ratio_effective" in top.columns else pd.Series(dtype=float)
    pfre_clean = pfre.dropna()
    pfre_n_valid = len(pfre_clean)
    pfre_mean = float(pfre_clean.mean()) if pfre_n_valid > 0 else None
    pfre_median = float(pfre_clean.median()) if pfre_n_valid > 0 else None
    pfre_nan_ratio = float(1 - pfre_n_valid / n_selected) if n_selected > 0 else None
    # ... 略
```

## 修正後 run_ga.py hook

```python
# imports セクション (ファイル上部、 line 46 周辺):
import pyarrow.parquet as pq

from src.alpha_factory.diagnostics_stage_a_top_fold import (
    stage_a_top_fold_relative_path,
    write_stage_a_top_fold,
)

# write_stage_a_provenance 直後の hook (line 1733 周辺):
top_fold_path_written: Path | None = None
if not args.no_report and archive_path is not None and archive_path.exists():
    archive_df = pq.read_table(archive_path).to_pandas()
    top_fold_path = REPO_ROOT / stage_a_top_fold_relative_path(run_number)
    top_fold_path_written = write_stage_a_top_fold(
        archive_df,
        top_fold_path,
        run_id=run_id,
        dataset_epoch_id=run_context.dataset_epoch_id,
    )
    # write_stage_a_top_fold 内部で fail-open、 例外時は warning + None。
    # 外側で握り潰さない (silent skip 防止)。
```

`_write_reports` 関数の引数に `top_fold_sidecar_path: Path | None = None` を追加し、 summary.json 構築箇所 (line 1056 周辺) で:

```python
if top_fold_sidecar_path is not None:
    try:
        summary["diagnostics_stage_a_top_fold"] = str(top_fold_sidecar_path.relative_to(REPO_ROOT))
    except ValueError:
        summary["diagnostics_stage_a_top_fold"] = str(top_fold_sidecar_path)
```

## 修正後 テスト 10 件

unit (7 件、 Round 1 と同じ + nan_ratio 計算修正):
1. test_build_top_fold_table_empty_returns_empty_table
2. test_build_top_fold_table_no_stage_a_pass_returns_empty
3. test_build_top_fold_table_basic
4. test_build_top_fold_table_multi_generation
5. test_build_top_fold_table_nan_handling
6. test_write_stage_a_top_fold_creates_parquet
7. test_write_stage_a_top_fold_fail_open_on_error

統合 (2 件):
8. test_run_ga_writes_stage_a_top_fold_sidecar — run_ga.py 経由 (smoke fixture) で sidecar が生成
9. test_run_ga_skips_sidecar_when_no_report — `--no-report` で skip

graceful (1 件):
10. test_build_top_fold_table_handles_missing_columns — archive に positive_fold_ratio_effective 列が無い古い RUN で None で埋まる

## 質問

1. Round 1 Critical/Warning 全採用の修正方針で APPROVED 可能か
2. スキーマ 20 列で過不足ないか (特に `n_selected` / `fold_sign_n_valid` / `pfre_n_valid` の 3 列追加は冗長か)
3. fold_sign_strict_positive_ratio の `> 0.0` は Stage B 契約と一致するが、 fold_sign_ratio 自体は signed (positive/negative fold sign の比率) なので「strict positive」 の意味解釈で合っているか
4. summary.json への `diagnostics_stage_a_top_fold` field 追加は consumer (run-report / analyze-run) の波及修正を要するか (本 cycle の責務範囲か)

ループ収束ルール: 本ラウンドで APPROVED または最小修正に絞る。
