あなたは Alpha Factory (FX イントラデイ GA) の分析レビュアーです。Run 89 を C9 falsification-first で簡潔に独立分析してください。

# 使命/制約
live_criteria全達成個体を1つ。graduation=Stage C pass AND cross-pair(ii-lite) pass。cross-pair pass: mean_sharpe_cross>=0.15 ∧ min_sharpe>=-0.20 ∧ (ratio opt-in) ∧ pair_failureなし(fail-fast)。閾値緩和禁止。取引回数を減らして見かけ改善も禁止。

# 7サイクル到達点
mission達成(R83)/cost-robust(R85)/再現可能(R86 warmstart)/汎化定量化(R87:0/599)/cross-pair選択圧 bool(R88飽和)→連続値(R89)。T116でpass3条件+pair_failureを観測列追加。

# Run 89 事実 (T116連続値選択圧 v3_5)
- A/B/C 2606/2142/892。ii_lite_pass=True 0/892。aggregate median0.026/max0.055。
- ★pass binding診断(Stage B 2142個体): mean_sharpe_cross median0.089/max0.143(pass>=0.15: 0)、min_sharpe>=-0.20: 2141/2142(ほぼ全達成)、ratio全NaN(sharpe_target_single未提供でopt-in除外)、★pair_failure_count median2/max3/=0はわずか7個体。
- mean上位5個体すべて pair_failure=2(target EUR_JPYは取引、両anchor EUR_USD/USD_JPYで取引不足=metric_unavailable→sharpe=0.0)。
- gen傾き平坦(mean_sharpe 0.0924→0.0830)。

# 診断仮説(反証可能性つき検討依頼)
真のbinding制約は mean<0.15(利益不足)でなく ★pair_failure(anchor取引枯渇): passed=base_all and not pair_failures で強制False。ほぼ全個体がanchorでmetric_unavailable(trade_count<min→sharpe0)。mean_sharpe≈target/3希釈。EUR_JPY特化のentryトリガーがanchorで発火しない構造問題。根本解=multi-pair training(fitnessを複数ペアで評価し全ペアで取引する戦略を進化)。

# 求める出力 (簡潔、日本語)
1. Facts要約(1-3行)
2. 解釈(各仮説の反証可能性): (a)真のbinding制約がpair_failure(anchor取引枯渇)でmean<0.15でないという診断は妥当か、(b)multi-pair trainingが根本解か/他の安価な代替(anchor選定見直し=似た価格帯ペア/cross-pair評価のtrade_count_min/正規化見直し)はあるか、(c)取引枠拡大が「取引回数操作(禁止事項)」に抵触しないか(=取引する戦略を進化させるのは正当か)
3. 次サイクル候補(Critical 1個/Warning 1-2個)。低リスク優先(multi-pairは最小spike先行)、閾値緩和・取引回数操作禁止、メタ過学習ガード、loop停止回避。
4. 全体判定 OK/CONCERN/CRITICAL_DRIFT
