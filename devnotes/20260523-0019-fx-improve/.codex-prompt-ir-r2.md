Round1 [Critical]+[Warning]2+[Suggestion] を反映しました。

[Critical] anchor pair文脈: RegistryEvaluator.with_pair(pair) を追加(全属性コピーでpairのみ差し替え、aux/strict維持、immutable)。_aggregate_multi_pair_stage_a で ev=_evaluator_for_stage(ctx,bars).with_pair(pair) として anchor評価。テスト: with_pair でpair差し替わり他属性維持を確認。
[Warning2] calibrate base_config_hash: multi_pair_training(enable/pairs/aggregate/scope)を payload に追加。テスト: ON vs OFF で hash 異なることを確認(single-pair threshold が multi-pair runに誤適用されない)。
[Warning3] anchor Stage A-only: _load_lane_bars に require_holdout:bool=True 追加、anchor は require_holdout=False で holdoutロード/fail-closed省略(bars_holdout=[]、I/O・失敗面削減)。
[Suggestion] config: pairs 重複禁止チェック追加(set長比較でfail-closed)。target含有はruntimeで anchors=pairs-target、空ならRuntimeError。

検証: T117 9テスト + with_pair + run_ga_parallel(bit-exact) pass。full suite 3347 passed(唯一失敗test_v31はmain由来既存)。ruff/mypy clean。

この反映で Critical解消しAPPROVEDか確認。残あれば最小変更で。
