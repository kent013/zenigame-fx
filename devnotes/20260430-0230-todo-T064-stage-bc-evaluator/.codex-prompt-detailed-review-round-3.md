## Round 2 → Round 3 改訂内容を反映済 — 再レビュー依頼

Round 2 で指摘された [Critical] 1 件 + [Warning] 3 件 + [Suggestion] 2 件すべてに対応しました。 同じフォーマット (`[VERDICT] / [Critical] / [Warning] / [Suggestion]`) で再評価をお願いします。 残修正が無ければ APPROVED で結構です。

### Round 2 → Round 3 改訂内容

| Round 2 指摘 | 改訂対応 |
|---|---|
| [C] Stage B concat DD 再流入 (b_pooled_cf.gate_pass が max_dd 含む総合判定) | **`compute_gate_pass_excluding_dd(cf_result, thresholds)` 新設**、 max_dd 軸を除いた 4 指標 (sharpe/pnl/tc/wr) の worst gap で gate_pass 判定。 `is_b_pass = gate_pass_ex_dd AND dd_pass` で per-fold DD max を採用、 concat DD 経路完全遮断 |
| [W1] INCONCLUSIVE と「厳密準拠」 文言混在 | docstring で「synthesis § 5.2 数式厳密準拠 + § 5.3/§ 5.4 閾値は smoke 後再校正 (INCONCLUSIVE)」 と明確分離 |
| [W2] truth table 優先順位衝突全列挙 | `test_stage_c_live_fail_and_cross_pair_fail_yields_reason_live_criteria`、 `test_stage_c_cross_pair_fail_and_stress_fail_yields_reason_cross_pair` 追加 |
| [W3] anchor_bundle 自体の妥当性チェック未記載 | evaluate_stage_c で `if anchor_bundle.pair != STAGE_C_ANCHOR_PAIR: raise StageBCInputError(...)` 追加 |
| [S1] is_b_pass 数式 docstring | StageBResult docstring に `is_b_pass = gate_pass_ex_dd ∧ dd_pass` 数式明記 |
| [S2] C2 grep DoD 実行結果添付 | 実装 PR (T064 PR) で grep 実行結果を PR description に添付する運用を DoD コメントに明記 |

---

### Round 3 詳細設計 — 主要改訂セクション抜粋

#### Stage B concat DD 再流入完全遮断 (Round 2 [Critical])

```python
def compute_gate_pass_excluding_dd(
    cf_result: CanonicalFiveResult,
    thresholds: CanonicalFiveThresholds,
) -> bool:
    """is_b_pass = gate_pass_ex_dd ∧ dd_pass の数式:
        gate_pass_ex_dd = max(max(0, -slack_m) for m in [sharpe, pnl, tc, wr]) <= GATE_PASS_TOLERANCE
                         AND cf_result.invariants.is_feasible
    """
    from src.alpha_factory.canonical_metrics import GATE_PASS_TOLERANCE
    if not cf_result.invariants.is_feasible:
        return False
    worst_ex_dd = max(
        max(0.0, -cf_result.slack_sharpe),
        max(0.0, -cf_result.slack_pnl),
        max(0.0, -cf_result.slack_tc),
        max(0.0, -cf_result.slack_wr),
    )
    return worst_ex_dd <= GATE_PASS_TOLERANCE


def evaluate_stage_b(...) -> StageBResult:
    ...
    pooled_dd_per_fold_max = pooled.pooled_dd_per_fold_max
    dd_pass = pooled_dd_per_fold_max <= thresholds.max_dd_max
    gate_pass_ex_dd = compute_gate_pass_excluding_dd(b_pooled_cf, thresholds)
    is_b_pass = gate_pass_ex_dd and dd_pass
    # b_pooled_cf.gate_pass / max_dd は記録のみで判定に使わない
```

#### anchor_bundle 妥当性チェック (Round 2 [W3])

```python
def evaluate_stage_c(...):
    if individual_input.anchor_bundle.pair != STAGE_C_ANCHOR_PAIR:
        raise StageBCInputError(...)
    # ... 12w main 評価 + cross-pair shadow + spread stress
```

---

レビュー観点 (Round 1, 2 と同じ): Round 2 1 Critical 修正完成度 / Warning 3 件吸収 / Suggestion 2 件吸収 / その他盲点。

`[VERDICT] / [Critical] / [Warning] / [Suggestion]` で返してください。
