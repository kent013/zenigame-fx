# Round 2 修正対応マトリクス (T064 follow-up 詳細レビュー)

Round 1 [VERDICT] CHANGES_REQUESTED の Critical 1 / Warning 2 / Suggestion 1 を全て反映しました。 修正後の状態を再評価してください.

## Round 1 → Round 2 対応表

| Round 1 [区分] 内容 | Round 2 修正対応 |
|---|---|
| [Critical] テスト計画「4 status × 4 n = 12」 算術不整合 (StagePassStatus は PASS/PENDING/FAIL の 3 値) | テスト計画を **「3 StagePassStatus × 4 n_pass_windows = 12 組合せ」** で再明記 (`test_compute_c_pass_depth_value_range_inclusive_zero_and_one_point_seven_five` 設計コメント)、 status 母集合 (PASS/PENDING/FAIL) を明示固定。 Follow-up Round 1 review 反映表にも同期記載 |
| [Warning 1] `compute_c_pass_depth` の `else: 0.0` で未知 enum 値が silent FAIL 等価扱い | StagePassStatus の 3 値 (PASS=1.0 / PENDING=0.5 / FAIL=0.0) を全て明示分岐、 未知値は `ValueError` raise (`unknown StagePassStatus: {value} (expected PASS / PENDING / FAIL)`)。 contract test `test_compute_c_pass_depth_rejects_unknown_status` 追加 (mock enum で未知値を渡す) |
| [Warning 2] `n_pass_windows ∈ [0, 3]` 生成時 invariant 検証なし | (1) `StageCLiteResult.__post_init__` で 2 段階 invariant 検証: `0 <= n_pass_windows <= STAGE_C_LITE_NUM_WINDOWS=3` + `n_pass_windows <= len(per_window_results)`。 (2) `compute_c_pass_depth` 入口でも guard を追加 (mock dataclass bypass test 用)。 (3) contract test `test_stage_c_lite_result_rejects_n_pass_windows_out_of_range` + `test_compute_c_pass_depth_rejects_n_pass_windows_out_of_range` 追加 |
| [Suggestion 2] T072 collider bias 規範独立性を一文で明示 | (1) `compute_c_pass_depth` docstring に「c_lite_result.n_pass_windows と c_result.mission_pass のみに依存、 holiday/DST/observability_flags 系には触れない」 を追加。 (2) detailed-design Follow-up section に「Collider bias 独立性 (T072 規範継承、 Round 1 [S2] 反映)」 専用ブロックを追加し、 Phase 2 配線時の継承責務を明記。 (3) conceptual-design 冒頭 Follow-up 反映 section にも一文追加で SSOT 一貫性確保 |
| [Suggestion 1] Critical 修正後は APPROVED 相当 | 全 Critical/Warning 反映済 |

## 主要修正箇所 (差分抜粋)

### 1. compute_c_pass_depth (明示 3 分岐 + 未知値 raise + n_pass_windows guard + collider bias 注記)

```python
def compute_c_pass_depth(
    c_lite_result: StageCLiteResult,
    c_result: StageCResult,
) -> float:
    """Follow-up (T066 Phase 0) 反映: c_pass_depth 計算 SSOT.

    計算式 (T066 概念 R4 確定):
        c_pass_depth = c_lite_result.n_pass_windows × 0.25
                     + bonus(c_result.mission_pass)
        bonus(PASS) = 1.0 / bonus(PENDING) = 0.5 / bonus(FAIL) = 0.0

    値域: [0.0, 1.75]
        - n_pass_windows: 0..3 (× 0.25 = [0.0, 0.75])
        - mission_pass status: PASS=1.0 / PENDING=0.5 / FAIL=0.0
        - 合計: [0.0, 0.75] + [0.0, 1.0] = [0.0, 1.75]

    Round 1 [W1] 反映: StagePassStatus 3 値全てを明示分岐、 未知値は ValueError raise
    (将来 enum 拡張時の silent FAIL 同等扱いを早期検知).

    Collider bias 独立性 (T072 規範継承): 本関数は c_lite_result.n_pass_windows と
    c_result.mission_pass のみに依存し、 holiday_markets / dst_transition_markets /
    observability_flags 系には触れない. stratified audit は本 module で扱わない.
    """
    if not 0 <= c_lite_result.n_pass_windows <= 3:
        raise ValueError(
            f"n_pass_windows must be in [0, 3], got {c_lite_result.n_pass_windows}"
        )
    base = c_lite_result.n_pass_windows * 0.25
    if c_result.mission_pass == StagePassStatus.PASS:
        bonus = 1.0
    elif c_result.mission_pass == StagePassStatus.PENDING:
        bonus = 0.5
    elif c_result.mission_pass == StagePassStatus.FAIL:
        bonus = 0.0
    else:
        raise ValueError(
            f"unknown StagePassStatus: {c_result.mission_pass!r} "
            "(expected PASS / PENDING / FAIL)"
        )
    return base + bonus
```

### 2. StageCLiteResult dataclass __post_init__ 検証

```python
@dataclass(frozen=True)
class StageCLiteResult:
    per_window_results: tuple[StageCLiteWindowResult, ...]
    cells_worst: float
    mission_pass: StagePassStatus
    progress_pass: StagePassStatus
    sample_size_flag: SampleSizeFlag
    n_pass_windows: int   # Follow-up: 値域 [0, STAGE_C_LITE_NUM_WINDOWS=3]、 c_pass_depth 計算 SSOT

    def __post_init__(self) -> None:
        """Follow-up Round 1 [W2] 反映: n_pass_windows 値域 invariant 検証.

        生成時に [0, STAGE_C_LITE_NUM_WINDOWS=3] 外なら ValueError raise.
        len(per_window_results) との整合性 (= n_pass_windows <= len) も検証 (caller bug 早期検知).
        """
        if not 0 <= self.n_pass_windows <= STAGE_C_LITE_NUM_WINDOWS:
            raise ValueError(
                f"n_pass_windows must be in [0, {STAGE_C_LITE_NUM_WINDOWS}], "
                f"got {self.n_pass_windows}"
            )
        if self.n_pass_windows > len(self.per_window_results):
            raise ValueError(
                f"n_pass_windows ({self.n_pass_windows}) cannot exceed "
                f"len(per_window_results)={len(self.per_window_results)}"
            )
```

### 3. テスト計画 (Round 1 [Critical] / [W1] / [W2] 反映)

```
- test_compute_c_pass_depth_value_range_inclusive_zero_and_one_point_seven_five
  (全組合せ **3 StagePassStatus (PASS/PENDING/FAIL) × 4 n (0/1/2/3) = 12 ケース** で
   0.0 <= depth <= 1.75、 status=FAIL は 0.0..0.75、 PENDING は 0.5..1.25、 PASS は 1.0..1.75.
   Round 1 [Critical] 反映で組合せ数を再明記)
- test_compute_c_pass_depth_rejects_unknown_status
  (Round 1 [W1] 反映: PASS/PENDING/FAIL 以外の値で ValueError raise)
- test_compute_c_pass_depth_rejects_n_pass_windows_out_of_range
  (Round 1 [W2] 反映: < 0 または > 3 で ValueError raise、 mock dataclass で迂回 bypass のみ test)
- test_stage_c_lite_result_rejects_n_pass_windows_out_of_range
  (Round 1 [W2] 反映: dataclass 生成時に値域外で ValueError、 len(per_window_results) 超でも ValueError)
```

### 4. Follow-up Round 1 review 反映表 + Collider bias 独立性 SSOT block 追加

`detailed-design.md` の Follow-up review 反映 section に以下を追加:

- **Follow-up Round 1 review 反映** マトリクス表 (Critical 1 / Warning 2 / Suggestion 1 の修正対応)
- **Collider bias 独立性 (T072 規範継承、 Round 1 [S2] 反映)** SSOT block (Phase 2 配線時の継承責務まで明記)

`conceptual-design.md` 冒頭 Follow-up 反映 section にも 1 行追加:

```
- Collider bias 独立性 (T072 規範継承): `c_pass_depth` / `n_pass_windows` 計算は holiday_markets / dst_transition_markets / observability_flags / schedule_status 系に依存しない (`per_window_results.cf_result.gate_pass` と `mission_pass` の 2 input のみ)、 stratified audit は本 module で扱わず T071 RunObservabilityReport 経由
```

## レビュー依頼

Round 1 で挙げた Critical 1 / Warning 2 / Suggestion 2 が **全て構造的に解消されているか** 評価し、 APPROVED または追加 CHANGES_REQUESTED を返してください.

### 求める出力

```
[VERDICT] APPROVED または CHANGES_REQUESTED

[Critical]
1. (なければ「なし」)

[Warning]
1. (なければ「なし」)

[Suggestion]
1. (なければ「なし」)
```

簡潔に、 Fact / Interpretation を明確に区別して記述してください.
