# Round 2: ファイル本文を直接 Read してレビュー再実施

Round 1 で「実装本文未提示のため監査不能 (INCONCLUSIVE)」 判定でした。

**重要**: あなたは Codex CLI の sandbox read-only モードで動作しており、 リポジトリ内のファイルを **Read ツール経由で直接読める** 状態です。 「ファイル読み込みは許可」 されています。 prompt 制約は「コマンド実行・ファイル書き込みのみ禁止」 (read は許可).

以下 2 ファイルを Read して、 Round 1 で求められた **6 観点** + Follow-up 反映 + 規範継承 の実装レビューを完遂してください:

## レビュー対象ファイル (絶対パス、 Read してください)

1. `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T064-pr1/src/alpha_factory/stage_bc_evaluator.py` (新規、 約 1100 行)
2. `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T064-pr1/tests/alpha_factory/test_stage_bc_evaluator.py` (新規、 97 test)

## 詳細設計参照 (Read 推奨)

- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T064-pr1/devnotes/20260430-0230-todo-T064-stage-bc-evaluator/detailed-design.md` (1078 行、 Round 1-3 + Follow-up Round 1-2 APPROVED 済)

## 依存ファイル参照 (Read 推奨)

- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T064-pr1/src/alpha_factory/canonical_metrics.py` (T061 マージ済)
- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T064-pr1/src/alpha_factory/partition.py` (T060 マージ済)
- `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T064-pr1/src/alpha_factory/stage_a_evaluator.py` (T063 マージ済、 規範参照)

## レビュー観点 (Round 1 と同一、 再掲)

### 1. 詳細設計適合性
- StagePassStatus / SampleSizeFlag / MissionFailReason の 3 enum
- 11 dataclass (PairBacktestBundle / StageBFoldResult / StageBResult / StageCLiteWindowResult / StageCLiteResult / StageCResult / PoolFoldedInput / BCEvaluationInput / BCEvaluationResult)
- 8 helper + 4 top-level + Constants + 例外
- truth table 優先順位 (LIVE_CRITERIA > CROSS_PAIR > STRESS)

### 2. Follow-up (T066 Phase 0) 反映
- BCEvaluationResult.c_pass_depth: float field
- StageCLiteResult.n_pass_windows: int field + __post_init__ で値域 [0, 3] および <= len(per_window_results) 二重検証
- compute_c_pass_depth が pure function、 StagePassStatus 3 値明示分岐 + 未知値 ValueError raise
- compute_c_pass_depth 入口で n_pass_windows 値域 guard
- compute_c_pass_depth docstring に collider bias 独立性 (T072 規範継承) 明記
- evaluate_bc_for_a_pass で c_pass_depth が compute_c_pass_depth 経由で BCEvaluationResult に注入

### 3. テスト計画網羅性
- 詳細設計テスト計画全項目
- Follow-up 12 ケース (3 status × 4 n)
- contract test (__dataclass_fields__ ベース)
- truth table 優先順位衝突テスト
- StageCLiteResult __post_init__ 値域違反 raise test

### 4. T058-T063 規範継承
- 既存 caller signature 完全維持
- status field 方式 (3 値明示分岐 + 未知値 raise)
- collider bias 独立性 (compute_c_pass_depth が holiday/DST/observability 系に触れない)
- SSOT 厳密性 (literal 3 を STAGE_C_LITE_NUM_WINDOWS 参照で集約)
- 半開区間 [start, end) 統一
- ruff B905 zip(strict=True) 規範
- no-raise contract 規範 (compute_c_pass_depth 等 pure function は no-raise + 構造化 result; ただし契約違反入力は ValueError raise が許容、 詳細 Follow-up Round 1 [W1] / [W2] 反映)
- NaN fail-fast の順序規範
- default-deny 契約 (forced_pass で eligible のみ通過)
- state を input dataclass に含めない single source of truth
- keyword 引数注入規範

### 5. 5 段階 grep DoD (PR 1 scope 確認、 verified)
- 段階 1 (直 import): tests のみ
- 段階 5 (runtime シンボル): stage_gate.py / swim_lane.py / cross_pair.py / run_ga.py に 0 hit
- 既存 stage_gate.py / swim_lane.py / cross_pair.py / run_ga.py / config.py / default.yaml / docs/alpha_factory/stage-gates.md 無変更

### 6. 数値計算正しさ
- compute_c_pass_depth: 整数 × 0.25 + 整数定数 のみ、 IEEE 754 で exact
- pooled_dd_per_fold_max = max(per-fold cf_result.max_dd) (synthesis § 5.2)
- compute_gate_pass_excluding_dd で max_dd 軸を除いた 4 軸 (sharpe/pnl/tc/wr) のみ判定
- compute_a_b_correlation で StatisticsError catch + (0.0, n) 戻り値 deterministic

## 規範適用境界 (Round 1 Warning 反映、 設計者明記)

`compute_c_pass_depth` の no-raise contract と未知値 ValueError raise の併存について:

- **正常入力ドメイン**: ``c_lite_result.n_pass_windows ∈ [0, STAGE_C_LITE_NUM_WINDOWS=3]`` および ``c_result.mission_pass ∈ {PASS, PENDING, FAIL}`` の場合、 no-raise + 浮動小数誤差なしで `[0.0, 1.75]` の有限値を返す.
- **契約違反入力**: 上記範囲外 (mock dataclass で ``__post_init__`` を bypass した bypass パス、 将来の enum 拡張) は **明示的に ValueError raise** (Follow-up Round 1 [W1] / [W2] で確定、 silent FAIL 同等扱いを早期検知)。 設計トレードオフ: caller (T065/T066) が SSOT 違反の早期検知を優先する.

## 出力フォーマット (Round 1 と同一)

```
## 観察された事実 (Facts)
- (実装ファイルから読み取れる客観的事実、 行参照付き)

## 解釈・推論 (Interpretations)
- (Facts に対する解釈、 設計準拠性の判定)

## 指摘事項

### Critical (修正必須)
### Warning (修正推奨)
### Suggestion (改善余地)

## Verdict
APPROVED / NEEDS CHANGES / REJECTED + 1 行理由
```

ファイルを Read して、 行参照付きで反証 / 確証を試みてください.
