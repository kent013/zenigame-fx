## Round 1 → Round 2 改訂内容を反映済 — 再レビュー依頼

Round 1 で指摘された [Critical] 5 件 + [Warning] 6 件 + [Suggestion] 4 件すべてに対応しました。 同じフォーマット (`[VERDICT] / [Critical] / [Warning] / [Suggestion]`) で再評価をお願いします。 残修正が無ければ APPROVED で結構です。

### Round 1 → Round 2 改訂内容

| Round 1 指摘 | 改訂対応 |
|---|---|
| [C1] Stage B fold 境界仕様自己矛盾 (StageBFoldResult に test なし、 pooled_dd_per_fold_max が dataclass 定義に存在しない) | StageBFoldResult に `fold_period_start: datetime` / `fold_period_end: datetime` 追加、 fold_max_dd property、 StageBResult に `pooled_dd_per_fold_max: float \| None` 追加、 PoolFoldedInput にも追加 |
| [C2] synthesis § 5.2 DD 集約準拠崩れ (concat DD 判定) | `is_b_pass = b_pooled_cf.gate_pass AND (pooled_dd_per_fold_max <= max_dd_max_threshold)` で per-fold DD max 採用、 concat DD は記録のみ |
| [C3] cross-pair provenance guard 不十分 | `PairBacktestBundle.validate_against(anchor_bundle)` method 追加、 genome_id / config_hash / partition_label 一致検証必須、 不一致で StageBCInputError raise。 BCEvaluationInput に anchor_bundle field 追加 |
| [C4] assert 依存契約 -O で無効化 | `if cross_pair_pass not in (PASS, FAIL): raise StageBCInputError(...)` に置換 |
| [C5] compute_a_b_correlation 定数系列 StatisticsError | try/except StatisticsError + math.isfinite check、 deterministic に `(0.0, sample_size)` 戻り値固定 |
| [W1] § 5.3/§ 5.4 閾値 Decision Pending | derive_stage_c_lite_thresholds / derive_stage_c_thresholds は INCONCLUSIVE タグ、 詳細設計で確定 (Phase 2 申し送り) |
| [W2] truth table テスト優先順位衝突ケース不足 | テスト計画に `test_stage_c_cross_pair_fail_takes_precedence_when_stress_also_fail` (cross_pair=FAIL かつ stress=FAIL なら mission_fail_reason=CROSS_PAIR を確認) 追加 |
| [W3] forced_pass invariant_ok field 独立フィールド無し | `StageCLiteResult.invariant_ok: bool` 派生 property 追加、 全 window 全 invariant OK で True |
| [W4] sample_size 全 window・全 bucket global min | docstring に「保守的な global min 採用、 1 window でも INSUFFICIENT なら全体 INSUFFICIENT」 1 行明記 |
| [W5] spread_stress_supported=True で NotImplemented 上位ハンドリング未記述 | docstring に「caller (T065) は spread_stress_supported=True 設定前に T070 完了確認の責務、 違反時は NotImplementedError raise」 明記 |
| [W6] Phase 2 「11 箇所」 列挙数不一致 | DoD で正確に 11 箇所列挙確認、 計数一致 |
| [S1] truth table を機械可読表 + parametrized test | テスト計画に `@pytest.mark.parametrize` で 6 パターン truth table 1:1 対応 |
| [S2] PairBacktestBundle.validate_against method | [C3] 統合済 |
| [S3] live_criteria を TypedDict 化 | 詳細設計の derive_*_thresholds で TypedDict 採用検討、 ただし Phase 1 では dict のまま (Phase 2 申し送り) |
| [S4] C2 DoD grep に __init__.py 明示追加 | DoD grep 段階 4 で `src/alpha_factory/__init__.py` を明示対象に |

---

### Round 2 詳細設計 — 主要改訂セクション抜粋

#### 1. StageBFoldResult / StageBResult / PoolFoldedInput 拡張 (Round 1 [C1] / [C2])

```python
@dataclass(frozen=True)
class StageBFoldResult:
    fold_index: int
    fold_period_start: datetime
    fold_period_end: datetime
    cf_result: CanonicalFiveResult
    is_feasible_invariant: bool

    @property
    def fold_max_dd(self) -> float:
        return self.cf_result.max_dd


@dataclass(frozen=True)
class StageBResult:
    b_pooled_cf_result: CanonicalFiveResult | None
    pooled_dd_per_fold_max: float | None  # per-fold DD max、 invariant_fail で None
    per_fold_results: tuple[StageBFoldResult, ...]
    is_feasible_invariant: bool
    is_b_pass: bool  # gate_pass AND pooled_dd_per_fold_max <= max_dd_max_threshold


@dataclass(frozen=True)
class PoolFoldedInput:
    pooled_trades: tuple[TradeRecord, ...]
    pooled_bars: BarEquitySeries
    pooled_business_day_universe: dict[SessionBucket, frozenset[int]]
    fold_boundaries: tuple[int, ...]
    pooled_dd_per_fold_max: float  # 明示 field
```

#### 2. evaluate_stage_b の DD 採用ロジック (Round 1 [C2])

```python
pooled = build_pooled_oos_input(per_fold_results, individual_input)
b_pooled_cf = evaluate_fn(pooled.pooled_trades, ...)
pooled_dd_per_fold_max = pooled.pooled_dd_per_fold_max
max_dd_max_threshold = thresholds.max_dd_max
dd_pass = pooled_dd_per_fold_max <= max_dd_max_threshold
is_b_pass = b_pooled_cf.gate_pass and dd_pass  # concat DD は使わない
```

#### 3. PairBacktestBundle.validate_against (Round 1 [C3])

```python
@dataclass(frozen=True)
class PairBacktestBundle:
    pair: str
    genome_id: str
    config_hash: str
    partition_label: str
    trades: ...
    bars: ...
    business_day_universe: ...

    def validate_against(self, anchor_bundle: "PairBacktestBundle") -> None:
        if self.genome_id != anchor_bundle.genome_id:
            raise StageBCInputError(...)
        if self.config_hash != anchor_bundle.config_hash:
            raise StageBCInputError(...)
        if self.partition_label != anchor_bundle.partition_label:
            raise StageBCInputError(...)


@dataclass(frozen=True)
class BCEvaluationInput:
    ...
    anchor_bundle: PairBacktestBundle  # provenance guard reference
    shadow_pairs: dict[str, PairBacktestBundle]
```

#### 4. assert→raise 置換 (Round 1 [C4])

```python
if cross_pair_pass not in (StagePassStatus.PASS, StagePassStatus.FAIL):
    raise StageBCInputError(
        f"cross_pair_pass must be PASS or FAIL (binary contract): {cross_pair_pass}"
    )
```

#### 5. compute_a_b_correlation 例外ハンドリング (Round 1 [C5])

```python
try:
    corr = statistics.correlation(a_values, b_values)
except statistics.StatisticsError:
    return 0.0, len(common_indices)
if not math.isfinite(corr):
    return 0.0, len(common_indices)
return corr, len(common_indices)
```

---

レビュー観点 (Round 1 と同じ): 5 Critical 修正完成度 / Warning 6 件吸収 / Suggestion 4 件吸収 / Phase 分離 / C2 grep / その他盲点。

`[VERDICT] / [Critical] / [Warning] / [Suggestion]` で返してください。
