【思考原則】仮説→検証、データに真摯、観測 vs 解釈分離、過度な複雑化禁止。

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。

---

# Conceptual Design Review Round 2 (label: conceptual-review)

Round 1 の指摘事項に対して以下の対応を行いました。判定をお願いします。

## Round 1 指摘 vs 対応

| Round 1 指摘 | 対応箇所 | 内容 |
|---|---|---|
| OandaClient 流用の事前条件が未確定 | §4.1 | 401/403/404/429/5xx/TransportError ごとの client 挙動と probe での扱いを表で固定。前提が崩れた場合の代替（probe 専用 HTTP 層）も明記。テストで前提担保。 |
| 404 の意味づけが弱く「永久に使えない」と解釈されかねない | §4.2 verdict 定義表 / §4.4 第 5 項 | NOT_FOUND を「現 live/account/environment で当該 instrument の candles を取得できなかった」事実のみと定義。レポートにも「観測 vs 解釈分離」注記を追加。403 についても同様に「永久に使えない」と読まないよう明記。 |
| Markdown 出力が実行フローに無く受け入れ基準と齟齬 | §4.2 / §4.4 | 「同一スクリプトが JSON 書き出し直後に probe-report.md も生成」と明記。`--output-md` オプション追加。 |

## 修正後の概念設計（本文）

# Conceptual Design: OANDA CFD Instrument Probe (T005)

## 1. 目的

OANDA の現行 FX **ライブ口座（OANDA_ENV=live, https://api-fxtrade.oanda.com）** において、以下 7 件の CFD 系 instrument に candles エンドポイント経由でアクセスできるかを試験する。

| Instrument | 用途 | 関連 Primitive |
|---|---|---|
| SPX500_USD | S&P 500 連動 | P7 RiskOnOffProxy |
| WTICO_USD | WTI 原油 | P9 OilPriceInverseFlow |
| XAU_USD | 金 (gold) | P12 GoldCorrelationBias |
| XCU_USD | 銅 (copper) | P8 CommodityFlowBias |
| JP225_USD | 日経 225 | 補助指標 |
| USB10Y_USD | 米 10 年債 CFD | 金利感応度 |
| USB02Y_USD | 米 2 年債 CFD | 金利感応度 |

これは **後続 primitive (P7-P12) 実装の戦略を分岐させる「重要な分岐点」**。

- 全 OK → OANDA で M1 candle 取り込み (FX と同じパイプライン拡張)
- 部分 OK → OK 分は OANDA、不可分は FRED 代替 / FX データ完結
- 全不可 → FRED + 自前計算で代替

## 2. 仮説

**H1**: OANDA 通常 FX 口座では CFD は権限制限により 403 が返る可能性が高い（特に米国規制対象 JP225/USB02Y/USB10Y）。SPX500/XAU/WTICO はプリミアム CFD account 別途開設が必要な可能性。

**H2**: 全 instrument がアクセス可能で追加開設なしで CFD candle が取れる。実証してみないと分からない。

成功判定: 7 件すべてについて HTTP status / candles 件数 / エラーメッセージを観測でき、verdict（OK / FORBIDDEN / NOT_FOUND / OTHER）が一意に決まる。

## 3. スコープ / 非スコープ

**スコープ**:
- 7 instrument それぞれに `/v3/instruments/{instrument}/candles?count=10&granularity=M1&price=BA` を 1 回ずつ
- HTTP status code, error message, candles 件数を記録
- JSON 出力 + Markdown レポート整形

**非スコープ**:
- DB 書き込み (CurrencyPair / price_bar_m1 は触らない)
- 他 granularity / count / from-to 試験
- アカウント開設手続き
- 後続 ingest スクリプト実装（別 TODO）

## 4. アプローチ

### 4.1 既存資産の再利用

**事前条件（verified by code-read）**: `src/api/oanda/client.py::_raise_for_status` の挙動を本設計の前提として固定する。

| HTTP Status | 既存 client の挙動 | probe での扱い |
|---|---|---|
| 401 | `OandaAuthError` を raise（リトライなし） | 即 abort |
| 403 | `httpx.HTTPStatusError`（`raise_for_status` 由来）を raise（リトライなし） | 捕捉して `verdict=FORBIDDEN` 記録 |
| 404 | `httpx.HTTPStatusError` を raise（リトライなし） | 捕捉して `verdict=NOT_FOUND` 記録 |
| 429 / 5xx | `OandaRateLimitError` / `OandaServerError` → tenacity 3 回リトライ後再 raise | 捕捉して `verdict=OTHER`、status_code 記録 |
| TransportError | tenacity 3 回リトライ | 捕捉して `verdict=OTHER` |
| 200 | 正常 return | `verdict=OK` |

「401 と 403 が異なる例外型として降ってくる」という事前条件が成立しているため `OandaClient` をそのまま流用する。**前提が将来崩れた場合（例: 403 も `OandaAuthError` 化）は probe 専用の薄い HTTP 呼び出し層に切り替える**。テストでこの前提を担保（403 シナリオで `OandaAuthError` が出ないことを確認）。

### 4.2 スクリプト構成

`scripts/oanda_cfd_probe.py`:

1. `OandaClient` を `with` で open
2. 各 instrument について:
   - `client.get_candles(name, granularity="M1", price="BA", count=10)` を try で実行
   - 成功 → `verdict=OK`, status=200, candles 件数, 先頭 candle の time
   - `httpx.HTTPStatusError` → 403→FORBIDDEN / 404→NOT_FOUND / その他→OTHER
   - `OandaAuthError` (401) → 全 probe 中断、stderr エラー出力、exit 1
   - その他例外 → `verdict=OTHER`, message
3. JSON で `probe-result.json` に保存
4. **同一スクリプトが続けて probe-report.md も生成**
5. 各 instrument の判定を stdout 出力
6. `--output-json` / `--output-md` で保存先上書き可
7. `--instruments` で対象変更可（デフォルト 7 件）

**verdict 定義（観測事実、解釈なし）**:

| verdict | 定義（観測事実） | 解釈の禁止事項 |
|---|---|---|
| OK | HTTP 200 + candles レスポンス取得 | - |
| FORBIDDEN | HTTP 403 観測 | 「永久に使えない」と解釈しない。account 区分・契約状態に依存可能性あり |
| NOT_FOUND | HTTP 404 観測 | 「OANDA に存在しない」と断定しない。**現 live/account/environment で当該 instrument の candles を取得できなかった** という事実のみ |
| OTHER | 上記以外 | 一時的問題の可能性が高いので再試行推奨 |

### 4.3 出力 JSON スキーマ（draft）

```json
{
  "probed_at": "2026-04-22T03:49:00+00:00",
  "base_url": "https://api-fxtrade.oanda.com",
  "results": [
    { "instrument": "SPX500_USD", "status_code": 200, "verdict": "OK", "candle_count": 10, "first_candle_time": "...", "error_message": null }
  ],
  "summary": { "total": 7, "ok": 5, "forbidden": 2, "not_found": 0, "other": 0 }
}
```

### 4.4 レポート構成

`probe-report.md` を **同一スクリプトが JSON 書き出し直後に生成**:

1. 試験条件 (環境, base_url, 試験日時)
2. 結果サマリー表 (instrument × verdict × status_code × candle_count × first_candle_time)
3. 判定 (全 OK / 部分 OK / 全不可)
4. 次アクション提案 (verdict 集計に応じた分岐)
5. **観測 vs 解釈の分離**: verdict は観測事実のみ。403/404 を「永久に使えない」と読まない注記。

実 API 走行後は Claude がレポートを読んで「次アクション提案」セクションに後続 TODO 候補（概要）を追記する。

## 5. テスト設計

`tests/scripts/test_oanda_cfd_probe.py`:

- `respx` で OANDA candles エンドポイントを mock
- シナリオ: 全 200 / 全 403 / 全 404 / 混在 / 401 (即 abort)
- output JSON ファイル生成と内容を assert
- exit code: 401 以外は 0

## 6. 想定リスク・mitigation

| リスク | mitigation |
|---|---|
| 401 (token 切れ) | 1 件目で abort + stderr |
| Rate limit (429) | tenacity 自動リトライ。7 件のみで実害なし |
| Weekend で空配列 | OK 判定に candle_count > 0 不要 |
| 実 API は token 必須 | テストは respx mock |

## 7. 受け入れ基準

- [ ] 7 instrument を試験できる
- [ ] JSON / Markdown 両方出力
- [ ] respx mock テスト 4 シナリオ以上
- [ ] レポートに次アクション提案明記
- [ ] runbook.md に手順追記
- [ ] cross-pair.md の external data 戦略を実測結果で更新

---

## 出力フォーマット

```
判定: APPROVED または NEEDS_REVISION

理由:
- ...
```
