あなたは zenigame-fx Alpha Factory GA の Codex です。high reasoning で cycle27 設計合議。
前回 (Round2) あなたは総括 D' を採用しつつ「将来唯一の方向=低自由度 regime 条件 primitive (時間帯/
vol regime) 1系統」を残しました。実装前 analyze で重大事実が判明したため再確認します。

# 重大事実: regime 条件付けは既に default 32-primitive でカバー済
あなたが推奨した「低DOF regime primitive」に相当するゲートが、R101 (frontier=723個体達成) を含む
**全 run で既に GA に提供済**でした:
- **M1 ATRRegimeGate** (MODULATOR, pure OHLC M1, 4 params): `sigmoid(±(atr/close - threshold)/scale)`
  = vol regime ゲート (高/低vol を prefer_high で選択)。
- **M2 SessionGate** (MODULATOR, pure OHLC, 2 params): Tokyo/London/NY セッション hour ゲート + soft edge
  = 低DOF time-of-day ゲート。
- **M6 TrendStrengthGate** (MODULATOR, pure OHLC, 3 params): ADX trend regime ゲート。
- F6 SessionMomentum (session内 directional), F12 RealizedVolZScore (vol metric) も既存。
- MODULATOR は乗算ゲート ([0,1], dir_score×Πgate)。GA は genome に自由に組込み可能。

つまり GA は vol regime (M1)・session/time (M2)・trend regime (M6) のゲートを持った状態で
pop96×gen60×warmstart 探索し、frontier 723個体 @ ann5.58/pnl82k に収束した。

# これまでの否定 (再掲、全 full run + in-loop 検証済)
- 閾値: dd2% のみ改善、pnl74k in-loop 崩壊。nsga2 median 退行。
- (B) F15 MTFTrendPullback (5 params): Stage A/B 通過も holdout 0 (overfit)、stage_c 534<723。REJECT。
- (D) robust-selection (anti-overfit 選択圧): R107a で frontier 崩壊 (stage_c 247<650, ann4.87)。REJECT。
- cross-pair: 8+ 連敗、ii_lite=0。

# 質問 (high reasoning, 簡潔に)
1. M1(vol regime)/M2(session)/M6(trend regime) が既に frontier 達成時の探索空間にある事実を踏まえ、
   「低DOF regime primitive 追加」は依然として正の期待値を持つか。既存ゲートとの**直交性が明確で、
   かつ frontier を動かす反証可能仮説**が立つ具体的低DOF信号 (1-2 params, pure OHLC, [0,1] or [-1,1])
   が1つでもあるか。あるなら特定し、なぜ M1/M2/M6 で代替できないか述べよ。
2. 無いなら、**総括 D' を最終化** (構造手段の探索を正式に閉じ、frontier=sharpe1.5/pnl70k/dd2%/
   trade50/pfr0.4=723個体@ann5.58 を最終達成上限として確定) すべきか。最終化する場合、AGENTS.md に
   追記すべき「regime も既存カバー」の1文と、improve-cycle ループを以後どう運用すべきか
   (例: 新規構造 run 停止し monitoring/shadow のみ、データ拡張等の前提条件が変わるまで凍結) を述べよ。
3. EV・実装コスト・過去の負の結果で次の一手を1つに収束。

最後に「全体判定: ...」で次の一手 (具体的低DOF信号1つ or 総括D'最終化) を明示。
