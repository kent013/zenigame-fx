【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

---

# Round 3 — 概念設計レビュー最終確認（最大 2 ラウンド規約の追加確認）

Round 2 で指摘された 3 つの Must-fix（内部不整合）を修正しました。上位 skill からの指示により round 数は最低限に抑えるため、この round で最終判定をお願いします。

## Round 2 Must-fix への対応マトリクス

| Round 2 Must-fix | 対応 | 反映箇所 |
|-----------------|------|---------|
| look-ahead bias 表の M5 が `bar_time.date() - 1` のままだった | `publication_ts_utc < bar_time`（bisect_left）に更新、`publication_ts_utc = obs_date の 21:15 UTC` 固定を明示 | look-ahead bias 対策表 M5 行 |
| Definition of Done に `event_calendar` / `vix_daily` 旧名称が残っていた | `event_snapshot` / `vix_snapshot` に統一、`EconomicEventSnapshot` / `VixSeriesSnapshot` dataclass 新設を DoD に明記 | DoD |
| テスト戦略の後方互換確認も旧名称のままだった | `event_snapshot` / `vix_snapshot` に統一。M5 に `publication_ts_utc >= bar_time` observation を排除するテストケース追加も明記 | テスト戦略 |

## Round 2 Should-consider 対応

- 共通仕様の M2 `required_data` 内部不整合: `("ohlc",)` に簡素化（S3 と統一）
- `EvaluationResult.meta` 後続委任の明確化: DoD に「warning は本 TODO 必須、meta 露出は後続 TODO」と明記

## 期待する verdict

全 Must-fix / 内部不整合を潰したので APPROVED を発出してください。残論点があれば箇条書きで示してください。

---

# 修正後 conceptual-design.md 全文

