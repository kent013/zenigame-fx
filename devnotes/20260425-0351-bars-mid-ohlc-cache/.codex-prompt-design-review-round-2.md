## Round 2 対応

| Round 1 | 対応 |
|---|---|
| [Critical] 型契約 `list[PriceBar]` vs `Sequence[PriceBar]` | `bars_to_mid_ohlc` / `_compute` / `_REFS` の型を全て `Sequence[PriceBar]` に変更 |
| [Warning] id 再利用 test が実質 no-op | `_REFS[key]` を sentinel に差し替え → 同 key で呼んで miss path に入り recompute されることを behavioral に検証する test に書き直し |
| [Suggestion] clear_cache race | 現状契約下 (single-thread backtest) を維持、thread 導入時は別 TODO で lock 追加と明記 (concept-design P8) |

判定をお願いします（APPROVED / CHANGES_REQUESTED / INCONCLUSIVE）。
