**前提（C4）**
- 本レビューは、提示された証跡（テスト結果・grep結果・対象仕様）に限定した検証です。
- 反証仮説「未発見の bug / regression / spec drift がある」を各観点で検討しましたが、提示情報内で成立する反証は確認できませんでした。

**verdict**: **APPROVED**（提示証跡ベース）

**rationale**
1. C2 parallel-path: `src/` から `audit` import 0件、`archive.py` の既存 `dsr` 経路未変更という証跡があり、Phase 1 の分離要件と整合。
2. AuditNullModel invariant 順序: F5 で I-1→I-6 の順序固定が検証対象に含まれ、回帰テスト通過と矛盾なし。
3. sentinel policy: `DSR=-1` / moment=`0` / status分岐 / `n_observations`実測維持の設計要件がテスト群（F1-F42）対象に含まれ、失敗報告なし。
4. `_make_sentinel_metric`: keyword-only と `status!="ok"` 制約が設計上のクリティカル点で、該当回帰なし。
5. PBO/SPA scaffold: `status + audit_calc_version` のみ・`not_implemented`固定・`scaffold-v1` 固定という縮退仕様と矛盾する証跡なし。
6. stratification API guard: default/allowlist、`schema_version="1.0.0"`・`flag_namespace="t072"`、`n<30` warning、不正キー系 `ValueError` の要件に対する失敗証跡なし。
7. collider bias 規範継承: F22d で `open_minutes > 0` フィルタのみを確認対象としており、`holiday_markets` 単独除外しない要件と整合。
8. canonical genome dedup: retry/cache hit/fold/seed/hash差分の raw/unique カウント仕様（F31-F35）に対し回帰なし。
9. schema_version helper: MAJOR不一致エラー、MINOR新旧警告、PATCH無視、不正フォーマットエラーの期待挙動に対する不整合証跡なし。
10. 既存挙動影響: `statistics.py` は docstring 同期のみで数式・signature不変、全体 `pytest` で regression 0 は整合的。

**fix_required**（NEEDS_FIX時）: 該当なし

**collider bias 規範継承**: **確認済**（提示証跡ベース）

**C2 parallel-path grep DoD**: **確認済**（提示証跡ベース）

補足: 一次ソース本文の逐語確認は未実施のため、最終監査でパッチ実体（対象3ファイル）を提示いただければ、行単位で再確認可能です。