**verdict**: NEEDS_FIX

**rationale**
- a. 確認済: `src/alpha_factory/audit.py:159` から I-1→I-6 の順で検証され、詳細設計 `devnotes/20260430-2301-todo-T073-audit-layer/detailed-design.md:230` の順序と一致。
- b. 確認済: `compute_audit_dsr_for_genome` の filter は `src/alpha_factory/audit.py:520` の `b.open_minutes > 0` のみ。holiday flag 単独除外なし。
- c. 確認済: `_make_sentinel_metric` は `src/alpha_factory/audit.py:467` で keyword-only、`src/alpha_factory/audit.py:481` で `"ok"` や scaffold status を拒否。
- d. 確認済: `AuditPBOMetric` は `src/alpha_factory/audit.py:321`、`AuditSPAMetric` は `src/alpha_factory/audit.py:348` で `status` + `audit_calc_version` の 2 field のみ。`src/alpha_factory/audit.py:901` でも module-level invariant 化済み。
- e. 確認済: `schema_version="1.0.0"` / `flag_namespace="t072"` は `src/alpha_factory/audit.py:730` と `src/alpha_factory/audit.py:805` で Phase 1 invariant として固定。
- f. 確認済: C7 n<30 warning は `src/alpha_factory/audit.py:879` で `stratum_insufficient_n` を emit。対応テストも `tests/alpha_factory/test_audit.py:1048` に存在。
- g. 確認済: `statistics.py` は `src/alpha_factory/statistics.py:162` で T073 文脈に同期更新され、`git diff` 上も docstring 差分のみ。式本体は `src/alpha_factory/statistics.py:234` 以降で不変。

**fix_required**
- 詳細設計と実装/test の stratification key 数が不一致。詳細設計は F39 で「3軸 marginal」とし、`by_schedule_status` を含めていますが、実装は `holiday_markets` / `dst_transition_markets` の 2軸のみで、テストも `schedule_status` を未対応 key として拒否しています。該当箇所: `devnotes/20260430-2301-todo-T073-audit-layer/detailed-design.md:722`, `devnotes/20260430-2301-todo-T073-audit-layer/detailed-design.md:845`, `src/alpha_factory/audit.py:756`, `tests/alpha_factory/test_audit.py:981`, `tests/alpha_factory/test_audit.py:1132`。
- 修正方針はどちらかに統一が必要です。設計通りに `schedule_status` を実装するか、Phase 1 は `ObservabilityFlags` 由来の2軸のみとするなら詳細設計・概念設計・F39記述を更新してください。

指定 a-g 自体は確認済みですが、設計承認済み contract と実体の gap が残るため最終 APPROVED にはできません。