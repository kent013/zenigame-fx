# Round 3: 詳細設計 schedule_status 注記更新後の最終確認

Round 2 で指摘された「詳細設計と実装/test の stratification key 数 (3 軸 vs 2 軸) gap」 への対応として、 詳細設計 md に以下の整合化を反映しました:

1. `devnotes/20260430-2301-todo-T073-audit-layer/detailed-design.md` § 4.5 の compute_marginal_dsr_strata docstring:
   - Phase 1 で受理する key を ObservabilityFlags 由来の 2 軸 (= holiday_markets / dst_transition_markets) に明示
   - schedule_status は SessionBlock.schedule_status 経由 (= flag ではなく block 由来 derived) のため Phase 1 では未対応
   - Phase 2 申し送り (= per-genome aggregate を caller 側 (T071) で計算 + flag_lookup signature 拡張 + AUDIT_REPORT_SCHEMA_VERSION MINOR bump) として明記

2. `compute_dsr_strata_with_allowlist` の allowlist 例も `("holiday_markets", "dst_transition_markets")` に修正 + 「Phase 1 ObservabilityFlags 由来 2 軸のみ受理」 明記

3. § 5.1 F39 の test 計画記述も「Phase 1 受理 2 軸」 + 「schedule_status は Phase 2 申し送り」 に整合化

`src/alpha_factory/audit.py` の docstring (= compute_marginal_dsr_strata) も既に Phase 1 で 2 軸 + schedule_status は Phase 2 申し送り と明示済み (再確認のため audit.py:756 周辺も読み込んでください)。

## Round 3 verdict

設計と実装の整合化が完了したので、 **APPROVED** verdict を再評価してください。

- **verdict**: APPROVED / NEEDS_FIX / INCONCLUSIVE
- **rationale**: 1-3 行で根拠
- **fix_required**: NEEDS_FIX 時のみ箇条書き

ファイル読み込み許可 (= 詳細設計 md 改訂部 + audit.py docstring) を使って整合確認してください。
