【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。**ファイル読み込みは許可**。

【監査・レビュー時の必須チェック — False-positive 再発防止】
C1: Design-first / C2: 「X が無い = バグ」禁止 / C3: Collider bias / C4: 前提明示 / C5: framing 独立性 / C6: Fact / Interpretation 分離 / C7: n>=30 規範 / C8: INCONCLUSIVE 第一級 / C9: Falsification-first

---

# T073 PR 1: audit layer 実装レビュー (impl-review-pr1)

## 役割

zenigame-fx Alpha Factory cascade port v2 Phase 2 配線、 16 番目 TODO (T073) の **実装レビュー**を行う。 概念設計と詳細設計は Round 3 + Round 1-2 で APPROVED 済 (= 数 13 Critical / 10 Warning / 10 Suggestion / 詳細 Round D1 C1-C2/W1-W6/S1-S3 全反映)。 本 review では「設計通りに実装されているか」 + 「設計が指摘していなかった bug / regression / spec drift がないか」 を Falsification-first で検証する。

## レビュー対象 (= 詳細設計 § 1)

新規ファイル:
- `src/alpha_factory/audit.py` (= AuditNullModel SSOT + AuditDSRMetric + AuditPBOMetric/AuditSPAMetric scaffold + AuditGenomeRecord + GenomeAuditInput + RunAuditReport + compute_* + check_audit_record_schema_version + compute_marginal_dsr_strata + compute_dsr_strata_with_allowlist)
- `tests/alpha_factory/test_audit.py` (= F1-F42 + dedup F31-F35 + collider bias 規範 + statistics 整合)

既存変更:
- `src/alpha_factory/statistics.py:148-238` docstring 同期更新 (= T073 audit layer 文脈追加、 数式不変)

## 設計参照

- 詳細設計: `devnotes/20260430-2301-todo-T073-audit-layer/detailed-design.md`
  - § 1 ファイル / 関数 / クラス
  - § 2 既存 caller signature
  - § 3 データモデル (擬似コード)
  - § 4 アルゴリズム (擬似コード)
  - § 5 テスト計画
  - § 6 既存挙動への影響
  - § 9 DoD (Definition of Done)
- 概念設計: `devnotes/20260430-2301-todo-T073-audit-layer/conceptual-design.md`
- 概念 review: `conceptual-review-round-1.md` 〜 `conceptual-review-round-3.md`
- 詳細 review: `detailed-review-round-1.md` 〜 `detailed-review-round-2.md`

## レビュー観点

### Round 1 (= Falsification-first、 C9)

「この実装には bug / regression / spec drift がある」 という反証仮説を立て、 各々を verify / refute する。 観点:

1. **C2 parallel-path (= grep DoD)**
   - audit.py を src/ 配下から import する経路が無い (= Phase 1 SSOT)
   - 既存 archive `dsr` field 経路 (archive.py:81 / 169 / 459) を touch していない
   - 検索語 (詳細設計 § 6.2 11 語: deflated_sharpe_ratio / dsr / sharpe_calc_version / archive_role / RunObservabilityReport / audit / sharpe_ratio / Sharpe / deflated / audit_calc_version / trial_counting_policy_version) で grep して期待挙動と整合
2. **AuditNullModel invariant 順序 (Round D1 [W1])**
   - I-1 → I-2 → I-3 → I-4 → I-5 → I-6 の順 (= test F5 で固定)
   - SSOT 値: null_model_kind / sr_scale / trial_source / trial_counting_policy_version
3. **sentinel policy (Round 3 [W1] [S2])**
   - DSR_VALUE_SENTINEL = Decimal("-1") / MOMENT_SENTINEL = Decimal("0")
   - status="ok": dsr_value ∈ [0, 1] / moment 系 finite
   - status != "ok": dsr_value == DSR_VALUE_SENTINEL / moment 系 == MOMENT_SENTINEL
   - n_observations は **常に実測値** (Round D1 [C2])
4. **_make_sentinel_metric (Round D1 [C1] [W2])**
   - keyword-only call
   - status は AuditDSRStatus かつ "ok" 以外の sentinel status のみ受理
5. **PBO/SPA scaffold (Round R2 [C4] [S3])**
   - 数値 field なし (= status + audit_calc_version のみ)
   - status="not_implemented" 固定
   - audit_calc_version="scaffold-v1"
6. **stratification API guard (Round 3 [W5] / Round D1 [S1])**
   - marginal default + interaction allowlist
   - schema_version="1.0.0" / flag_namespace="t072" Phase 1 固定
   - C7 規範 n>=30 不充足 → warning emit
   - 未対応 key / 空 tuple / 重複 key → ValueError
7. **collider bias 規範継承 (詳細設計 § 1.4 / 概念 § 11.4)**
   - compute_audit_dsr_for_genome は `open_minutes > 0` のみで filter (= holiday_markets 単独除外しない)
   - test F22d で確認
8. **canonical genome dedup (Round 3 [W2] / Round D1 [W4])**
   - retry: raw=2 / unique=1
   - cache hit: raw=1 / unique=1 (= raw にも含めない)
   - fold 別評価: raw=fold 数 / unique=1
   - seed 違い: raw=seed 数 / unique=1
   - 異 hash: unique=2
9. **schema_version helper (Round 3 [W4] / Round D1 [W3])**
   - MAJOR mismatch → ValueError
   - MINOR newer → warning (= record newer than consumer)
   - MINOR older → warning (= record older than consumer)
   - PATCH 差 → silent
   - 不正フォーマット ("1" / "1.a" / "1.0.0.1") → ValueError
10. **既存挙動への影響**
    - statistics.py docstring が T073 文脈で同期更新されており数式不変
    - 既存 caller signature 不変 (= deflated_sharpe_ratio の signature touch なし)

### Round 2 以降 (= 残課題集中)

Round 1 で claim された反証 / 不整合のうち、 actionable なもののみ修正対象。 false positive (= grep 不足 / 設計再読 漏れ) は C1-C9 規範に従い retract。

## verdict 形式

最終 round で以下を出力:
- **verdict**: APPROVED / NEEDS_FIX / INCONCLUSIVE
- **rationale**: 各観点 1-2 行で根拠
- **fix_required** (NEEDS_FIX 時): 修正項目を箇条書き
- **collider bias 規範継承**: 確認済 / 不整合
- **C2 parallel-path grep DoD**: 確認済 / 不整合

## 入力データ

実装ファイルは worktree 内で読み込み可能 (= 「ファイル読み込みは許可」):
- `src/alpha_factory/audit.py`
- `tests/alpha_factory/test_audit.py`
- `src/alpha_factory/statistics.py:148-238` (= docstring 更新)
- `src/backtest/session_block.py` (= SessionBlock SSOT)
- `src/backtest/calendar.py` (= ObservabilityFlags SSOT)

worktree path: `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T073-pr1`

実装結果サマリ (= caller report):
- pytest tests/alpha_factory/test_audit.py: 68 passed (regression なし)
- pytest tests/ 全体: 2634 passed, 1 skipped, 1 xfailed (regression 0)
- ruff check src/alpha_factory/audit.py / statistics.py / test_audit.py: clean
- mypy src/alpha_factory/audit.py: clean
- grep DoD: src/ から audit module の import 0 件、 tests のみ
- 既存 archive `dsr` field 経路: touch なし (= grep で確認)

Falsification-first で「この実装に未発見の bug がある」 仮説を反証ベースで検証してください。
