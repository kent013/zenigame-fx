# Round 19 — Gap analysis + zenigame コード参照 + final consensus push

## 状況更新

ユーザ確認:
1. **zenigame 側でも合意取れた** (双方向に思想 sync 済み)
2. **後方互換性・段階導入は不要、 ザクっと big-bang で導入**
3. **これまでの実装はベースラインにもならない、 今回の実装が新しいベースライン**
4. **NSGA-II と CPPS の切り替え機構は不要** (fallback flag 温存案は撤回、 純 CPPS のみ)
5. **不要なものは削除して良い** (旧実装、 dual-path、 fallback flag 等は全削除)
6. **zenigame 側のコードをしっかり参考にする** (思想 docs だけでなく実装ファイルを読む)

## zenigame 側参考実装ファイル (Codex は読み込んで参照すること)

リポジトリ root: `/Users/ishitoya/repository/zenigame/`

### Selection Cascade core
- `docs/alpha-factory/selection-cascade-design.md` (T508/T509/T511/T513 反映済の思想 SSOT)
- `docs/alpha-factory/stage-gates.md` (各 Stage 閾値仕様)
- `docs/alpha-factory/alpha-sieve.md` (Sieve 詳細)
- `docs/alpha-factory/nsga2-architecture.md` (NSGA-II 構造)
- `config/alpha_factory/default.yaml` (各種 hyper-parameter SSOT)

### NSGA-II + CPPS 実装 (T509)
- `src/trading/alpha_factory/ga/nsga2/core.py` (NSGA-II core, lines 200-600+)
- `src/trading/alpha_factory/ga/nsga2/stage_a_gate.py` (Adaptive Gate T151/T402/T511 = canonical 5 worst gate_score)
- `src/trading/alpha_factory/ga/nsga2/stage_c.py` (Pareto selection + L425-458: select_b_pass_top_k, L1125: c_lite_mode="ranking")
- `src/trading/alpha_factory/ga/nsga2/archives.py` (CA/DA Two-Archive 実装)
- `src/trading/alpha_factory/ga/nsga2/config.py` (CPPS 設定)

### Evaluation
- `src/trading/alpha_factory/evaluation/fitness.py` (calc_stage_a_gate_score, dominates, calc_fitness, compute_adaptive_mission_gap, compute_mission_gap_b)
- `src/trading/alpha_factory/evaluation/stage_gate.py` (evaluate_stage_a, evaluate_stage_c_lite, evaluate_stage_c_multi_window)
- `src/trading/alpha_factory/evaluation/live_criteria_gap.py` (T510/T512: compute_clite_individual_worst_gap, compute_normalized_gap, GAP_DENOM_FLOOR helpers)
- `src/trading/alpha_factory/evaluation/date_sampler.py` (Stage C bucket rotation, _generate_stage_c_buckets)

### Mission shortfall (T504-T508)
- `src/trading/alpha_factory/evaluation/mission_shortfall.py` (compute_regime_bucket_proxy_s, smooth-worst aggregation)

### Alpha Sieve / Archive / Warmstart
- `src/trading/alpha_factory/alpha_sieve/filter.py` (filter_pass_genomes, 4 層流入 + T499 transition_pass)
- `src/trading/alpha_factory/alpha_sieve/archive_updater.py` (admission, run_quota, per_seed_min, _evict_lowest_score)
- `src/trading/alpha_factory/alpha_sieve/sieve_score.py` (regime-spectral 集約)
- `src/trading/alpha_factory/alpha_sieve/sieve_archive.py` (is_injection_eligible, select_injection_candidates, select_injection_candidates_with_split = T509 CA/DA 分離注入)

### Genome Archive schema
- `src/trading/alpha_factory/ga/genome_archive.py` (GENOMES_SCHEMA, _create_row_template, collect_stage_*)

### 議論の最終確定 devnotes
- `devnotes/20260428-1100-selection-cascade-gate-audit/` (T508)
- `devnotes/20260428-1300-stage-a-c-consistency/` (T511)
- `devnotes/20260428-1500-nsga-pareto-axis-handover/` (T509)
- `devnotes/20260428-2050-cascade-audit-2nd-round-handover/` (T509 監査 13 件 fix)

これらを読んで、 fx 移植時の具体形 (helper 関数 signature, schema field, default 値の根拠) を提案すること。

---

## 確定済み構成 (Round 11-18 + Round 14 修正)

[A] 計算リソース: lane=1, pop=192/256, gen=64, max_workers 自動
[B] Dataset: 24m primary, epoch-rolling 4w, partition `[B 62w][A 8w][emb][C-lite 6w×3][emb][C 12w]`
[C] Metric: canonical 5 (SR_session_worst, net_pnl, max_dd, trade_count, session_block_win_rate_worst) gate / Pareto 3 軸 (max net, min dd, min mission_inf_gap) search
[D] GA: NSGA-II + 純 CPPS (multi-state FSM、 ★one-switch fallback 撤回、 純 multi-state のみ★), CA/DA 動的比率, archive epoch aware, exec_floor 不採用
[E] Loop: archive=120, inflow=8/per_run_max=12, warmstart=20% ramp, 3 層流入, log_pf_clip tie-break
[F] P2: session 3 bucket
[G] Cross-cutting: A-proxy/B-pooled 分離, emergency 25%, dataset_epoch_id 全経路必須
[H] 適用順序: Phase 1-9, TODO T901-T918

★ Round 14 修正: 「one-switch fallback flag 温存」 を撤回し、 純 multi-state FSM (zenigame T509 と同等) に統一 ★

---

## Claude が identify した 20 件の gap

以下を Codex に zenigame コード参照しながら診断してもらう。

### 設計仕様 gap

**G1. Stage A の gate 性質**
- (a) hard gate = A-pass のみ B 進出 / (b) soft proxy = top q_force% フィルタ / (c) 両立
- zenigame `stage_a_gate.py` の実装 (T151/T402/T511) を確認して fx 用に確定

**G2. canonical 5 worst の stage 別閾値**
- 全 stage 同値か stage 別か
- zenigame の `live_criteria_gap.py:compute_clite_individual_worst_gap` と Stage A `gate_score` を比較

**G3. A-fail 個体の扱い**
- GA selection に participate するが archive 進出不可
- zenigame でこの個体は次世代の crossover/mutation parent になるか除外されるか確認

**G4. q_force の意味**
- zenigame `stage_a_gate.py:stage_b_ratio=0.17` (T151) を参考に「動的閾値で適合率調整」 vs 「top-N% 強制通過」 を確定

**G5. Pareto 3 軸の評価値ソース**
- 同 generation 内で B 評価された個体は次世代 Pareto に B-based mission_inf_gap 使うか A-based 維持か
- zenigame の Stage A → Stage B 連動を `core.py` で確認

### 機構詳細 gap

**G6. GA operators**
- crossover_rate, mutation_rate, 演算子
- zenigame `nsga2/core.py` の crossover/mutation 実装を参照
- plateau-aware mutation_rate bump (旧 mutation_rate_max=0.6) は zenigame で維持されているか

**G7. Parent selection method**
- NSGA-II 標準 binary tournament + CA:DA 比率サンプリングの整合
- zenigame T509 (`archives.py`, `core.py`) で確認

**G8. 初期 population (Run 1) 生成方法**
- archive 空、 warmstart 不可
- zenigame の Clause grammar sampling 実装を確認

**G9. Determinism / reproducibility 契約**
- seed 固定で同一選抜結果
- NSGA-II tie-break: rank 同 → crowding 同 → 何で tie 解決 (genome.id ハッシュ?)
- zenigame `core.py` の tie-break 実装

**G10. 失敗 genome 扱い**
- backtest crash / NaN / Inf 個体の扱い
- zenigame の error handling 実装を確認

### Engine / infra 依存 gap

**G11. backtest engine 更新範囲**
- session block PnL series 出力
- session_bucket label per bar
- block-level trade_count
- fx の `src/backtest/engine.py` に追加実装が必要、 TODO 化

**G12. DSL / Clause / primitive 変更必要性**
- session-aware primitive、 cross-pair primitive 拡張
- 現状 P1-P12 の存続/廃止判断
- zenigame primitive 一覧と fx primitive 一覧の比較

**G13. timezone / DST / holiday 境界処理**
- session_bucket 計算: UTC hour vs DST aware vs FX market 公式境界
- zenigame は株式市場 (固定 9:00-15:00 JST) なので参考にならない、 fx 独自設計必要
- holiday: クリスマス、 新年、 国別の扱い
- 週末 gap: 24/7 fill 中の non-trading 時間

**G14. cost model 時変性**
- max_spread_bps 固定 vs epoch 別校正
- zenigame は小売株 spread モデル、 fx は broker spread でやや違う
- swap rate (intraday 前提なら 0)

**G15. aux データ依存**
- preflight HARD_REQUIRED と cascade で要求する aux の整合
- T057 (fx aux preflight) と zenigame aux 取り扱いの差異確認

### Audit / observability gap

**G16. PBO/DSR/SPA 計算 method 詳細**
- Bailey CSCV (PBO)、 Lo deflator (DSR)、 Hansen SPA の実装 spec
- zenigame で実装済か、 schema scaffold のみか確認

**G17. graduation lane 評価 method**
- multi-pair 評価ロジック sketch:
  - 6 anchor pair で同一 genome 再 backtest
  - 集約 (mean / worst / etc) で graduation_pass 判定
- zenigame の graduation lane 実装は無い (universe 横断が default なため)、 fx 独自設計必要

**G18. A→B 乖離の自動 fail 条件**
- `corr(A_proxy_score, B_pooled_score) < 0.3` 等の閾値仕様
- 自動 Run fail vs warning のみ

### 運用・実装 gap

**G19. Run / Epoch / Population の境界**
- 1 Run = 64 gen、 Run 終了で population 破棄、 archive のみ persist
- 次 Run の population 初期化 = warmstart 20% + fresh 80%
- epoch 内 max 6 Run、 epoch 切替で dataset_epoch_id 更新
- これを synthesis に明示

**G20. 既存 config / module の clean up 範囲 (★ ザク削除前提 ★)**

以下を **削除 or 全面置換**する前提で list 化:

**削除対象** (fresh design で対象消失):
- `factor_shadow` (T036) — P2 主軸外で metadata 用に縮退、 enabled=false なら現状コードも削除可
- `swim_lane.tier1.population_size=30` — lane=1 設計で意味消失
- `swim_lane.graduation.seed_strategy: "union_of_tier1_graduates"` — graduation lane Phase 4 で別設計
- `ga.feasibility.{entry_count_min, apply_from_generation, enable_fallback_when_all_infeasible}` — fresh design の bypass 品質床に置換
- `improve_cycle.{plateau_cycles, plateau_mutation_bump, mutation_rate_max}` — fresh GA operators で再設計または廃止
- `stage_gate.stage_a.{target_pass_rate, alpha, threshold, min_exposure_trade_count, calibrate.*}` — fresh の動的 q_force に置換
- `stage_gate.stage_b.{wf_*, median_oos_sharpe_min, positive_fold_min, dsr_min, fold_trade_count_min}` — fresh の WF gate 仕様に置換
- `stage_gate.stage_c.{spread_stress_*, feasibility_apply}` — fresh の Stage C 仕様に置換 (live_criteria + stress + cross-pair)
- 旧 `scripts/alpha_factory/run_alpha_sieve.py` (post-RUN MD-only) — fresh の archive 連動 sieve に置換
- 旧 `scripts/alpha_factory/calibrate_gate.py` (T027 旧版) — fresh の 3 Run freeze + epoch scope 仕様に置換

**全面置換対象**:
- `stage_gate.stage_a/b/c` 全ブロック → fresh `stage_gate` schema (canonical 5 worst, mission_inf_gap, partition, fold)
- `ga.{population_size, generations, fitness_metric, max_workers}` → fresh 値 (192/64/Pareto/auto)
- `cross_pair.*` → validation axis のみ、 selection 圧から除外
- archive schema (Parquet) → schema_version=2 で fresh 設計

**保持対象** (fresh と整合):
- `dataset.{instrument, start, end}` — 24m epoch-rolling で動的更新
- `backtest.{initial_cash, leverage, units, max_spread_bps, holding_cost_per_day_bps}` — 維持
- `live_criteria.*` — Stage C mission gate として維持
- aux 関連 config (T057) — preflight 整合性のため維持

---

## Round 19 で Codex に求めるもの

1. **G1〜G20 各 gap への診断 + zenigame コード参照**:
   - 「実 gap (修正必要)」 / 「曖昧だが synthesis 補足で OK」 / 「非 gap (既決)」
   - zenigame の対応する実装ファイル / 行番号を引用しつつ fx 用 spec を提案
2. **Codex 側で identify した追加 gap**: Claude が見落としているもの
3. **Big-bang 削除対象の最終確認**: 上記「削除対象」 リストに過不足がないか
4. **NSGA-II + 純 CPPS (fallback なし) への影響**:
   - Round 14 で one-switch fallback flag 温存と書いていた箇所を全削除
   - multi-state FSM の state 数 (3 vs 4 vs 5) を zenigame T509 実装と揃える
5. **逆輸入候補**: fx → zenigame に持ち帰る価値ある改善 (session_block_win_rate, HAC Sharpe, schema lint, A-proxy/B-pooled 分離)
6. **synthesis 章立て最終形**: gap 反映後の 12 章 (1-12) + § 13-18 補足を統合する提案
7. **TODO T901-T918 の更新**: gap 反映で TODO 追加 / 削除 / 統合 / 順序変更
8. **最終 consensus 確認**: 全構成について「合意」「異論あり」「INCONCLUSIVE」 一覧

big-bang 前提なので、 「並走」 「fallback」 「段階導入」 系の提案は不要。 「これがベースライン」 の前提で純粋に最適設計を出す。
