# Round 2: Round 1 Warning への対応報告

Round 1 verdict: **INCONCLUSIVE** (archive 実列伝搬・ログ観測の実証不足)

## 対応マトリクス

| Round 1 指摘 | 区分 | 対応 |
|------------|------|------|
| archive 伝搬: `stage_b_reason_codes` の単一/複数 reason 仕様確認 | [Warning] | **解消**: `src/alpha_factory/archive.py:541` で `";".join(rc) if rc else None` を確認。 複数 reason は ";" で結合保存される (単一 reason 優先保存ではない)。 archive 列実体テスト 2 件追加で契約担保。 |
| テスト網羅性: archive 列実体確認テスト不足 | [Warning] | **解消**: `tests/alpha_factory/test_archive.py` に T092 専用 2 件追加 (`test_t092_n_fold_below_safe_floor_reason_persists_to_archive` / `test_t092_n_fold_below_safe_floor_coexists_with_other_reasons_in_archive`)。 後者は 4 reason 同時記録時にも全て serialized string に残ることを assert。 |
| `all_folds_unavailable` と `n_fold_below_safe_floor` 同時発火テスト | [Suggestion] | **採用**: `TestT092NFoldSafeFloor::test_co_records_with_all_folds_unavailable` 追加。 |
| `wf_min_safe_folds` / `wf_min_folds_required` の責務差分コメント圧縮 | [Suggestion] | **採用**: 6 行 → 4 行に圧縮。 役割差分のみ簡潔に記載。 |
| logger capture (caplog) でログ観測 | [Suggestion] | **不採用**: `evaluate_stage_b` 内に新 logger 呼び出しは追加していない。 `stage_result.reason_codes` 経由で全 reason は呼び出し元 (run_ga.py / loop_closure.py) の既存ログ出力に乗る。 ログ観測を要するなら別 PR で `evaluate_stage_b` 全体に logger.debug を追加する設計判断が必要。 本 PR の範囲外。 |

## 追加・修正差分

### 1) `src/alpha_factory/stage_gate.py` — コメント圧縮

```diff
-    # T092: 統計安全水域 (wf_min_safe_folds) を下回る n_fold_effective は
-    # Walk-Forward 評価として信頼に足る fold 数を確保できておらず、
-    # Run 60 (n_fold_effective=4 で fp=0.54) のような artifact 個体が
-    # Stage A→B を通過して GA selection で best として浮上する経路を塞ぐ。
-    # `wf_min_folds_required` (worker 短絡判定 graceful degrade) と独立した
-    # statistical safety floor として L417 で意図された値を個体評価層に貫徹する。
+    # T092: `wf_min_safe_folds` は statistical safety floor (個体評価層)、
+    # `wf_min_folds_required` は worker 短絡判定 graceful degrade で役割が異なる。
+    # 後者が満たされても前者を下回る effective fold 数は WF 評価として信頼不足のため、
+    # ここで Stage B fail にして archive selection で best 浮上経路を塞ぐ (Run 60 artifact 対策)。
```

### 2) `tests/alpha_factory/test_stage_gate.py` — co-occurrence 1 件追加

```python
def test_co_records_with_all_folds_unavailable(self) -> None:
    """全 fold unavailable のとき all_folds_unavailable と n_fold_below_safe_floor が併記される."""
    bars = _make_continuous_bars(20, bars_per_day=4)  # → 多数 fold だが全 unavailable
    ev = ConstantPrimitiveEvaluator(value=0.0)
    stage_cfg = self._multi_fold_stage_cfg(wf_min_safe_folds=5)
    res = evaluate_stage_b(_one_clause_genome(), bars, usd_jpy_meta(),
                          _backtest_config(), ev, stage_cfg)
    payload = cast(dict[str, Any], _envelope(res)["payload"])
    assert payload["n_fold"] >= 2
    assert payload["n_fold_unavailable"] == payload["n_fold"]
    assert payload["n_fold_effective"] == 0
    assert not res.passed
    assert "all_folds_unavailable" in res.reason_codes
    assert "n_fold_below_safe_floor" in res.reason_codes
```

### 3) `tests/alpha_factory/test_archive.py` — archive 実列伝搬テスト 2 件追加

```python
def test_t092_n_fold_below_safe_floor_reason_persists_to_archive() -> None:
    """T092: n_fold_below_safe_floor reason が archive parquet 列まで伝搬する.

    archive.py:541 の ";".join(rc) で全 reason を結合保存する契約担保。"""
    arc = GenomeArchive(run_id="test_run_t092", run_number=1)
    g = _stub_genome("g0_i0")
    sr = StageResult(stage="B", passed=False, metrics={...}, 
                     reason_codes=("n_fold_below_safe_floor",))
    arc.collect_stage_b(g, lane_id="lane1", generation=0, stage_result=sr,
                         instrument="EUR_JPY")
    assert rows[0]["stage_b_pass"] is False
    assert rows[0]["stage_b_reason_codes"] == "n_fold_below_safe_floor"
    assert rows[0]["n_fold_effective"] == 4

def test_t092_n_fold_below_safe_floor_coexists_with_other_reasons_in_archive() -> None:
    """guard reason が他 reason と併記されても ";" 区切りで全部 archive 列に残る."""
    sr = StageResult(..., reason_codes=(
        "median_oos_sharpe<min", "positive_fold_ratio<min",
        "n_fold_below_safe_floor", "all_folds_unavailable",
    ))
    arc.collect_stage_b(g, lane_id="lane1", generation=0, stage_result=sr,
                         instrument="EUR_JPY")
    parts = set(rows[0]["stage_b_reason_codes"].split(";"))
    assert "n_fold_below_safe_floor" in parts
    assert "median_oos_sharpe<min" in parts
    assert "positive_fold_ratio<min" in parts
    assert "all_folds_unavailable" in parts
```

## テスト結果

```
tests/alpha_factory/test_stage_gate.py::TestT092NFoldSafeFloor  7 passed (6 → 7、 all_folds_unavailable 併発 1 件追加)
tests/alpha_factory/test_archive.py::test_t092_n_fold_below_safe_floor_reason_persists_to_archive  PASSED (新規)
tests/alpha_factory/test_archive.py::test_t092_n_fold_below_safe_floor_coexists_with_other_reasons_in_archive  PASSED (新規)

stage_gate + archive 既存テスト 169 件: 全 PASS

ruff check: All checks passed!
mypy src/alpha_factory/stage_gate.py: Success: no issues found in 1 source file
```

## 再判定要求

- 上記 5 件の対応で Round 1 Warning は全て解消 (logger capture は範囲外と判断)
- 全体判定を再評価してください: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
