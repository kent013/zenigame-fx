【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

---

# 概念設計レビュー依頼

## 文脈

zenigame 本家の `zenigame-run-alpha-factory` skill を zenigame-fx 用に移植する作業（cycle 26 / Phase 3）。skill は md only。
レビュー対象: `devnotes/20260424-1236-port-run-alpha-factory/conceptual-design.md`

参考にすべき周辺コード:
- `scripts/alpha_factory/run_ga.py` (T018 完了。CLI 引数: --config / --run-id / --instrument / --start / --end / --population-size / --generations / --mutation-rate / --crossover-rate / --tournament-size / --elite-count / --max-depth / --fitness-metric / --seed)
- run_ga.py 完了サイン: `logger.info("ga.run.done", run_id=..., run_number=..., best_name=..., best_fitness=..., stage_c_pass=...)` および stdout `[done] run_id=... run_number=... best=... fitness_pen=... stage_c=... report=...`
- run_ga.py 出力: `reports/run-reports/run-{N}/summary.json`, archive Parquet `.cache/alpha_factory/runs/genomes_{run_id}.parquet`, `.cache/alpha_factory/runs/{run_id}.json`
- 既存 skill: `.claude/skills/zenigame-fx-improve-cycle/SKILL.md`（Phase 3 で run_ga.py 直叩き）
- 既存 skill: `.claude/skills/zenigame-fx-run-report/SKILL.md`（同様の wrapper パターン）

## レビュー観点（label: conceptual-review）

1. 移植スコープの妥当性 — fx 側の現状（Phase 2 完了、Phase 3 開始）に対して過不足ないか
2. zenigame との差分管理 — 削除した要素（LLM変異、Director、cost-stress、smoke、profile、warmstart、validate-only、analyze-genome-archive 連携、ショート禁止）が妥当か、漏れがないか
3. 完了検出のロバスト性 — 「ga.run.done ログ + archive Parquet 存在」の二段確認が信頼できるか。代替案や弱点
4. 状態ファイル設計 — improve-cycle の `current_cycle_state.json` と分離した `run_alpha_factory_state.json` というアプローチの是非
5. --run-id を skill 側で生成する方針 — タイミング問題回避は妥当か。run_ga.py が `--run-id` を尊重するか確認すべき点
6. 残課題 (improve-cycle 切り替え、analyze-genome-archive 移植、multi-pair) の優先度判断
7. 検証手段（grep + ファイル存在）が十分か

## 期待する出力

- APPROVED / NEEDS_REVISION のいずれか
- NEEDS_REVISION の場合、具体的な指摘と修正案

簡潔に。冗長な前置き不要。
