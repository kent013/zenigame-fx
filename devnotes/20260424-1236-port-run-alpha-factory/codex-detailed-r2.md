**NEEDS_REVISION**

1. Step 5 の `uv run python <<'PY'` はシェル変数展開を無効化するため、`open("${summary_path}")` がそのまま文字列になり失敗します。`<<PY` にするか、環境変数で渡す形に修正が必要です。  
2. Step 7 の猶予待ちは `kill -0 ${pid}` 監視のみなので、親終了後に子プロセスが残っても見逃します。プロセスグループ監視（`kill -0 -${pgid}`）基準で待機・最終 `SIGKILL` まで統一してください。