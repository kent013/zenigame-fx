【思考原則】まず仮説を立てろ。データに真摯に向き合え。仮説なき改善は試行錯誤。
【使命】zenigame-fx Alpha Factory: live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出す。
【絶対制約】イントラデイ前提 / ロング・ショート両方向 / スワップ・スプレッドを fitness に反映。
【禁止事項】期間延長で過学習隠蔽、見た目の数値操作、GA ハック、閾値緩和、複雑案、取引回数削減、オーバーナイト保有設計、archive スキーマ伝搬漏れ。
【ツール】コマンド実行・書き込み禁止。読み取りのみ。

---

# 概念設計レビュー Round 2 (label: conceptual-review)

Round 1 でいただいた指摘 6 点を反映済みです。判定をお願いします。

レビュー対象: `devnotes/20260424-1236-port-run-alpha-factory/conceptual-design.md`（改訂版）

## R1 指摘への対応サマリ

1. `--run-id` 前提 → 「前提（verified）」セクションを新設し、run_ga.py L720 / archive.py L492 / summary["run_id"] / .cache/.../{run_id}.json の 4 点に同一 run_id が伝搬されることを明記
2. 完了判定 → 「Step 4: 完了検出 — run_id 整合まで含めた 3 点確認」に書き直し、log + Parquet ファイル名 + summary.json JSON 内 run_id の 3 点で整合を取る
3. 状態ファイル責務境界 → 「状態ファイルの責務境界」表を追加。current_cycle_state.json (orchestration SoT) と run_alpha_factory_state.json (single run execution SoT) を分離、復旧時の併読ルールを明記
4. 削除差分の分類 → 「削除する zenigame 由来要素の分類」表を追加。各要素を「不要」「deferred」に分類し理由を記載
5. 検証手段の限界 → 「検証手段の限界」セクションを新設。「skill 構造妥当性 + 完了検出ロバスト性」と「run 内容妥当性」を分離
6. 残課題優先度 → P1: improve-cycle Phase 3 切替 / P2: analyze-genome-archive 移植 / P3: multi-pair の順で確定

## レビュー観点

- R1 指摘への対応が十分か
- 新規に発生した懸念がないか
- 概念レベルでの不整合 / 抜け漏れがないか

## 期待する出力

APPROVED または NEEDS_REVISION（具体的指摘付き）。簡潔に。
