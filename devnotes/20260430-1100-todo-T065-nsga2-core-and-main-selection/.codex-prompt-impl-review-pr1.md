【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。**ファイル読み込みは許可**（実装ファイルや設計ファイルを Read で参照することを推奨）。

【監査・レビュー時の必須チェック】
C1 Design-first / C2 「X が無い = バグ」禁止 / C3 Collider bias / C4 前提明示 / C5 並列独立性 / C6 Fact / Interpretation 分離 / C7 Sample size / C8 INCONCLUSIVE / C9 Falsification-first

---

# 役割

あなたは zenigame-fx Alpha Factory における T065 PR 1 (= NSGA-II core + 主選抜 (B-pooled) の単体実装) の実装レビュアーである。

# レビュー対象

## 詳細設計 (SSOT)

`devnotes/20260430-1100-todo-T065-nsga2-core-and-main-selection/detailed-design.md`

(全 1048 行、 全 Round APPROVED 済。 これが SSOT、 deviation は要正当化)

ただし詳細設計に 1 箇所の **データ構造誤記** があった: 詳細設計は `bc_result.pooled_dd_per_fold_max` と書いているが、 実装の `BCEvaluationResult` (T064 main 実装) には `pooled_dd_per_fold_max` field は存在せず、 正しい source は `bc_result.b_result.pooled_dd_per_fold_max` (StageBResult 内、 `float | None`). 実装ではこちらを採用し、 docstring も整合させた. レビューでこの判断 (T064 actual main 実装に合わせる) が妥当か確認してほしい.

## 概念設計

`devnotes/20260430-1100-todo-T065-nsga2-core-and-main-selection/conceptual-design.md`

## 実装ファイル (T065 PR 1 単独完結スコープ)

1. `src/alpha_factory/nsga2_selection.py` (新規、 約 700 行)
2. `tests/alpha_factory/test_nsga2_selection.py` (新規、 約 900 行、 全 85 件 pass)

## 依存 main 実装 (touch 禁止、 contract 確認のみ)

- `src/alpha_factory/canonical_metrics.py` (T061: InvariantFlags / CanonicalFiveResult)
- `src/alpha_factory/mission_inf_gap.py` (T062: MissionGapResult)
- `src/alpha_factory/stage_a_evaluator.py` (T063)
- `src/alpha_factory/stage_bc_evaluator.py` (T064: BCEvaluationResult / StageBResult)

# T065 PR 1 スコープ

実装内容:

1. **施策 1**: `nsga2_selection.py` 新規 (詳細設計 § 施策 1、 行 48-762)
   - 3 dataclass (frozen): ParetoAxis / IndividualEvaluation / GenerationSelectionResult
   - 1 const: INVARIANT_VIOLATION_PENALTY = 1.0e6
   - 7 public helper:
     - extract_pareto_axis (= b_pooled CanonicalFiveResult + b_result.pooled_dd_per_fold_max + mission_inf_gap)
     - compute_effective_constraint_violation (= invariant 違反で INVARIANT_VIOLATION_PENALTY 加算)
     - constrained_dominates (Deb 2000)
     - non_dominated_sort (Deb et al. 2002 fast non-dominated sort、 i<j 比較で半減)
     - crowding_distance (3 軸正規化、 端 +inf、 front_size <= 2 全員 +inf)
     - select_survivors (rng 不要、 deterministic)
     - binary_tournament (crowded-comparison only、 sort_keys 4-tuple lex 比較)
     - select_parent_pair (自己交配回避リトライ + (p1, p1) fallback)
   - 3 private helper (DRY): _validate_t064_contract / _compute_eligible_axes_violations_feasibility / _compute_front_assignments_and_crowding / _build_sort_keys
   - 1 top-level entry: run_generation_selection
   - 1 seed helper: make_selection_seed (blake2b + JSON 構造化)

2. **施策 2**: `test_nsga2_selection.py` 全 85 件 (詳細設計 § 施策 2、 13 sub-suite)

# T065 PR 1 スコープ外 (touch 禁止)

- T058-T064 で main にある実装ファイル
- 既存 stage_gate.py / cross_pair.py / swim_lane.py / archive.py / run_ga.py (Phase 2 で T066/T067/T070/T071 と同時の別 PR、 詳細設計 行 951)

# DoD 結果

- **pytest**: tests/alpha_factory/test_nsga2_selection.py 85/85 pass。 tests/alpha_factory/ 全体 1444 passed + 1 xfailed (T062 inherited)。 tests/scripts/ 189 passed.
- **ruff check**: clean
- **mypy src/**: clean (109 source files)
- **C2 5 段階 grep DoD**:
  - 段階 1 (直 import、 tests 除外): 0 hit
  - 段階 1b (tests 内): test_nsga2_selection.py のみ hit (期待通り)
  - 段階 2 (alias import): 0 hit
  - 段階 3 (relative import): 0 hit
  - 段階 4 (再エクスポート、 src/alpha_factory/__init__.py): 0 hit
  - 段階 5 (runtime 配線、 stage_gate / cross_pair / swim_lane / archive / run_ga): 0 hit (Phase 2 まで配線禁止)

# レビュー対象範囲

PR 1 単独完結スコープでの実装妥当性をレビューしてほしい:

1. **詳細設計準拠**: シグネチャ・dataclass field 構成・関数挙動が詳細設計 / 概念設計 § 11.2 SSOT と完全一致しているか
2. **データ構造誤記の対応妥当性**: `bc_result.pooled_dd_per_fold_max` → `bc_result.b_result.pooled_dd_per_fold_max` への補正は妥当か (T064 actual main 実装基準)
3. **constrained-domination (Deb 2000)**: 数式準拠が正しいか
4. **non_dominated_sort (Deb et al. 2002)**: i<j 比較最適化、 dominance 対称性、 front 構築の正確性
5. **crowding distance**: 3 軸正規化、 端 +inf、 front_size <= 2 全員 +inf、 軸範囲 0 寄与 0 (NaN 防御)
6. **deterministic tie-break**: sort_keys 4-tuple (rank, -crowding, genome_hash, index)、 sorted 安定順
7. **make_selection_seed**: blake2b + JSON 構造化、 process salt 排除、 delimiter 衝突回避
8. **fail-fast 契約**: T064 contract (a_pass=True ⇔ bc_result is not None)、 finite domain (Round 1 [C2]/[C3])、 index 一致 (詳細 Round 2 [W3])
9. **immutability**: frozen dataclass、 MappingProxyType、 frozenset、 tuple
10. **テスト網羅性**: 13 sub-suite の各項目、 PR DoD 必須 2 件、 negative test、 edge case
11. **Phase 2 申し送り 9 箇所**: 詳細設計 行 951-970 の各項目が触らずに残っているか確認 (= touch 禁止規律)
12. **規範継承**: T058-T064 で確立した規範 (default-deny / SSOT / 半開区間 / ruff B905 zip(strict=True) / no-raise / MappingProxyType / NaN fail-fast / keyword 引数注入) が継承されているか
13. **残論点 (R1-R6)**: smoke 後再校正候補としての扱いが妥当か

# 出力フォーマット

冒頭に APPROVED / CHANGES_REQUESTED の 1 語判定を出力。 続けて以下の構成:

```
## verdict: APPROVED | CHANGES_REQUESTED

## 前提検証 (C4)
- bullet で本レビューの前提が verified であることを明示

## Critical (必須修正)
- なし or 各項目を bullet で

## Warning (推奨修正)
- なし or 各項目を bullet で

## Suggestion (任意)
- なし or 各項目を bullet で

## Phase 2 申し送り確認
- 9 箇所が touch 禁止規律で残っていることを確認

## 結論
```

CHANGES_REQUESTED の場合は具体的な修正案 (file:line + 修正内容) を Critical/Warning/Suggestion ごとに記述すること。
