【思考原則】まず仮説を立てろ / データに真摯に向き合え / 先人の知恵を探せ / 機能の名前に立ち返れ / 仕組みが機能していない段階で値を弄るな / 因果ループを切断するな。

【使命】zenigame-fx Alpha Factory の使命は live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと。 絶対制約: イントラデイ / ロング・ショート両方向 / スワップ・スプレッド反映。 禁止事項 1-8。

【ツール使用制限】コマンド実行・ファイル書き込み禁止、 ファイル読み込みは許可 (read 不可なら本文は inline で貼ってある)。

【監査・レビュー】C1-C9。 特に C7 sample size、 C8 INCONCLUSIVE、 C9 Falsification-first。

---

# T065 概念設計 — レビュー依頼 (Round 1)

## 役割

zenigame-fx Alpha Factory selection cascade port (synthesis 18 章 + Codex Round 11-21 確定) における **T065 (NSGA-II core + 主選抜 B-pooled) 概念設計** のレビュアです。 M3 (GA 中核) の最初の TODO で、 T058-T064 evaluator 群を消費し、 NSGA-II + constrained-domination (Deb 2000) の世代内主選抜を pure function 群で実装する。

## 文脈

- M1 (T058-T060) + M2 (T061-T064) 全 7 TODO 設計 APPROVED + TODO 登録済
- synthesis Round 21 改訂 (2026-04-30) で `mission_signed_margin` SSOT 昇格、 archive CA #5 が確定
- 上位文書: synthesis.md § 3 / § 6.4-6.5 / § 7.1-7.7 / § 8.3 / § 17
- 依存先 (全 APPROVED): T060 Partition+Fold / T061 canonical_metrics / T062 mission_inf_gap / T063 stage_a_evaluator / T064 stage_bc_evaluator

## レビュー観点 (Falsification-first、 Round 1)

1. **constrained-domination (Deb 2000) 数式**
   - feasibility-first → infeasible-violation-ordering → feasible-Pareto の 3 段階
   - effective_constraint_violation の合成 (T062 constraint_violation + invariant violation × penalty 1.0e6) は Deb 2000 純準拠か
   - 別案 (lex 最高 priority key として invariant 独立扱い) を不採用とする根拠は妥当か
   - INVARIANT_VIOLATION_PENALTY=1.0e6 の根拠 / smoke 後再校正方針

2. **Pareto 軸 source の二重契約**
   - f1 = `bc_result.b_pooled_cf.net_pnl_after_cost`
   - f2 = `bc_result.pooled_dd_per_fold_max` (concat DD でない、 synthesis § 5.2 準拠)
   - f3 = `mission_gap.mission_inf_gap` (T062)
   - Stage B 主選抜原則 (synthesis § 3) との整合
   - DD 軸で `b_pooled_cf.max_dd` (concat 擬似 DD 含む) を使わない契約を T065 PR DoD で test 必須

3. **A-fail / B-invariant-fail 個体除外**
   - eligible 集合: stage_a_pass=True AND bc_result is not None AND pareto_axis_usable=True
   - excluded set 計算と sample_size_warnings の伝達契約
   - PR DoD test 2 件 (a_fail / b_invariant_fail 除外) の妥当性

4. **eligible が空 / pop_size 未満 / 1 体のみの degenerate cases**
   - silent 退避ではなく warning で caller (T067) に伝えるか
   - mutation only fallback ((p1, p1) 返し) の妥当性 (caller 責務分担)

5. **non-dominated sorting + crowding distance**
   - 標準 NSGA-II (Deb et al. 2002) 純準拠か
   - 3 軸正規化、 端 +inf 規約
   - 軸範囲 0 (max == min) 時の crowding 寄与 0 ハンドリング
   - ε 厚 tie-break (zenigame T333 拡張) を採用しない判断は妥当か

6. **deterministic tie-break**
   - sort key (front_no, -crowding, genome_hash) の一意性
   - genome_hash の SHA-256 hex 仮定
   - rng は `random.Random` で seed = `hash(run_id, gen_no, "selection")` 等

7. **binary tournament + 自己交配回避**
   - 2 体抽出 + constrained-domination で勝者抽出
   - max_retry=3 で同一 hash 個体回避、 超過で (p1, p1) → mutation only fallback
   - synthesis § 7.5 binary tournament + CA:DA 比率は T066 確定との整合

8. **T062 申し送り [W2] (invariant violation 組込み) の解決**
   - effective_constraint_violation = constraint_violation + INVARIANT_VIOLATION_PENALTY × invariant_count
   - constraint_violation は T062 で 4 slack 由来 [0, +inf)、 +inf 取り得る
   - invariant_count は T061 InvariantFlags の strategic_* counts 合計
   - +inf + 1e6 × N の数値挙動 (overflow / NaN 防御)

9. **Phase 1 / Phase 2 分離**
   - Phase 1 = 単体 module + テストのみ、 既存 ga loop 未配線
   - Phase 2 (T066 / T067 / T070 / T071 と同時、 別 PR) で 8 箇所申し送り
   - Phase 1 単独 merge で runtime 影響なしの厳密性

10. **synthesis Round 21 改訂後の整合性**
    - mission_signed_margin SSOT 昇格 (archive CA #5 = T066-T067 責務)
    - mission_inf_gap (Pareto f3) と mission_signed_margin (CA #5) の用途分離
    - T065 では mission_inf_gap のみ消費、 mission_signed_margin は参照しない宣言

11. **C2 parallel-path 5 段階 grep DoD**
    - 直 import / `import as` / relative / 再エクスポート / runtime シンボル
    - T058-T064 で確立した 5 段階手順を踏襲

12. **C7 sample size guard**
    - eligible < 10 / 30 等 thresholds
    - silent fallback ではなく warning 経路で caller に伝える

13. **degenerate case の正常終了規約**
    - eligible=0 / 1 / pop_size 未満で warning + 部分結果返し
    - ValueError raise する場面 (運用 bug 確定時) と warning する場面 (運用上想定可) の境界

14. **zenigame との差分明示**
    - constrained-domination は zenigame 未実装 → fx 新設 (Deb 2000 純準拠)
    - MG tie-break (zenigame T333) は採用しない (B-pooled 主選抜統一)
    - crowding distance / binary tournament 構造は zenigame 流用

15. その他、 設計の弱点・盲点 (M3 最初の TODO のため特に上流契約整合)

レビュー後 `[VERDICT] / [Critical] / [Warning] / [Suggestion]` で報告してください。
APPROVED / CHANGES_REQUESTED / BLOCKED の verdict を必ず明記。

---

## 概念設計本文 (file 読み込み代替、 inline)

# 概念設計: T065 — NSGA-II core + 主選抜 (B-pooled)

**作成日時**: 2026-04-30 11:00 JST
**前提**: synthesis Round 21 改訂後 (`devnotes/20260428-2300-cascade-port-debate/synthesis.md`、 § 7.1-7.7 + § 8.3 + § 17、 2026-04-30 確定)、 T058-T064 設計 APPROVED
**マイルストーン**: M3 (GA 中核) の最初の TODO、 T066 (CPPS 2-state FSM + CA/DA archive) の前段
**位置付け**: T058-T064 evaluator 群を消費し、 NSGA-II + constrained-domination の世代内主選抜を pure function 群で提供する

---

## 1. 背景・課題

synthesis § 7 で確定した **NSGA-II + 純 CPPS** の core 部分を zenigame-fx に big-bang 移植する。 既存実装 (絶対閾値直列フィルタ + 単純 fitness ranking) はベースラインにせず全廃。

T065 が提供する責務 (Phase 1 = 単体 module + テストのみ、 既存 ga loop に未配線):

1. **constrained-domination (Deb 2000)**: feasible vs infeasible は feasible 優先、 infeasible 同士は constraint_violation 小さい方優先、 feasible 同士は Pareto 3 軸 dominance
2. **non-dominated sorting (NSGA-II rank)**: 候補集合を Pareto front に分解
3. **crowding distance**: 3 軸正規化、 標準 NSGA-II (Deb et al. 2002)
4. **deterministic tie-break**: rank 同値 → crowding 同値 → `genome_hash` 昇順
5. **survivor selection**: rank + crowding で 親+子 (2*pop) から **pop 体抽出**
6. **binary tournament**: parent pair 抽出 (CA:DA 配分は T066 で確定、 T065 は配分関係なく候補から 2 体抽出)
7. **B-pooled 主選抜の axis 確定**: T064 BCEvaluationResult を Pareto 軸 source として使う唯一の経路 (synthesis § 3「主選抜評価値 = Stage B pooled」)
8. **A-fail / B-invariant-fail 個体除外**: T063 default-deny + T064 pareto_axis_usable を消費し、 Pareto 候補集合から両者を除外

T065 が**触らない** (別 TODO 担当):

- CPPS Push/Pull FSM (push/pull state 切替) — **T066**
- CA / DA 別 archive admission / eviction — **T066**
- archive 流入 (mission_pass / progress_pass / score_bypass) — **T067**
- crossover / mutation 実装 (genome operator) — **既存 + Phase 2 別 TODO**
- run_ga.py 配線 — **Phase 2 別 PR (T065-T067 統合と同時)**
- warmstart 注入 / Run 初期化 — **T067 (Loop closure)**
- observability (A→B 乖離 / entropy / front1 cardinality) — **T071**

### 1.1 synthesis § 7.1 主要パラメータ確認

| 項目 | 値 (synthesis Round 20 確定) |
|---|---|
| algorithm | NSGA-II + 純 CPPS (fallback flag なし) |
| population_size | 192 baseline / 256 promotion |
| generations | 64 baseline / 48 fallback |
| objectives | (max net_pnl, min max_dd, min mission_inf_gap) |
| selection | NSGA-II rank + 標準 crowding (3 軸正規化) |
| tie-break | rank → crowding → **genome_hash 決定論的** |
| parent selection | binary tournament + CA:DA 比率サンプリング (state 依存、 T066 確定) |
| crossover_rate | 0.7 |
| mutation_rate | 0.3 |

### 1.2 主選抜が B-pooled である宣言 (synthesis § 3 + Round 19)

```
A-pass のみ Stage B 進出、 主選抜評価値 = Stage B pooled (Round 19 確定)
A-fail は当世代 Pareto 圧計算からも除外、 親選択母集団からも除外、 archive にも入らない
```

T065 では Pareto 軸 (f1, f2, f3) を **T064 BCEvaluationResult から抽出** することで synthesis § 3 を runtime で具現化する。 A-fail 個体は T063 出力の `a_pass_indices` に基づき除外、 B-fold-invariant-fail (`bc_result.b_pooled_cf is None`、 もしくは `pareto_axis_usable=False`) も除外。

### 1.3 T062 申し送り (T065 詳細設計時に確定する事項)

T062 詳細設計 line 181-187 から:

> 現仕様では `constraint_violation` は 4 slack 由来のみ。 `is_feasible=False` の原因が slack 外 (例: synthesis § 6.6 の `session_close_drop_count > 0` / `negative_equity_drop_open_count > 0` のみで slack は問題なし) の場合、 constraint_violation は 4 slack 計算値 (potentially 0 or 小さい値) になる。 T065 (constrained-domination) では「invariant violation の有無を Deb 2000 比較にどう組込むか」 を明文化する必要 (例: `constraint_violation + invariant_violation_penalty × Σ(strategic_* counts)` 等)

T065 の責務として:

- T062 `MissionGapResult.constraint_violation` (slack 由来) と T061 `InvariantFlags` (`session_close_drop_count`, `negative_equity_drop_open_count`) を **合成した violation スカラー** を constrained-domination 用に新規定義
- 候補1: `effective_constraint_violation = constraint_violation + invariant_penalty × Σ(strategic_* counts)`
- 候補2: invariant violation を独立の最高 priority key として扱う (= invariant_violation > 0 個体は invariant_violation 同値以下の任意 feasible/infeasible より dominate されない side)
- どちらが Deb 2000 整合かを T065 詳細設計で確定 (Round 1 議論候補)

### 1.4 T064 申し送り (T065 で消費する確定 contract)

T064 から T065 への申し送り (handoff M2 § 3.2 + T064 詳細 Phase 2 申し送り):

- f1 (max net_pnl_after_cost) source: `bc_result.b_pooled_cf.net_pnl_after_cost` (T061 経由)
- f2 (min max_dd) source: **`bc_result.pooled_dd_per_fold_max`** (concat DD でない、 synthesis § 5.2 準拠、 擬似 DD 排除)
- f3 (min mission_inf_gap) source: `mission_gap.mission_inf_gap` (T062 経由)
- `bc_result.b_pooled_cf is None` (= invariant_fail at any fold) 個体は Pareto 軸 source として使用不可
- `bc_result.pareto_axis_usable: bool` を T065 で消費 (T064 BCEvaluationResult.pareto_axis_usable=True で進入)

### 1.5 zenigame 参考実装の流用方針

| 機構 | zenigame 出典 | fx 流用方針 |
|---|---|---|
| crowding_distance | `sorting.py:60-125` | 3 軸正規化を踏襲、 ε 端 +inf 規約も踏襲 |
| non-dominated sorting | `optimize.py` 内部 | 標準 fast non-dominated sort (Deb et al. 2002) を pure function で再実装 |
| binary tournament | `breeding.py:211` | 2 体抽出 + constrained-domination で比較する fx 専用関数 |
| MG tie-break (selection.py 93-107) | zenigame 独自拡張 (T333) | **採用しない** (Round 19 で B-pooled 主選抜統一、 mg tie-break は廃止) |
| RNG seed (core.py:114) | 固定 seed 使用 | 同等の deterministic RNG (genome_hash + run_id seed) を採用 |
| constrained-domination | zenigame では未実装 | **fx で正式新設** (Deb 2000) |

---

## 2. 前提検証 (C4) — current HEAD `main@ea56484` 基準

| 前提 | verified | 出典 |
|---|---|---|
| synthesis § 7.1 NSGA-II + 純 CPPS、 pop=192/256、 gen=64 | ✓ | synthesis § 7.1 |
| Pareto 3 軸 = (max net_pnl, min max_dd, min mission_inf_gap) | ✓ | synthesis § 7.1 / § 6.5 |
| 主選抜 = Stage B pooled、 A-fail 当世代 Pareto 圧から除外 | ✓ | synthesis § 3、 Round 19 |
| tie-break: rank → crowding → genome_hash 決定論的 | ✓ | synthesis § 7.1 |
| crossover_rate=0.7、 mutation_rate=0.3 | ✓ | synthesis § 7.5 |
| T063 出力に `a_pass_indices`、 default-deny 契約 | ✓ | T063 詳細設計 APPROVED |
| T064 BCEvaluationResult に `pareto_axis_usable` / `b_pooled_cf` / `pooled_dd_per_fold_max` | ✓ | T064 詳細設計 APPROVED |
| T062 MissionGapResult に `mission_inf_gap` / `constraint_violation` / `is_feasible` | ✓ | T062 詳細設計 APPROVED |
| synthesis § 5.2 「Stage B pass 判定の DD 軸は per-fold max 採用、 concat DD は記録用のみ」 | ✓ | synthesis § 5.2 / T064 [C2] |
| Deb 2000 constrained-domination は zenigame 未実装 → fx で新設 | ✓ | grep `constrained_dominate` zenigame `src/` で hit なし |
| zenigame `breeding.py:211` の binary tournament 構造を fx で再実装 | ✓ | zenigame `breeding.py:211` |
| 既存 zenigame-fx には NSGA-II core 実装は未存在 (`/src/alpha_factory/ga/` ディレクトリ自体未作成) | ✓ | grep `non_dominated_sort\|constrained_dominates` `find /Users/ishitoya/repository/zenigame-fx/src -name "*.py"` で hit なし |
| invariant flag (T061) と constraint_violation (T062) の組込み定義は T065 で確定 | ✓ | T062 詳細 line 181-187 申し送り |

---

## 3. 責務範囲とスコープ分離

### 3.1 Phase 1 (T065 PR、 単体実装 + テスト)

**新規 module**: `src/alpha_factory/ga/nsga2_selection.py`

提供 API:

```python
# Pareto axis 抽出
extract_pareto_axis(bc_result: BCEvaluationResult, mission_gap: MissionGapResult) -> ParetoAxis

# Constrained domination
compute_effective_constraint_violation(
    mission_gap: MissionGapResult,
    invariant_flags: InvariantFlags,
) -> float
constrained_dominates(p: ParetoAxis, q: ParetoAxis, *, p_violation: float, q_violation: float) -> bool

# Non-dominated sorting + crowding
non_dominated_sort(candidates: Mapping[int, IndividualEvaluation]) -> dict[int, int]
crowding_distance(front_indices: Iterable[int], axes: Mapping[int, ParetoAxis]) -> dict[int, float]

# Survivor selection + parent selection
select_survivors(
    population: Mapping[int, IndividualEvaluation],
    *,
    target_size: int,
    rng: random.Random,
) -> tuple[int, ...]
binary_tournament(survivors: Sequence[int], evaluations: Mapping[int, IndividualEvaluation], *, rng: random.Random) -> int

# Top-level entry (per generation)
run_generation_selection(
    population: Mapping[int, IndividualEvaluation],
    *,
    pop_size: int,
    rng: random.Random,
) -> GenerationSelectionResult
```

**新規テスト**: `tests/alpha_factory/test_nsga2_selection.py`

### 3.2 Phase 2 (T066 / T067 統合 + run_ga.py 配線、 別 PR)

- `run_ga.py` per-generation chain: T063 evaluate_generation → T064 evaluate_bc_for_a_pass → T062 evaluate_mission_inf_gap → **T065 run_generation_selection** → T066 push_pull_state_advance → archive admission
- `config/alpha_factory/default.yaml` に GA core パラメータ (pop, gen, crossover_rate 等) を schema v2 準拠で追加 (T058 contract 内)
- 旧 GA selection 経路全廃

### 3.3 non-責務 (touch しない)

- archive admission/eviction (T066)
- crossover / mutation 実装本体 (既存 zenigame-fx 既存 breeding を流用 or 別 TODO)
- warmstart 注入 (T067)
- observability metrics emit (T071)
- spread_cost field 追加 (T070)

---

## 4. データフロー (per generation)

### 4.1 入力

```
population: dict[int, IndividualEvaluation]   # index -> 評価結果
pop_size: int                                  # 192 baseline / 256 promotion
rng: random.Random                             # deterministic seed (run_id + gen_no)
```

各 `IndividualEvaluation`:

```python
@dataclass(frozen=True)
class IndividualEvaluation:
    """T063/T064/T062/T061 の結果を統合した世代単位の個体評価."""
    index: int
    genome_hash: str                       # SHA-256 hex32 等、 deterministic tie-break
    stage_a_pass: bool                     # T063 a_pass_indices 由来
    bc_result: BCEvaluationResult | None   # T064 出力 (a_pass のみ非 None)
    mission_gap: MissionGapResult          # T062 出力 (全個体)
    invariant_flags: InvariantFlags        # T061 出力 (全個体)
```

### 4.2 処理ステップ

```
1. Pareto 候補集合の決定
   eligible = {idx for idx, eval in population.items()
               if eval.stage_a_pass
               and eval.bc_result is not None
               and eval.bc_result.pareto_axis_usable}
   excluded = set(population) - eligible

2. Pareto 軸の抽出 (eligible のみ)
   axes[idx] = extract_pareto_axis(eval.bc_result, eval.mission_gap)
              = ParetoAxis(
                  net_pnl=bc_result.b_pooled_cf.net_pnl_after_cost,
                  max_dd=bc_result.pooled_dd_per_fold_max,         # synthesis § 5.2
                  mission_inf_gap=mission_gap.mission_inf_gap,
                )

3. effective_constraint_violation 計算 (eligible のみ)
   violations[idx] = compute_effective_constraint_violation(
                       eval.mission_gap,
                       eval.invariant_flags,
                     )

4. Non-dominated sorting (eligible のみ)
   front_no[idx] = non_dominated_sort(eligible, axes, violations)
                   # constrained-domination 適用、 1=最優、 2=次優、 ...

5. Crowding distance (front 内 per-front)
   crowding[idx] = crowding_distance(front_indices, axes)
                   # 3 軸正規化、 端は +inf

6. Survivor 抽出 (target_size = pop_size)
   sorted by (front_no, -crowding, genome_hash)
   excluded individuals (A-fail, B-invariant-fail) は survivor に含めない
   survivors = top pop_size of sorted

7. Parent pair 抽出 (binary tournament)
   pairs = []
   for _ in range(pop_size):    # offspring を 2*pop 生成する場合は 2*pop ペア
       p1 = binary_tournament(survivors, evaluations, rng=rng)
       p2 = binary_tournament(survivors, evaluations, rng=rng)
       pairs.append((p1, p2))
   # 自己交配回避: p1==p2 の場合 N 回までリトライ (synthesis § 7.5)
```

### 4.3 出力

```python
@dataclass(frozen=True)
class GenerationSelectionResult:
    front_assignments: types.MappingProxyType[int, int]    # eligible のみ
    crowding_distances: types.MappingProxyType[int, float] # eligible のみ
    survivor_indices: tuple[int, ...]                       # 長さ <= pop_size
    parent_pairs: tuple[tuple[int, int], ...]               # 長さ pop_size
    excluded_indices: frozenset[int]                        # A-fail or B-invariant-fail
    sample_size_warnings: tuple[str, ...]                   # eligible < pop_size 等
```

excluded_indices は T067 archive 系で「Pareto 圧除外個体」 として観測される。 T065 では計算のみ、 archive 注入は T066-T067。

---

## 5. constrained-domination (Deb 2000) の確定

### 5.1 数式 (Deb 2000)

```
constrained_dominates(p, q):
    # Step 1: feasibility-first
    if p.is_feasible AND NOT q.is_feasible:
        return True
    if NOT p.is_feasible AND q.is_feasible:
        return False
    # Step 2: both infeasible → smaller violation wins
    if NOT p.is_feasible:   # both infeasible
        return p.effective_constraint_violation < q.effective_constraint_violation
    # Step 3: both feasible → Pareto 3 軸 dominance
    return p.pareto_dominates(q)
```

### 5.2 effective_constraint_violation の確定 (T062 申し送り [W2] 解決)

T062 line 181-187 の申し送りに対する T065 の確定回答:

```
effective_constraint_violation =
    mission_gap.constraint_violation                      # 4 slack 由来 (T062)
    + INVARIANT_VIOLATION_PENALTY × invariant_count       # invariant 違反加算
where:
    invariant_count = (
        invariant_flags.session_close_drop_count +
        invariant_flags.negative_equity_drop_open_count
    )
    INVARIANT_VIOLATION_PENALTY = 1.0e6                   # 4 slack の通常スケール (≤ 数十) を圧倒する大値
```

**根拠**:
- Deb 2000 は「single scalar violation」 を仮定。 fx では 2 種類の violation 源 (slack-based + invariant-based) があるため、 加算合成で single scalar 化
- `INVARIANT_VIOLATION_PENALTY = 1.0e6` は invariant 1 件で slack 由来 violation を支配する大値。 「invariant 違反 1 件 = どんな slack 不足個体より悪」 を実現
- count=0 (invariant violation なし) なら effective_constraint_violation == constraint_violation で T062 と整合
- pure function、 副作用なし、 deterministic

**代替案 (Round 1 議論候補、 不採用)**:
- 案 A: invariant_violation を独立 lex 最高 priority key にする → constrained-domination の 1 scalar 仮定を破る、 Deb 2000 純準拠崩れ
- 案 C: penalty を adaptive に (世代依存) → smoke 前は固定値が安全

**Decision Pending (smoke 後再校正候補、 § 13 残論点)**:
- INVARIANT_VIOLATION_PENALTY=1.0e6 は仮固定、 smoke で「invariant violation 個体が rank 末尾に確実に行くか」 観測してから再校正

### 5.3 is_feasible の判定

```
is_feasible(individual) ⟺
    mission_gap.is_feasible                       # T062 (slack 4 指標すべて feasible)
    AND invariant_flags.is_feasible               # T061 (session_close_drop=0 AND negative_equity_drop_open=0)
```

両方 True で全体 feasible。 どちらか False で infeasible (= effective_constraint_violation > 0)。

### 5.4 Pareto 3 軸 dominance (feasible 同士)

```
p.pareto_dominates(q):
    # f1 maximize, f2 minimize, f3 minimize
    not_worse = (
        p.net_pnl >= q.net_pnl
        AND p.max_dd <= q.max_dd
        AND p.mission_inf_gap <= q.mission_inf_gap
    )
    strictly_better = (
        p.net_pnl > q.net_pnl
        OR p.max_dd < q.max_dd
        OR p.mission_inf_gap < q.mission_inf_gap
    )
    return not_worse AND strictly_better
```

NaN / Inf 防御:
- net_pnl: 通常 finite、 +inf/-inf 出現は T064 / T061 fail-fast でキャッチ済 (T065 では検証のみ)
- max_dd: T064 pooled_dd_per_fold_max (per-fold max)、 値域 [0, +inf)、 NaN は T064 で fail-fast
- mission_inf_gap: T062 で値域 [0, +inf)、 NaN は T062 fail-fast

→ T065 では NaN check は assertion レベル、 通常運用で発生しない (caller 責務)

---

## 6. Pareto 軸 source の厳密確定 (T064 申し送り解決)

| 軸 | 方向 | source field | source 由来 | 値域 |
|---|---|---|---|---|
| f1 net_pnl | max | `bc_result.b_pooled_cf.net_pnl_after_cost` | T064 → T061 (Stage B pooled OOS で計算) | (-inf, +inf) finite |
| f2 max_dd | min | `bc_result.pooled_dd_per_fold_max` | T064 (per-fold max、 concat 擬似 DD 排除、 synthesis § 5.2) | [0, +inf) finite |
| f3 mission_inf_gap | min | `mission_gap.mission_inf_gap` | T062 (synthesis § 6.4 純粋式) | [0, +inf) finite |

**重要**: f2 は `b_pooled_cf.max_dd` (concat DD) **ではなく** `pooled_dd_per_fold_max` を使う。 T064 [C2] / synthesis § 5.2 / handoff § 3.7 で確立。 `b_pooled_cf.max_dd` は記録用のみで Pareto 軸 source としては使用禁止。

---

## 7. A-fail / B-invariant-fail 個体の Pareto 圧除外

### 7.1 除外契約

| 個体状態 | 除外理由 | T065 振る舞い |
|---|---|---|
| `stage_a_pass=False` | T063 default-deny (synthesis § 3、 handoff § 3.2) | Pareto 候補集合から除外、 survivor に入らない、 parent_pairs にも入らない |
| `bc_result is None` | T064 で B 評価 skip (a_pass でも skip 可能?)。 通常 a_pass=True なら必ず B 評価される | 同上、 除外 |
| `bc_result.pareto_axis_usable=False` | T064 [C2]、 b_pooled_cf=None (1 fold でも invariant fail) | 同上、 除外 |

excluded set = `{idx | NOT (stage_a_pass AND bc_result is not None AND bc_result.pareto_axis_usable)}`

### 7.2 PR DoD で必須テスト (handoff § 3.2 明示)

- `test_a_fail_individual_excluded_from_nsga_selection`: stage_a_pass=False の個体が survivor / parent_pairs に含まれない
- `test_b_invariant_fail_individual_excluded_from_pareto_axis_source`: bc_result.pareto_axis_usable=False の個体が survivor / parent_pairs に含まれない

### 7.3 Edge case: eligible が空 (全 A-fail or 全 B-invariant-fail)

```
if len(eligible) == 0:
    return GenerationSelectionResult(
        front_assignments=MappingProxyType({}),
        crowding_distances=MappingProxyType({}),
        survivor_indices=(),
        parent_pairs=(),
        excluded_indices=frozenset(population.keys()),
        sample_size_warnings=("eligible_set_empty",),
    )
```

caller (T067) で warmstart 強制注入 / Run abort 等の判断。 T065 自身は warning 出して終了 (silent ではない)。

### 7.4 Edge case: 0 < eligible < pop_size

```
sorted_survivors = sort(eligible)  # by (front_no, -crowding, genome_hash)
survivor_indices = tuple(sorted_survivors)   # 全 eligible を survivor に
sample_size_warnings += (f"eligible_size={len(eligible)} < pop_size={pop_size}",)

# parent_pairs は eligible のみから抽出 (binary tournament)
# eligible が 1 体のみの場合: 自己交配回避リトライで mutation only に fallback (caller 責務)
```

---

## 8. tie-break (rank → crowding → genome_hash)

### 8.1 sort key 定義

```python
def sort_key(idx: int) -> tuple[int, float, str]:
    return (
        front_assignments[idx],          # 1, 2, ... 昇順 (front 番号、 小さい=優)
        -crowding_distances[idx],        # 大きい=優、 マイナスで昇順 sort
        evaluations[idx].genome_hash,    # 文字列昇順 (deterministic)
    )

survivor_sorted = sorted(eligible, key=sort_key)
```

### 8.2 genome_hash の生成方針

- SHA-256 hex digest (固定長 64 文字)
- 入力: genome の deterministic JSON 表現 (key 順固定、 float 精度規約)
- T065 では genome_hash は `IndividualEvaluation.genome_hash` で受け取り、 計算は caller 責務 (T058 schema v2 contract で確定済)

### 8.3 crowding +inf 端規約 (zenigame `sorting.py` 同等)

front 内で各軸 sort 時:
- 端 2 個 (最小 / 最大) の crowding に `+inf` を加算 (端は常に保持)
- 中間個体は `(next - prev) / (max - min)` を 3 軸合計

NaN / 軸範囲ゼロ (max == min):
- 軸範囲 0 の場合、 当該軸の crowding 寄与は 0 (zenigame `sorting.py:97-104` 同等)
- NaN は事前に防御 (axes 抽出時に finite check、 § 6 NaN/Inf 防御参照)

### 8.4 ε 厚 fallback (zenigame `selection.py:140-159` 同等)

zenigame では crowding 中央値 ε ≈ 1e-12 で「ほぼ同値の crowding」 を tie-break する。 fx 流用方針:

- **Phase 1 では採用しない**。 単純に `(rank, -crowding, genome_hash)` の lex で確定的に解決
- ε 厚 tie-break は INCONCLUSIVE Decision (smoke 後 selection 多様性が低い場合に検討)

---

## 9. binary tournament (parent selection)

### 9.1 数式

```python
def binary_tournament(survivors, evaluations, *, rng):
    candidates = rng.sample(survivors, k=2)
    # constrained-domination で勝者抽出
    a, b = candidates
    if dominates(a, b):
        return a
    if dominates(b, a):
        return b
    # 同位 (互いに dominate しない or 完全同値) → tie-break (rank → crowding → genome_hash)
    return min(candidates, key=lambda idx: sort_key(idx))
```

### 9.2 自己交配回避 (synthesis § 7.5)

```python
def select_parent_pair(survivors, evaluations, *, rng, max_retry=3):
    p1 = binary_tournament(survivors, evaluations, rng=rng)
    for _ in range(max_retry):
        p2 = binary_tournament(survivors, evaluations, rng=rng)
        if evaluations[p2].genome_hash != evaluations[p1].genome_hash:
            return (p1, p2)
    # max_retry 超過 → mutation only fallback (caller 責務、 T065 では (p1, p1) で返す)
    return (p1, p1)
```

T065 内で `(p1, p1)` を返すと caller (Phase 2 breeding) で「mutation only」 経路に入る合意。 詳細設計時に caller 責務範囲を確定。

### 9.3 CA:DA 配分は T066 確定

- T065 では survivors 全体から binary tournament で抽出するのみ
- T066 で push/pull state に応じて「CA partition から N 体、 DA partition から M 体」 と source partition 分割
- T065 API は `survivors` を引数で受け取るので、 CA 用 / DA 用に partition して別々に呼ぶことで T066 が利用可能

---

## 10. degenerate cases / sample size guard

| ケース | T065 振る舞い | warning |
|---|---|---|
| eligible=0 (全 A-fail or 全 B-invariant-fail) | survivor=()、 parent_pairs=()、 全 excluded | `eligible_set_empty` |
| 0 < eligible < pop_size | survivor=eligible 全部、 parent_pairs は eligible 同士で binary tournament | `eligible_size_below_pop_size` |
| eligible=1 | survivor=(idx,)、 parent_pairs=((idx, idx),)*pop_size (mutation only fallback) | `single_eligible_individual` |
| 全 feasible 個体ゼロ (eligible 内全 infeasible) | constrained-domination で violation 順位付け、 通常通り pop_size 体抽出 | `no_feasible_individuals` |
| 全 infeasible で violation 同値 | crowding tie-break → genome_hash で確定 | (warning なし、 deterministic) |
| pop_size < 1 | ValueError raise | (運用 bug) |

C7 sample-size guard (T063 に倣う):
- eligible < 10: ValueError or `eligible_set_critically_small` warning + caller 確認
  - T065 では「ValueError は raise しない」、 silent fallback ではなく warning 経由で caller (T067) に伝える
  - 詳細設計で thresholds 確定 (10 / 30 / N)

---

## 11. 主要 dataclass / API シグネチャ (Phase 1 概要)

### 11.1 dataclass

```python
@dataclass(frozen=True)
class ParetoAxis:
    """Pareto 3 軸の単一個体抽出値."""
    net_pnl: float           # f1 max
    max_dd: float            # f2 min, pooled_dd_per_fold_max source
    mission_inf_gap: float   # f3 min

@dataclass(frozen=True)
class IndividualEvaluation:
    """T063/T064/T062/T061 の結果統合."""
    index: int
    genome_hash: str
    stage_a_pass: bool
    bc_result: BCEvaluationResult | None
    mission_gap: MissionGapResult
    invariant_flags: InvariantFlags

@dataclass(frozen=True)
class GenerationSelectionResult:
    """per-generation 主選抜結果."""
    front_assignments: types.MappingProxyType[int, int]
    crowding_distances: types.MappingProxyType[int, float]
    survivor_indices: tuple[int, ...]
    parent_pairs: tuple[tuple[int, int], ...]
    excluded_indices: frozenset[int]
    sample_size_warnings: tuple[str, ...]
```

### 11.2 関数シグネチャ

```python
def extract_pareto_axis(
    bc_result: BCEvaluationResult,
    mission_gap: MissionGapResult,
) -> ParetoAxis: ...

def compute_effective_constraint_violation(
    mission_gap: MissionGapResult,
    invariant_flags: InvariantFlags,
) -> float: ...

def constrained_dominates(
    p_axis: ParetoAxis,
    q_axis: ParetoAxis,
    *,
    p_violation: float,
    q_violation: float,
    p_feasible: bool,
    q_feasible: bool,
) -> bool: ...

def non_dominated_sort(
    eligible: Mapping[int, IndividualEvaluation],
    axes: Mapping[int, ParetoAxis],
    violations: Mapping[int, float],
    feasibility: Mapping[int, bool],
) -> dict[int, int]: ...   # idx -> front_no (1-origin)

def crowding_distance(
    front_indices: Iterable[int],
    axes: Mapping[int, ParetoAxis],
) -> dict[int, float]: ...

def select_survivors(
    population: Mapping[int, IndividualEvaluation],
    *,
    target_size: int,
) -> tuple[int, ...]: ...   # rng 不要 (deterministic tie-break)

def binary_tournament(
    survivors: Sequence[int],
    evaluations: Mapping[int, IndividualEvaluation],
    *,
    rng: random.Random,
) -> int: ...

def select_parent_pair(
    survivors: Sequence[int],
    evaluations: Mapping[int, IndividualEvaluation],
    *,
    rng: random.Random,
    max_retry: int = 3,
) -> tuple[int, int]: ...

def run_generation_selection(
    population: Mapping[int, IndividualEvaluation],
    *,
    pop_size: int,
    rng: random.Random,
) -> GenerationSelectionResult: ...
```

`rng` は `random.Random` (deterministic)。 caller (T067) が `seed = run_id ^ generation_no` 等で生成。

---

## 12. テスト計画 (代表のみ、 詳細設計で完全リスト化)

### 12.1 PR DoD 必須 (handoff § 3.2)

- `test_a_fail_individual_excluded_from_nsga_selection`
- `test_b_invariant_fail_individual_excluded_from_pareto_axis_source`

### 12.2 constrained-domination

- `test_constrained_dominates_feasible_vs_infeasible_returns_true`
- `test_constrained_dominates_infeasible_vs_feasible_returns_false`
- `test_constrained_dominates_both_feasible_uses_pareto_three_axes`
- `test_constrained_dominates_both_infeasible_smaller_violation_dominates`
- `test_compute_effective_constraint_violation_invariant_violation_dominates_slack`
- `test_compute_effective_constraint_violation_zero_when_fully_feasible`

### 12.3 Pareto axis 抽出

- `test_extract_pareto_axis_uses_pooled_dd_per_fold_max_not_concat_dd`
- `test_extract_pareto_axis_uses_t062_mission_inf_gap`
- `test_extract_pareto_axis_uses_t064_b_pooled_cf_net_pnl_after_cost`

### 12.4 Non-dominated sort + crowding

- `test_non_dominated_sort_three_axis_separates_pareto_fronts`
- `test_non_dominated_sort_constrained_domination_propagates`
- `test_crowding_distance_three_axis_normalized`
- `test_crowding_distance_endpoints_marked_inf`
- `test_crowding_distance_zero_range_axis_contributes_zero`

### 12.5 Survivor + tournament

- `test_select_survivors_uses_rank_then_crowding_then_genome_hash`
- `test_select_survivors_excludes_a_fail_and_b_invariant_fail`
- `test_binary_tournament_deterministic_with_seeded_rng`
- `test_binary_tournament_constrained_domination_chooses_winner`
- `test_select_parent_pair_avoids_self_mating_within_max_retry`
- `test_select_parent_pair_falls_back_to_self_when_pool_exhausted`

### 12.6 Edge cases

- `test_run_generation_selection_eligible_set_empty_warns_and_returns_empty`
- `test_run_generation_selection_eligible_below_pop_size_warns`
- `test_run_generation_selection_single_eligible_uses_self_mating_fallback`
- `test_run_generation_selection_all_infeasible_uses_violation_ordering`

### 12.7 Determinism

- `test_run_generation_selection_same_seed_same_result_byte_for_byte`
- `test_run_generation_selection_different_seed_different_result`

---

## 13. 残論点 / Decision Pending

### 13.1 INVARIANT_VIOLATION_PENALTY 値の確定

仮: `1.0e6`、 smoke 後再校正候補 (synthesis § 15 残論点に追記候補、 詳細設計で扱う)。 INCONCLUSIVE タグで 5 run 観測まで固定、 invariant violation 個体が rank 末尾に確実に行くか観測。

### 13.2 effective_constraint_violation の Decomposition

T065 内では加算合成で single scalar 化。 archive eviction (T066) で「invariant 違反個体は CA に入れない」 等の lex 順序を別途定義することは T066 の責務。 T065 の constrained-domination 単体では single scalar で十分。

### 13.3 binary tournament k=2 vs k=3

synthesis § 7.1 で「binary tournament」 と確定。 k=2 で固定、 k=3 は INCONCLUSIVE (smoke 後 selection 多様性低下なら検討、 採用しない)。

### 13.4 crowding ε 厚 tie-break

zenigame `selection.py:140-159` の ε 厚 tie-break を fx でも採用するか。 Phase 1 では採用しない (genome_hash で十分)、 INCONCLUSIVE。

### 13.5 RNG seed 規約

caller (T067) が `seed = hash(run_id, generation_no, "selection")` 等で生成。 T065 は受け取るのみ。 詳細設計で seed 規約を明文化 (T058 schema v2 contract と整合)。

### 13.6 Survivor 抽出時の eligible 体数判定

eligible < pop_size で「全 eligible を survivor に」 する場合、 caller (Phase 2) は不足分を warmstart や fresh で補う必要。 T065 の sample_size_warnings で通知、 caller 責務の明文化は T067 で。

### 13.7 zenigame `optimize.py:2125,2141` の主選抜実装

handoff § 4.2 で zenigame 主選抜は `optimize.py:2125,2141` 同等とある。 詳細設計時に当該箇所を実 grep で確認、 fx 側 implementation の差分明示。

---

## 14. Phase 2 申し送り (T066 / T067 / T070 / T071 と同時、 別 PR)

| # | ファイル / 箇所 | 担当 | 内容 |
|---|---|---|---|
| 1 | (新規) `src/alpha_factory/ga/nsga2_selection.py` を import | T066 | run_ga.py per-generation chain で T065 を呼ぶ |
| 2 | `src/alpha_factory/scripts/run_ga.py`: per-generation T063 → T064 → T062 → T065 chain | T066 | 旧 ga loop selection 経路全廃 |
| 3 | (新規) `src/alpha_factory/ga/push_pull_fsm.py` で T065 survivors を CA:DA に partition | T066 | T066 で push/pull state に応じて partition |
| 4 | `config/alpha_factory/default.yaml`: GA core パラメータ (pop, gen, crossover_rate, mutation_rate, INVARIANT_VIOLATION_PENALTY) を追加 | T066 | T058 schema v2 準拠 |
| 5 | `src/alpha_factory/archive.py`: A-fail / B-invariant-fail を archive admission 全段階から排除 + default-deny test | T067 | excluded_indices を T067 が消費 |
| 6 | (新規) `src/alpha_factory/observability/selection_metrics.py`: front1 cardinality / feasible_ratio / violation distribution を観測 | T071 | T065 GenerationSelectionResult を消費 |
| 7 | (新規) `tests/integration/test_ga_run_with_t065_selection.py`: per-generation chain integration test | T066 | T065 単体は Phase 1、 統合は Phase 2 |
| 8 | `docs/alpha_factory/ga-architecture.md` を NSGA-II + constrained-domination の新仕様に書き換え | T066 | 旧 selection 経路全廃 + T065 仕様反映 |

---

## 15. zenigame コード参考 (詳細設計時に深堀)

| 機構 | zenigame ファイル | 行番号 |
|---|---|---|
| Crowding distance | `src/trading/alpha_factory/ga/nsga2/sorting.py` | 60-125 |
| Selection (front sort + tie-break) | `src/trading/alpha_factory/ga/nsga2/selection.py` | 36-159, 293-353 |
| Breeding (parent selection, crossover) | `src/trading/alpha_factory/ga/nsga2/breeding.py` | 211, 255 |
| GA optimize loop (主選抜呼出) | `src/trading/alpha_factory/ga/nsga2/optimize.py` | 950, 1006/1195, 2125/2141 |
| Determinism (RNG seed) | `src/trading/alpha_factory/ga/nsga2/core.py` | 114, 1026/1085 |

zenigame との差分 (本概念設計確立):

- **constrained-domination**: zenigame 未実装、 fx で正式新設 (Deb 2000 純準拠)
- **Pareto 3 軸 source**: zenigame は `optimize.py:2125,2141` で B-pooled 系を直接参照、 fx は T064 BCEvaluationResult 経由で contract 化
- **MG tie-break (T333)**: zenigame `selection.py:93-107` 独自拡張、 fx は **採用しない** (genome_hash + crowding で十分)
- **invariant violation 組込み**: zenigame は invariant violation を別経路で扱う、 fx は effective_constraint_violation で single scalar 化

---

## 16. 完了判定 (概念設計 APPROVED 条件)

- [ ] § 1 で T065 の責務範囲 (Phase 1 / Phase 2 分離) が明確
- [ ] § 2 で前提検証 (C4) が verified、 出典が明示
- [ ] § 5 で constrained-domination の数式が確定 (effective_constraint_violation 含む)
- [ ] § 6 で Pareto 3 軸 source が T064 / T062 から厳密に確定
- [ ] § 7 で A-fail / B-invariant-fail 個体除外契約が明示
- [ ] § 11 で主要 dataclass / API シグネチャが pure function 群として揃う
- [ ] § 12 で PR DoD 必須テスト (handoff § 3.2) が含まれる
- [ ] § 13 で残論点 (INVARIANT_VIOLATION_PENALTY 等) が Decision Pending タグ付き
- [ ] § 14 で Phase 2 申し送りが具体ファイル名 + 担当 TODO 名で 8 箇所以上
- [ ] Codex 概念レビューで CHANGES_APPLIED → APPROVED

---

## 17. 補足: synthesis Round 21 改訂後の整合性

本概念設計は synthesis Round 21 (2026-04-30 改訂、 mission_signed_margin SSOT 昇格) 後の確定値に基づく:

- archive CA #5 = `mission_signed_margin` (T066-T067 の責務、 T065 では言及のみ)
- `mission_inf_gap` は Pareto f3 minimize 用 (T065 で消費)
- `constraint_violation` は T062 確定式 (NSGA-II constrained-domination 用、 T065 で消費)
- `mission_margin` は BACKWARD COMPAT (T065 では参照しない)

T065 詳細設計時に synthesis 章番号を再 grep で整合性確認。
