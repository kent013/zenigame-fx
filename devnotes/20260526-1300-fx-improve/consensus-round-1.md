1. 最適解は **(A) 探索効率改善** です。  
mission整合（品質向上継続）を維持しつつ、(B)(C)より低コスト・高成功確度で、(D)より将来の閾値引き上げ余地を再生成できます。

2. 今は「一区切り」ではなく、**軽量なフレーム内拡張（探索ダイナミクス改善）**を優先すべきです。  
dd2% frontier は成果として固定しつつ、分布上方シフトを先に狙うのが合理的です。

3. (A)での単一変更は **`--nsga2-selection` の有効化** が最有力です。  
`pop/gen`増はコスト線形増、`warmstart`強化はモード崩壊リスクがあり、NSGA-IIは既実装レバーで計算コスト増が小さく、上位尾部（高pnl・低dd）の押し上げ期待が最も高いです。

4. R104 の反証可能な判定基準（5条件）:
1. `mission_candidate_count >= 650`（R101比で大幅退行しない）
2. `median(trade_sharpe_stage_c_annualized) >= 5.75`（R101 5.582比 +3%以上）
3. `median(total_pnl_stage_c) >= 86000`（R101 82k比 +5%以上）
4. live_criteria達成個体で `max_dd <= 0.02` を100%維持し、かつ `p95(max_dd) <= 0.019`
5. 追試 seed（同設定・seed+1）で 2 と 3 が本run比 -10%以内（再現性）

判定規則: **5条件すべて満たせば成功、1つでも未達なら失敗、必要データ欠損は INCONCLUSIVE**。

全体判定: **(A)を採用し、R104は `--nsga2-selection` 単独変更で実施**。