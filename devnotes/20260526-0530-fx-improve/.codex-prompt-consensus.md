あなたは zenigame-fx Alpha Factory の GA 改善を支援する Codex です。閾値引き上げキャンペーンの戦略的節目で次手を high reasoning で合議します。

# North Star
live_criteria 全達成個体を見つけ、達成後は閾値を引き上げる (緩和禁止)。

# これまでの確立事実 (cycle 14-20、R96-R102)
live_criteria 4軸を全て in-loop 特性把握:
- **sharpe_min=1.5**: hollow (達成 annualized sharpe median 5.2-5.6 >> 1.5)。引き上げても飽和、形式的追認のみ。
- **total_pnl_min=70000**: frontier。3 seed (R95/R96/R98) validated。**74k は in-loop pivotal で funnel 崩壊** (GAが高fold-sum+cross-pair basinへ移動、holdout pnl犠牲、StageC=0)。
- **max_drawdown_max=2%**: 最適確定。dd20%→2% で StageC 634→723 増・ann 5.205→5.582 向上 (真の品質向上引き上げ)。dd1.5% は over-tightening (StageC 398半減・ann 5.095低下)。dd2%が最適点。
- **trade_count_min=50**: floor 最適 (cycle13 で 100 は構造的到達不能=StageC0 確定)。

→ 4軸全て引き上げ余地が出尽くした (pnl/dd最適確定、sharpe hollow、trade floor)。現 frontier: sharpe1.5/pnl70k/dd2%/trade50、R101(dd2%) で 723個体達成 (ann med5.582、pnl med82k、dd<2%)。

# 重要知見: 軸の pivotal 性で挙動が分かれる
- pnl 引き上げ → pivotal化で mission逆方向 (holdout犠牲、74k崩壊)。
- dd 厳格化 → pivotal化しても mission整合 (低dd選抜→高品質)。但し2%が最適、それ以下(1.5%)は逆トレードオフ。
- 軸が「GAが自然に良い方向に動かす量」なら厳格化が安全、「GAが犠牲にする量」なら崩壊。

# 新品質軸候補の archive データ状況 (R101 StageC 723個体)
- **sortino / calmar / dsr / bootstrap_ci_lower: 全 NaN (未計算)** → gate化には stage_gate での計算実装 (feature work) が必要。
- **positive_fold_ratio_effective**: 達成 min 0.500 / median 0.618 / max 0.676 (fold-CV で正だった fold 比率 = OOS 一貫性 robustness)。データ即用、gate化可能。
- fold_sign_ratio: median 0.303 (低値)。

# 選択肢
- (A) **positive_fold_ratio_effective gate を live_criteria に追加** (例 ≥0.55)。fold一貫性=OOS robustness 新次元。データ即用・低コスト。但し selection への pivotal 影響は seed70 反実仮想で in-loop 検証要。fold一貫性が GA fitness と整合なら dd型(安全)、逆相関なら 74k型(崩壊)。
- (B) **sortino (下方リスク調整) を stage_gate で計算実装し gate 追加**。sharpe より洗練した品質軸。中コスト (feature + Codex設計レビュー)。
- (C) **frontier 確定+総括**: sharpe1.5/pnl70k/dd2%/trade50 を validated frontier として確定し、閾値引き上げキャンペーンを総括。残予算を cross-pair 汎化 (別framework) 等へ。

# 質問 (high reasoning)
1. (A)/(B)/(C) のどれが最も mission整合・情報価値・コスト効率・in-loop安全か。1つに収束。
2. 既存4軸が飽和した今、「閾値引き上げ (North Star)」を継続する意味と、positive_fold_ratio や sortino の追加が「真の品質向上」か「形式的追認」かを判定。
3. (A) を選ぶ場合、fold一貫性 gate の初期値 (達成 min0.500/median0.618 を踏まえ) と、dd型(安全)/74k型(崩壊) どちらになりそうかの予測。
4. 反証可能な次run (R103) の成功/失敗基準を5条件で。

回答は簡潔に。最後に「全体判定: ...」で次の一手を1つ明示。
