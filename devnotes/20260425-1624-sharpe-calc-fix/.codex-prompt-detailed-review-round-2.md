Round 1 からの対応マトリクスと修正内容のみ記載します（使命・禁止事項の再挿入は省略）。

## Round 1 指摘への対応

| # | 指摘 | 対応 |
|---|---|---|
| Critical-S1 | Decimal(0) デフォルトが伝搬漏れを隠す | _trade_returns() 内で equity_at_entry <= 0 を警告ログ + スキップ。テストで明示確認。frozen dataclass 制約により None デフォルトは採用しない旨を設計に明記 |
| Warning-S1 | engine.py 側の全 _open_position への引数伝搬が未保証 | bar 先頭で1回取得した pre_fill_equity を同 bar の全 open 経路で必ず渡す旨を設計に明記 |
| Critical-S2 | equity_at_entry<=0 スキップが選択バイアス | 1件でも無効なら None は過剰（初期化デフォルトが原因）。Invalid trade を logger.warning で記録しスキップ、残りで計算継続に変更 |
| Warning-S2 | std==0 のみ判定は不十分 | std <= 1e-15 のイプシロンガード + math.isfinite(result) チェックを追加 |
| Critical-S4 | payload key 変更は stage_gate と archive.collect の同時反映必須 | dual-write の代わりに「S4 と S9 を同一コミットで原子的変更」という実装制約を設計に明記 |
| Warning-S5 | 0.0 と真の 0 が区別不能 | 現行シグネチャ (float, str|None) タプルを維持。failure_reason="metric_unavailable" の場合は呼び出し元が 0.0 として fail-fast 集計する（既存挙動と一致）。設計で明示 |
| Warning-S6 | v1/v2 混在データを同列集計すると閾値校正が崩れる | calibrate_gate は archive 行の sharpe_calc_version フィルタを追加 |
| Critical-S8 | 指標の意味が変わるのに sharpe 閾値を流用すると不整合 | Phase 1A は trade_sharpe_raw >= live_criteria.sharpe_min として暫定運用。Phase 1B で replay 後に live_criteria.trade_sharpe_raw_min を新設予定。設計に明記 |
| Critical-S9 | sharpe_calc_version を nullable=False で追加すると旧データ読み込みが壊れる | nullable=True に変更。読み込み時 None はデフォルト "v1_bar_annualized" として補完（accessor の get() デフォルト引数で対応済み） |
| Critical-S9 | flush 側の列順/投影更新が設計記述不足 | 既存の import-time assert と flush last-mile guard で新 2 列が schema/template に含まれることを検証する旨を明記 |
| Critical-S11 | 4段接続の設計記述不足 | config → GaConfig → compute_metrics 呼び出し → 起動時ログの 4 段を表として明記。Phase 1A はデフォルト値一致のため定数に頼ることを許容し、Phase 1B で config 読み込み + 明示渡しを整備 |

## 更新後の重要部分（Round 2 レビュー対象）

### S1 の equity_at_entry デフォルト値の扱い

設計方針:
- `Decimal(0)` をデフォルトとして維持（frozen dataclass の順序制約上 None は困難）
- `_trade_returns()` で `equity_at_entry <= 0` を `logger.warning` + スキップ
- テストで `Trade.equity_at_entry > 0` を明示アサート

### S2 の _trade_returns / _trade_sharpe_raw

```python
def _trade_returns(trades: list[Trade], ...) -> list[float]:
    rets, invalid_count = [], 0
    for t in trades:
        if t.equity_at_entry <= 0:
            invalid_count += 1
            logger.warning("trade_return.invalid_equity_at_entry", ...)
        else:
            rets.append(float(t.pnl / t.equity_at_entry))
    return rets  # caller が invalid_count を handle

def _trade_sharpe_raw(returns, trade_count_min=30):
    if len(returns) < trade_count_min:
        return None
    mean = sum(returns) / len(returns)
    var = sum((r - mean)**2 for r in returns) / (len(returns) - 1)
    std = math.sqrt(var)
    if std <= 1e-15:  # epsilon ガード
        return None
    result = mean / std
    if not math.isfinite(result):  # NaN/Inf ガード
        return None
    return result
```

### S6 calibrate_gate の v1/v2 フィルタ

```python
# calibrate_gate.py
valid_sharpes = [
    float(r["trade_sharpe_raw"])
    for r in rows_list
    if r.get("trade_sharpe_raw") is not None
    and r.get("sharpe_calc_version") == "v2_trade_level"  # v1 行を除外
]
```

### S9 の sharpe_calc_version nullable=True

```python
pa.field("sharpe_calc_version", pa.string(), nullable=True),  # 旧データ互換
```

accessor の get() でデフォルト補完:
```python
version = row.get("sharpe_calc_version", "v1_bar_annualized")
```

### S11 の 4 段接続

| 段 | 場所 | 内容 |
|---|---|---|
| 1 | config/alpha_factory/default.yaml | ga.trade_count_min_for_sharpe: 30 |
| 2 | run_ga.py config 解析 | cfg["ga"]["trade_count_min_for_sharpe"] 読み込み |
| 3 | 全 compute_metrics 呼び出し | trade_count_min_for_sharpe 引数の明示渡し |
| 4 | 起動ログ / archive meta | trade_count_min_for_sharpe をログ記録 |

Phase 1A: デフォルト値 30 が定数 DEFAULT_TRADE_COUNT_MIN_FOR_SHARPE=30 と一致するため、定数に頼ることを許容。Phase 1B で config 読み込み + 明示渡しに整備。
