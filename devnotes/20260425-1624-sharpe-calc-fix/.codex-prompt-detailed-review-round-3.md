Round 2 からの対応マトリクスと修正内容のみ記載します（使命・禁止事項の再挿入は省略）。

## Round 2 指摘への対応

| # | 指摘 | 対応 |
|---|---|---|
| Critical-1 | `row.get("sharpe_calc_version", ...)` は None を補完できない | `row.get(...) or "v1_bar_annualized"` に変更（accessor と calibrate_gate 両方） |
| Critical-2 | `_trade_sharpe_raw` で `trade_count_min=1` 時に `len-1=0` でゼロ除算 | `len(returns) < max(2, trade_count_min)` に修正 |
| Warning-3 | `invalid_count` を caller が扱えない | 関数内でログ集計して完結させる（invalid_count はログのみ、return はリストのみ） |
| Warning-4 | unknown version の静かな混入 | calibrate_gate で unknown version を `logger.warning` で可視化するコードを追加 |

## 更新後の重要部分（Round 3 レビュー対象）

### _trade_returns（S2）

```python
def _trade_returns(trades: list[Trade], ...) -> list[float]:
    rets, invalid_count = [], 0
    for t in trades:
        if t.equity_at_entry <= 0:
            invalid_count += 1
            logger.warning("trade_return.invalid_equity_at_entry", ...)
        else:
            rets.append(float(t.pnl / t.equity_at_entry))
    if invalid_count > 0:
        logger.warning("trade_return.invalid_equity_at_entry_total", invalid_count=invalid_count, total_trades=len(trades))
    return rets  # 件数は返さない（関数内で完結）
```

### _trade_sharpe_raw（S2）

```python
def _trade_sharpe_raw(returns, trade_count_min=30):
    if len(returns) < max(2, trade_count_min):  # ゼロ除算防止
        return None
    mean = sum(returns) / len(returns)
    var = sum((r - mean)**2 for r in returns) / (len(returns) - 1)
    std = math.sqrt(var)
    if std <= 1e-15:
        return None
    result = mean / std
    if not math.isfinite(result):
        return None
    return result
```

### calibrate_gate の v1/v2 フィルタ（S6）

```python
def _get_sharpe_version(r):
    return r.get("sharpe_calc_version") or "v1_bar_annualized"  # None を v1 とみなす

unknown_versions = set()
valid_sharpes = []
for r in rows_list:
    version = _get_sharpe_version(r)
    if version == "v2_trade_level":
        v = r.get("trade_sharpe_raw")
        if v is not None:
            valid_sharpes.append(float(v))
    elif version != "v1_bar_annualized":
        unknown_versions.add(version)

if unknown_versions:
    logger.warning("calibrate_gate.unknown_sharpe_calc_version", ...)
```

### canonical accessor の None 補完（S9）

```python
# get_trade_sharpe
version = row.get("sharpe_calc_version") or "v1_bar_annualized"  # None も v1 扱い

# get_legacy_bar_sharpe
version = row.get("sharpe_calc_version") or "v1_bar_annualized"  # None も v1 扱い
```
