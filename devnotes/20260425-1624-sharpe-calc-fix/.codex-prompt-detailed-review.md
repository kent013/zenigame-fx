【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック】
C1-C9 の全 discipline を適用すること（Design-first / parallel-path / collider bias / 前提検証 / fact-interpretation 分離 / INCONCLUSIVE 第一級 / falsification-first）。

---

あなたは経験豊富なクオンツ・システムアーキテクトです。zenigame-fx Alpha Factory の Sharpe 計算根本修正（Phase 1A）の詳細設計をレビューしてください。

【前提環境】
- Python 3.13 + numpy + pandas
- FX イントラデイ戦略の GA 最適化
- DSL ベースの戦略表現: Clause ベース合成
- 実行環境: macOS, 24GB メモリ, 6 ワーカー並列（1 ワーカー最大約 3GB）

【レビュー観点】
1. コードの正確性（ロジックエラー、エッジケース、off-by-one、NaN 伝播）
2. 既存コードとの整合性（特に archive 4 点セット、collect_stage_a の payload key 変更）
3. パフォーマンスへの影響
4. テスト計画の網羅性（各施策にテストが含まれているか）
5. 副作用・後退リスク
6. 考慮漏れ（特に呼び出し元の全網羅）
7. 実装モードの妥当性
8. 波及変更の網羅性

【特に注意すべき確認事項】
- `collect_stage_a` が現在 `_opt_float(payload, "sharpe_raw")` で `sharpe` 列を埋めている。S4 で payload key を `"trade_sharpe_raw"` に変更した場合、`collect_stage_a` も同じく `"trade_sharpe_raw"` で読む必要がある
- Stage B の `collect_stage_b` も `is_full_sharpe` で `sharpe` 列を上書きしている。Phase 1A では `trade_sharpe_raw` 列を更新するよう変更が必要
- `Trade.equity_at_entry` のデフォルト値 `Decimal(0)` は既存テストが静かにパスするリスク
- `MockBroker._open_position` に `equity_at_entry` を渡す実装で、engine.py 側で `pre_fill_equity` を取得するコードが必要

【出力形式】
- 各施策ごとに判定: APPROVE / REQUEST_CHANGES / INCONCLUSIVE (C8)
- 指摘は [Critical] [Warning] [Suggestion]
- Critical/Warning には修正案
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 日本語

---

## 詳細設計書

# 詳細設計: Sharpe Ratio 計算の根本修正 (Phase 1A)

## 施策一覧

| # | 施策名 | 変更ファイル |
|---|--------|------------|
| S1 | Position/Trade に equity_at_entry 追加 | `src/broker/orders.py`, `src/broker/mock.py` |
| S2 | compute_metrics に trade_sharpe_raw 追加 | `src/backtest/metrics.py` |
| S3 | GA fitness を trade_sharpe_raw に切り替え | `src/ga/fitness.py` |
| S4 | Stage Gate を trade_sharpe_raw に切り替え | `src/alpha_factory/stage_gate.py` |
| S5 | cross_pair を trade_sharpe_raw に切り替え | `src/alpha_factory/cross_pair.py` |
| S6 | calibrate_gate を trade_sharpe_raw に切り替え | `src/alpha_factory/calibrate_gate.py` |
| S7 | Alpha Sieve を trade_sharpe_raw に切り替え | `scripts/alpha_factory/run_alpha_sieve.py` |
| S8 | run_ga の live_criteria 判定を trade_sharpe_raw に切り替え | `scripts/alpha_factory/run_ga.py` |
| S9 | archive schema + canonical accessor | `src/alpha_factory/archive.py` |
| S10 | DSR コメント追記 | `src/alpha_factory/statistics.py` |
| S11 | config に trade_count_min_for_sharpe 追加 | `config/alpha_factory/default.yaml` |

## S1: Position / Trade に equity_at_entry 追加

### 変更後コード（重要部分）

```python
# orders.py
@dataclass(frozen=True)
class Position:
    ...
    equity_at_entry: Decimal = Decimal(0)  # T-sharpe: bar 開始時 pre-fill equity (SSOT)

@dataclass(frozen=True)
class Trade:
    ...
    equity_at_entry: Decimal = Decimal(0)  # T-sharpe: Position から伝搬
```

```python
# mock.py _open_position: equity パラメータ追加
def _open_position(
    self, side: PositionSide, units: int, entry_price: Decimal, entry_time, leverage: int,
    *, equity_at_entry: Decimal
) -> Position:
    pos = Position(..., equity_at_entry=equity_at_entry)
```

```python
# mock.py _close_one: Trade に伝搬
trade = Trade(..., equity_at_entry=pos.equity_at_entry)
```

エンジン側（engine.py）では bar 処理開始時に `pre_fill_equity = broker.snapshot().equity` を取得し、同 bar 内の全 `_open_position` に `equity_at_entry=pre_fill_equity` を渡す（same-bar 複数 fill で共通値を使用）。

## S2: compute_metrics に trade_sharpe_raw 追加

```python
def _trade_returns(trades: list[Trade]) -> list[float]:
    rets: list[float] = []
    for t in trades:
        if t.equity_at_entry > 0:
            rets.append(float(t.pnl / t.equity_at_entry))
    return rets

def _trade_sharpe_raw(returns: list[float], trade_count_min: int = 30) -> float | None:
    if len(returns) < trade_count_min:
        return None
    mean = sum(returns) / len(returns)
    var = sum((r - mean) ** 2 for r in returns) / (len(returns) - 1)
    std = math.sqrt(var)
    if std == 0:
        return None
    return mean / std

# BacktestMetrics: フィールド追加（末尾にデフォルト値付き）
trade_sharpe_raw: Decimal | None = None
sharpe_calc_version: str = "v2_trade_level"

# compute_metrics: 追加計算（既存 sharpe は維持）
trade_rets = _trade_returns(trades)
trade_sharpe_f = _trade_sharpe_raw(trade_rets, trade_count_min=trade_count_min_for_sharpe)
trade_sharpe_raw = Decimal(str(trade_sharpe_f)) if trade_sharpe_f is not None else None
```

## S3: GA fitness を trade_sharpe_raw に切り替え

```python
if metric == "sharpe":
    if metrics.trade_sharpe_raw is None:
        logger.info("ga.fitness.metric_unavailable", ..., sharpe_calc_version=metrics.sharpe_calc_version)
        return _FAILURE_FITNESS
    return metrics.trade_sharpe_raw
```

## S4: Stage Gate を trade_sharpe_raw に切り替え

Stage A の計算箇所:
```python
bt = compute_metrics(result.trades, result.equity_curve)
trade_count = bt.trade_count
sharpe_raw = float(bt.trade_sharpe_raw) if bt.trade_sharpe_raw is not None else None
```

Stage A の payload key を変更:
```python
"payload": {
    ...
    "trade_sharpe_raw": sharpe_raw,  # "sharpe_raw" → "trade_sharpe_raw"
}
```

Stage B: `bt.sharpe` → `bt.trade_sharpe_raw`
Stage C: `base_sharpe = float(bt.trade_sharpe_raw) if bt.trade_sharpe_raw is not None else None`

## S5: cross_pair を trade_sharpe_raw に切り替え

```python
bt = compute_metrics(result.trades, result.equity_curve)
if bt.trade_sharpe_raw is None:
    return 0.0, "metric_unavailable"
return float(bt.trade_sharpe_raw), None
```

## S6-S8: calibrate_gate / Alpha Sieve / run_ga

calibrate_gate: `r["sharpe"]` → `r["trade_sharpe_raw"]`
Alpha Sieve: `bt.sharpe` → `bt.trade_sharpe_raw`
run_ga: `row.get("sharpe")` → `row.get("trade_sharpe_raw")`

## S9: archive schema + canonical accessor

GENOMES_SCHEMA に追加 (28 → 30 カラム):
```python
pa.field("trade_sharpe_raw", pa.float64(), nullable=True),
pa.field("sharpe_calc_version", pa.string(), nullable=False),
```

_create_row_template に追加:
```python
"trade_sharpe_raw": None,
"sharpe_calc_version": "v2_trade_level",
```

collect_stage_a を変更:
```python
row["sharpe"] = _opt_float(payload, "sharpe_raw")  # 旧: v1 後方互換のため残存
row["trade_sharpe_raw"] = _opt_float(payload, "trade_sharpe_raw")  # 新
row["sharpe_calc_version"] = "v2_trade_level"  # 新
```

collect_stage_b を変更:
```python
is_sharpe = _opt_float(payload, "is_full_trade_sharpe_raw")  # 旧: "is_full_sharpe"
if is_sharpe is not None:
    row["trade_sharpe_raw"] = is_sharpe  # roq["sharpe"] の更新はやめる
row["sharpe_calc_version"] = "v2_trade_level"  # 追加
```

canonical accessor:
```python
@staticmethod
def get_trade_sharpe(row: Mapping[str, Any]) -> float:
    version = row.get("sharpe_calc_version", "v1_bar_annualized")
    if version != "v2_trade_level":
        raise ValueError(f"v1 archive との比較は禁止: sharpe_calc_version={version!r}")
    return row["trade_sharpe_raw"]

@staticmethod
def get_legacy_bar_sharpe(row: Mapping[str, Any]) -> float | None:
    version = row.get("sharpe_calc_version", "v1_bar_annualized")
    if version == "v2_trade_level":
        raise ValueError("v2 archive には v1 bar-level Sharpe は存在しない")
    v = row.get("sharpe")
    return float(v) if v is not None else None
```

## 関連する現行コード（抜粋）

### src/alpha_factory/archive.py collect_stage_a (現行)
```python
row["fitness_raw"] = _required_float(payload, "fitness_raw")
row["fitness_pen"] = _required_float(payload, "fitness_pen")
row["stage_a_pass"] = bool(stage_result.passed)
row["trade_count"] = _required_int(payload, "trade_count")
row["sharpe"] = _opt_float(payload, "sharpe_raw")  # ← "sharpe_raw" キーで読んでいる
```

### src/alpha_factory/archive.py collect_stage_b (現行)
```python
is_sharpe = _opt_float(payload, "is_full_sharpe")
if is_sharpe is not None:
    row["sharpe"] = is_sharpe  # ← sharpe 列を上書き
```

### src/alpha_factory/stage_gate.py evaluate_stage_a payload (現行)
```python
"payload": {
    ...
    "sharpe_raw": sharpe_raw,  # ← キー名が "sharpe_raw"
}
```

### src/alpha_factory/stage_gate.py evaluate_stage_b payload (現行)
```python
"payload": {
    ...
    "is_full_sharpe": is_full_sharpe,  # ← キー名が "is_full_sharpe"
}
```
