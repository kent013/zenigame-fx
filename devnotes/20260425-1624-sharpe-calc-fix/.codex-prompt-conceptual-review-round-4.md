Round 3 からの対応マトリクスと修正内容のみ記載します（使命・禁止事項の再挿入は省略）。

## Round 3 指摘への対応

| # | 指摘 | 対応 |
|---|---|---|
| Critical-1 | `BacktestMetrics.sharpe` フィールドを置換すると非 AF consumer（report/grid/ensemble/walk_forward）が壊れる | Phase 1A は `BacktestMetrics` に `trade_sharpe_raw` を追加、既存 `sharpe` フィールドは一時残存（compute_metrics 実行後は None に設定）。報告系の改名は Phase 2 |
| Critical-2 | `get_sharpe(row)` v1 挙動が曖昧 - None だと静かに no-metric 扱いになる | accessor を2本立てに変更: `get_trade_sharpe(row)`（比較用、v1 で必ず例外）+ `get_legacy_bar_sharpe(row)`（閲覧専用、比較禁止、H2 検証用） |
| Open-Q | same-bar 複数 fill 時の `equity_at_entry` 扱い | bar 開始時の pre-fill equity を全注文共通に固定すると明記 |

## 更新後の概念設計（Round 4 レビュー対象）

# 概念設計: Sharpe Ratio 計算の根本修正

## 採用する指標定義

**trade_sharpe_raw**:
- return 単位 = `net_pnl_base_currency / equity_at_entry`（bar 開始時 pre-fill equity）
- `trade_sharpe_raw = mean(trade_returns) / std(trade_returns)`（annualize なし）

### `equity_at_entry` SSOT

- `Trade` オブジェクトに `equity_at_entry: float` を追加（broker mock + engine が bar 開始時 pre-fill equity を記録）
- same-bar 複数 fill 時: bar 開始時の pre-fill equity を全注文共通に固定（fill 順依存不可）
- `compute_metrics(trades)` は `trade.equity_at_entry` を参照

### dispatch matrix

| 条件 | trade_sharpe_raw |
|---|---|
| `trade_count < trade_count_min_for_sharpe` (default 30, config 化) | `None` |
| std(trade_returns) == 0 | `None` |
| else | mean / std |

N_MIN=30 は Lo 2002 参照。config 化し Phase 1B で N=20/30/50 感度分析。

### cross-pair の None 処理

per-pair `trade_sharpe_raw=None` → fail-fast（skip 禁止）

## BacktestMetrics の変更

- `trade_sharpe_raw: Optional[float]` フィールドを追加
- 既存 `sharpe: Optional[float]` は一時残存（非 AF consumer の後方互換）。ただし compute_metrics 実行後は `sharpe=None` に設定し旧値が紛れ込まないよう
- `sharpe_calc_version: str` フィールドを追加
- Phase 1A の AF consumer は `trade_sharpe_raw` のみを参照、`sharpe` フィールドは参照しない
- `report.py` / `ensemble_report.py` 等の外部公開フィールド名変更は Phase 2

## archive 後方互換 + canonical accessor

- `sharpe` 列（旧値）残存、`trade_sharpe_raw` を別名列として追加
- `sharpe_calc_version` 追加

### accessor 2本立て

- `GenomeArchive.get_trade_sharpe(row)` — **比較用**: v2 → `row["trade_sharpe_raw"]`。v1 → **必ず例外**（静かな no-metric 混入を防ぐ）
- `GenomeArchive.get_legacy_bar_sharpe(row)` — **閲覧専用**（比較禁止）: v1 → `row["sharpe"]`。H2 検証など参照専用

全 consumer（GA fitness / Stage Gate / cross_pair / calibrate_gate / run_ga.py / run_alpha_sieve.py 含む）を `get_trade_sharpe()` 経由に切り替え。`row["sharpe"]` 直接参照は廃止。

## Phase 区切り

| Phase | 内容 |
|---|---|
| Phase 1A | `Trade.equity_at_entry` 追加 + `trade_sharpe_raw` 実装 + `BacktestMetrics` 更新 + 全 consumer accessor 切替 + reader hard-fail + archive schema |
| Phase 1A | DSR: 呼び出し側変更なし、「v1 Sharpe 前提」コメントのみ |
| Phase 1B | replay で H1/H2 検証 + N_MIN 感度分析 + live_criteria.sharpe_min 暫定再校正（replay 根拠必須） |
| Phase 2 | daily Sharpe / DSR 意味論 / report 系命名 / live_criteria 本格校正 |

## 値伝搬チェックリスト (7点)

1. config: `trade_count_min_for_sharpe` が `default.yaml` に追加
2. metrics API: `compute_metrics()` が `trade_sharpe_raw` を返す
3. genome.meta: `trade_sharpe_raw` が meta dict に注入
4. archive schema: `GENOMES_SCHEMA` に `trade_sharpe_raw` + `sharpe_calc_version` 定義
5. row_template / collect / flush: 4 点セット完結
6. accessor hard-fail: v1 で `get_trade_sharpe()` が例外、全 consumer が accessor 経由
7. logger: 新値伝搬に対応するログ追加

## DoD — Phase 1A

- [ ] `Trade.equity_at_entry` フィールド追加（broker mock + engine が bar 開始時 pre-fill equity を記録）
- [ ] `compute_metrics` に `trade_sharpe_raw` + `trade_count_min_for_sharpe` 引数追加
- [ ] `BacktestMetrics.trade_sharpe_raw` / `sharpe_calc_version` フィールド追加、`sharpe` は None に設定
- [ ] 既存 bar-level Sharpe 計算削除
- [ ] `get_trade_sharpe(row)`（v1 例外）+ `get_legacy_bar_sharpe(row)`（閲覧専用）実装
- [ ] GA fitness / cross_pair / Alpha Sieve / calibrate_gate / run_ga.py / run_alpha_sieve.py を accessor 経由に変更
- [ ] cross-pair `None` fail-fast
- [ ] archive schema: `trade_sharpe_raw` + `sharpe_calc_version` 追加
- [ ] DSR コメント追記
- [ ] 値伝搬 7 点確認
- [ ] tests 更新
