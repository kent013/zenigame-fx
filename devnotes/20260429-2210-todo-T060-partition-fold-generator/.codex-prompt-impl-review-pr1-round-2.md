# Round 2 修正対応マトリクス (T060 PR 1 impl-review)

Round 1 [REQUEST_CHANGES] の Blocker 3 件 + 関連 NIT を以下の通り反映しました。 修正後の状態を再評価してください.

## Round 1 → Round 2 対応表

| Round 1 [区分] 内容 | Round 2 修正対応 |
|---|---|
| [Blocker H1] `PeriodLabel.FOLD_TRAIN/FOLD_EMBARGO/FOLD_TEST` が dead code 化、 fold suffix base が文字列リテラル直書き (`f"fold_{k}_train"` 等) | `src/alpha_factory/partition.py` の `FoldGenerator.generate` で `f"fold_{k}_{PeriodLabel.FOLD_TRAIN.value}"` 等に変更. enum 値経由で SSOT 一貫性確保 (= 文字列リテラル散在を排除). 生成される label 文字列は不変 (= 既存 test 互換維持) |
| [Blocker H2] `PartitionGenerator.generate` のスライス順序入替 regression 検出力不足 | `tests/alpha_factory/test_partition.py` に `test_generate_periods_match_expected_label_order_and_week_lengths` 追加. 10 領域の (label, 週数) を fix tuple `((STAGE_B, 62), (STAGE_A, 8), (EMBARGO_AFTER_A, 1), (STAGE_C_LITE_1, 6), (EMBARGO_AFTER_C_LITE_1, 1), (STAGE_C_LITE_2, 6), (EMBARGO_AFTER_C_LITE_2, 1), (STAGE_C_LITE_3, 6), (EMBARGO_AFTER_C_LITE_3, 1), (STAGE_C, 12))` で順次検証. 順序入替 / 週数誤りで明示 fail |
| [Blocker H5] helper (`_canonical_24m_window` / `_canonical_stage_b`) 依存テストの同方向バイアス懸念 | `tests/alpha_factory/test_partition.py` に `test_generate_period_starts_match_cumulative_week_offsets_inline_window` 追加. inline で `datetime(2025, 1, 1, 0, 0, 0, tzinfo=UTC)` から window を直接構築 (helper 非依存). 各 period の start を window_start からの累積週数オフセット `(0, 62, 70, 71, 77, 78, 84, 85, 91, 92)` と比較 + 末尾 period.end == window.end 確認 |

## 主要修正箇所 (差分抜粋)

### 1. partition.py FoldGenerator.generate の enum 経由化

```python
folds.append(
    Fold(
        fold_index=k,
        train=Period(
            start=train_start,
            end=train_end,
            label=f"fold_{k}_{PeriodLabel.FOLD_TRAIN.value}",
        ),
        embargo=Period(
            start=train_end,
            end=embargo_end,
            label=f"fold_{k}_{PeriodLabel.FOLD_EMBARGO.value}",
        ),
        test=Period(
            start=embargo_end,
            end=test_end,
            label=f"fold_{k}_{PeriodLabel.FOLD_TEST.value}",
        ),
    )
)
```

### 2. test_partition.py 追加 test 1: 順序 + 週数 fix list

```python
def test_generate_periods_match_expected_label_order_and_week_lengths() -> None:
    partition = PartitionGenerator.generate(_canonical_24m_window())
    expected: tuple[tuple[PeriodLabel, int], ...] = (
        (PeriodLabel.STAGE_B, 62),
        (PeriodLabel.STAGE_A, 8),
        (PeriodLabel.EMBARGO_AFTER_A, 1),
        (PeriodLabel.STAGE_C_LITE_1, 6),
        (PeriodLabel.EMBARGO_AFTER_C_LITE_1, 1),
        (PeriodLabel.STAGE_C_LITE_2, 6),
        (PeriodLabel.EMBARGO_AFTER_C_LITE_2, 1),
        (PeriodLabel.STAGE_C_LITE_3, 6),
        (PeriodLabel.EMBARGO_AFTER_C_LITE_3, 1),
        (PeriodLabel.STAGE_C, 12),
    )
    actual = partition.all_periods
    assert len(actual) == len(expected)
    for i, (period, (expected_label, expected_weeks)) in enumerate(
        zip(actual, expected)
    ):
        assert period.label == expected_label.value
        assert period.end - period.start == timedelta(weeks=expected_weeks)
```

### 3. test_partition.py 追加 test 2: helper 非依存 inline 検証

```python
def test_generate_period_starts_match_cumulative_week_offsets_inline_window() -> None:
    window_start = datetime(2025, 1, 1, 0, 0, 0, tzinfo=UTC)
    window_end = window_start + timedelta(weeks=104)
    partition = PartitionGenerator.generate(
        EpochWindow(start=window_start, end=window_end)
    )
    expected_offsets_weeks = (0, 62, 70, 71, 77, 78, 84, 85, 91, 92)
    actual = partition.all_periods
    assert len(actual) == len(expected_offsets_weeks)
    for i, (period, offset_weeks) in enumerate(zip(actual, expected_offsets_weeks)):
        expected_start = window_start + timedelta(weeks=offset_weeks)
        assert period.start == expected_start
    assert actual[-1].end == window_end
```

## DoD 結果 (実測、 Round 1 で証跡未取得だった項目)

- pytest tests/alpha_factory/test_partition.py: **36 passed** (= 既存 34 + 新規 2)
- pytest tests/alpha_factory/ + tests/scripts/: **1244 passed** (regression 0)
- ruff check src/ tests/: **All checks passed**
- mypy src/: **Success: no issues found in 104 source files**

## レビュー依頼

Round 1 で挙げた Blocker 3 件 (H1/H2/H5) が **構造的に解消されているか** 評価し、 APPROVED または追加 CHANGES_REQUESTED を返してください.

### 求める出力

```
[VERDICT] APPROVED または CHANGES_REQUESTED

[Critical]
1. (なければ「なし」)

[Warning]
1. (なければ「なし」)

[Suggestion]
1. (なければ「なし」)
```

簡潔に Fact / Interpretation 分離で記述してください.
