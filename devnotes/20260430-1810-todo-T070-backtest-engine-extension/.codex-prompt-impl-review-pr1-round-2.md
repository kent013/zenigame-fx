# Round 2: 一次証跡を提示したうえで再検証してください

Round 1 で「ファイル本文を直接検証していない」という根拠で全 hypothesis が INCONCLUSIVE 判定でした。 本 Round では実装ファイル本文 + 設計 SSOT 該当行をインライン提示し、 一次証跡ベースで H1-H5 を CONFIRMED / REJECTED / INCONCLUSIVE の 3 値で再判定してもらいます。

**ファイル読み込みは許可** されています。 worktree `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T070-pr1/` 配下を必要に応じて `Read` で参照してください。 以下の inline 提示は突合のための便宜です。

---

## 1. `src/backtest/session_block.py` (新規ファイル全文、 行番号付)

```python
$(該当 src/backtest/session_block.py の本文を Read で確認してください)
```

該当ファイルパス: `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T070-pr1/src/backtest/session_block.py`

主要シンボル:
- L52-65: `SessionBlockBucket` Literal、 `BLOCK_BUCKET_RANGES_UTC` Final dict、 `_BUCKETS` (dict insertion 順 SSOT)、 `_M1_EXPECTED_BAR_COUNT=480`
- L68-140: `SessionBlock` dataclass (frozen) + `__post_init__` で 5 invariant (bar_count >= 0 / trade_count >= 0 / spread_cost_total >= 0 / holding_cost_total >= 0 / `pnl_before_costs == pnl_net + spread_cost_total + holding_cost_total`)
- L143-176: `compute_bucket_for_bar` (tz validation + RuntimeError defense-in-depth)
- L179-190: `compute_bucket_for_trade` (= compute_bucket_for_bar(trade.exit_time))
- L193-272: `aggregate_session_blocks` (date universe = bars + trades の union)
- L275-330: `apply_spread_stress` (NaN/Inf/<1.0 raise + multiplier=1 no-op)

## 2. `src/broker/orders.py` 改造箇所 (該当 diff)

```diff
@@ src/broker/orders.py:33-46 ↓ 改造後 (33-54) ↓
 @dataclass(frozen=True)
 class Trade:
     position_id: int
     instrument: str
     side: PositionSide
     units: int
     entry_price: Decimal
     entry_time: datetime
     exit_price: Decimal
     exit_time: datetime
     pnl: Decimal
     exit_reason: ExitReason
     equity_at_entry: Decimal = Decimal(0)  # T-sharpe: Position から伝搬
+    # T070: cascade port v2 Phase 2 配線.
+    # 概念設計 §3.4.0 / §3.5 SSOT 参照. spread_cost は entry/exit spread 推定値で
+    # **Trade.pnl 未反映** (監査・spread stress 用記録)、 holding_cost は
+    # `MockBroker._holding_cost_by_position[pos.id]` の転記で **既に Trade.pnl に
+    # 控除済** (= raw_pnl - holding_cost). 不変条件:
+    # `Trade.pnl + Trade.holding_cost == raw_pnl (= price-diff pnl)`、
+    # `spread_cost` は raw_pnl と独立に記録される.
+    spread_cost: Decimal = Decimal(0)
+    holding_cost: Decimal = Decimal(0)
```

該当ファイルパス: `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T070-pr1/src/broker/orders.py`

新 field は **dataclass field 順序末尾** に追加され、 default=Decimal(0) のため既存 keyword 構築は無破壊。

## 3. `src/broker/mock.py` 改造箇所 (該当 diff、 主要 `_close_one` + 新設 `_compute_trade_spread_cost`)

```diff
@@ src/broker/mock.py:400-432 ↓ 改造後 ↓
 def _close_one(self, position_id: int, bar: PriceBar, exit_kind: str, reason: ExitReason) -> Trade | None:
     pos = self._positions.pop(position_id, None)
     if pos is None:
         return None
     exit_price = self._exit_price(pos.side, bar, exit_kind)
     raw_pnl = self._realized_pnl(pos, exit_price)
     # T009: 累積 holding cost を pnl から差し引いて Trade.pnl に net_pnl として記録
     # cash は apply_bar_holding_cost で既に cost を減算済みなので、raw_pnl のみ加算
     # （二重控除を回避）。
     cost_accum = self._holding_cost_by_position.pop(pos.id, Decimal(0))
     net_pnl = raw_pnl - cost_accum
+    # T070: entry/exit spread を別 field 化 (Trade.pnl 未反映、 監査・stress 用).
+    spread_cost = self._compute_trade_spread_cost(pos, bar)
     self._cash += raw_pnl
     trade = Trade(
         position_id=pos.id,
         instrument=pos.instrument,
         side=pos.side,
         units=pos.units,
         entry_price=pos.entry_price,
         entry_time=pos.entry_time,
         exit_price=exit_price,
         exit_time=bar.bar_time,
         pnl=net_pnl,
         exit_reason=reason,
         equity_at_entry=pos.equity_at_entry,
+        spread_cost=spread_cost,
+        holding_cost=cost_accum,
     )
     self._trades.append(trade)
     self._invalidate_snapshot_cache()
     return trade

+@staticmethod
+def _compute_trade_spread_cost(pos: Position, bar: PriceBar) -> Decimal:
+    """exit bar の bid/ask spread × 2 を entry+exit 往復近似 (Roll 1984)."""
+    exit_spread = bar.spread_close
+    if exit_spread < 0:
+        exit_spread = Decimal(0)
+    spread_cost_per_unit = exit_spread * Decimal(2)
+    return abs(Decimal(pos.units)) * spread_cost_per_unit
```

該当ファイルパス: `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T070-pr1/src/broker/mock.py`

ポイント:
- `self._cash += raw_pnl` 行は **不変** (= F19 二重計上防御、 既存挙動維持)
- `cost_accum` は既に `apply_bar_holding_cost` で `_cash` から控除済 (L319 `self._cash -= total_cost`)
- 新 field 2 つを Trade 構築時に **keyword 引数として追加渡し**

## 4. `src/backtest/engine.py` 改造箇所 (該当 diff)

```diff
@@ src/backtest/engine.py imports ↓
-from src.broker.mock import MockBroker
+from src.backtest.session_block import SessionBlock, aggregate_session_blocks
+from src.broker.mock import MockBroker

@@ src/backtest/engine.py:74-78 ↓ 改造後 (74-83) ↓
 @dataclass
 class BacktestResult:
     config: BacktestConfig
     trades: list[Trade]
     equity_curve: list[tuple[datetime, Decimal]] = field(default_factory=list)
+    # T070: cascade port v2 Phase 2 配線. session_blocks は run_backtest 内で 1 回
+    # 計算し、 caller は読むのみ (再計算禁止、 概念設計 §6.4 SSOT). default_factory で
+    # backward-compat を維持 (= 既存 caller への影響なし).
+    session_blocks: tuple[SessionBlock, ...] = field(default_factory=tuple)

@@ src/backtest/engine.py:185-196 ↓ run_backtest 末尾 ↓
     logger.info(
         "backtest.finished",
         instrument=config.instrument,
         bars=len(bars_list),
         trades=len(broker.trades),
         final_equity=str(broker.snapshot().equity),
         session_close_drop_open_count=session_close_drop_open_count,
         session_close_drop_pending_count=session_close_drop_pending_count,
         first_drop_open_bar_time=first_drop_open_bar_time,
         negative_equity_drop_open_count=negative_equity_drop_open_count,
     )
-    return BacktestResult(config=config, trades=broker.trades, equity_curve=equity_curve)
+
+    # T070: SessionBlock 集計 (Round 1 [C3] transport SSOT). caller 再計算禁止 (§6.4).
+    session_blocks = aggregate_session_blocks(bars_list, broker.trades)
+
+    return BacktestResult(
+        config=config,
+        trades=broker.trades,
+        equity_curve=equity_curve,
+        session_blocks=session_blocks,
+    )
```

該当ファイルパス: `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T070-pr1/src/backtest/engine.py`

## 5. caller 経路 grep 結果 (= H2 / Phase1-PR-CHECK-F9 一次証跡)

```
$ grep -rn "Trade(" src/ tests/ scripts/ --include="*.py" | grep -v "OrderSignal\|class Trade\|TradeRecord"
src/broker/mock.py:416:        trade = Trade(                # T070 で改造、 keyword 構築
tests/broker/test_orders.py:34:    trade = Trade(**_trade_kwargs())  # T070 新 test (kwargs)
tests/broker/test_orders.py:44:    trade = Trade(**kwargs)
tests/broker/test_orders.py:51:    trade = Trade(            # full keyword 12 fields
tests/broker/test_orders.py:75:    trade = Trade(**_trade_kwargs())
tests/backtest/test_session_block.py:55:    return Trade(    # T070 新 test helper (full keyword)
tests/backtest/test_session_block.py:296:    trade = Trade(  # F16 test (full keyword)
tests/backtest/test_advanced_metrics.py:14:    return Trade( # 既存、 keyword 構築
tests/backtest/test_metrics.py:21:    return Trade(          # 既存、 keyword 構築
tests/alpha_factory/test_stage_gate.py:854:    overnight_trade = Trade(    # 既存、 keyword
tests/alpha_factory/test_stage_gate.py:867:    overnight_trade2 = Trade(   # 既存、 keyword
```

```
$ grep -rn "BacktestResult(" src/ tests/ scripts/ --include="*.py"
src/backtest/engine.py:205:    return BacktestResult(                  # T070 改造、 keyword
tests/backtest/test_engine.py:411:    result = BacktestResult(config=config, trades=[], equity_curve=[])  # T070 新 test (default_factory)
tests/alpha_factory/test_stage_gate.py:895:    result = BacktestResult( # 既存、 keyword
```

→ 全 caller が keyword 構築。 既存 fixture は新 field を渡さないが default=Decimal(0) / default_factory=tuple で正しく通る。

## 6. テスト pass 一次証跡

```
$ uv run pytest tests/backtest/test_session_block.py tests/broker/test_orders.py tests/broker/test_mock.py tests/backtest/test_engine.py -x --tb=short
============================== 56 passed in 0.10s ==============================

$ uv run pytest --tb=short -q
2412 passed, 1 skipped, 1 xfailed, 20 warnings in 50.34s

$ uv run ruff check src/backtest/session_block.py src/broker/orders.py src/broker/mock.py src/backtest/engine.py tests/backtest/test_session_block.py tests/broker/test_orders.py tests/broker/test_mock.py tests/backtest/test_engine.py
All checks passed!

$ uv run mypy src/backtest/session_block.py src/broker/orders.py src/broker/mock.py src/backtest/engine.py
Success: no issues found in 4 source files
```

## 7. 主要 unit test の対応行 (H1-H5 ↔ test_id)

| 仮説 | 検証 test |
|---|---|
| H1 (aggregate SSOT) | `tests/backtest/test_session_block.py::TestAggregateSessionBlocks::test_F22_pnl_net_uses_spread_subtraction` (L266-289)、 `test_F12_invariant_holds_for_aggregated` (L249-264)、 `test_F7_empty_block_generated_when_no_trades` (L226-235) |
| H2 (Trade field 破壊なし) | `tests/broker/test_orders.py::test_F23_trade_default_costs_are_zero` (L31-35)、 `test_F23_existing_keyword_construction_remains_compatible` (L48-67) |
| H3 (cash 不変) | `tests/broker/test_mock.py::test_F19_no_double_counting_of_holding_cost` (L109-137)、 `test_F13_invariant_pnl_plus_holding_equals_raw_pnl` (L87-106) |
| H4 (transport SSOT) | `tests/backtest/test_engine.py::test_F4_run_backtest_returns_session_blocks`、 `test_F4_default_session_blocks_is_empty_tuple` |
| H5 (apply_spread_stress 代数) | `tests/backtest/test_session_block.py::TestApplySpreadStress::test_F22_multiplier_2_doubles_spread_cost`、 `test_F22_multiplier_1_is_noop`、 `test_F5_multiplier_below_one_raises`、 `test_F5_multiplier_nan_raises`、 `test_F5_multiplier_infinity_raises` |

## 再検証要請

上記一次証跡 + worktree 内ファイル直接 Read を併用し、 H1-H5 を CONFIRMED / REJECTED / INCONCLUSIVE で再判定してください。 INCONCLUSIVE の場合は「何が verify できればその判定に至るか」を具体的に明記してください。

最終判定 (APPROVED / NEEDS_REVISION / REJECTED) を Round 1 と同じフォーマットで出力してください。
