## Round 1 → Round 2 改訂内容を反映済 — 再レビュー依頼

Codex Round 1 で指摘された [Critical] 3 件 + [Warning] 5 件 + [Suggestion] 3 件すべてに対応しました。 同じフォーマット (`[VERDICT] / [Critical] / [Warning] / [Suggestion]`) で再評価をお願いします。 残修正が無ければ APPROVED で結構です。

### Round 1 → Round 2 改訂内容

| Round 1 指摘 | 改訂対応 |
|---|---|
| [C1] WR neutral 0.5 が top-level で実質無効 (compute_session_blocks が trade>=1 block しか生成しない) | `evaluate_canonical_five` の入力契約に `business_day_universe: dict[SessionBucket, frozenset[int]]` を追加。 T070 が「評価期間で発生し得た全 (bucket, business_day_index) ペア」 を渡し、 trade=0 でも空 block (count=0, pnl=0) を生成 → `compute_session_block_win_rate_worst` で 0.5 neutral 規約適用 |
| [C2] top-level no-raise 契約が `_validate_utc_aware` 例外伝播で破れる | no-raise 契約の境界を明文化: dataclass 構築時 (`__post_init__`) は raise する (caller 責務、 既存 frozen dataclass パターン)、 `evaluate_canonical_five` の no-raise 契約は **有効 instance 受領後の内部処理** のみ。 例外 → InfeasibleReasonCode 変換マップを明記 |
| [C3] compute_max_dd の値域・ゼロ除算 | 仕様厳密化: `max_dd = max_t max(0, (running_max - eq) / max(running_max, MAX_DD_DENOM_FLOOR))`、 running_max <= 0 の bar は drawdown 計算からスキップ、 全 bar negative なら max_dd=1.0 sentinel、 値域 [0, 1] に clip。 docstring + test 名に `_clipped_to_unit_interval` 含める |
| [W1] `_validate_utc_aware` が基底例外を raise (例外階層の一貫性不足) | `_validate_utc_aware(name, dt, *, exc_class)` シグネチャに変更、 caller が派生例外クラス (TradeRecordInvalidError / BarEquityInvalidError / ThresholdsInvalidError) を指定。 `ThresholdsInvalidError` を新規追加 |
| [W2] 計算量 `O(N+B+3*Q)` が実装と乖離 | 正確化: `O(N + B + Σ_b n_b * Q)`。 fx 想定: N~5000, B~30,000, Σn_b~750, Q=5 → 1 評価 ~0.5ms (Python dict + math.fsum で OK) |
| [W3] DoD「Enum + 4 dataclass」 が乖離 | 正確化: Enum 2 (`SessionBucket`, `InfeasibleReasonCode`) + dataclass 7 (TradeRecord, BarEquityPoint, BarEquitySeries, SessionBlockSummary, CanonicalFiveThresholds, InvariantFlags, CanonicalFiveResult) + Provider protocol 1 + 例外 4 |
| [W4] C2 grep が文字列経由動的参照を取りこぼす | T064 PR DoD に **runtime wiring smoke 1 本** (`evaluate_stage_*` を実際に呼んで `evaluate_canonical_five` が configured に走ることを確認する E2E smoke) を申し送り |
| [W5] Newey-West 引用の粒度不足 | 学術引用 full citation 形式: `Newey, W. K., & West, K. D. (1987). ... Econometrica, 55(3), 703-708.` (Lo / Bailey も同様に full citation 化) |
| [S1] `infeasible_reason_codes` を StrEnum 化 | `InfeasibleReasonCode` StrEnum を新設、 `infeasible_reason_codes: frozenset[InfeasibleReasonCode]` で型強制。 prefix 規約を型で固定 (STRATEGIC_*, INPUT_*) |
| [S2] provider 例外注入テスト追加 | `test_evaluate_canonical_five_provider_exception_converted_to_reason_code` + `test_evaluate_canonical_five_no_raise_contract_for_invalid_bucket_validator` を test 計画に追加 |
| [S3] max_dd 値域明記 | docstring に「値域 [0.0, 1.0]、 1.0=full drawdown sentinel」 を明記、 test 名 `_clipped_to_unit_interval` / `_returns_one_when_running_max_remains_non_positive` 等含む |

---

### Round 2 詳細設計 — 主要改訂セクション抜粋

#### 1. business_day_universe 入力 + WR neutral 0.5 経路有効化 (Round 1 [C1])

```python
def compute_session_blocks(
    trades: Iterable[TradeRecord],
    business_day_universe: dict[SessionBucket, frozenset[int]],
) -> dict[SessionBucket, list[SessionBlockSummary]]:
    """
    1. business_day_universe[bucket] = frozenset[business_day_index] で full grid を初期化
       (各 (bucket, day) ペアで pnl=0.0, count=0 の空 block を生成)
    2. trades を走査し、 (trade.session_bucket, trade.business_day_index) の block に
       pnl_net を加算、 count を +1
    3. trade.business_day_index が business_day_universe[trade.session_bucket] に含まれない
       場合、 universe を拡張 (T070 出力の整合性確保)
    4. 各 bucket 内を business_day_index 昇順でソート
    """

def evaluate_canonical_five(
    trades, bars, thresholds,
    business_day_universe: dict[SessionBucket, frozenset[int]],  # 必須引数
    *,
    bucket_validator=None,
    q_bartlett=5,
):
    ...
```

#### 2. no-raise 契約の境界明文化 + 例外→reason code 変換マップ (Round 1 [C2])

```python
def evaluate_canonical_five(...) -> CanonicalFiveResult:
    """
    no-raise 契約の境界:
    - dataclass 構築時 (__post_init__) は raise する (caller 責務)
    - evaluate_canonical_five の no-raise は 有効 instance 受領後の内部処理 のみ

    例外 → InfeasibleReasonCode 変換マップ:
    - BarEquityInvalidError → INPUT_INVALID_BAR_SERIES
    - bucket_validator.expected_*() 例外 → INPUT_BUCKET_VALIDATOR_EXCEPTION
    - bucket_validator mismatch → INPUT_BUCKET_ATTRIBUTION_MISMATCH
    - len(trades) == 0 → INPUT_EMPTY_TRADE_LIST
    - business_day_universe 任意 bucket key で空集合 → INPUT_EMPTY_BUSINESS_DAY_UNIVERSE
    """
```

#### 3. compute_max_dd 値域 [0,1] clip + ゼロ除算保護 (Round 1 [C3])

```python
MAX_DD_DENOM_FLOOR: Final[float] = 1e-12

def compute_max_dd(bars: BarEquitySeries) -> float:
    """値域 [0.0, 1.0] (1.0=full drawdown sentinel).

    手順:
    1. 各 bar 走査、 running_max = max(running_max, equity_t)
    2. running_max <= 0 の bar は drawdown 計算からスキップ
    3. running_max > 0 の bar:
        bar_dd = max(0, (running_max - equity_t)) / max(running_max, MAX_DD_DENOM_FLOOR)
    4. max_dd = max_t bar_dd
    5. 全 bar negative なら max_dd = 1.0
    6. final clip: min(1.0, max(0.0, max_dd))
    """
```

#### 4. 例外階層の一貫性 (Round 1 [W1])

```python
def _validate_utc_aware(
    name: str, dt: datetime, *, exc_class: type[CanonicalMetricsInputError]
) -> None:
    if dt.tzinfo is None:
        raise exc_class(f"{name} must be timezone-aware datetime, got naive: {dt!r}")
    ...

# caller 側 __post_init__:
class TradeRecord:
    def __post_init__(self):
        _validate_utc_aware(..., exc_class=TradeRecordInvalidError)
        ...
class BarEquitySeries:
    def __post_init__(self):
        _validate_utc_aware(..., exc_class=BarEquityInvalidError)
        ...
class CanonicalFiveThresholds:
    def __post_init__(self):
        if value <= 0.0:
            raise ThresholdsInvalidError(...)
```

#### 5. InfeasibleReasonCode StrEnum (Round 1 [S1])

```python
class InfeasibleReasonCode(StrEnum):
    # synthesis § 6.6 戦略的 fail-fast
    STRATEGIC_SESSION_CLOSE_DROP = "strategic_session_close_drop"
    STRATEGIC_NEGATIVE_EQUITY_DROP_OPEN = "strategic_negative_equity_drop_open"
    # engine 検出の入力不正
    INPUT_INVALID_BAR_SERIES = "input_invalid_bar_series"
    INPUT_BUCKET_ATTRIBUTION_MISMATCH = "input_bucket_attribution_mismatch"
    INPUT_BUCKET_VALIDATOR_EXCEPTION = "input_bucket_validator_exception"
    INPUT_EMPTY_TRADE_LIST = "input_empty_trade_list"
    INPUT_EMPTY_BUSINESS_DAY_UNIVERSE = "input_empty_business_day_universe"
    INPUT_NON_FINITE_PNL = "input_non_finite_pnl"

class InvariantFlags:
    infeasible_reason_codes: frozenset[InfeasibleReasonCode]  # 型で prefix 強制
```

#### 6. パフォーマンス計算量正確化 (Round 1 [W2])

`O(N + B + Σ_b n_b * Q)`、 fx 想定 1 評価 ~0.5ms

#### 7. DoD 正確化 (Round 1 [W3])

- Enum 2 + dataclass 7 + Provider protocol 1 + 例外 4 + helper 関数群 + top-level entry

#### 8. T064 PR runtime wiring smoke 申し送り (Round 1 [W4])

T064 PR DoD に「`evaluate_stage_*` を実際に呼んで `evaluate_canonical_five` が configured に走ることを確認する E2E smoke 1 本」 追加

#### 9. 学術引用 full citation 形式 (Round 1 [W5])

- Lo, A. W. (2002). "The Statistics of Sharpe Ratios." Financial Analysts Journal, 58(4), 36-52.
- Newey, W. K., & West, K. D. (1987). "A Simple, Positive Semi-Definite, Heteroskedasticity and Autocorrelation Consistent Covariance Matrix." Econometrica, 55(3), 703-708.
- Bailey, D. H., & López de Prado, M. (2014). "The Deflated Sharpe Ratio." Journal of Portfolio Management, 40(5), 94-107. ([HAC 必須要件は要確認])

#### 10. テスト追加 (Round 1 [S2] / [S3])

- `test_evaluate_canonical_five_provider_exception_converted_to_reason_code`
- `test_evaluate_canonical_five_provider_mismatch_records_attribution_mismatch_reason`
- `test_evaluate_canonical_five_no_raise_contract_for_invalid_bucket_validator`
- `test_evaluate_canonical_five_empty_business_day_universe_records_input_reason`
- `test_evaluate_canonical_five_zero_trade_blocks_get_neutral_half_win_rate` (Round 1 [C1])
- `test_max_dd_clipped_to_unit_interval_when_equity_drops_to_zero`
- `test_max_dd_returns_one_when_running_max_remains_non_positive`
- `test_max_dd_skips_bars_with_non_positive_running_max`
- `test_max_dd_zero_division_protected_by_denom_floor`

---

### 残論点 (詳細設計 Round 2 で確定)

1. `business_day_universe` の caller 構築責任 — T070 backtest engine が evaluation period (Period from T060) と DST/holiday boundary (T072) を結合して構築する。 T064 PR で Period → business_day_universe 変換 helper を追加する申し送り
2. `MAX_DD_DENOM_FLOOR=1e-12` の妥当性 — equity は通常 ~50,000 JPY ~ 50M JPY (initial_cash=100k 想定で 0.5x ~ 500x)、 1e-12 は十分小さく ゼロ除算保護として妥当

---

レビュー観点 (Round 1 と同じ): 数式 § 6 厳密準拠 / 入出力契約 / 設計判断 6 件 / no-raise 契約境界 / max_dd 値域 / 例外階層 / Phase 分離 / C2 grep / 学術引用 / その他盲点。

`[VERDICT] / [Critical] / [Warning] / [Suggestion]` で返してください。
