【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。ファイル読み込みは許可。

【レビュー重点項目 — 転記漏れパターン】
zenigame（姉妹プロジェクト）では「値の転記漏れ・伝搬漏れ・記録漏れ」が繰り返し発生した。zenigame-fx でも以下を重点的にチェックすること:
1. 新規パラメータ/カラム追加時: config 定義→GaConfig 読み込み→genome.meta 注入→consumer 参照の 4 段階が全て接続されているか
2. archive 記録: GENOMES_SCHEMA にフィールド定義→_create_row_template 初期値→collect_stage_* 書き込み→flush 出力の 4 点セット
3. 関数パラメータ: 新規パラメータ追加時に全呼び出し元で引数が渡されているか
4. ログ出力: 新規の値伝搬に対応する logger 出力が追加されているか

【監査・レビュー時の必須チェック — False-positive 再発防止】
以下の discipline を全レビュー・監査で遵守すること。

### C1. Design-first 原則 (code grep より先に docs を読む)
コードの bug / 設計欠陥について claim を出す前に、以下を verify:
- docs/alpha_factory/ 配下の該当セクション（stage-gates.md / clause-architecture.md 等）を Read で確認
- devnotes/ 配下の関連 T-number（関数コメントや git log に出てくる T-xxx）を ls devnotes/ | grep T{xxx} で探して読む
- git log --all -S "識別子" で該当 code の変更履歴を辿る
- 該当機能と並行計算される可能性のある経路を grep で広く検索

### C2. 「X が無い = バグ」禁止ルール
関数 Y の signature / body に識別子 X が無いことを根拠に bug 判定してはならない。別経路での計算を grep で広く検索してから。

### C3. Collider bias 必須チェック（相関分析時）
2 つの変数 A, B の相関を計算して report する場合、Conditioning set と collider の有無を必ず明記。

### C4. 前提明示ルール
Claim / verdict を出す前に、prompt の先頭に「本分析の前提」を bullet でリスト化し、各前提が verified であることを明示。

### C5. 並列 sub-agent の framing 独立性チェック
独立検証のつもりで同じ framing の sub-agent を N 個回しても、それは 1 つの誤読の N 倍冗長実行。

### C6. Fact / Interpretation 分離ルール
「観察された事実 (Facts)」と「解釈・推論 (Interpretations)」を明確に区別する。1 文で mix しない。

### C7. Sample size ガード
n < 30 の相関 claim は明示的に因果解釈を避ける。n < 10 は相関 claim 禁止（observation のみ許可）。

### C8. INCONCLUSIVE を第一級 verdict に
INCONCLUSIVE は CONFIRMED / REJECTED と同格の選択肢。データ不足は正当な結論。

### C9. Falsification-first プロンプト
Round 1 は「この hypothesis の反証を探せ」という指示を先に書く。confirmation bias を強制しない。

---

# T061 PR 1 実装レビュー (Round 1) — Falsification-first

## 役割
あなたは zenigame-fx Alpha Factory の cascade port v2 Phase 2 配線、 T061 (canonical 5 engine) PR 1 実装レビュアーである。 詳細設計 (3 round 全 APPROVED 済) と完全照合し、 設計違反・実装不整合・回帰リスクを falsification-first で探せ。

## レビュー対象 (worktree: todo-T061-pr1, branch: todo/T061-pr1)
- 新規実装: `src/alpha_factory/canonical_metrics.py`
- 新規テスト: `tests/alpha_factory/test_canonical_metrics.py`
- 詳細設計: `devnotes/20260429-2300-todo-T061-canonical-five-engine/detailed-design.md` (1038 行、 全 round APPROVED 済)
- 概念設計: `devnotes/20260429-2300-todo-T061-canonical-five-engine/conceptual-design.md`
- 依存先 (touch しないが import する可能性あり): `src/alpha_factory/partition.py` (T060 main merge 済)

## 本レビューの前提 (C4 適用)
- worktree は `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T061-pr1`、 branch `todo/T061-pr1`、 base = main `e7acb96` (T060 完了時点)
- T058 / T059 / T060 main merge 済、 T061 PR 1 はそれらの後段
- T061 PR 1 スコープ = canonical_metrics.py 新規 + test 新規のみ。 stage_gate / swim_lane / archive / cross_pair / config / default.yaml 等 9 箇所への組込は **Phase 2 (T063-T064 と同時、 別 PR)** で行う
- DoD で要求される 5 段階 grep checklist 適用済 (worktree 内で 0 hit を確認):
  - Step 1 直 import: src/, scripts/ 内で canonical_metrics.py 自身以外 0 hit (test は除外)
  - Step 2 再エクスポート: 0 hit
  - Step 3 alias / wrapper: 0 hit
  - Step 4 runtime 配線: 0 hit (Phase 2 で実施)
- pytest: tests/alpha_factory/test_canonical_metrics.py 全 81 pass、 tests/alpha_factory/ 全 1138 pass、 tests/scripts/ 全 189 pass、 regression 0
- ruff check src/ tests/ clean (RUF022 / I001 / RUF005 / RUF059 / E501 全解消)
- mypy src/ clean (105 source files)

## 出力形式
レビュー結論を以下の形式で出力。

```
## 結論
[APPROVED | NEEDS_REVISION | REJECTED]

## 検出された問題 (検出順、 severity 別)
### Critical (= merge blocker)
- (列挙、 なければ "なし")
### Warning (= 修正推奨だが merge は可)
### Suggestion (= 改善提案)

## Falsification (反証探索) サマリー
- 試した反証仮説と verify 結果
```

## レビュー観点 (Falsification-first 必須)

Round 1 は **「この実装が詳細設計に違反している証拠を探せ」** という観点で書け。 確認バイアスを排除し、 まず反証を試みること。

### A. 設計遵守 (詳細設計 1038 行との完全照合)
1. **Enum 構成**: SessionBucket (3 値) + InfeasibleReasonCode (StrEnum, prefix 規約 strategic_*/input_*) が設計通りか
2. **DataClass 構成**: TradeRecord / BarEquityPoint / BarEquitySeries / SessionBlockSummary / CanonicalFiveThresholds / InvariantFlags / CanonicalFiveResult の 7 dataclass、 全 frozen
3. **invariant fail-fast**:
   - TradeRecord: UTC-aware + utcoffset==0 / entry<exit / business_day_index>=0 / pnl_net finite
   - BarEquitySeries: len>=1 / 全 UTC-aware / 全 finite / strict monotone increasing
   - CanonicalFiveThresholds: 全 finite + 各 field range
4. **例外階層**: CanonicalMetricsInputError 基底 → TradeRecordInvalidError / BarEquityInvalidError / ThresholdsInvalidError 派生 (detail Round 1 [W1] 反映、 _validate_utc_aware が exc_class 引数を取る)
5. **constants 確定値**: DENOM_FLOOR=1e-6 / N_BLOCKS_PER_YEAR=756 / GATE_PASS_TOLERANCE=1e-9 / HAC_BARTLETT_DEFAULT_Q=5 / SIGMA2_LR_EPS=1e-12 / MAX_DD_DENOM_FLOOR=1e-12 / LOG_PF_CLIP_RANGE=(-2,2) / LOG_PF_SMOOTH=1e-6 / LOW_SAMPLE_BLOCK_THRESHOLD=30
6. **GATE_PASS_TOLERANCE 適用**: gate_pass <=> gate_worst_gap <= GATE_PASS_TOLERANCE (concept Round 3 [W2])
7. **canonical 5 worst aggregation**:
   - HAC Bartlett kernel (Newey-West 1987) population formula、 mean-centered
   - sigma2_LR = gamma(0) + 2 Σ_{k=1..q}(1 - k/(q+1)) gamma(k)
   - SR_b = mu / sqrt(max(sigma2_LR, eps))
   - sr_session_worst = min over 3 buckets
   - 欠損 bucket / block 数<=1 で SR=-inf
   - low_sample_buckets: block 数 < 30
8. **WR neutral 0.5 規範** (detail Round 1 [C1]): business_day_universe で trade=0 day も空 block 生成、 trade_count=0 → win=0.5
9. **max_dd 値域 [0, 1] clip** (detail Round 1 [C3]): running_max<=0 skip、 全 bar 非陽性なら 1.0、 final clip 必須
10. **signed slack** (synthesis § 6.1): SR annual 換算 sqrt(756)、 全 denom_floor=1e-6
11. **slack_to_range**: lower 不足 → value-lower、 upper 超過 → upper-value、 範囲内 → min(value-lower, upper-value)
12. **log_pf_clip**: log((GP+1e-6)/(GL+1e-6)) を [-2, 2] に clip
13. **provider double-check**: provider=None → ("unvalidated") skip、 provider!=None で全 trade 検証、 mismatch 時 (False, version)
14. **InfeasibleReasonCode 変換マップ**:
    - BarEquityInvalidError → INPUT_INVALID_BAR_SERIES
    - bucket_validator 例外 → INPUT_BUCKET_VALIDATOR_EXCEPTION
    - bucket_validator mismatch → INPUT_BUCKET_ATTRIBUTION_MISMATCH
    - len(trades)==0 → INPUT_EMPTY_TRADE_LIST
    - business_day_universe 空 → INPUT_EMPTY_BUSINESS_DAY_UNIVERSE
    - business_day_universe mismatch → INPUT_BUSINESS_DAY_UNIVERSE_MISMATCH (detail Round 2 [W1])
15. **gate_pass 条件**: (gate_worst_gap <= GATE_PASS_TOLERANCE) AND invariants.is_feasible
16. **strategic_* reason 統合**: session_close_drop_count>0 → STRATEGIC_SESSION_CLOSE_DROP / negative_equity_drop_open_count>0 → STRATEGIC_NEGATIVE_EQUITY_DROP_OPEN を infeasible_reason_codes に追加
17. **no-raise 契約**: 有効 instance 受領後の内部処理は raise しない (detail Round 1 [C2])

### B. テスト充足度 (詳細設計テスト計画行 818-959 の全項目)
- Constants 3 件、 SessionBucket 2 件、 TradeRecord 6 件、 BarEquitySeries 7 件、 CanonicalFiveThresholds 5 件
- compute_session_blocks 3 件、 compute_sr_session_worst 6 件
- compute_session_block_win_rate_worst 4 件、 compute_max_dd 6 件
- slack_to_range 4 件、 log_pf_clip 4 件、 compute_signed_slacks 7 件
- evaluate_canonical_five 14 件、 invariants 3 件
- 合計約 74 件 (実装は 81 件) → 計画より多い (許容)
- 振る舞いベース命名 / pytest fixture 不要 / 数値検証 / fail-fast assert

### C. 数値計算精度
- HAC Bartlett 既知系列 [1,2,3,4,5] q=2 で SR ≈ 1.7521 を hand-computed と一致確認 (test 含む)
- GATE_PASS_TOLERANCE 浮動小数 noise 許容 test
- compute_max_dd 既知 drawdown pattern (100→200→100→50→75 で 0.75) test
- log_pf_clip [-2, 2] 境界 test (only profits / only losses / balanced / 0/0)

### D. 規範遵守 (T060 同様)
- import (`from src.alpha_factory.partition import ...` style 等の T060 規範): T061 では partition.py に依存しないので import なし、 これは設計通り
- ruff B905 `zip(strict=True)`: 該当箇所 (zip 使用) があるか確認
- 半開区間 [start, end) 統一 (T061 では Period 直接使わないが、 datetime 比較は exit > entry の半開規範を踏襲)
- SSOT 厳密性 (Enum 値経由参照、 文字列リテラル避ける): SessionBucket / InfeasibleReasonCode / PeriodLabel 文字列の直接参照箇所がないか
- SchemaContract / RunContext との独立性 (T061 PR 1 では touch 禁止)

### E. 5 段階 grep DoD (Phase 1 スコープ厳守)
- 段階 1 直 import: 0 hit (test と自身を除く)
- 段階 2 再エクスポート: 0 hit
- 段階 3 alias / wrapper: 0 hit
- 段階 4 runtime 配線: stage_gate.py / swim_lane.py / cross_pair.py / archive.py / run_ga.py で 0 hit (Phase 2 で実施、 申し送り済)

### F. 落とし穴 (典型バグ)
- iterable trades の二重走査による副作用 (Iterable は 1 度しか走査できない) → list materialize 済か
- frozen dataclass mutation 試行 (test で確認しているか)
- 浮動小数 division by zero (max(denom, floor) が複数箇所、 全て適用されているか)
- BarEquityInvalidError は __post_init__ で raise されるが、 evaluate_canonical_five の no-raise 契約を破らないか (catch 境界が正しいか)
- empty trades AND empty universe 等の重複 reason code 追加で frozenset 重複が発生しないか
- evaluate_canonical_five の戻り値 dict (per_bucket_sr / per_bucket_wr) は frozen dataclass field だが、 dict 自体は mutable → 設計許容かどうか

### G. 設計外配線の有無 (T061 PR 1 は他 file 一切 touch しない)
- canonical_metrics.py 以外で T061 関連シンボルへの参照が出ていないか (= 5 段階 grep の通り)

---

## 期待出力例 (APPROVED 時)

```
## 結論
APPROVED

## 検出された問題
### Critical
なし
### Warning
なし
### Suggestion
- (任意の改善提案、 0-3 件)

## Falsification サマリー
- 「signed slack の denom が分母固定でなく floor 化されていない可能性」 を検証 → compute_signed_slacks 各 ratio で max(|x|, DENOM_FLOOR) 確認 → 反証成立
- 「invariants.is_feasible が strategic_* reason のみ参照していない可能性」 → infeasible_reason_codes 集合の non-empty が is_feasible=False に直結 → 反証成立
- (他 N 件)
```
