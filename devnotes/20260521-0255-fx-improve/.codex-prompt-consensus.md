あなたは zenigame-fx Alpha Factory（FX イントラデイ GA 探索）の改善を Claude と合議する Codex です。マージ分析に基づき、次 Run（R84）の改善計画を 1 つに収束させてください。

# 使命
live_criteria を全て同時に満たす個体を見つける。達成後は閾値を引き上げて次水準へ（緩和は禁止）。

# FX 制約 / 禁止事項
イントラデイ前提 / ロング・ショート両方向許容 / スワップ・スプレッド控除後純益。期間延長・見かけ改善・閾値緩和・取引回数削減・オーバーナイトは REJECT。

# メタ過学習ガード
GA ハイパラ数値変更は Structural/Principled Parametric/Reactive Parametric に分類必須。Reactive は原則 REJECT。

# 状況（cycle 1 で mission 達成済）
Run 83 で live_criteria 全達成個体 43 出現（best g51_i71: sharpe4.24/pnl65210/dd1.49%/tc51）。North Star 到達。ただし両分析（Claude=OK要追検証 / Codex=CONCERN）が一致した 3 つの未充足点と 1 つの整合性問題:

## 未充足点
1. 再現性: 単一 seed=68・単一ペア・実質~6 genotype（P7+P9+F4+P11 1ファミリー）収束。profit_safe_pfr は seed variance 極端（Run75 lucky 前例）。
2. graduation=0: ii_lite_pass 全件 None（cross-pair ii-lite shadow-only、gate 未配線）。
3. B→C gap 主因は profit magnitude（pnl_only 162 が near-miss median36k）、cost 非要因。

## ★ 整合性問題（確定）
Stage C の「spread×1.5 stress」は cost robustness を検証していない。max_spread_bps は spread フィルタ閾値（broker が spread>閾値 の trade を skip）であり per-trade コストではない（src/broker/mock.py, src/backtest/engine.py:169）。spread×1.5 はフィルタを緩めるだけでコスト増やさず、stress_pnl_degradation が全 436 個体 0。「stress」が名前通りの役割（高コスト耐性検証）を果たしていない。mission 個体の cost robustness は実質未検証。

# 候補レバー
- P1【再現性検証】別 seed で R84 を回し mission 再現率・genotype 多様性を観測（コード変更なし or 最小、ゼロ risk）。
- P2【stress の cost-robustness 化】spread filter 緩和でなく per-trade コスト割増 stress を導入、または stress を診断計測可能化（Structural、整合性修正、中 risk）。
- P3【cross-pair ii-lite 計測可能化→段階 gate 昇格】（Structural、graduation 配線、中-大 risk）。

# 制約・方針
- 本ループは「フルサイズ・無制限 repeat・絶対に止まらない」。**loop 停止リスク（テスト破壊・大規模変更）を避け低リスク最優先**。cycle 2 は確実に前進する 1 つの最小変更が望ましい。
- mission 達成済のため North Star は「robustness 確認後に閾値引き上げ（緩和禁止）」。
- improve-cycle 守備範囲: GA パラメータ軽量調整 / 新ガード追加 / バグ修正。

# 求める出力（Markdown、日本語）
1. 各候補（P1/P2/P3）に APPROVE/MODIFY/REJECT + 分類（Structural/Principled/Reactive）+ target_metric/failure_mode/causal_path/falsification/success_criterion
2. 総合推薦: R84 に向けた **1 反証可能仮説 + 1 最小変更** に収束（低リスク最優先）。整合性問題(stress)と再現性のどちらを先にやるべきか明確に。
3. cycle_focus 判断（ga_improvements 確定の是非）
4. 全体判定: CONSENSUS REACHED / NEEDS ANOTHER ROUND

【ループ収束ルール】本ラウンドで CONSENSUS REACHED または「1 反証可能仮説 + 1 最小変更」に絞る。低リスク 1 変更に収束できれば 1 ラウンドで確定可。
