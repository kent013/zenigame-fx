# Round 3: run-effective config 取得経路の確定 + Round 2 指摘対応

## 検証完了 (= summary.json 直接 grep)

```
$ grep -E "max_clause|stage_a_alpha|stage_a_threshold" reports/run-reports/run-27/summary.json
    "max_clause": 1,                    ← ga_config.max_clause
    "stage_a_alpha": 0.03,              ← stage_gate_config.stage_a_alpha
    "stage_a_threshold": 0.0,           ← stage_gate_config.stage_a_threshold
```

= **summary.json は run-effective config 全項目を記録済み**。 default.yaml を読まず、 summary.json から取得する経路で Critical 解消。

## 対応マトリクス (R2 → R3)

| # | 指摘 | 重要度 | 対応 | 詳細 |
|---|---|---|---|---|
| R2-Cr1 | `max_clause_config` provenance 未定義 (= default.yaml 読み込みで過去 Run 判定汚染) | **Critical** | **採用** | `summary.json["ga_config"]["max_clause"]` から取得、 不能時は `P3=N/A: config_unavailable` + confidence cap medium |
| R2-W1 | `alpha` も run-effective から取得 | Warning | **採用** | `summary.json["stage_gate_config"]["stage_a_alpha"]` から取得、 不能時は `alpha_source: "fallback_default"` + size_norm を参考値扱い |
| R2-W2 | `compute_valid_mask` に np.isfinite 必須 | Warning | **採用** | `np.isfinite(df["fitness_pen"]) & np.isfinite(df["trade_sharpe_raw"])` で inf 除外 |
| R2-W3 | `compute_plateau_length` の浮動小数完全一致依存 | Warning | **採用** | `math.isclose(v, last_value, abs_tol=1e-12)` で判定 |
| R2-Sg1 | JSON に `evaluated_hypotheses=["P1","P2"]` を明示 | Suggestion | **採用** | P3 N/A 時は P3 を evaluated_hypotheses から除外 |
| R2-Sg2 | `raw_positive_count` / `fitness_pen_positive_count` / `sentinel_by_generation` / `best_generation` / `plateau_start_generation` 追加 | Suggestion | **採用** | data_quality + diagnosis セクションに追加 |
| R2-Sg3 | run-effective config snapshot 不在時の信頼度低下を設計に明記 | Suggestion | **採用** | 詳細設計の「リスク」 セクションに追加 |

## 改訂提案 (= R2 指摘反映後の Delta)

### Delta A: run-effective config 取得関数 (R2-Cr1 / R2-W1)

```python
def load_run_effective_config(run_number: int) -> tuple[dict[str, Any], list[str]]:
    """summary.json から run-effective config を取得、 不能項目を warnings に記録.

    Returns:
        (config_dict, warnings_list)
        config_dict: {
            "max_clause": int | None,
            "stage_a_alpha": float | None,
            "stage_a_threshold": float | None,
            "_source": "run_effective" | "fallback_default",
        }
    """
    summary_path = RUN_REPORTS_DIR / f"run-{run_number}" / "summary.json"
    warnings_list: list[str] = []
    config: dict[str, Any] = {"_source": "run_effective"}

    if not summary_path.exists():
        warnings_list.append(f"summary.json 不在: {summary_path}、 fallback_default 使用")
        config["_source"] = "fallback_default"
        config.update({"max_clause": None, "stage_a_alpha": None, "stage_a_threshold": None})
        return config, warnings_list

    summary = json.loads(summary_path.read_text(encoding="utf-8"))
    ga = summary.get("ga_config", {})
    sg = summary.get("stage_gate_config", {})

    config["max_clause"] = ga.get("max_clause")
    config["stage_a_alpha"] = sg.get("stage_a_alpha")
    config["stage_a_threshold"] = sg.get("stage_a_threshold")

    for key in ("max_clause", "stage_a_alpha", "stage_a_threshold"):
        if config[key] is None:
            warnings_list.append(
                f"summary.json から {key} 取得不能、 該当判定を degrade_confidence"
            )

    return config, warnings_list
```

### Delta B: diagnose_root_cause の signature 変更 (R2-Cr1)

```python
def diagnose_root_cause(
    decomposition: dict[str, Any],
    diversity: list[dict[str, Any]],
    penalty_effect: dict[str, Any],
    *,
    n_valid: int,
    run_effective_config: dict[str, Any],  # ← Delta A の出力
) -> tuple[str, list[str], dict[str, Any]]:
    rationale: list[str] = []
    falsification: dict[str, Any] = {}
    raw_max = decomposition.get("trade_sharpe_raw", {}).get("max")
    confidence_cap = "high"
    evaluated_hypotheses: list[str] = []

    # P1
    p1_match = raw_max is not None and raw_max < 0.005
    falsification["P1"] = {"condition": "raw_max < 0.005", "actual": raw_max, "match": p1_match}
    evaluated_hypotheses.append("P1")

    # P2
    p2_threshold = max(3, math.ceil(0.01 * n_valid))
    p2_killed = penalty_effect["n_raw_positive_and_penalty_killed"]
    p2_match = p2_killed >= p2_threshold
    falsification["P2"] = {
        "condition": f"penalty_killed >= max(3, 0.01*N_valid={n_valid}) = {p2_threshold}",
        "actual": p2_killed, "match": p2_match,
    }
    evaluated_hypotheses.append("P2")

    # P3 — run-effective config から判定
    max_clause_config = run_effective_config.get("max_clause")
    if max_clause_config is None:
        # config 取得不能 → N/A: config_unavailable + confidence_cap=medium
        falsification["P3"] = {
            "condition": "n_nodes std drop > 0.5 ∧ active_clause unique == 1",
            "actual": "non_evaluable",
            "match": False,
            "non_evaluable_reason": "summary.json から max_clause 取得不能",
        }
        p3_match = False
        confidence_cap = "medium"
    elif max_clause_config <= 1:
        # 設定上 P3 が非識別 (= R1-Cr1)
        falsification["P3"] = {
            "condition": "n_nodes std drop > 0.5 ∧ active_clause unique == 1",
            "actual": "non_evaluable",
            "match": False,
            "non_evaluable_reason": (
                f"run-effective config max_clause={max_clause_config} <= 1 のため "
                "active_clause unique==1 は config 由来の擬陽性。 P3 識別力なし"
            ),
        }
        p3_match = False
    elif diversity:
        first_std = diversity[0]["n_nodes_std"]
        last_std = diversity[-1]["n_nodes_std"]
        active_unique = diversity[-1]["active_clause_unique"]
        p3_match = (first_std - last_std) > 0.5 and active_unique == 1
        falsification["P3"] = {
            "condition": "n_nodes std drop > 0.5 ∧ active_clause unique == 1",
            "actual": {"std_drop": first_std - last_std, "active_unique": active_unique},
            "match": p3_match,
        }
        evaluated_hypotheses.append("P3")
    else:
        p3_match = False

    # 信頼度判定
    matches = [m for m, ok in [("P1", p1_match), ("P2", p2_match), ("P3", p3_match)] if ok]
    if len(matches) == 1:
        diagnosis = matches[0]
        confidence = "high" if confidence_cap == "high" else confidence_cap
    elif len(matches) > 1:
        diagnosis = "INCONCLUSIVE"
        confidence = "low"
        rationale.append(f"複数仮説該当: {matches}")
    else:
        diagnosis = "INCONCLUSIVE"
        confidence = "low"
        rationale.append("どの仮説にも明確該当せず")

    return diagnosis, rationale, {
        "confidence": confidence,
        "falsification": falsification,
        "evaluated_hypotheses": evaluated_hypotheses,
        "config_source": run_effective_config.get("_source", "unknown"),
    }
```

### Delta C: valid_mask に np.isfinite + plateau に math.isclose (R2-W2 / R2-W3)

```python
import numpy as np
import math

def compute_valid_mask(df: pd.DataFrame) -> pd.Series:
    """sentinel 除外 + finite + 必須列存在の共通 mask (R1-W2 / R2-W2 対応)."""
    fp_finite = df["fitness_pen"].apply(lambda x: np.isfinite(x) if x is not None else False)
    ts_finite = df["trade_sharpe_raw"].apply(lambda x: np.isfinite(x) if x is not None else False)
    sentinel_excluded = df["fitness_pen"] > -1e6
    return fp_finite & ts_finite & sentinel_excluded


def compute_plateau_length(df: pd.DataFrame) -> tuple[int, int | None]:
    """best fitness_pen plateau 長と開始世代 (R2-W3 / R2-Sg2 対応)."""
    valid = df[compute_valid_mask(df)].copy()
    if len(valid) == 0:
        return 0, None
    by_gen = valid.groupby("generation")["fitness_pen"].max().sort_index()
    if len(by_gen) < 2:
        return 0, None
    last_value = by_gen.iloc[-1]
    plateau = 0
    plateau_start = None
    for gen, v in reversed(list(by_gen.items())):
        if math.isclose(v, last_value, abs_tol=1e-12):
            plateau += 1
            plateau_start = int(gen)
        else:
            break
    return plateau, plateau_start
```

### Delta D: 追加 metric (R2-Sg2)

```python
def compute_extra_metrics(df: pd.DataFrame) -> dict[str, Any]:
    """R2-Sg2: raw_positive_count / sentinel_by_generation / best_generation 等."""
    valid = df[compute_valid_mask(df)]
    raw_positive_count = int((valid["trade_sharpe_raw"] > 0).sum())
    fitness_pen_positive_count = int((valid["fitness_pen"] > 0).sum())

    sentinel_by_gen = (
        df[df["fitness_pen"] <= -1e6].groupby("generation").size().to_dict()
    )
    sentinel_by_gen = {int(k): int(v) for k, v in sentinel_by_gen.items()}

    if len(valid) > 0:
        best_idx = valid["fitness_pen"].idxmax()
        best_generation = int(valid.loc[best_idx, "generation"])
    else:
        best_generation = None

    return {
        "raw_positive_count": raw_positive_count,
        "fitness_pen_positive_count": fitness_pen_positive_count,
        "sentinel_by_generation": sentinel_by_gen,
        "best_generation": best_generation,
    }
```

### Delta E: JSON 出力 schema 確定

```json
{
  "run_id": "run_20260504_032451",
  "run_number": 27,
  "diagnosis": "P1",
  "confidence": "high",
  "config_source": "run_effective",
  "evaluated_hypotheses": ["P1", "P2"],
  "diagnosis_rationale": [...],
  "falsification": {
    "P1": {"condition": "raw_max < 0.005", "actual": 0.001134, "match": true},
    "P2": {"condition": "penalty_killed >= max(3, 0.01*N_valid=630) = 7", "actual": 1, "match": false},
    "P3": {"condition": "...", "actual": "non_evaluable", "match": false, "non_evaluable_reason": "run-effective config max_clause=1 <= 1 のため active_clause unique==1 は config 由来の擬陽性"}
  },
  "plateau_length": N,
  "plateau_start_generation": N,
  "stage_pass_counts": {...},
  "fitness_pen_decomposition": {...},
  "trade_sharpe_by_generation": [...],
  "trade_count_buckets": [...],
  "diversity": [...],
  "penalty_effect": {...},
  "extra_metrics": {
    "raw_positive_count": M,
    "fitness_pen_positive_count": K,
    "sentinel_by_generation": {0: 1, 1: 0, ...},
    "best_generation": 11
  },
  "data_quality": {
    "sentinel_breakdown": {...},
    "n_valid": N,
    "n_total": 640,
    "warnings": [...]   ← load_run_effective_config からの warning list
  }
}
```

### Delta F: リスクセクション追記 (R2-Sg3)

詳細設計の「リスク」 に追加:
- **Risk 4**: run-effective config snapshot (= summary.json の ga_config / stage_gate_config) が不完全 / 不在の場合、 P3 判定が `config_unavailable` で degrade、 confidence は最大 medium

## Run-27 改訂後分類予測

- **summary.json から取得**: max_clause=1 / stage_a_alpha=0.03 / stage_a_threshold=0.0 (= 全項目取得済)
- **config_source**: `"run_effective"` (= summary.json 由来)
- **P1**: raw_max=0.001134 < 0.005 → ✅ match
- **P2**: penalty_killed ≈ 1 (= best のみ raw>0、 penalty で fitness_pen<=0)、 K=1 < 7 → ❌ no-match
- **P3**: max_clause=1 → **N/A** (= config 由来非識別、 evaluated_hypotheses から除外)
- **diagnosis = "P1"** with **confidence = "high"** (= 単一仮説 match、 config_source=run_effective)
- **evaluated_hypotheses = ["P1", "P2"]**

## 質問

### Q1. R2 [Critical] (= max_clause_config provenance) が完全解消されたか
Delta A + Delta B で run-effective config 取得経路を確定したが、 他に provenance 関連の漏れがあれば指摘。

### Q2. R2 [Warning] (W1/W2/W3) が完全解消されたか

### Q3. JSON schema (Delta E) で Run-29 判断に十分な情報量か
特に evaluated_hypotheses / falsification / config_source の 3 セットで「過去 Run の診断結果が当時の config に紐づく」 を担保できているか。

### Q4. 全体判定 (APPROVED / CHANGES_REQUESTED / INCONCLUSIVE)
本ラウンドで APPROVED に至るか判定してください。 ループ収束 (R3) のため。
