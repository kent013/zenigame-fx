【思考原則 — 全議論に適用】
まず仮説を立てろ。何を検証したいのか、なぜそう考えるのか、どうなれば成功と判断するのかを明確にしてから手を動かせ。

データに真摯に向き合え。数値を見て即座に閾値を弄るな。何が起きているのかを理解し、なぜそうなったのかを考え、どの方向に進むべきかを判断してから手を動かせ。

先人の知恵を探せ。学術論文・実務文献の引用を歓迎する（著者・年・タイトルを明記。記憶が曖昧なら「要確認」と明記）。

機能の名前に立ち返れ。名前はその機能が果たすべき役割を示している。

仕組みが機能していない段階で値を弄るな。設計の方向性が正しいと確認できてから値を弄れ。

因果ループを切断するな。「XはYに影響しない」という主張に出会ったら、間接経路を含む因果ループ全体を追え。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。
Stage C 通過は最低条件。使命達成 = live_criteria の全指標（Sharpe, Total PnL, Max Drawdown, Trade Count 範囲）を同時に満たし、Cross-pair (ii-lite) 評価も通過した個体の出現。
達成後は閾値を引き上げて次の水準を設定する（漸進的目標更新）。

【絶対制約】
- イントラデイ前提（オーバーナイト保有を前提にする設計は避ける）
- ロング・ショート両方向許容（FX の性質上）
- スワップ・スプレッドを fitness に反映（見かけの PnL ではなく純利益）

【禁止事項】
1. A・B・C 評価期間を強い根拠なしに延長する（過学習の隠蔽）
2. 見た目の数値をよくしようとする改善（Sharpe / WR 等の数値操作）
3. GA をハックしてステージを進めようとする（fitness 関数の歪曲等）
4. live_criteria 閾値をいたずらに緩和してステージを飛ばす
5. やたらに複雑な案を提案する（大規模リファクタリング、新フレームワーク導入等）
6. 取引回数を削減して見かけの成績を上げようとする（entry_threshold 過剰引き上げ等）
7. オーバーナイト保有前提の設計（スイングトレード化）
8. ゲノム archive スキーマ変更時の値伝搬漏れ（config → GaConfig → meta → consumer の 4 段接続漏れ）

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中すること。**ファイル読み込みは許可** (Read tool 使用可、 worktree 内の任意ファイルを参照してよい)。

【監査・レビュー時の必須チェック — False-positive 再発防止】
- C1 Design-first: コードの bug claim を出す前に、設計ドキュメント・devnotes・git 履歴を必ず先に読む。 grep だけで「バグ」判定しない。
- C2 「X が無い = バグ」禁止: 関数 Y に識別子 X が無いだけでは bug ではない。 別経路での計算を広く探す。
- C7 Sample size: n<30 の相関 claim は明示的に因果解釈を避ける。 n<10 の相関 claim 禁止。
- C8 INCONCLUSIVE: データ不足は正当な結論。 無理に CONFIRMED/REJECTED に寄せない。

---

# T071 PR 1 実装レビュー (impl-review-pr1)

## レビュー対象

zenigame-fx Alpha Factory cascade port v2 Phase 2 配線の 14 番目 TODO (T071):
**Observability hub (RunObservabilityReport)** の実装 PR。

worktree: `/Users/ishitoya/repository/zenigame-fx/worktrees/todo-T071-pr1`
branch: `todo/T071-pr1` (main `492afb4` から分岐)

## 設計参照 (必読)

- 詳細設計 (Round 3 APPROVED): `devnotes/20260430-1925-todo-T071-observability/detailed-design.md` (1365 行)
- 概念設計 (Round 2 APPROVED): `devnotes/20260430-1925-todo-T071-observability/conceptual-design.md`

## 実装ファイル一覧 (本 PR の差分)

新規:
- `src/alpha_factory/observability/__init__.py` (公開 API export)
- `src/alpha_factory/observability/run_metrics.py` (11 dataclass + 9 関数 + 主要定数)
- `tests/alpha_factory/observability/__init__.py` (空ファイル)
- `tests/alpha_factory/observability/test_run_metrics.py` (F1-F15 unit test、 68 test)

既存ファイル変更:
- `docs/alpha_factory/stage-gates.md` T071 セクション追記 (+107 行)

## main 実装 SSOT 規範に基づく重要な設計調整 (← レビューで特に確認してほしい点)

T058-T070 で確立した「**詳細設計と main 実装に乖離がある場合は main 実装を SSOT として T071 側を調整**」 規範を本 PR でも踏襲した。 T071 詳細設計 § 2 で hard dependency として記載された field の多くが main 実装に **存在しない** ことを grep で確認:

### main 実装に **不在** の field (= 詳細設計が前提とした field)

1. **T065 GenerationSelectionResult**: `pareto_front1_size` / `feasible_ratio` / `mean_constraint_violation` / `generation` 不在
   - 実装方針: `front_assignments` (= eligible のみの dict) から front1 cardinality を T071 側で再計算 + 残り 3 field は **caller 注入** (関数引数) に変更

2. **T066 AdmissionReport**: `n_admitted_ca` / `n_admitted_da` / `n_evicted_ca` / `n_evicted_da` / `n_admitted_by_role` 不在
   - 実装方針: main 実装の `n_admitted_mission` / `n_admitted_progress` / `n_admitted_bypass` を str key dict に集約。 eviction count は `len(evicted_genome_ids)` で代替

3. **T066 ArchiveRole enum**: 不在 (= Literal 文字列)
   - 実装方針: str key (`"mission_pass"` / `"progress_pass"` / `"score_bypass"`) に変更

4. **T066 ArchiveMember.session_pass_pattern**: 不在
   - 実装方針: `compute_session_entropy` の入力を `Sequence[str]` (= 3 bit string list) に変更し、 caller (Phase 2) が事前計算した pattern list を引数注入

5. **T067 WarmstartConfig / warmstart_share_target / warmstart_share_actual / per_source_run_violations**: 全て不在 (WarmstartConfig 自体が main 実装に存在しない)
   - 実装方針: `warmstart_share_target` を caller 注入。 `WarmstartReport.share` (float) を Decimal 化して actual 扱い。 `per_source_run_violations` は caller 注入 (default 0)

6. **T068 RunFailureSummary.run_aborted**: 不在
   - 実装方針: main 実装の `any_stage_all_failed` を `run_aborted` 相当として抽出

7. **T068 FailureSummary.fingerprint_dedup_top_n**: 不在
   - 実装方針: caller 注入 (per-stage fingerprint top-N dict、 default 空)

これらの調整により T071 PR は self-contained (= T065-T068 の追加 field merge 不要) で commit 可能になっている。 Phase 2 配線時に `scripts/alpha_factory/run_ga.py` 側で必要な値を計算して T071 関数に注入する設計。

## DoD 確認結果

- pytest: `tests/alpha_factory/observability/` 68 pass、 全 regression `tests/` 2480 pass / 0 fail (1 skipped / 1 xfailed)
- ruff: T071 範囲 0 error (既存 main の `tests/alpha_factory/primitives/test_indicators_causality.py:136` の B905 は無関係 / scope 外)
- mypy: T071 範囲 (4 source files) 0 error、 src/alpha_factory/ 全体 (42 source files) 0 error

## レビューしてほしい観点

### 観点 1: main 実装 SSOT 規範の適用妥当性
詳細設計の hard dependency field が main 実装に不在のため、 T071 側を caller 注入式に変更した。 この方針は T058-T070 の規範を踏襲しているが:
- 規範の適用範囲が妥当か (= caller 注入が増えすぎて T071 の責務 = observability hub を逸脱していないか)
- Phase 2 配線時に caller (`run_ga.py`) が確実に値を計算できる前提が docstring に明記されているか
- T065-T068 PR の field 追加 (= main 実装に詳細設計通りの field を追加) を **Phase 2 で行うか/しないか** の判断は妥当か

### 観点 2: F1-F15 失敗モード対応の網羅性
詳細設計 § 6 のマトリクスに対し、 unit test が 1:1 対応しているか:
- F1: `test_F1a` / `test_F1b`
- F2: `test_F2a` / `test_F2b`
- F3: `test_F3a` / `test_F3b` / `test_F3_clamp_keeps_corr_in_range`
- F4: `test_F4a` ~ `test_F4f`
- F5: `test_F5a` / `test_F5b`
- F6: `test_F6a` ~ `test_F6d`
- F7: `test_F7a` ~ `test_F7c`
- F8: `test_F8a` ~ `test_F8g`
- F9: `test_F9a` ~ `test_F9d`
- F10: `test_F10a` ~ `test_F10c` (status 不整合) + その他 invariant
- F11: `test_F11_extract_*` (cross-PR field rename 検出)
- F12: `test_F12_length_mismatch_raises`
- F13: 仮説値定数として明文化 (= unit test なし、 Phase 2-IT 申し送り)
- F14: `test_F14_*` (RunObservabilityReport invariant)
- F15: `test_F15a` / `test_F15b` / `test_F15c`

### 観点 3: Decimal vs float の精度・境界
- `compute_ab_divergence_on_b_evaluated` の `var_a * var_b` を `math.sqrt` 経由 (float) で計算後 `Decimal(repr(...))` に戻す部分の数値精度
- `compute_session_entropy` の `math.log2` 経由 (float) からの Decimal 復元、 数値誤差で max_entropy を超える場合の clamp が後続 `__post_init__` で破綻しないか

### 観点 4: status field 方式 (None 排除) の徹底
詳細設計 Round 2 [W2] / [C1] で確立した status field 方式が全 metric (ABDivergenceMetric / ArchiveChurnMetric / SessionEntropyMetric) で完全強制されているか:
- `__post_init__` で全 status の invariant を完全強制 (= sentinel 不整合で raise)
- 不明 status (= Literal 範囲外) は runtime ValueError で拒否

### 観点 5: 既存 main への副作用
新設 module 追加で既存 import 経路に影響なし、 全 regression test 通過。 schema 変更ナシを確認。

## 期待する出力 (Round 1)

詳細設計 Round 3 / 概念設計 Round 2 が APPROVED 済のため、 Round 1 では以下を判定:
1. **APPROVED** / **NEEDS_CHANGES** / **INCONCLUSIVE** の verdict
2. 観点 1-5 それぞれの評価
3. (NEEDS_CHANGES の場合) 修正必要箇所と理由を C1-C9 ID で分類
4. main 実装 SSOT 規範の適用が T058-T070 の前例と整合しているかの確認

ファイル読み込みは許可されているので、 必要に応じて以下を Read で確認してよい:
- `src/alpha_factory/observability/run_metrics.py`
- `src/alpha_factory/observability/__init__.py`
- `tests/alpha_factory/observability/test_run_metrics.py`
- `docs/alpha_factory/stage-gates.md` (T071 セクション)
- `src/alpha_factory/cpps_archive.py` (T066 main 実装、 SSOT)
- `src/alpha_factory/loop_closure.py` (T067 main 実装、 SSOT)
- `src/alpha_factory/failure_handling.py` (T068 main 実装、 SSOT)
- `src/alpha_factory/nsga2_selection.py` (T065 main 実装、 SSOT)
- `devnotes/20260430-1925-todo-T071-observability/detailed-design.md`
- `devnotes/20260430-1925-todo-T071-observability/conceptual-design.md`
