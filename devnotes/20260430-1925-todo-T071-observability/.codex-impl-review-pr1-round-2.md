**Verdict**
- **APPROVED**
- C1/C4/C6 に従い、設計・実装・テスト・T065-T068 main 実装 SSOT・docs を照合しました。blocking な不整合は見つかりません。

**観点 1**
- `src/alpha_factory/observability/run_metrics.py:1` の module docstring で、main 実装に存在しない field と caller 注入対象が明記されています。
- `extract_selection_metrics`、`compute_session_entropy`、`extract_inflow_consistency`、`extract_failure_metrics` の各 docstring でも Phase 2 caller 契約が具体化されています。
- T065-T068 の実ファイルでも、提示された不在 field は実際に不在で、T071 側を main 実装 SSOT に寄せる判断は妥当です。

**観点 2**
- F1-F15 は `tests/alpha_factory/observability/test_run_metrics.py` で実アサーション付きで概ね 1:1 対応しています。
- F13 は設計どおり unit test ではなく仮説値定数 + Phase 2 IT 申し送り扱いです。
- F11 は詳細設計の旧 `pareto_front1_size` 前提ではなく、main SSOT に合わせて `front_assignments` 抽出テストへ調整されており妥当です。

**観点 3**
- `compute_ab_divergence_on_b_evaluated` は `math.sqrt` 後に `Decimal(repr(...))` へ戻し、`[-1, 1]` clamp と `ABDivergenceMetric.__post_init__` で境界破綻を防いでいます。
- `compute_session_entropy` は `math.log2` 後に `Decimal` 復元し、`h` と `relative` を上限 clamp しているため、max entropy 超過で `__post_init__` が破綻する経路は抑止されています。
- 極端な巨大/微小 Decimal 入力では float 変換由来の残余リスクはありますが、詳細設計どおりの実装であり、この PR の blocking issue とは判断しません。

**観点 4**
- `ABDivergenceMetric`、`ArchiveChurnMetric`、`SessionEntropyMetric` は status 別 invariant と未知 status reject を `__post_init__` で強制しています。
- sentinel 不整合も `corr=0`、`entropy=0`、`n_runs_used`、`n_archive_members` などで明示的に reject されています。
- None 経路は入っておらず、Round 2 の status field 方式に整合しています。

**観点 5**
- `src/alpha_factory/observability/__init__.py:14` の公開 export は新設 observability API に閉じています。
- `run_metrics.py` の import は `src.alpha_factory.cpps_archive`、`failure_handling`、`loop_closure`、`nsga2_selection` の flat 配置で揃っています。
- 既存 schema 変更や既存 import 経路への副作用は見当たりません。

**SSOT 整合**
- T058-T070 の「詳細設計と main 実装が乖離する場合は main 実装を SSOT」と整合しています。
- Phase 2 で T065-T068 に field 追加するより、現時点では caller 注入で観測層を薄く保つ方針が妥当です。