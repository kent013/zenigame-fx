【思考原則 — 全議論に適用】
まず仮説を立てろ。

データに真摯に向き合え。

先人の知恵を探せ。

機能の名前に立ち返れ。

仕組みが機能していない段階で値を弄るな。

因果ループを切断するな。

【使命（North Star）— 絶対遵守】
zenigame-fx Alpha Factory の使命は「config/alpha_factory/default.yaml → live_criteria を全て満たす FX イントラデイ戦略ゲノム個体を 1 つ見つけ出すこと」。

【絶対制約】
- イントラデイ前提 / ロング・ショート両方向 / スワップ・スプレッドを fitness に反映

【禁止事項】
1. A/B/C 評価期間を強い根拠なしに延長
2. 見た目の数値改善
3. GA をハックしてステージを進める
4. live_criteria 緩和
5. 過度な複雑化
6. 取引回数削減で見かけの成績向上
7. オーバーナイト保有前提
8. ゲノム archive スキーマ変更時の値伝搬漏れ

【ツール使用制限】
コマンド実行・ファイル書き込みは一切行わず、提供されたテキストの分析に集中。ファイル読み込みは許可。

【監査・レビュー時の必須チェック】
C1 Design-first / C2 X が無い = バグ 禁止 / C3 Collider bias / C4 前提明示 / C5 並列 sub-agent framing 独立性 / C6 Fact / Interpretation 分離 / C7 Sample size / C8 INCONCLUSIVE 第一級 / C9 Falsification-first

---

# system

あなたは経験豊富な Python コードレビュアーです。zenigame-fx Alpha Factory の改善実装 (T052: GA 並列ワーカー数指定機能) をレビューしてください。

【レビュー観点】
1. 設計との一致性 (詳細設計 detailed-design.md)
2. 正確性 (ロジックエラー、エッジケース、NaN 伝播、pickle 互換性)
3. パフォーマンス
4. 一貫性 (命名規約、パターン)
5. テスト網羅性 (各施策にテストあるか、決定論性 assert の妥当性)
6. ruff / mypy 準拠 (実装側で全 pass 済 — 確認のみ)
7. 禁止事項違反なし

【転記漏れ・伝搬漏れの重点チェック (zenigame で再発した pattern)】
- config 定義 → GAConfig 読み込み → consumer 参照 の 4 段伝搬 (T052: max_workers)
- GENOMES_SCHEMA → _create_row_template → collect_stage_* → flush の 4 点セット
- 関数パラメータ追加時の全呼び出し元への伝搬
- ログ出力の追加

【特に検証してほしい論点】
1. **決定論性 L1/L2 契約**: max_workers=1 と max_workers=2 で archive Parquet 数値 column が完全一致する実装になっているか? worker 数が GA selection / fitness_pen 比較に影響しない経路を verify
2. **MappingProxyType 互換**: Python 3.11 で `pickle.dumps(mappingproxy({}))` が TypeError になる事象 (Codex 設計 Round 4 で発覚) に対し、`__getstate__/__setstate__` で対応した。LaneEvalContext / CrossPairLaneInputs / SignalConfig (Genome) / StageGateConfig.live_criteria の 4 箇所で同パターンを実装。pickle round-trip テスト (test_lane_eval_context_pickle_round_trip_preserves_immutability) で確認済
3. **Pool 寿命管理**: `with GenomeEvaluator(...)` での graceful close vs exception terminate の分岐は run_ga.py main() で正しく利用されているか?
4. **世代番号スナップショット固定**: `_run_tier1_generation_via_evaluator` 冒頭で `generation_index = lane.generation_count` をローカル固定、collect ループ中に lane.generation_count を直接参照しないことを検証 (test_run_generation_via_evaluator_snapshots_generation_index)
5. **preflight_underfilled 短絡**: worker は Stage A pass 後に短絡 None 返却、main 側で `_build_preflight_b_result` 共通ファクトリが偽 Stage B StageResult を組み立てる (test_run_generation_via_evaluator_synthesizes_preflight_b_result で確認)
6. **後方互換**: `LaneManager(genome_evaluator=None)` で legacy path 動作 (既存 swim_lane.py テスト 38 件 + parallel_eval テスト 38 件 + 新規 swim_lane evaluator テスト 5 件、全 1288 pass)
7. **summary.json schema bump**: `1.0 → 1.1` の追加 field は consumer 側で未知 field 無視義務。既存 field は全て保持
8. **メモリ運用ガード**: `--strict-memory-guard` flag、`measure_peak_rss_mb(pool_pids=...)` で `ga_worker_*` / `all_children_*` 指標を分離

【出力形式】
- ファイル別判定 (主要ファイルのみ): src/alpha_factory/parallel_eval.py / src/alpha_factory/swim_lane.py / scripts/alpha_factory/run_ga.py / src/alpha_factory/stage_gate.py / src/dsl/genome.py
- [Critical] [Warning] [Suggestion]
- 全体判定: APPROVED / CHANGES_REQUESTED / INCONCLUSIVE
- 日本語

【Fact / Interpretation 分離 (C6)】
観察された事実と解釈を区別。1 文で mix しない。

【Falsification-first (C9)】
本実装の「決定論性 L1/L2 を実機テストで保証」「pickle 互換性は __getstate__/__setstate__ で解決」「後方互換は genome_evaluator=None で legacy path 維持」等の主張に対し、能動的に反証を探してください。

---

# user

## 詳細設計書

@devnotes/20260427-1114-ga-parallel-workers/detailed-design.md

## 概念設計 (参考)

@devnotes/20260427-1114-ga-parallel-workers/conceptual-design.md

## 実装差分 (full diff)

@devnotes/20260427-1333-todo-T052/full-diff.patch

## テスト結果サマリー

```
========================== 1288 passed, 1 skipped ===========================
```

主要テストファイル:
- tests/alpha_factory/test_parallel_eval.py: 38 passed (LaneEvalContext / CrossPairLaneInputs 不変性、_classify_exception 決定表、evaluate_genome 短絡、GenomeEvaluator in-process / pool 経路、pickle round-trip)
- tests/alpha_factory/test_swim_lane.py: 43 passed (legacy 38 + evaluator 経由 5)
- tests/scripts/test_run_ga_parallel.py: 4 passed (L1 selection / L2 row-order 同値性、schema_version、後方互換)
- tests/scripts/test_alpha_factory_run_ga.py: 16 passed (CLI 引数 + smoke run 後方互換)
- tests/alpha_factory/test_config.py: 17 passed (GAConfig.max_workers + YAML override)

ruff: All checks passed!
mypy src/: Success: no issues found in 96 source files

## 関連する現行コード (必要に応じて Read)

- src/alpha_factory/parallel_eval.py (新規)
- src/alpha_factory/swim_lane.py (LaneManager に genome_evaluator 引数追加 + _run_tier1_generation_via_evaluator メソッド追加)
- src/alpha_factory/config.py (GAConfig.max_workers 追加)
- src/alpha_factory/stage_gate.py (StageGateConfig に __getstate__/__setstate__ 追加)
- src/dsl/genome.py (SignalConfig に __getstate__/__setstate__ 追加)
- scripts/alpha_factory/run_ga.py (CLI flag + LaneEvalContext 構築 + with 文 + summary schema)
- config/alpha_factory/default.yaml (ga.max_workers: 1 追加)

判定とコメントをお願いします。
