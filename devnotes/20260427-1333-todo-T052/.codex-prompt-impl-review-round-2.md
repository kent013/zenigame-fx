# Round 2: Round 1 Critical/Warning 反映後の再レビュー

## 対応マトリクス (Round 1 → 修正)

| Round 1 指摘 | 分類 | 修正 |
|---|---|---|
| §1 Critical: worker 側 preflight 短絡が本番経路で発火していない (run_ga.py で `LaneEvalContext(preflight_underfilled=False)` 固定) | Critical | `run_ga.py main()` で **bundle.bars_stage_b ベースに preflight 値を計算**、`PreflightPayload` を組み立てて `LaneEvalContext` に伝搬。新規テスト `test_main_constructs_lane_eval_context_with_preflight_payload` で実機検証 |
| §2 Warning: `pool_pids` AttributeError fallback 文言と挙動の不整合 (空 set だと all_children 経路が走らない) | Warning | `run_ga.py` の `measure_peak_rss_mb` 呼び出し側で `pool_pids if pool_pids else None` に変更。`_write_reports` の RSS 集計でも ga_worker_* が 0 なら all_children_* にフォールバックする実装を追加 |
| §3 Warning: L1 テストで `max_drawdown` キーを見ているが実装は `max_drawdown_pct` (実 schema は `live_criteria.{checks: {...}, all_pass: bool}`) | Warning | テストを実 schema (`checks` 内 sharpe/total_pnl/max_drawdown_pct/trade_count + all_pass) に合わせて修正 |
| §4 Suggestion: StageGateConfig / SignalConfig 単体 pickle round-trip テスト | Suggestion | `TestStageGateConfigPicklability::test_stage_gate_config_picklable_directly` および `TestSignalConfigPicklability::test_signal_config_picklable_directly` を追加 |

## テスト結果

```
1291 passed, 1 skipped (Round 1 から +3: preflight 統合テスト + StageGateConfig pickle + SignalConfig pickle)
ruff: All checks passed!
mypy src/: Success: no issues found in 96 source files
```

## 修正版抜粋

### run_ga.py main() の preflight 計算

```python
# bundle.bars_stage_b から preflight 値を計算
bars_b_list = list(bundle.bars_stage_b)
lane_n_unique_dates = n_unique_dates(bars_b_list)
wf_min_dates = wf_min_unique_dates(
    cfg.stage_gate.wf_train_days,
    cfg.stage_gate.wf_embargo_days,
    cfg.stage_gate.wf_test_days,
)
lane_max_folds = compute_max_folds(
    lane_n_unique_dates,
    cfg.stage_gate.wf_train_days,
    cfg.stage_gate.wf_embargo_days,
    cfg.stage_gate.wf_test_days,
    cfg.stage_gate.wf_step_days,
)
wf_min_folds = cfg.stage_gate.wf_min_folds_required
preflight_underfilled = lane_max_folds < wf_min_folds
preflight_payload = PreflightPayload(
    n_unique_dates=lane_n_unique_dates,
    wf_min_unique_dates=wf_min_dates,
    max_folds=lane_max_folds,
    wf_min_folds_required=wf_min_folds,
    n_bars=len(bars_b_list),
)

lane_ctx = LaneEvalContext(
    lane_id=lane_id,
    bars_a=tuple(bundle.bars_stage_a),
    bars_b=tuple(bundle.bars_stage_b),
    bars_holdout=tuple(bundle.bars_holdout),
    meta=bundle.meta,
    bt_cfg=bt_factory(cfg.dataset.instrument),
    cp_inputs=None,
    preflight_underfilled=preflight_underfilled,
    preflight_payload=preflight_payload,
)
```

### RSS fallback (run_ga.py)

```python
# pool_pids が空 (sequential or AttributeError fallback) なら None を渡す
_pids = genome_evaluator.pool_pids
peak_rss_per_generation.append(
    measure_peak_rss_mb(pool_pids=_pids if _pids else None)
)

# _write_reports 内: ga_worker_* が 0 なら all_children_* を使う
worker_max = float(rss.get("ga_worker_max_rss_mb", 0.0))
worker_total = float(rss.get("ga_worker_total_rss_mb", 0.0))
if worker_max == 0.0 and worker_total == 0.0:
    worker_max = float(rss.get("all_children_max_rss_mb", 0.0))
    worker_total = float(rss.get("all_children_total_rss_mb", 0.0))
```

### L1 テスト修正 (実 schema 対応)

```python
seq_lc = seq_summary["live_criteria"]
par_lc = par_summary["live_criteria"]
assert seq_lc["all_pass"] == par_lc["all_pass"]
seq_checks = seq_lc.get("checks", {})
par_checks = par_lc.get("checks", {})
expected_keys = ("sharpe", "total_pnl", "max_drawdown_pct", "trade_count")
found_keys = [k for k in expected_keys if k in seq_checks]
assert found_keys, f"live_criteria.checks has no expected keys"
for key in found_keys:
    assert seq_checks[key] == par_checks[key]
```

### 追加 pickle テスト (Suggestion 対応)

```python
class TestStageGateConfigPicklability:
    def test_stage_gate_config_picklable_directly(self) -> None:
        cfg = StageGateConfig()
        restored = pickle.loads(pickle.dumps(cfg))
        assert restored.stage_a_window_days == cfg.stage_a_window_days
        assert isinstance(restored.live_criteria, MappingProxyType)
        assert dict(restored.live_criteria) == dict(cfg.live_criteria)


class TestSignalConfigPicklability:
    def test_signal_config_picklable_directly(self) -> None:
        sig = SignalConfig(name="X", weight=1.0, params={"a": 1, "b": 2})
        restored = pickle.loads(pickle.dumps(sig))
        assert restored.name == "X"
        assert isinstance(restored.params, MappingProxyType)
        assert dict(restored.params) == {"a": 1, "b": 2}
```

### 新規 preflight 統合テスト

```python
def test_main_constructs_lane_eval_context_with_preflight_payload(monkeypatch):
    """run_ga.main() が LaneEvalContext を構築する際に preflight 値を埋め込む."""
    captured = {}
    class _SpyEvaluator(_OrigEvaluator):
        def __init__(self, max_workers, stage_gate_cfg, cross_pair_cfg, prim_evaluator, lane_contexts):
            captured["lane_contexts"] = dict(lane_contexts)
            super().__init__(...)
    monkeypatch.setattr(run_ga_module, "GenomeEvaluator", _SpyEvaluator)
    # ... smoke run setup ...
    run_ga_module.main([..., "--no-report"])
    lane_ctx = next(iter(captured["lane_contexts"].values()))
    assert lane_ctx.preflight_payload is not None
    assert lane_ctx.preflight_payload.n_bars == len(stage_b_rows)
```

## 再レビュー依頼

1. **§1 Critical 解消確認**: `LaneEvalContext.preflight_underfilled` / `preflight_payload` が main 経路で必ず埋まり、worker が Stage B 短絡を発火できるようになったか?
2. **§2 Warning 解消確認**: pool_pids 空集合時に measure_peak_rss_mb が all_children_* で集計され、`_write_reports` の集計ロジックも fallback を尊重するか?
3. **§3 Warning 解消確認**: L1 テストが実 schema に整合し、決定論性違反を検出できる粒度か?
4. **§4 Suggestion**: StageGateConfig / SignalConfig 単体 pickle テスト追加で十分か (RegistryEvaluator は既存)?
5. **新たな Critical / Warning**: 修正で生まれた retrocession・副作用がないか?

判定 (APPROVED / CHANGES_REQUESTED / INCONCLUSIVE) と残る指摘を出力してください。

## 関連変更ファイル (再 Read 推奨)

- scripts/alpha_factory/run_ga.py (preflight 計算、imports、_check_memory_budget、measure_peak_rss_mb 呼び出し、_write_reports の RSS 集計)
- tests/scripts/test_run_ga_parallel.py (L1 schema 修正)
- tests/scripts/test_alpha_factory_run_ga.py (preflight 統合テスト追加)
- tests/alpha_factory/test_parallel_eval.py (StageGateConfig / SignalConfig pickle テスト追加)
